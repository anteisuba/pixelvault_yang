# 全局应用壳施工图 — app-shell

> 2026-09-05 owner 确认竞技场退役（导航、站点地图、页面、对战 API、专用客户端/组件/服务与三语文案已移除）；2026-09-17 完成整删——`arena-winrate.service` 与模型路由的胜率加权、`ArenaMatch` / `ArenaEntry` / `ModelEloRating` 的 schema 声明一并删除，历史数据不迁移、不删表。

> **状态：施工基准（2026-08-18 owner 确认「分段浮岛」方向 + 关键切片，指示开工）。**
> 范围：**只管壳** —— 侧栏 / 移动导航 / 默认页面表面（`bg-background`）/ 全局语义槽。
> **不管**各业务域皮肤（画布 `canvas.css`、LoRA `lora.css`、首页 `home-v4.css`、legal `legal.css` 各自为政，见 `brand-dna.md`「共享行为不共享皮肤」）。
> 设计原型（讨论期证据：诊断 + 三方向稿、「分段浮岛」关键切片稿）已随任务包清理，git 历史可取；owner 确认的结论已全部写入本文。
> 上一轮基准（2026-07-31「全浅 A'」）的未竟事项收在 §8。

---

## 1 · 域定义

侧栏属于 **品牌脊柱**（`brand-dna.md`「应用壳、全局导航位置与导航行为」），**不是业务域皮肤**。因此：

- 它的长相由本文一处定义，**不吸收任何单个业务域的性格**（不像画布，也不像 LoRA）。
- 它的设计目标是 **安静、恒定、永不抢戏** —— 主区永远是作品/画布。

| 负责                                         | 不负责                             |
| -------------------------------------------- | ---------------------------------- |
| 告诉你在哪 · 让你去别处 · 通往个人主页与设置 | 任何业务操作、生成、参数、账户数字 |

**最高频任务**：在 7 个工具之间来回切。整个设计围绕这一条优化。

---

## 2 · 方向：分段浮岛（owner 2026-08-18 拍板）

**B 的结构 + C 的材质**：

- **结构（B）**：推挤式分段面板；三段「去处 / 工具 / 账户」，段间用留白分不用线；整栏只有**一个**运动主体 —— 会滑的活动指示器。
- **材质（C）**：壳底浅灰，侧栏与主卡各成一片浮在上面；边界靠**几何**（缝 + 圆角 + 投影），不靠发丝线。

落选：A 静轨（主区零回流，但常驻标签消失）· 纯 B（两个白面层次弱）· 纯 C（长列表仍是一坨）。

> 指针：本节是**应用壳**的「侧栏灰底 + 主卡」，与**工作台脊柱**（配音间 / 图像 / 视频 / LoRA 四个工作台共用的 `.workbench-ground` 灰底地台 + `.workbench-card` 白卡，owner 2026-09-03）是两层不同的东西——壳套着工作台，工作台自己再套一层灰底+白卡。工作台脊柱见 `../ui-defaults.md` §2.2 与 `src/app/globals.css`。

---

## 3 · 尺寸

|                | 桌面 ≥1024                      | 平板 768–1023      | 手机 <768          |
| -------------- | ------------------------------- | ------------------ | ------------------ |
| 轨宽 · 展开    | **144**（容器 160 含 8px 外沟） | — 无竖轨           | — 无竖轨           |
| 轨宽 · 收起    | **40**（容器 56）               | —                  | —                  |
| 行高           | 36                              | 顶栏 44            | 顶栏 44            |
| 图标           | 16                              | 20（面板格）       | 20（面板格）       |
| 收起态点击目标 | 32 × 32（fine pointer 下限）    | 面板格 72 高       | 面板格 72 高       |
| 对比现状       | 192 / 48 → **144 / 40**         | 44 死轨 → **撤销** | 44 死轨 → **撤销** |

桌面导航默认收起为窄图标栏，用户主动打开后显示完整导航；应用内切页保留开关选择，刷新后重新收起，不从 cookie 恢复展开状态。

⚠ **收起态整个标签不渲染**（`display:none`），不是靠 `overflow` 裁。只裁的话会被
`text-overflow` 截成「G.」「P.」这种单字 —— 2026-08-18 真机上出过。

**⚠ 触屏的 44px 触摸目标下限仍然成立** —— 只是 <1024 不再有竖轨去承载它，改由顶栏按钮
（44 高）与面板格（72 高）满足。

**⚠ 撑宽侧栏的是一个英文字符串，不是布局。** 三语标签实测（14px Geist）：

| locale | 最长标签                | 宽度                      |
| ------ | ----------------------- | ------------------------- |
| zh     | 卡片管理 / 分镜故事     | 56px                      |
| ja     | ギャラリー / プロンプト | 70px                      |
| en     | **Card Management**     | **119px（600 字重 124）** |

所以收宽度的第一步是**改标签**，不是压别的元素：

| key                     | 现状              | 改为        | 理由                                         |
| ----------------------- | ----------------- | ----------- | -------------------------------------------- |
| `en.Navbar.links.cards` | `Card Management` | **`Cards`** | 路由本来就叫 `/cards`；zh 也只是「卡片管理」 |

⚠ `en.StudioTools.comingSoon`（`Coming Soon` → `Soon`）曾是这张表的第二行；2026-09-17 owner 拍板删掉「敬请期待」分组后，该键已随组一起删除。

两处一改，132 / 144 / 156 三档在三语下**零截断**（原型逐档实测）。**取 144**：比日文地板（约 122px）留 ~20px 余量，不至于以后加一个稍长的标签就崩。
⚠ 改 `src/messages/*.json` **禁用正则**，必须逐键对比（跨 256KB 的 `.*?` 会静默删掉别处的键，全量闸门照过）。

---

## 4 · 令牌（全部 node 确定性计算，禁目测、禁信 review agent 算术）

| 用途              | 值                    | 底        | 对比度    | 判定               |
| ----------------- | --------------------- | --------- | --------- | ------------------ |
| 壳底 ground       | `#e4e7ec`             | —         | —         | 面                 |
| 浮卡 / 浮片       | `#ffffff`             | `#e4e7ec` | **1.24**  | ⚠ **不承重**       |
| 未选中标签 14px   | `#454c55`             | `#e4e7ec` | 7.01      | ✅                 |
| 激活标签 14px/600 | `#0b0e12`             | `#ffffff` | 19.34     | ✅                 |
| 段标题 11px 大写  | `#555d67`             | `#e4e7ec` | 5.38      | ✅                 |
| 激活竖条 3×18     | `#0b0e12`             | `#e4e7ec` | **15.60** | ✅                 |
| Focus 环 2px      | `#0b0e12`             | `#e4e7ec` | 15.60     | ✅                 |
| Hover 底          | `rgba(11,14,18,.055)` | `#e4e7ec` | —         | 与 active **反向** |

### 4.1 · 两条不许推翻的算术结论

1. **hover 和 active 必须反极性。** 初稿让 hover 用「更白的底」、active 用纯白浮片 —— 算出来 **1.1:1**，就是把「hover 任一项都长得像当前项」这个现存 bug 换身浅色衣服重犯。定案：**hover 往暗走（墨 5.5%）、active 往亮走（白浮片）**。
2. **浅壳里「面对面」到不了 3:1。** 白卡 vs 灰底 = 1.24（试到 `#dce1e8` 也只有 1.31）。因此 **任何状态语义都不许压在面色差上**：
   - 壳与内容的分离 → **几何**（10px 缝 + 14px 圆角 + 投影）。几何没有对比度阈值。
   - 状态语义 → 墨竖条（15.60:1）+ 墨色跃迁（7.01 → 19.34）+ 字重（400 → 600）。

**激活态三个信号叠加**：白浮片（材质）+ 墨竖条（承重）+ 字重墨色（承重）。撤掉任意一个承重信号都算破坏本文。

---

## 5 · 动效

| 动作                     | 时长             | 曲线              | 动的属性            |
| ------------------------ | ---------------- | ----------------- | ------------------- |
| 指示器滑动（切工具）     | 320ms            | `--ease-standard` | **transform**       |
| hover 幽灵块滑动         | 200ms            | 同                | **transform**       |
| 墨色 / 底色变化          | 120ms            | 同                | color · background  |
| 收展 · 桌面（推挤）      | 320ms            | 同                | ⚠ width             |
| <1024 面板落下 / 收起    | Radix Sheet 默认 | 同                | transform · opacity |
| 标签淡出 + 位移          | 120 / 200ms      | 同                | opacity · transform |
| `prefers-reduced-motion` | 0ms              | —                 | 全部瞬时            |

**曲线与时长一律取 `src/constants/motion.ts` / `globals.css @theme` 的 token，壳内不得出现裸 ms。**

⚠ **桌面收展仍动 `width`** —— 这是选 B 结构就付的代价（只有落选的 A 能物理消掉）。缓解：只在收展这一个动作发生，**切工具时不触发**；`will-change` 只在过渡期间挂。

### 5.1 · 交互态（五个，都不靠颜色单打独斗）

| 态            | 表达                                                                                                            |
| ------------- | --------------------------------------------------------------------------------------------------------------- |
| 静息          | 墨 `#454c55`                                                                                                    |
| Hover         | 底色**变暗** 5.5% + 墨色加深 + 图标 `scale(1.08)`；底色由一块**跟着光标在行间滑的幽灵块**承担，不是每行各自开关 |
| Focus-visible | 2px 墨环 inset                                                                                                  |
| 激活          | 白浮片 + 墨竖条 + 600 字重                                                                                      |
| 切换中        | 白浮片 transform 滑向新目标                                                                                     |

**幽灵块细则**：首次进入列表 → 就位后淡入，**不从别处滑过来**；划过当前激活项 → 让位（白浮片赢，不在它下面压暗块）；`pointerType !== 'mouse'` → 不触发（触屏没有 hover，否则会糊一块在那）。

⚠ **收起 40 档：看不见的东西一律不许接住鼠标。** 2026-09-20 owner 真机撞到过——分组标题收起态只 `opacity-0` + `-mt-8`（shadcn 默认写法），仍然 `pointer-events: auto`，那块 28px 高的透明标题**压在上一组最后一项身上**（实测重叠 24px，`elementFromPoint` 命中的是标题不是链接），于是那一项悬停有反馈却点不动。已在 `ui/sidebar.tsx` 的 `SidebarGroupLabel` 上补 `pointer-events-none`；⛔ 不改成 `hidden`——那条 `transition-[margin,opacity]` 就是标题滑走滑回的动画。⚠ 消费方也不许用 `pointer-events-auto` 把它翻回来（tailwind-merge 会让后者赢），`AppSidebar.test.tsx` 有一条扫描用例守着整棵轨。

---

## 6 · 断点行为（一套组件三档）

**只有两档**（owner 2026-08-18 为 <1024 选了方向 M2，不再需要平板第三形态）：

| 断点           | 形态                                                                                            | 依据                                     |
| -------------- | ----------------------------------------------------------------------------------------------- | ---------------------------------------- |
| **≥1024 桌面** | 竖轨 144 ↔ 40，在流中，展开**推挤**主卡                                                         | 宽度充裕，常驻标签的认路价值大于回流成本 |
| **<1024**      | **没有竖轨**。顶栏 44px，中间一颗「当前位置」按钮，点开从顶部落下一张面板（工具 / 去处 / 账户） | 见下方五条                               |

**<1024 为什么不照搬桌面竖轨**（这五条都不是审美问题）：

1. **没有 hover** —— 桌面版一半的交互设计是那块跟随光标的幽灵块，触屏上它不存在。
2. **左上是右手拇指最差可达区** —— 最高频的「切工具」被放在最难够的角落。
3. **宽度比高度贵** —— 44px 竖轨吃掉 375 的 12%，永久占用。
4. **触摸目标要 44px** —— 桌面收到 40/32，手机反而要回到 52/44。**越小的屏轨越宽**，形态本身就不对。
5. **导航层级不同** —— 手机上几乎只有切工具是高频，桌面那种两段平铺是把低频与高频按同权重摆着。

⚠ **断点必须留在 1024。** 别为了让平板拿到桌面竖轨而下调到 768 —— `use-mobile.ts` 记着：
768–1023 挂桌面侧栏会把 studio 内容裁出视口。M2 天然覆盖整个 <1024。

⚠ **<1024 的激活态用墨色填充块，不是桌面的白浮片。** 没有 hover 就不需要反极性那套，
而深色块在 375 上更容易一眼认出。

**<1024 的账户与设置**（2026-09-20 D11 ④ 改写）：导航形态仍是**现有的顶部落下面板**，不新开第二种载体；但账户不再住在面板里 —— 顶栏胶囊右端那颗头像挂的是与桌面**同一颗** `AccountMenu`（语言 / 设置 / 退出登录都在里面）。⛔ 面板里不再有「我」区、也不再有最底那一行「设置」；「我的主页」在「去处」段里，与桌面同一条清单。详见 §8。

落选：M1 底部工具坞（切工具一次点击、落在拇指区，但 owner 2026-08-07 刚删过底部 TabBar）·
M3 边缘手势（与画布 pan、画廊/素材横滑直接冲突）。

**⛔ 不得再出现第二套导航组件。** 桌面与移动的条目清单必须来自**同一个数据源**（`src/constants/navigation.ts`）。历史教训：`AppSidebar` 曾有一个桌面独有的分组，`MobileCollapsedRail` 没有 —— 那几个入口在小屏**不可达**，就是两份手工清单漂移的直接后果。

**导航不再有折叠分组。** 2026-09-17 owner 拍板删除「敬请期待」组：`/studio/enhance` 与 `/studio/analyze` 只是 `ToolPlaceholder` 占位页（能力实际在画布视频节点里），整条路由与入口一并删除；故事板是真实页面，已并入「去处」组。

---

## 7 · 图标（11 个换 7 个，全部来自已装的 `lucide-react`，不加依赖）

| 条目     | 现状       | 改为           | 理由                                                                         |
| -------- | ---------- | -------------- | ---------------------------------------------------------------------------- |
| 画廊     | LayoutGrid | **Images**     | 四个方块在 16px 下和「卡片管理」「画布」撞轮廓                               |
| 提示词   | FileText   | FileText       | 不换                                                                         |
| 素材     | Library    | **Archive**    | 四条竖线原意是书脊，中文「素材」下认不出，16px 像音量条                      |
| 卡片管理 | Layers     | **IdCard**     | Layers 与 3D 的 Box 同属方块堆叠；换成横向矩形分开                           |
| 图像     | Image      | Image          | 不换                                                                         |
| 视频     | Video      | Video          | 不换                                                                         |
| 音频     | Mic        | **AudioLines** | 麦克风是「录音」，但该域还含 TTS 与音乐                                      |
| 3D       | Box        | Box            | 不换 —— 前提是「卡片管理」先让出方块                                         |
| LoRA     | Palette    | **SwatchBook** | Palette 的四个点半径 0.6，16px 下糊成一坨                                    |
| 画布     | Workflow   | **Waypoints**  | Workflow 又是两个方块；节点图用「点 + 连线」更准更轻                         |
| 竞技场   | Swords     | **Trophy**     | 交叉双剑 8 条线段，全列最糊；owner 2026-08-18 定 Trophy（备选 Gavel 未采纳） |

**三条依据（换的不是「好看」）**：① 拆散方块系（现状 4 个方块图标互撞，提案只留 Box）② 拉平笔画密度（收进 3–6 一档）③ 语义直给。

---

## 8 · 账户与设置入口（2026-09-20 D11 ④ 落地，覆盖 09-18 那版）

侧栏上与「人」有关的只剩**一个位置**：最底那一行。

| 位置 | 内容                                                |
| ---- | --------------------------------------------------- |
| 顶端 | 品牌名 + 折叠钮（⛔ 不再有头像）                    |
| 最底 | **账号入口**：头像 + 名字 + ▾（1px 分隔线在它上面） |

- **头像点开账号菜单**，⛔ 不再是个人主页的快捷方式 —— 主页已经是导航里的一项（`SHELL_NAV_GO` 的 `profile`），一件事只留一个家。
- **菜单内容**自上而下：头部（头像 · 名字 · @用户名）→ 分隔线 → 语言（带当前值 + ›，展子菜单）→ 设置 → 分隔线 → 退出登录（红字）。⛔ 不放额度、不放 key 数、⛔ 不挂红点 / 角标 —— 那三样各有各的页（沿用 D3 ④ 收口）。⛔ 不做退出二次确认。
- **⛔ 没有「外观」那一行**：这个应用**没有主题切换机制**（无 next-themes / setTheme / 任何挂 `.dark` 的开关），画板上那一行 owner 2026-09-20 明确本轮不做。⛔ 不许画一颗点了没反应的。
- **语言**与设置 → 偏好走**同一条路**（`src/hooks/use-locale-switch.ts`）：当前值、落点（当前地址 + 原样带回 query）、三档名册都只有一份。⛔ 菜单里不另存一份当前值，否则「界面显示的」和「服务端认得的」会开始漂。当前项靠**选中底色 + 字重 + 右侧圆点 + `aria-current`**四样说话，⛔ 不只靠颜色。
- **收起 40**：底部只剩头像，是一颗**真 `button`**，tooltip「账号」，菜单锚到右侧。⚠ 那一档它是**唯一**的账号入口，键盘必须到得了。
- **退出登录**只有一条路（`src/hooks/use-sign-out.ts`）：Clerk `signOut` + 回首页。`/settings` 的两处导航底部与这颗菜单共用它，⛔ 别再抄第四份。
- **引导锚点 `data-onboarding="apiKey"` 挂在账号触发器上**，⛔ 不在菜单内容里 —— 菜单只在打开后才挂载，挂进去就是死锚点（`OnboardingTooltip` 找不到目标只能把气泡居中）。
- **动效**（D11 ④ 判据板）：菜单开 `--duration-fast` ease-out，淡入 + 上移 4px，origin 贴底行，⛔ 不缩放；关只淡出，⛔ 不位移；语言子菜单同一档，⛔ 不做二级滑入；底行 hover / pressed 只过渡背景色，⛔ 不动字重、⛔ 行不抖。全部带 `motion-reduce:` 降级。浮层那两档动效住在 `ui/dropdown-menu.tsx` 的 `motionPreset`（`zoom` 默认 / `lift` 账号菜单），⛔ 不在调用方用 className 覆盖。
- ⚠ **底色仍走侧栏的反极性**：hover 往暗（`--sidebar-accent`）、pressed / 菜单打开更暗（`--sidebar-accent-strong`）、激活浮片往亮（`--sidebar-active-surface`）。⛔ 账号入口不许自己破例。
- **<1024 同形**：顶栏胶囊右端那颗头像挂**同一颗** `AccountMenu`。⛔ 抽屉里因此不再有「我」区、也不再有最底那一行「设置」—— 两个入口并存就是下一次漂移的起点。

**「我的主页」的地址是静态的 `/u/me`**（owner 2026-09-20 拍板）。那条路由在服务端解析当前用户后**就地渲染**个人主页（`app/[locale]/(main)/u/me/page.tsx`），⛔ **不 redirect 到 `/u/<username>`**。

- ⛔ **不要让壳在渲染时按 `useMyProfile()` 拼地址。** 上一版这么做过（条目带一个 `dynamicHref` 标记 + 解析层，解析不出就不产出这一项），真机在日语档看得见**侧边栏当着用户的面回流一次**——列表过几秒多出一行。运行时依赖已整层删除，⛔ 别再引回来。
- ⭐ **就地渲染而不是跳转，是为了激活态**：地址栏停在 `/u/me`，静态清单认得出这一项就是当前位置；跳到 `/u/<username>` 的话落地 URL 带着用户名，只能把刚删掉的运行时解析接回来。判据上它也与仓库既有的 `me` 约定同构——`/api/users/me/profile` 同样是就地返回当前用户，不跳到 `/api/users/<username>/profile`。
- ⚠ 同一个页面两个地址，所以 `/u/me` 的 `alternates.canonical` 指向 `/u/<username>`。⛔ 不要再叠 `noindex`：canonical 已经回答了重复内容，两个信号同时挂是互相矛盾的。
- ⚠ 段名 `me` 沿用仓库既有约定（`/api/users/me/*`），⛔ 不造 `/u/self` / `/profile` 第二套说法；静态段优先于同级 `[username]`，所以 `me` 已进 `PROFILE.RESERVED_USERNAMES`（2026-09-20 查库确认线上零占用）。
- ⚠ `/u/(.*)` 在 `proxy.ts` 里是公开路由（别人的主页要能匿名看），未登录这一档中间件不管，由该页自己 `redirect` 到登录页——与 `/settings` 同一个口径。

仍未收的遗留：

- **深色岛**（`CardsPageContent` · `AssetSelectorDialog` · `StudioCardSection` · `assets/loading`）各有域级设计理由，**不属于壳**，逐个域另行决策。画布两处归 `canvas-skin.md`。
- **79 处 `dark:` 变体 / 20 文件** —— 失效 ≠ 出错，真机逐页扫出真坏的再改，别盲改。

---

## 9 · 施工切片

| 切片                               | 做什么                                                                                                                                         | 落点                                                            | 验收                                                                          |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| **S1** 材质与令牌                  | 灰底 + 浮卡 + §4 令牌落地；尺寸收到 144 / 44                                                                                                   | `globals.css` · `ui/sidebar.tsx`                                | 真机 canvas 解析复算 §4 全部数值                                              |
| **S2** 指示器与交互态              | 单一滑动指示器 + hover 幽灵块；颜色进过渡属性                                                                                                  | `ui/sidebar.tsx` · `AppSidebar.tsx`                             | `getComputedStyle` 确认 `transition-property` 含 color/background；切工具录屏 |
| **S3** 分段 / 账户行 / 图标 / 文案 | 三段重排；footer 收一行；图标换 7 个；EN 两个标签                                                                                              | `AppSidebar.tsx` · `messages/*.json`                            | footer 高度 167.5 → ≤48；三语零截断                                           |
| **S4** 断点合并 ✅                 | 导航清单抽到 `constants/navigation.ts`；**删掉 `MobileTabBar.tsx` 整个文件**，新建 `MobileShell.tsx`（M2 顶栏切换器）；`AppSidebar` 变桌面专属 | `MobileShell.tsx` · `(main)/layout.tsx` · 契约测试 · `lora.css` | **真机** 1440 / 900 / 430 / 375 逐档目检                                      |

⚠ **S4 的连带清理**：`lora.css` 里有两条给已删 chrome 让的位 —— `bottom: 3rem`（给 2026-08-07
就删掉的底部 TabBar，那天起就是空的）与 `left: 2.75rem`（给本次删掉的左轨）。两条都已拆。
这不是改 LoRA 的视觉答案，是拆掉对已删壳构件的悬空引用。

⚠ **S4 删组件前 `grep` 全部调用方，一次改完，不留兼容层**（Engineering Principle 1）。
⚠ `studio-navigation.contract.test.ts` 的 `NAVIGATION_SOURCES` 硬编码了 `MobileTabBar.tsx` —— S4 必须**在同一个改动里**把它指向新的单一来源，否则测试红。

---

## 10 · 禁改 / 红线

- **不动各业务域皮肤**（`canvas.css` / `lora.css` / `home-v4.css` / `legal.css`）。壳换材质**不构成**任何域返工的理由；改完这四张皮肤的外观必须**零变化**，变了就说明脊柱漏进域里了。
- **不撤激活态的两个承重信号**（墨竖条、字重墨色跃迁）。只留白浮片 = 1.24:1，等于没有激活态。
- **触屏 52px 不得压到 44**。
- **不得恢复第二套导航组件**，也不得在组件里手抄第二份条目清单。
- 改任何颜色前跑 `contrast-check`，**禁目测**。
- 壳内**禁裸 ms 与裸色值**，一律走 token（Hard Rule 1 / 5）。

---

## 11 · 验收

- [ ] §4 令牌表逐项真机 canvas 解析复算，数值一致
- [ ] `getComputedStyle` 确认菜单项 `transition-property` **包含** `color` 与 `background-color`
- [ ] 展开/收起/切工具三段录屏，无跳变、无半帧错位
- [ ] footer = 单行设置，高度 ≤48px；语言与显示名在 `/settings/preferences` 内可达
- [ ] 三语 × 132/144/156 零截断复测（真机字体栈，非原型）
- [ ] **真机** 1440 / 900 / 430 / 375 四档逐档目检 + 截图 —— ⚠ 本机 `resize_window` 对视口无效，此项**必须**在真设备或改用可切视口的手段完成，不得按已验处理
- [ ] 画布与 LoRA 两域外观**零变化**
- [ ] 键盘：Tab 序、focus 环可见、Esc 关抽屉、focus return
- [ ] `prefers-reduced-motion` 下全部瞬时
- [ ] 全量 tsc + 全量 vitest（`full-gate`）

## Last Verified

**2026-08-18 · 设计确认，实现进行中。** 方向与关键切片经 owner 确认（含图标定 Trophy、尺寸定 144/44）。§4 的对比度数值来自 node 确定性计算 + 原型真机复核；§3 的三语宽度来自真机 canvas `measureText`。

**未验，不得按已验处理**：768–1023 与 <768 的**真机渲染**（本机 `resize_window` 返回成功但 `innerWidth` 恒为 1920，视口切不过去）。

**2026-09-18 · §8 与 §6 的账户 / 设置形态**：随 `/settings` 整页与入口收口落地，事实源为 `AppSidebar.tsx` · `MobileShell.tsx` · `ProfileAvatar.tsx`。真机 1440 / 820 / 375 已登录态目检仍未做。
