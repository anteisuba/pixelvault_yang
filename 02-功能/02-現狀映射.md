# 02 · 功能 — 現狀映射（基於當前代碼庫）

> 基準日期：2026-04-22 · 掃描範圍：`src/services/**`, `src/app/api/**`, `src/lib/**`, `src/constants/**`
>
> 總量：**37 個 service · 80 個 API route · 8 個 provider adapter**

---

## 2.1 身份與帳戶體系

- **Code Mapping**
  - Service: `user.service.ts`, `apiKey.service.ts`, `civitai-token.service.ts`
  - Route: `/api/users/[username]`, `/api/users/me/*`, `/api/api-keys/*`, `/api/civitai-token`, `/api/webhooks/clerk`
  - Constants: `api-keys.ts`, `config.ts`
  - Test: `api-keys/route.test.ts`, `api-keys/[id]/route.test.ts`, `users/[username]/route.test.ts`, `users/me/profile/route.test.ts`, `users/me/avatar/route.test.ts`, `users/me/banner/route.test.ts`（service 無測試）
- **Current Status**：已存在
- **Current Problem**：3 個 service 無單測；Civitai token 已改為 AES-256-GCM 加密存儲但缺少加解密/輪換測試；Clerk webhook 5 分鐘簽名驗證可靠，但目前僅處理 `user.created`，無失敗重放/補償
- **Next Breakdown**：Clerk webhook · User CRUD · ApiKey CRUD+verify · CivitaiToken
- **Dependencies**：Clerk、Prisma、webhook signer
- **Risks**：webhook 失敗導致用戶同步漂移；`CIVITAI_TOKEN_SECRET` 配置不當導致 token 不可解或洩漏
- **Acceptance Criteria**：用戶創建→登入→Key 驗證→Civitai 連通全鏈路 E2E 通過

---

## 2.2 圖像生成能力

- **Code Mapping**
  - Service: `generate-image`, `generation`, `studio-generate`, `image-edit`, `image-decompose`, `image-analysis`
  - Route: `/api/generate`, `/api/studio/generate`, `/api/image/*`, `/api/images`, `/api/generations/*`
  - Constants: `models.ts`, `provider-capabilities.ts`, `studio.ts`
  - Test: `generate-image.service.test.ts` · route test for generate, studio/generate, studio/select-winner, image/analyze, images, generations/[id]/\*
- **Current Status**：已存在（8 provider 適配）
- **2026-04-17 更新**：`generateImageForUser` 管道拆分為 3 階段函數（callProviderWithFallback + persistGeneratedImage + 薄編排器）；新增 image-transform 全棧（5 維度 schema + style/pose 兩維已實現 + service + route + hook）；discriminated union 防止 fallback 雙重持久化
- **Current Problem**：`generation.service` 與 `generate-image.service` 職責邊界模糊；`image-edit` 僅部分 provider 支援；240s 超時對某些 provider 不足
- **Next Breakdown**：T2I / I2I / Edit / Decompose / Analysis / Studio 編排
- **Dependencies**：`providers/**`、R2、prompt-guard、llm-output-validator、retry、circuit-breaker
- **Risks**：長任務超時；跨 provider 降級策略不一致
- **Acceptance Criteria**：6 個入口 × 8 個 provider 組合可通 · 失敗路徑可觀測

---

## 2.3 視頻生成能力

- **Code Mapping**
  - Service: `generate-video`, `video-pipeline`, `video-generation-validation`
  - Route: `/api/generate-video`, `/api/generate-video/status`, `/api/generate-long-video`, `/api/generate-long-video/status`, `/api/generate-long-video/retry`, `/api/generate-long-video/cancel`
  - Constants: `video-options.ts`, `video-model-capabilities.ts`
  - Test: `video-generation-validation.service.test.ts` · `generate-video/route.test.ts`, `generate-video/status/route.test.ts`, `video-model-capabilities.test.ts`
- **Current Status**：已存在（Replicate / FAL 雙支持）
- **Current Problem**：`video-pipeline` 無測試；長片分段恢復策略未驗證；成本控制無上限
- **Next Breakdown**：短片生成 · 長片 Pipeline · 狀態輪詢 · 取消/重試
- **Dependencies**：Replicate、FAL、R2、retry、circuit-breaker
- **Risks**：長片成本失控；pipeline 失敗無恢復點
- **Acceptance Criteria**：短片+長片完整生成 · 取消/重試生效 · 成本單次可預估

---

## 2.4 音頻生成能力

- **Code Mapping**
  - Service: `generate-audio.service`, `fish-audio-voice.service`
  - Route: `/api/generate-audio`, `/api/generate-audio/status`, `/api/voices/*`
  - Constants: `audio-options.ts`
  - Test: `generate-audio.service.test.ts`, `generate-audio/status/route.test.ts`
- **Current Status**：已存在
- **2026-04-23 更新**：async audio submit/status 已升級到 outbox-backed server-owned contract；`POST /api/generate-audio` 只持久化 `generationJob + executionOutbox + route identity` 並返回 `jobId`，首次 `/api/generate-audio/status` 輪詢會由服務端 claim/dispatch outbox，若 `generationJob.externalRequestId` 尚未補齊，會優先從 outbox result 自愈；async free-tier finalize 也會正確回寫 `Generation.isFreeGeneration`
- **Current Problem**：單一 provider（Fish Audio / FAL）；即使有 execution outbox，仍保留最小非阻塞殘餘窗口：若 provider 已接受請求，但在 queue result 寫回 outbox 之前進程中斷，仍需要後續 reconciliation / 更強的 provider 能力（idempotency、callback、cancel）
- **Next Breakdown**：TTS · 音色選擇 · 樣本試聽 · 狀態查詢
- **Dependencies**：Fish Audio API、R2
- **Risks**：provider 單點；音色列表大時加載延遲；submit-time 最後一小段仍需後續 reconciliation / 恢復策略
- **Acceptance Criteria**：音色分頁+試聽+TTS 閉環 · 超時可控

---

## 2.5 Character Card 體系

- **Code Mapping**
  - Service: `character-card`, `character-card.mapper`, `character-refine`, `character-scoring`, `background-card`, `style-card`, `card-recipe`, `recipe-compiler`
  - Route: `/api/character-cards/*`, `/api/background-cards/*`, `/api/style-cards/*`, `/api/card-recipes/*`
  - Constants: `character-card.ts`, `card-types.ts`, `character-prompt-templates.ts`
  - Test: `lib/card-management.test.ts` · 無 service / route 測試
- **Current Status**：已存在（LLM 驅動屬性提取+精化+評分）
- **Current Problem**：8 個 service 皆無單測；character-refine 輸出結構驗證不完整；Recipe 組合空間大未 golden set
- **Next Breakdown**：Character CRUD · Refine · Scoring · Background · Style · Recipe 編譯
- **Dependencies**：LLM（Gemini/OpenAI）、prompt-guard、llm-output-validator
- **Risks**：LLM 結構化輸出失敗 → 卡片屬性缺；Recipe 組合爆炸
- **Acceptance Criteria**：4 類卡片 CRUD + Recipe 編譯成功率 ≥ 90%

---

## 2.6 Storyboard / 敘事

- **Code Mapping**
  - Service: `story.service`, `project.service`
  - Route: `/api/projects/*`, `/api/stories/*`（含 reorder, history）
  - Test: route test for `stories/route`, `stories/[id]`, `projects/[id]`（service 無測試）
- **Current Status**：已存在
- **Current Problem**：service 無測試；4 種敘事音調量化驗證缺；長篇 token 預算無保護
- **Next Breakdown**：Project CRUD · Story CRUD · Narrative 生成 · 頁面重排
- **Dependencies**：LLM text、character-card、generations history
- **Risks**：story 與 project 狀態一致性；token 超限截斷
- **Acceptance Criteria**：project→story→pages→導出鏈路通過

---

## 2.7 社群互動

- **Code Mapping**
  - Service: `follow.service`, `like.service`, `collection.service`
  - Route: `/api/follows`, `/api/likes`, `/api/collections/*`
  - Test: ❌（service 與 route 均無）
- **Current Status**：已存在
- **Current Problem**：無任何測試；重複 like/follow 幂等性未驗證；collection 批量操作並發未測
- **Next Breakdown**：Follow · Like · Collection · Gallery 可見性
- **Dependencies**：user.service、generations
- **Risks**：並發寫入衝突；計數漂移
- **Acceptance Criteria**：關係/計數/可見性三者一致 · 幂等保證

---

## 2.8 Arena 競技

- **Code Mapping**
  - Service: `arena.service`
  - Route: `/api/arena/matches/*`, `/api/arena/leaderboard`, `/api/arena/personal-stats`, `/api/arena/history`
  - Constants: `config.ts`（`ARENA.INITIAL_ELO` 等）
  - Test: 6 個 route.test.ts（matches/[id]/, vote, entries, leaderboard, personal-stats, history）
- **Current Status**：已存在（route 測試覆蓋最完整）
- **Current Problem**：service 無單測；ELO 算法邊界未測；leaderboard 實時性策略未定
- **Next Breakdown**：Match 創建 · Vote · History · Leaderboard · PersonalStats
- **Dependencies**：generation service、user.service、ELO 配置
- **Risks**：並發投票競爭；leaderboard 讀放大
- **Acceptance Criteria**：完整對戰週期 · ELO 正確 · 排行穩定

---

## 2.9 Prompt & LLM 輔助

- **Code Mapping**
  - Service: `prompt-assistant`, `prompt-enhance`, `prompt-feedback`, `llm-text`
  - Route: `/api/prompt/assistant`, `/api/prompt/enhance`, `/api/prompt/feedback`
  - Constants: `prompt-presets.ts`, `model-strengths.ts`
  - Test: `prompt/enhance/route.test.ts`, `lib/prompt-guard.test.ts`（service 無測試）
- **Current Status**：已存在（enhance 5 種風格）
- **Current Problem**：service 無測試；enhance 無 golden set；assistant 多輪上下文無窗口保護
- **Next Breakdown**：Enhance · Assistant · Feedback · LLM Text
- **Dependencies**：Gemini、OpenAI、prompt-guard、llm-output-validator
- **Risks**：LLM 成本；assistant 上下文溢出
- **Acceptance Criteria**：5 風格可用 · feedback 閉環 · LLM 失敗降級可觀測

---

## 2.10 模型治理

- **Code Mapping**
  - Service: `model-config.service`, `model-health.service`, `providers/**`（8 adapter）
  - Route: `/api/models`, `/api/models/health`, `/api/health`, `/api/health/providers`, `/api/admin/models/*`
  - Constants: `models.ts`, `provider-capabilities.ts`, `providers.ts`
  - Test: `provider-capabilities.test.ts`, `video-model-capabilities.test.ts`（adapter 無測試）
- **Current Status**：已存在（HF/OpenAI/Gemini/NovelAI/VolcEngine/Replicate/FAL/FishAudio）
- **Current Problem**：8 個 adapter 無單測；health 緩存 TTL 已配置化（300s）但 refresh/admin 路徑無測試；provider down 時降級路徑仍依賴各 adapter 各自實作
- **Next Breakdown**：Model 註冊 · Health 監測 · Provider 適配 · Admin 操作
- **Dependencies**：circuit-breaker、retry、logger
- **Risks**：provider down 時降級路徑不完整
- **Acceptance Criteria**：8 個 provider 全監測 · 降級路徑可觀測

---

## 2.11 LoRA 訓練

- **Code Mapping**
  - Service: `lora-training.service`
  - Route: `/api/lora-training`, `/api/lora-training/[id]/status`
  - Constants: `config.ts`（`LORA_TRAINING`）
  - Test: ❌
- **Current Status**：已存在（Replicate/FAL 雙支持）
- **Current Problem**：無任何測試；`.tar→.safetensors` 提取失敗僅退回 tar 上傳；訓練輪詢/超時/取消缺少端到端驗證
- **Next Breakdown**：提交訓練 · 狀態輪詢 · 取消 · 模型落地
- **Dependencies**：Replicate/FAL、R2
- **Risks**：訓練失敗無恢復；成本失控
- **Acceptance Criteria**：從數據 → 可用 LoRA 鏈路通

---

## 2.12 計費與用量

- **Code Mapping**
  - Service: `usage.service`, `generation-feedback.service`
  - Route: `/api/usage-summary`, `/api/generation/feedback`
  - Constants: `config.ts`（`API_USAGE`, `FREE_TIER`）
  - Test: `usage-summary/route.test.ts` · `usage.service.test.ts`(6) · `free-tier-boundary.test.ts`(7)
- **Current Status**：已存在（GenerationJob + ApiUsageLedger 並行記錄；當前為 `FREE_TIER + requestCount`，非 `User.credits` 餘額）
- **2026-04-17 更新**：usage.service 新增 6 個單測（createJob/completeJob/failJob/createEntry/attachEntry）；FreeTier 並發邊界 7 個測試（19<20 allow / 20>=20 reject / 並發競態 / BYOK bypass）
- **Current Problem**：~~扣費並發競爭未測~~ ✅；~~free tier 日額限制邊界未覆蓋~~ ✅；ledger 漂移無校正任務；前端 `credits` 文案與 `requestCount` 成本語義並存
- **Next Breakdown**：請求成本語義整理 · Usage 查詢 · Ledger · FreeTier 限額
- **Dependencies**：Prisma transaction、user.service、generations
- **Risks**：`requestCount` / `credits` 語義漂移；ledger/job 漂移
- **Acceptance Criteria**：並發 requestCount 一致 · 日報表準確

---

## 2.13 存儲與資產

- **Code Mapping**
  - Service: `storage/r2.ts`（Cloudflare R2 wrapper）
  - Route: `/api/og`, `/api/image/proxy`, `app/sitemap.ts`, `app/robots.ts`
  - Test: `storage/r2.test.ts`(15)
- **Current Status**：已存在（9+ service 依賴 R2）
- **2026-04-17 更新**：R2 wrapper 15 個單測全覆蓋（generateStorageKey 6 + fetchAsBuffer 4 + uploadToR2 1 + streamUploadToR2 3 + deleteFromR2 1）
- **Current Problem**：~~無 wrapper 測試~~ ✅；永久歸檔策略未定；R2 失敗無降級/備援
- **Next Breakdown**：R2 wrapper · OG 生成 · Sitemap · Robots · 歸檔策略
- **Dependencies**：Cloudflare R2、@vercel/og
- **Risks**：R2 單點；@vercel/og 依賴複雜
- **Acceptance Criteria**：上傳/讀取/代理 + OG + Sitemap 三件正常

---

## 2.14 基礎設施

- **Code Mapping**
  - Lib: `logger.ts`, `with-retry.ts`, `circuit-breaker.ts`, `prompt-guard.ts`, `llm-output-validator.ts`, `api-route-factory.ts`, `database-utils.ts`, `rate-limit.ts`, `errors.ts`, `toast.ts`, `design-tokens.ts`
  - Webhook: `/api/webhooks/clerk`
  - Test: `circuit-breaker.test.ts`, `prompt-guard.test.ts`, `api-route-factory.test.ts`, `database-utils.test.ts`, `errors.test.ts`, `utils.test.ts`, `with-retry.test.ts`, `studio-history.test.ts`, `studio-remix.test.ts`（9 個 lib test）
- **Current Status**：已存在
- **2026-04-17 更新**：`with-retry` 新增 9 個單測（5xx/429 retry、4xx no-retry、maxAttempts、backoff+jitter、custom isRetryable）；`NotImplementedError` 新增至 errors.ts（501）；`toast.ts` 新增（封裝 sonner）；design-tokens.ts 新增（品牌色 hex 常量）
- **Current Problem**：~~`with-retry` 無單測~~ ✅；`llm-output-validator` 無單測；請求入口治理集中於 `api-route-factory`，當前無顯式 `middleware.ts`
- **Next Breakdown**：Logger · Retry · CircuitBreaker · Guard · Validator · RouteFactory · Webhooks
- **Dependencies**：全項目
- **Risks**：factory 內嵌邏輯難以集中調整；retry 默認參數不透明
- **Acceptance Criteria**：所有工具核心路徑單測覆蓋 · 錯誤分類可觀測
