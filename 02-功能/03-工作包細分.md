# 02 · 功能 — 工作包細分（L3）

> 基準日期：2026-04-15 · 覆蓋：2.2 圖像 / 2.3 視頻 / 2.9 Prompt&LLM / 2.12 計費 / 2.13 存儲 / 2.14 基礎設施 + 2.5 風格一致性切片
>
> 全部工作包**嚴格基於當前代碼庫**，只針對已存在/部分存在且適合近期推進的功能。
>
> 狀態說明：本文件是**近期候選工作包**，不等於當前已上線功能清單；判斷現況請先回看 `02-現狀映射.md`。
>
> **掃描修正**（覆蓋 `02-現狀映射.md` 誤判）：
>
> - 當前**無顯式 `middleware.ts`**；請求入口治理集中在 `api-route-factory.ts`
> - `User.credits` 已自 schema 移除；當前成本語義以 `FREE_TIER + requestCount + ApiUsageLedger` 為主
> - `generate-image.service.ts` / `video-pipeline.service.ts` 仍是大文件，改動需嚴控範圍
> - `generate-audio.service` 帶 `TODO: job lifecycle is incomplete` — 影響 2.4，不在本輪範圍

---

## 2.2 圖像生成能力

### WP-Image-01 · generation.service / generate-image.service 職責分層文檔化

- **Goal**：釐清 `generation.service`（DB 層）與 `generate-image.service`（業務層）邊界，避免混用
- **Current Code Touchpoints**：`src/services/generation.service.ts`（DB 持久化、`createGenerationJob` L52-80、`$transaction` L238-263 僅用於 isWinner）· `src/services/generate-image.service.ts`（414 行業務邏輯、withRetry+breaker L293-300）· `studio-generate.service.ts`（代理到 generate-image）
- **Files to Modify**：兩個 service 頂部加 JSDoc 章節 · `CLAUDE.md`（加功能層級約定）
- **Files to Add**：`src/services/CLAUDE.md`（若不存在）記錄「DB 層 vs 業務層」分工
- **Concrete Tasks**
  1. 在每個 service 頂部加 `@layer` 標註（`@layer db` / `@layer business`）
  2. 畫出調用圖：`route → generate-image → generation (DB)`
  3. grep 確認沒有 route 直接 import `generation.service`（若有記錄白名單）
  4. 補 `generate-image.service.test.ts` 的「透過 generation.service 持久化」路徑測試
- **Dependencies**：無
- **Risks**：重構方向需與團隊對齊；若存在白名單調用需逐一確認
- **Acceptance Criteria**：`services/CLAUDE.md` 有分層圖 · JSDoc 標註齊全 · 違規調用有 lint/grep 檢查
- **Priority**：P2
- **Effort**：S
- **Out of Scope**：實際合併/拆分 service

---

### WP-Image-02 · image-edit FAL 硬編碼文檔化 + 擴展點預留

- **Goal**：`image-edit.service` L121 hardcoded `provider: 'fal'`，需明確「FAL-only 限制」並預留擴展點
- **Current Code Touchpoints**：`src/services/image-edit.service.ts`（125 行，僅支持 upscale/remove-bg 經 FAL）· `/api/image/edit/route.ts` · `providers/fal.adapter.ts`
- **Files to Modify**：`image-edit.service.ts`（抽取 provider 判斷到函數 `resolveEditProvider(editType)`；目前只返回 'fal'）· `constants/provider-capabilities.ts`（補 edit 能力矩陣）
- **Files to Add**：`src/services/image-edit.service.test.ts`
- **Concrete Tasks**
  1. 在 constants 加 `IMAGE_EDIT_CAPABILITIES = { upscale: ['fal'], removeBg: ['fal'] }`
  2. 重構 service 從 hardcoded 改為查能力矩陣（單一 provider 時行為不變）
  3. 補測試：upscale/remove-bg 正常路徑 + FAL 不可用時錯誤碼
  4. 在 service JSDoc 明確聲明「當前僅 FAL」
- **Dependencies**：無
- **Risks**：抽取後若其他 edit 端點誤讀矩陣會破壞行為
- **Acceptance Criteria**：無 hardcoded `'fal'` 字串在業務邏輯 · 測試覆蓋 2 個 edit type · 能力矩陣可讀
- **Priority**：P2
- **Effort**：S
- **Out of Scope**：引入第二個 edit provider（屬於新功能）

---

### WP-Image-03 · 8 個 Provider Adapter 單測骨架

- **Goal**：所有 adapter（5k-21k B 各自）全無單測，建立通用測試骨架 + 覆蓋 3 個最熱門（FAL、Replicate、Gemini）
- **Current Code Touchpoints**：`src/services/providers/{openai,novelai,fal,replicate,gemini,huggingface,volcengine,fish-audio}.adapter.ts` · `providers/registry.ts` · `provider-capabilities.test.ts`（已有 constants 測試，非 adapter 測試）
- **Files to Modify**：無
- **Files to Add**：
  - `src/services/providers/__tests__/adapter-contract.ts`（共用契約斷言）
  - `src/services/providers/fal.adapter.test.ts`
  - `src/services/providers/replicate.adapter.test.ts`
  - `src/services/providers/gemini.adapter.test.ts`
- **Concrete Tasks**
  1. 定義 adapter 通用契約：`generateImage` 返回結構、錯誤映射到 `ProviderError`、timeout 處理
  2. 用 `vi.mock` fetch 覆蓋 FAL queue submit、Replicate predictions、Gemini generateContent
  3. 每個 adapter 至少：正常 / 4xx / 5xx / timeout 四個路徑
  4. 驗證 breaker + retry 在 adapter 層無重複包裝
- **Dependencies**：`api-helpers.ts` 的 fetch mock 模式
- **Risks**：真實 provider 響應結構複雜，mock 容易失真
- **Acceptance Criteria**：3 個 adapter 每個 ≥ 4 個測試 · contract 可複用於剩餘 5 個
- **Priority**：P1
- **Effort**：M
- **Out of Scope**：為剩下 5 個 adapter 補齊測試（留下一輪）

---

### WP-Image-04 · 240s Timeout 與降級可觀測性

- **Goal**：`maxDuration = 240` 配置在 Next.js route segment（`generate/route.ts:7`, `studio/generate/route.ts:7`, `generate-video/route.ts:6`），超時行為對 client / logs 未定，需加可觀測性
- **Current Code Touchpoints**：3 個 route 的 `export const maxDuration = 240` · `generate-image.service.ts` L293-300（withRetry + breaker）· `lib/logger.ts`
- **Files to Modify**：3 個 route（加入 Promise.race 與 timeout 日誌）· `lib/with-retry.ts`（可選：expose attempts 日誌）
- **Files to Add**：無
- **Concrete Tasks**
  1. route 層加 `console.time` / logger.info 記錄 provider 耗時分布
  2. 在接近 maxDuration 時（e.g. 220s）主動返回 `GenerationTimeoutError` 而非讓平台 500
  3. logger 結構化字段：`{ provider, attempt, elapsedMs, outcome }`
  4. 驗證 circuit-breaker 在 240s 超時後是否正確計入失敗
- **Dependencies**：logger 升級（見 WP-Infra-01 可選並行）
- **Risks**：220s vs 240s 的邊界值取決於 Vercel 平台冷啟動；跨 provider 耗時基線未知
- **Acceptance Criteria**：timeout 結構化日誌 · 超時拋 `ProviderError(timeout=true)` 而非 500 · CI 無回歸
- **Priority**：P1
- **Effort**：M
- **Out of Scope**：將 maxDuration 改為可配置（需 Vercel plan 升級）

---

## 2.3 視頻生成能力

### WP-Video-01 · video-pipeline.service 單測補齊（933 行最大缺口）

- **Goal**：`video-pipeline.service.ts` 是項目第二大 service（933 行，僅次 generate-image），完全無測試；長片分段計算 L75-84 是核心風險點
- **Current Code Touchpoints**：`src/services/video-pipeline.service.ts`（`createLongVideoPipeline`、`checkPipelineStatus` L51、`cancelPipeline` L568、`retryPipelineClip` L445）· Prisma `VideoPipeline` + `PipelineClip` 表
- **Files to Modify**：無
- **Files to Add**：`src/services/video-pipeline.service.test.ts`
- **Concrete Tasks**
  1. 測試 clip 數計算：`firstClipDuration + remainingDuration / extensionClipDuration`（邊界：整除 / 餘數 / 超長）
  2. 測試 `checkPipelineStatus` 所有 clip COMPLETED/FAILED/RUNNING 組合的聚合狀態
  3. 測試 `cancelPipeline` 只能取消 RUNNING 狀態
  4. 測試 `retryPipelineClip` 只重試 FAILED clip，不影響其他
  5. 驗證 Prisma transaction 建立 pipeline + N 個 clips 的原子性
- **Dependencies**：Prisma test 模式（需確認已有測試 DB 策略）
- **Risks**：933 行單測工程量大；DB mock 深度取決於項目策略（真 DB vs mock）
- **Acceptance Criteria**：覆蓋 4 個核心函數各 3 個路徑 · 計算邏輯 100% branch coverage
- **Priority**：P0
- **Effort**：L
- **Out of Scope**：重構 pipeline 狀態機

---

### WP-Video-02 · 長視頻 Clip 失敗恢復與 Cancel/Retry 端到端

- **Goal**：`/api/generate-long-video/{cancel,retry}/route.ts` 已實現但無測試；需端到端驗證失敗 → 取消 → 重試循環
- **Current Code Touchpoints**：`src/app/api/generate-long-video/cancel/route.ts` · `/retry/route.ts` · `video-pipeline.service.ts` L445/L568
- **Files to Modify**：兩個 route（若缺 `auth` 邊界則補）
- **Files to Add**：
  - `src/app/api/generate-long-video/cancel/route.test.ts`
  - `src/app/api/generate-long-video/retry/route.test.ts`
  - `e2e/long-video-recovery.spec.ts`（E2E 可選）
- **Concrete Tasks**
  1. cancel：運行中 pipeline → 狀態轉 CANCELLED；已完成 pipeline → 400；他人 pipeline → 403
  2. retry：FAILED clip → 重新提交到 provider queue；COMPLETED clip → 400；跨用戶 → 403
  3. 驗證 retry 不重複扣費（見 WP-Usage-02）
  4. E2E 可選：mock provider 首次失敗 → 客戶端觸發 retry → 恢復
- **Dependencies**：video-pipeline 單測（WP-Video-01）· calibration provider mock
- **Risks**：retry 與計費互動邊界需對齊（目前無 $transaction）
- **Acceptance Criteria**：兩個 route 有 auth/validation/業務三層測試 · retry 不重複扣費
- **Priority**：P1
- **Effort**：M
- **Out of Scope**：將 client polling 改為 webhook（屬架構變動，非近期）

---

### WP-Video-03 · 視頻 Validation 規則矩陣補測

- **Goal**：`video-generation-validation.service` 有基礎測試，但模型能力矩陣（duration / resolution / aspect ratio）各模型分支未覆蓋
- **Current Code Touchpoints**：`src/services/video-generation-validation.service.ts`（L21-80 三類驗證）· `src/constants/video-model-capabilities.ts` · 既有 `video-generation-validation.service.test.ts`
- **Files to Modify**：既有測試擴充
- **Files to Add**：無
- **Concrete Tasks**
  1. 對 Kling / MiniMax / Runway / SeedDance 各拿一個模型跑矩陣
  2. 邊界：duration 不在 `supportedDurations`、resolution 不支持、需參考圖時缺失
  3. 無效 modelId / 非 VIDEO 類型模型
- **Dependencies**：無
- **Risks**：模型能力矩陣變更會讓測試失效，需同步
- **Acceptance Criteria**：4 個 provider 模型各 4 個驗證路徑測試 green
- **Priority**：P1
- **Effort**：S
- **Out of Scope**：改變驗證算法

---

## 2.9 Prompt & LLM 輔助

### WP-Prompt-01 · 4 個 Prompt/LLM Service 單測骨架

- **Goal**：`prompt-enhance` / `prompt-assistant` / `prompt-feedback` / `llm-text` 四個 service 皆無測試（僅 `/prompt/enhance/route.test.ts` 有 route 測試）
- **Current Code Touchpoints**：`src/services/prompt-enhance.service.ts`（76 行）· `prompt-assistant.service.ts`（147 行）· `prompt-feedback.service.ts`（60+ 行）· `llm-text.service.ts`（150+ 行）
- **Files to Modify**：無
- **Files to Add**：
  - `src/services/prompt-enhance.service.test.ts`
  - `src/services/prompt-assistant.service.test.ts`
  - `src/services/prompt-feedback.service.test.ts`
  - `src/services/llm-text.service.test.ts`
- **Concrete Tasks**
  1. mock `resolveLlmTextRoute` / `llmTextCompletion`
  2. 測試 LLM fallback 鏈（Gemini → OpenAI → VolcEngine）在各種錯誤下的跳轉
  3. 用戶未綁任何 LLM key → 錯誤訊息明確
  4. 驗證 `validateLlmPromptOutput` 被調用（prompt-enhance + prompt-assistant）
  5. prompt-feedback 的 Zod 結構化輸出 schema 驗證
- **Dependencies**：無
- **Risks**：LLM mock 需模擬 JSON 回傳的多種變異
- **Acceptance Criteria**：4 個 service 每個正常 + 至少 2 個錯誤路徑 · fallback 鏈有測試
- **Priority**：P1
- **Effort**：M
- **Out of Scope**：新增 LLM provider

---

### WP-Prompt-02 · 5 種 Enhance 風格 Golden Set

- **Goal**：enhance 5 風格（detailed/artistic/photorealistic/anime/lora）無量化驗證；建立 golden set 回歸防護
- **Current Code Touchpoints**：`src/services/prompt-enhance.service.ts` L14-24（5 風格定義）· `constants/model-strengths.ts`（ADAPTER_PROMPT_HINTS 7 個 adapter）
- **Files to Modify**：無
- **Files to Add**：
  - `src/services/__fixtures__/prompt-enhance-golden.json`（5 風格 × 3-5 輸入 golden pairs）
  - `src/services/prompt-enhance.service.golden.test.ts`
- **Concrete Tasks**
  1. 收集 5 風格代表性輸入（各 3-5 條）
  2. 實際調用 LLM 生成 baseline 輸出，人工審核 + 固化為 fixture
  3. 測試：mock LLM 返回 fixture → 斷言 service 不做額外變形
  4. 對 lora 風格（NovelAI tag-based）驗證格式（逗號分隔 Danbooru tags）
- **Dependencies**：LLM access（建立 baseline 時）· WP-Prompt-01 的 mock 框架
- **Risks**：baseline 主觀；LLM 模型版本變化需重新 baseline
- **Acceptance Criteria**：5 風格各有 golden fixture · 每次 CI 回歸跑通
- **Priority**：P2
- **Effort**：M
- **Out of Scope**：調整 5 種風格的具體提示內容

---

### WP-Prompt-03 · Prompt-Assistant 多輪上下文窗口保護

- **Goal**：`prompt-assistant.service` 用 `flattenConversation` 支援多輪，但無顯式 token 計數，長對話可能溢出 LLM context
- **Current Code Touchpoints**：`src/services/prompt-assistant.service.ts` L62-87（flattenConversation）· L91-102（output extraction）
- **Files to Modify**：`prompt-assistant.service.ts`（加 `MAX_HISTORY_MESSAGES` 或 `MAX_HISTORY_CHARS` 守護）· `constants/prompt-presets.ts` 或 `constants/config.ts`（添加窗口常量）
- **Files to Add**：無（測試在 WP-Prompt-01 裡追加）
- **Concrete Tasks**
  1. 定義保守窗口：`MAX_HISTORY_CHARS = 12000`（約 3k tokens）
  2. 實作：超過時保留首條 system + 最後 N 條 user/assistant
  3. 測試：50 條對話輸入 → 只保留有效窗口
  4. 日誌：截斷時 `logger.info` 記錄
- **Dependencies**：WP-Prompt-01 的 mock 框架
- **Risks**：截斷策略可能丟失關鍵上下文；字符數 ≠ tokens 需 buffer
- **Acceptance Criteria**：長對話不超限 · 有截斷日誌 · 測試覆蓋
- **Priority**：P1
- **Effort**：S
- **Out of Scope**：引入真實 tokenizer（tiktoken 等）

---

### WP-Prompt-04 · LLM-Output-Validator 擴展與測試

- **Goal**：`llm-output-validator` 已覆蓋 meta-commentary + system prompt leak，但無單測；建議補測並擴展敏感詞檢查
- **Current Code Touchpoints**：`src/lib/llm-output-validator.ts`（L34-40 meta patterns · L43-48 leak patterns · 長度截斷 L75-77）· 消費者：`prompt-enhance.service` 和 `prompt-assistant.service`
- **Files to Modify**：`llm-output-validator.ts`（加敏感詞 list + 可選嚴格模式）
- **Files to Add**：`src/lib/llm-output-validator.test.ts`
- **Concrete Tasks**
  1. 補測當前 3 種驗證：meta / leak / 長度
  2. 加 minimal 敏感詞檢測（項目已有禁詞則從 constants 引入）
  3. 測試：fallback 行為（驗證失敗時的 fallback prompt 選擇）
  4. 與 prompt-guard（input 側）協同測試：輸入被 guard 攔 → 不會到達 validator
- **Dependencies**：`prompt-guard.test.ts` 模式可參考
- **Risks**：敏感詞 list 過度過濾影響合法用戶；需要 false-positive 監控
- **Acceptance Criteria**：有單測 · fallback 路徑可觀測 · false-positive rate < 1% in golden set
- **Priority**：P1
- **Effort**：S
- **Out of Scope**：Perspective API / 外部 moderation 服務

---

## 2.12 計費與用量

### WP-Usage-01 · GenerationJob ↔ ApiUsageLedger 原子化（P0 關鍵）

- **Goal**：目前雙寫無 `$transaction`（agent 掃描確認），存在 job COMPLETED 但 ledger 缺失的漂移風險
- **Current Code Touchpoints**：`src/services/generation.service.ts` L52-80（createGenerationJob）· L238-263（現有 $transaction 僅用於 isWinner）· `src/services/usage.service.ts` · Prisma `GenerationJob` + `ApiUsageLedger` models（1:N 關係）
- **Files to Modify**：`generation.service.ts`（將 job 完成 + ledger 寫入包入 `db.$transaction`）· `usage.service.ts`（如有重複扣費邏輯需對齊）
- **Files to Add**：`src/services/generation.service.test.ts`（新增原子性測試）
- **Concrete Tasks**
  1. 定位所有「job 狀態變更 + ledger 寫入」的位置
  2. 包入 `db.$transaction([...])` 或 interactive transaction
  3. 測試：ledger 寫入失敗 → job 狀態回滾（或不前進）
  4. 測試：job 寫入失敗 → ledger 不產生
  5. 加漂移指標：`logger.warn` 檢測 job.status=COMPLETED 但關聯 ledger 缺失
- **Dependencies**：Prisma test transaction 策略
- **Risks**：transaction 範圍過大會拖慢生成；長事務 + provider 回調可能超時
- **Acceptance Criteria**：雙寫原子化 · 有回滾測試 · 漂移日誌可觀測
- **Priority**：P0
- **Effort**：M
- **Out of Scope**：加入全局 event sourcing / outbox pattern

---

### WP-Usage-02 · FreeTier 日限額並發邊界測試

- **Goal**：`FREE_TIER.DAILY_LIMIT = 20`（config.ts L274-280），並發扣費是否能超額未驗證
- **Current Code Touchpoints**：`src/constants/config.ts` L274-280（FREE_TIER）· `src/services/generation.service.ts`（`getFreeGenerationCountToday`）· `/api/generate/route.ts` 前置檢查
- **Files to Modify**：若發現超額則加原子 counter 或 DB 行鎖 · 可選 Redis counter
- **Files to Add**：`src/services/generation.service.test.ts`（追加並發場景）· `e2e/free-tier-concurrency.spec.ts`（可選）
- **Concrete Tasks**
  1. 寫測試：同一用戶 25 個並發生成請求 → 最終通過數 ≤ 20
  2. 若超額 → 加 `SELECT ... FOR UPDATE` 或 Redis INCR 門檻檢查
  3. 20 個並發正好達限 → 全部成功
  4. 驗證：跨時區的 daily 邊界（UTC vs 用戶時區）
- **Dependencies**：WP-Usage-01 的 transaction 基礎設施
- **Risks**：行鎖擴大會降低吞吐；時區策略需跟 product 對齊
- **Acceptance Criteria**：並發 25 → 通過 20 嚴格一致 · 時區邊界文檔化
- **Priority**：P0
- **Effort**：M
- **Out of Scope**：引入持久化 credits 餘額模型（目前系統無）

---

### WP-Usage-03 · Ledger 漂移檢測任務

- **Goal**：即使加了 transaction（WP-Usage-01），也需要定期巡檢檢測歷史漂移與新漂移
- **Current Code Touchpoints**：Prisma `GenerationJob.apiUsageLedger`（1:N）· `usage.service.ts`
- **Files to Modify**：無
- **Files to Add**：
  - `src/services/usage-audit.service.ts`（新增巡檢邏輯）
  - `src/app/api/admin/usage-audit/route.ts`（admin 觸發，已有 admin 目錄）
  - `src/services/usage-audit.service.test.ts`
- **Concrete Tasks**
  1. 查詢：所有 `GenerationJob.status=COMPLETED` 且無關聯 ledger
  2. 查詢：ledger 存在但 job.status=FAILED（反向漂移）
  3. 補償策略：記錄 warning、產生 report，不自動寫入（先人工處理）
  4. admin route：觸發巡檢，限制 admin 身份
- **Dependencies**：WP-Usage-01 完成後意義更大
- **Risks**：補償邏輯若設計錯會雙倍扣費
- **Acceptance Criteria**：巡檢服務可運行 · admin route 有 role 檢查 · 有測試
- **Priority**：P2
- **Effort**：S
- **Out of Scope**：自動補償寫入（要先觀察歷史再決策）

---

## 2.13 存儲與資產

### WP-Storage-01 · R2 Wrapper 單測

- **Goal**：`storage/r2.ts` 被 23 個文件依賴，完全無測試；建立契約層測試
- **Current Code Touchpoints**：`src/services/storage/r2.ts` L15-22（S3Client init）· exports: `generateStorageKey` / `fetchAsBuffer` / `uploadToR2` / `streamUploadToR2` / `deleteFromR2` · `withRetry` 封裝（image upload maxAttempts=3, video stream maxAttempts=2）
- **Files to Modify**：無
- **Files to Add**：`src/services/storage/r2.test.ts`
- **Concrete Tasks**
  1. mock `@aws-sdk/client-s3` 的 `PutObjectCommand` / `DeleteObjectCommand` / `GetObjectCommand`
  2. 測試 `generateStorageKey`：outputType × userId × audioFormat 組合
  3. 測試 `uploadToR2`：成功返回 publicUrl · 失敗經 withRetry · CacheControl header 正確
  4. 測試 `fetchAsBuffer`：正常 / 404 / 500 拋錯
  5. 測試 `streamUploadToR2`：multipart 流式 · retry=2 次
  6. 測試 `deleteFromR2`：火忘式（不因失敗阻塞）
- **Dependencies**：無
- **Risks**：AWS SDK mock 需覆蓋多個命令類；streamUpload 的 multipart 邏輯複雜
- **Acceptance Criteria**：5 個 export 全有測試 · retry 策略可觀測 · 無真實 R2 調用
- **Priority**：P0
- **Effort**：M
- **Out of Scope**：替換 AWS SDK 為其他 R2 客戶端

---

### WP-Storage-02 · Sitemap 50 URL 上限解除

- **Goal**：`src/app/sitemap.ts` 當前只取 50 個公開 generations，SEO 嚴重不完整
- **Current Code Touchpoints**：`src/app/sitemap.ts`（`export const dynamic='force-dynamic'`；`getPublicGenerations({page:1, limit:50})`）· `robots.ts`（禁 /api、/studio、/profile、/storyboard）· `generation.service.getPublicGenerations`
- **Files to Modify**：`sitemap.ts`（改為分頁或提高 limit）· 可能需新增 `sitemap-[index]/route.ts` 支持 sitemap index
- **Files to Add**：
  - `src/app/sitemap-[index]/route.ts`（sitemap index 分片，若走分頁路線）
  - `src/app/sitemap.test.ts`
- **Concrete Tasks**
  1. 確認 Google sitemap 規範：單文件 ≤ 50,000 URL / ≤ 50 MB
  2. 方案 A：直接提 limit 到 1000-5000（簡單）
  3. 方案 B：用 sitemap index + 分片（支持 10k+ URL）
  4. 對 3 個 locale 複製 URL 的邏輯保留
  5. 測試：1/50/1000 generation 下 sitemap 正確
- **Dependencies**：無
- **Risks**：動態 sitemap 每次 DB 查詢性能壓力；大量 generation 時可能需要快取
- **Acceptance Criteria**：支持至少 1000 public generation · 三語 URL 複製正確 · 有單測
- **Priority**：P1
- **Effort**：M
- **Out of Scope**：引入 sitemap 快取層（如 Edge Cache）

---

### WP-Storage-03 · OG Route 測試與快取策略

- **Goal**：`/api/og` 2 個分支（generation / profile），無測試；與 UI 的 `WP-Gallery-05` / `WP-Profile-05` 共用產出
- **Current Code Touchpoints**：`src/app/api/og/route.tsx`（分支：`type=generation` 取 Generation.url、`type=profile` 取用戶）· Satori + next/og
- **Files to Modify**：`og/route.tsx`（補 Cache-Control header）
- **Files to Add**：`src/app/api/og/route.test.ts`
- **Concrete Tasks**
  1. 測試 type=generation 不存在 id → 404
  2. 測試 type=profile 不存在 username → 404
  3. 測試 video 類型 generation → 渲染海報（非視頻直接嵌入）
  4. 驗證 Cache-Control: `public, max-age=60, s-maxage=3600` 或類似
  5. 驗證 fallback OG（無參數時）
- **Dependencies**：UI 線 WP-Gallery-05 + WP-Profile-05（可合並執行）
- **Risks**：Satori 測試環境冷啟動慢（> 1s）；字體加載失敗時渲染空白
- **Acceptance Criteria**：3 個分支有測試 · Cache-Control 設定清晰
- **Priority**：P1
- **Effort**：S
- **Out of Scope**：OG 視覺基線（屬視覺回歸，被 4.6 延後）

---

## 2.14 基礎設施

### WP-Infra-01 · with-retry 單測

- **Goal**：`with-retry.ts` 被 `generate-image` / R2 wrapper / LLM text 共用，是項目最關鍵的韌性工具之一，無測試
- **Current Code Touchpoints**：`src/lib/with-retry.ts`（預設：maxAttempts=3, baseDelayMs=1000, maxDelayMs=15000, isRetryable 預設檢測 5xx/429/network；L81-83 指數退避 + jitter）
- **Files to Modify**：無
- **Files to Add**：`src/lib/with-retry.test.ts`
- **Concrete Tasks**
  1. 用 `vi.useFakeTimers` 測試 backoff delay 正確
  2. 測試：5xx / 429 / network error 重試；4xx（除 429）不重試；驗證錯誤不重試
  3. 測試：達 maxAttempts 後拋原錯
  4. 測試：jitter 在預期範圍
  5. 測試：isRetryable 可自定義覆蓋預設
- **Dependencies**：Vitest fake timers
- **Risks**：時間敏感測試 flaky；jitter 隨機性需固定 seed
- **Acceptance Criteria**：6 個路徑 + 時間測試 · CI 穩定性 100%
- **Priority**：P0
- **Effort**：S
- **Out of Scope**：改 backoff 策略

---

### WP-Infra-02 · Errors 映射矩陣文檔化

- **Goal**：`errors.ts` 6 個子類（ApiRequest/Provider/Validation/RateLimit/Auth/InsufficientCredits）有 test，但無「錯誤 → HTTP 狀態 → i18n key → 前端處理」的端到端矩陣文檔
- **Current Code Touchpoints**：`src/lib/errors.ts`（L10-29 基類 · L31-99 六個子類）· `src/lib/errors.test.ts`（已有測試）· `api-route-factory.ts` L142-172（錯誤到 response 映射）· `messages/*.json` 的 error 文案
- **Files to Modify**：無
- **Files to Add**：`docs/architecture/error-handling.md`（錯誤矩陣 + 前後端處理指引）
- **Concrete Tasks**
  1. 列出 6 個錯誤類 × errorCode × httpStatus × i18nKey × retryable 矩陣
  2. 對照 factory 的映射邏輯驗證一致
  3. 前端側：grep `errorCode` 使用點，確認所有分支有 UI 處理
  4. 新增錯誤類時的 checklist
- **Dependencies**：無
- **Risks**：文檔易過期 — 可加 CI guard：錯誤類數量 ≠ i18n key 數量時 fail
- **Acceptance Criteria**：矩陣完整 · CLAUDE.md 引用 · 前端分支覆蓋清單
- **Priority**：P2
- **Effort**：S
- **Out of Scope**：重構錯誤類層次

---

### WP-Infra-03 · Rate-Limit Upstash 降級路徑測試

- **Goal**：`lib/rate-limit.ts` Upstash Redis 主路徑 + in-memory Map fallback 路徑，fallback 無測試
- **Current Code Touchpoints**：`src/lib/rate-limit.ts`（116 行，Upstash + Map fallback）· `api-route-factory.ts` L112-140 · `constants/config.ts` L236-251（RATE_LIMIT_CONFIGS）
- **Files to Modify**：`rate-limit.ts`（若 fallback 切換無日誌則補）
- **Files to Add**：`src/lib/rate-limit.test.ts`
- **Concrete Tasks**
  1. mock Upstash Redis 正常 → 使用 sliding window
  2. mock Upstash 連接失敗 → 切 in-memory，驗證仍限流
  3. in-memory Map 在 serverless 多實例下**不一致**，需日誌警告
  4. 驗證不同 key 獨立計數 · 窗口過期後重置
  5. 驗證 config.ts 的預設限額（generate/video/prompt 各自）被正確套用
- **Dependencies**：無
- **Risks**：in-memory 在 serverless 本質限制，測試只能驗證單實例行為
- **Acceptance Criteria**：兩路徑均有測試 · fallback 切換有 warning 日誌
- **Priority**：P1
- **Effort**：S
- **Out of Scope**：把 in-memory 換成全 Redis（會影響本地開發無 Redis 的情況）

---

### WP-Infra-04 · Circuit Breaker 持久化策略決斷

- **Goal**：breaker in-memory、伺服器重啟重置（agent 3 確認），需決斷「保留 in-memory + 文檔化」或「改 Redis」
- **Current Code Touchpoints**：`src/lib/circuit-breaker.ts`（failureThreshold=5, resetTimeoutMs=30s, failureWindowMs=60s）· `circuit-breaker.test.ts`（已有）· 消費者：`generate-image.service` L293-300
- **Files to Modify**：`circuit-breaker.ts` 頂部 JSDoc 明確 in-memory 特性
- **Files to Add**：`docs/architecture/resilience.md`（breaker 策略 + rate-limit 策略）
- **Concrete Tasks**
  1. 加 JSDoc：`@scope in-memory, per-instance, resets on restart`
  2. 評估 Vercel serverless 下 breaker 有效性（冷啟動頻繁 → breaker 基本失效）
  3. 文檔化決策：短期保留 in-memory + 加每實例觀測日誌；長期改 Redis
  4. 加 `logger.warn` 當 breaker 狀態轉 OPEN
- **Dependencies**：無
- **Risks**：若 breaker 實際失效，下游 provider 洪水風險被掩蓋
- **Acceptance Criteria**：JSDoc 明確 · 文檔化 · OPEN 轉換有結構化日誌
- **Priority**：P2
- **Effort**：S
- **Out of Scope**：實作 Redis-backed breaker

---

## 2.5 Character Card — 風格一致性切片（僅勾連圖像/視頻的部分）

### WP-StyleConsistency-01 · recipe-compiler compileRecipe 單測

- **Goal**：`compileRecipe()` 是卡片 → 圖像生成的**唯一鏈路**（L331-495），完全無測試
- **Current Code Touchpoints**：`src/services/recipe-compiler.service.ts`（compileRecipe L331 · Stage 2 LLM Fusion L239-327 · Stage 1 Template Fallback L213-226）· `studio-generate.service.ts` L78-120（card mode 入口）· `CompileRecipeInput` / `CompiledRecipe` 類型
- **Files to Modify**：無
- **Files to Add**：`src/services/recipe-compiler.service.test.ts`
- **Concrete Tasks**
  1. 測試 Stage 1 Template fallback：character + freePrompt + background + style 拼接順序
  2. 測試 Stage 2 LLM Fusion：mock LLM 返回融合 prompt → 驗證調用的 system prompt 含 CHARACTER/BACKGROUND/STYLE 標籤
  3. 測試 adapter 分支：NovelAI tag-based vs Gemini/OpenAI natural language 的 system prompt 差異
  4. 測試 freePrompt only（無卡片）路徑
  5. 測試 modelId / adapterType 正確從 styleCard 取得
- **Dependencies**：WP-Prompt-01 的 LLM mock 基礎
- **Risks**：兩階段邏輯複雜，測試需覆蓋 LLM 失敗 → Template fallback 路徑
- **Acceptance Criteria**：兩階段 + 3 種 adapter 分支有測試 · fallback 路徑覆蓋
- **Priority**：P0
- **Effort**：M
- **Out of Scope**：改變 Stage 2 / Stage 1 策略

---

### WP-StyleConsistency-02 · LoRA 合併 / 去重 / Civitai Token 注入測試

- **Goal**：`recipe-compiler.service` L441-494 的 LoRA 合併邏輯涉及多源優先級、去重、Civitai token 注入，完全無測試
- **Current Code Touchpoints**：`recipe-compiler.service.ts` L441-494（合併邏輯）· L468-480（Civitai token 注入）· `civitai-token.service.ts` · `constants/provider-capabilities.ts`（FAL=5 LoRA, Replicate=1 LoRA 限制）
- **Files to Modify**：無
- **Files to Add**：（合併於 WP-StyleConsistency-01 的 test 文件中）
- **Concrete Tasks**
  1. 測試優先級：character LoRA → style LoRA → styleCard.advancedParams.loras → background LoRA
  2. 測試去重：同 URL 不同源，保留第一個
  3. 測試 provider 限制：FAL 上限 5 個（截斷）、Replicate 上限 1 個
  4. 測試 Civitai token 注入：有 token → URL 加參數；無 token → 保持原 URL
  5. 測試邊界：無任何 LoRA、全部無效 URL
- **Dependencies**：civitai-token.service mock · WP-StyleConsistency-01 共用測試檔案
- **Risks**：provider 限制變化（上限調整）會讓測試失效
- **Acceptance Criteria**：優先級 / 去重 / 限制 / token 注入四類各有測試
- **Priority**：P0
- **Effort**：S
- **Out of Scope**：LoRA 權重的語義驗證

---

### WP-StyleConsistency-03 · Character Attributes → Prompt 三格式映射測試

- **Goal**：`character-card.service.ts` L153-186 的 `buildPromptFromAttributes` + `character-prompt-templates.ts` L136-152 的 3 種格式適配（tag / weighted / natural），無測試
- **Current Code Touchpoints**：`src/services/character-card.service.ts` `buildPromptFromAttributes` L153-186 · `src/constants/character-prompt-templates.ts` L136-152（3 格式）· `character-card.mapper.ts`
- **Files to Modify**：無
- **Files to Add**：`src/services/character-card.service.test.ts`（只覆蓋與風格一致性相關方法）
- **Concrete Tasks**
  1. 測試 NovelAI tag 格式：逗號分隔、Danbooru style
  2. 測試 FLUX/SD weighted 格式：`(attribute:weight)` 語法
  3. 測試 Gemini/OpenAI natural 格式：完整句子
  4. 測試屬性優先級：artStyle → expression → hair → eyes → ...（L153-186 順序）
  5. 測試 fallback：無結構屬性時用 freeformDescription
- **Dependencies**：無
- **Risks**：模板字串變動需同步更新 fixture
- **Acceptance Criteria**：3 格式 × 5 屬性組合測試 · fallback 路徑覆蓋
- **Priority**：P1
- **Effort**：S
- **Out of Scope**：`character-refine` / `character-scoring`（屬 Character Card 全域，非風格一致性核心）

---

## 工作包索引

| ID                     | 標題                                     | Priority | Effort |
| ---------------------- | ---------------------------------------- | -------- | ------ |
| **2.2 圖像生成**       |                                          |          |        |
| WP-Image-01            | generation/generate-image 職責分層文檔化 | P2       | S      |
| WP-Image-02            | image-edit FAL 硬編碼解構與擴展點        | P2       | S      |
| WP-Image-03            | 8 provider adapter 單測骨架              | P1       | M      |
| WP-Image-04            | 240s timeout 與降級可觀測性              | P1       | M      |
| **2.3 視頻生成**       |                                          |          |        |
| WP-Video-01            | video-pipeline.service 單測（933 行）    | **P0**   | L      |
| WP-Video-02            | 長視頻 cancel/retry 端到端               | P1       | M      |
| WP-Video-03            | 視頻 validation 模型矩陣補測             | P1       | S      |
| **2.9 Prompt & LLM**   |                                          |          |        |
| WP-Prompt-01           | 4 個 Prompt/LLM service 單測骨架         | P1       | M      |
| WP-Prompt-02           | 5 種 enhance 風格 golden set             | P2       | M      |
| WP-Prompt-03           | prompt-assistant 上下文窗口保護          | P1       | S      |
| WP-Prompt-04           | llm-output-validator 擴展與測試          | P1       | S      |
| **2.12 計費與用量**    |                                          |          |        |
| WP-Usage-01            | Job ↔ Ledger $transaction 原子化         | **P0**   | M      |
| WP-Usage-02            | FreeTier 並發邊界測試                    | **P0**   | M      |
| WP-Usage-03            | Ledger 漂移檢測任務                      | P2       | S      |
| **2.13 存儲與資產**    |                                          |          |        |
| WP-Storage-01          | R2 wrapper 單測                          | **P0**   | M      |
| WP-Storage-02          | Sitemap 50 URL 上限解除                  | P1       | M      |
| WP-Storage-03          | OG route 測試與快取策略                  | P1       | S      |
| **2.14 基礎設施**      |                                          |          |        |
| WP-Infra-01            | with-retry 單測                          | **P0**   | S      |
| WP-Infra-02            | Errors 映射矩陣文檔化                    | P2       | S      |
| WP-Infra-03            | Rate-limit Upstash 降級路徑測試          | P1       | S      |
| WP-Infra-04            | Circuit breaker 持久化策略決斷           | P2       | S      |
| **2.5 風格一致性切片** |                                          |          |        |
| WP-StyleConsistency-01 | compileRecipe 兩階段單測                 | **P0**   | M      |
| WP-StyleConsistency-02 | LoRA 合併/去重/Civitai 注入測試          | **P0**   | S      |
| WP-StyleConsistency-03 | Character attributes → prompt 3 格式測試 | P1       | S      |

**共 24 個工作包** · P0: 7 · P1: 11 · P2: 6 · Effort S: 13 · M: 10 · L: 1

---

## 跨線依賴提示（供下一輪規劃參考，不做 roadmap）

- WP-Infra-01（with-retry）→ 被 WP-Image-03 / WP-Storage-01 依賴
- WP-Usage-01（transaction）→ 是 WP-Usage-02 / WP-Usage-03 的前置
- WP-Prompt-01（LLM mock 框架）→ 被 WP-Prompt-02 / WP-Prompt-03 / WP-StyleConsistency-01 共用
- WP-Storage-03（OG route test）→ 與 UI 線 WP-Gallery-05 / WP-Profile-05 可合並執行
- WP-Video-01（video-pipeline 單測）→ 是 WP-Video-02（cancel/retry）的前置

---

## 本輪刻意不展開的項（保持近期範圍）

- 2.4 音頻生成：`generate-audio.service` 有 `TODO: job lifecycle is incomplete` 與 `needs complete redesign once submitAudioGeneration is job-based`。屬中期重構，非本輪。
- 2.6 Storyboard：Story 不接收卡片，**跨 panel 一致性缺失**屬產品決策 + 架構擴展，非近期。
- 2.11 LoRA 訓練：完整流程無測試，但屬獨立能力域，本輪用戶未要求。
- 視頻 webhook 替代 client polling：架構變動，非近期。
- 視頻生成支持 character/style card：新功能（current code 明確不支持），非近期。
