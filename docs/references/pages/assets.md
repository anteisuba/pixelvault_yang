# Assets 页面设计（`/assets` + 素材选择器）

> 状态：**方向 B「大厅与展馆」已选定 · 桌面/1280/375/picker/状态矩阵已成稿 · 2026-08-11 交付施工**。
> ⚠ 本文只记录**已确认的结构与契约**；未在此写明的造型、动效、文案不构成实现依据，实现会话不得自行补造。
> 上游域契约：[`../domains/assets.md`](../domains/assets.md)。
> 设计过程账本（assets-cf-design-2026-08-09 / assets-claude-directions-2026-08-11）、施工切片包（assets-cf-task-packet-2026-08-11）与交接（assets-cf-session-handoff-2026-08-11，含判断来历 / owner 偏好 / 未决事项）均已随任务包清理，git 历史可取。

**确认载体（可交互原型，非可合入代码；原型文件已随任务包清理，git 历史可取）**

| 用途                                                    | 文件                                                                                                                                |
| ------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 主原型（含 dev 状态切换器：上传/失败/空态/错误/picker） | `assets-claude-b-atrium-2026-08-11.html`                                                                                            |
| 响应式三档并排（1440 / 1280 / 375，真实 iframe 视口）   | `assets-claude-b-responsive-2026-08-11.html`                                                                                        |
| 被否方向（保留作比较证据，不进实现阅读链）              | `assets-claude-a-lighttable-*.html`（灯箱馆）· `assets-claude-c-bench-*.html`（装配抽屉）· Codex `assets-context-dock-v1/v2-*.html` |

---

## 1. 页面职责

`/assets` 是**统一私有资产中心**：所有已完成的生成结果与用户上传资源在此汇集，用户在这里**找、看、整理、复用**。

它首先要回答三个问题：

1. 我刚生成的那个东西在哪？
2. 这一堆东西怎么归到它该在的地方？
3. 我要的那张（那段）是不是这张，能不能马上拿去用？

**不负责**：生成执行（属各创作域）· 公开展示（属 Gallery）· credit · 生成中/失败任务的状态跟踪（仍由原创作域负责，Assets 只承接已完成产物）。

## 2. 已确认方向

- 结构方向：**B「大厅与展馆」**。首页两段纵向叙事：**文件夹门牌行 → 全部素材大河**。
- 不增加第二个永久左栏。全局应用导航是唯一左栏，Assets 不改它的皮。
- 分类不是一棵互斥树，是**可叠加维度**：视图（全部/收藏/已发布/本地上传）× 媒体类型（图片/视频/音频/3D）× 文件夹范围 × 检索（提示词/模型/时间）。
- 视觉方法：**跟随本地 shadcn token，不为 Assets 发明独立视觉世界**（owner 2026-08-11 明确）。纯白底、`--muted #f7f7f7`、`--border #e8e8e8`、瓦片 `rounded-lg` + 1px border 无阴影、卡片 `rounded-xl` + 极轻影、选中态黑底反白。
- 密度基调：**紧凑**。作品是唯一有色层，chrome 全部中性。

### ⛔ 明确被 owner 否掉的（不要再提案）

| 被否项                                             | 日期  | 理由                                                                             |
| -------------------------------------------------- | ----- | -------------------------------------------------------------------------------- |
| 「新回流」独立段（横滚专席 + 来源角标 + 快捷归档） | 08-11 | 「不知道有什么用」。回流由数据层保证，展示只留段头轻徽标                         |
| Codex V1 的视觉                                    | 08-11 | 「UI 有点丑」；其 Dock 结构实验价值保留                                          |
| 暖纸/自定义配色的 Assets 专属视觉世界              | 08-11 | 要求遵循本地设计                                                                 |
| 「馆藏」这个命名                                   | 08-11 | 统一叫**文件夹**                                                                 |
| picker 里的**横向文件夹 chips rail**               | 08-11 | 「文件夹多了这边就不好选择了」= 原始需求 F1 本身。改 176px 可折叠导航栏，见 §8.1 |

## 3. 桌面结构（1440 关键切片）

### 顶栏（单行，高约 56，吸顶长条）

`素材 + 总数` · 类型 · 状态 · 模型 · 时间 · 排序 · ——弹性—— · 上传（主按钮）· 选择 · 密度 S/M/L

- **Owner 2026-08-30 最终确认（第二轮 mock + 真机反馈）**：顶栏固定在素材滚动容器上方，滚动网格时不消失；桌面收为内容区 `11/12`、最大 `1536px` 的居中浮动工具条，约 56px 高、`rounded-2xl`、实色白底、细边框与轻投影，不再使用横贯内容区的全胶囊。后续真机反馈明确移除低价值的顶部搜索框，把五个分面筛选直接放进原搜索区域；框内按“标题 + 数量 + 分面筛选｜弹性留白｜上传主动作 + 选择次动作 + 密度分段”分组，上传是唯一大面积黑色控件。默认无筛选时保持 56px；条件生效后，可删除 chip 与“清除全部筛选”留在同一个吸顶框内并自然增加一行。首次进入以 320ms 轻微下落淡入，滚动吸顶后以 200ms 平滑增强边框/投影，密度选中块以 200ms 滑动；全部遵循 `prefers-reduced-motion`。移动端保持全宽、允许控件换行。

- 密度控制的是**目标行高**（见 §5），不再等价于固定列数 4/6/8。
- ⛔ **顶栏不再放搜索框或两组图标 segmented** —— 统一使用五个文字分面，见 §3.1。

### 3.1 分面筛选栏（取代原来的两组图标 segmented）

放在吸顶顶栏内、紧跟 `素材 + 总数`，占用原搜索框区域；「全部素材」段头只保留当前命中数量。五个**文字**下拉：`类型 · 状态 · 模型 · 时间 ｜ 排序`。

| 维度 | 选择方式    | 选项                           | 数据来源                                          |
| ---- | ----------- | ------------------------------ | ------------------------------------------------- |
| 类型 | 多选        | 图片 / 视频 / 音频 / 3D        | `outputType`                                      |
| 状态 | 多选        | 收藏 / 已发布 / 我上传的       | `isLiked` · `isPublic` · `provider='user-upload'` |
| 模型 | 多选 + 搜索 | 实测 **23 种**                 | `model`                                           |
| 时间 | 单选        | 今天 / 7 天 / 30 天 / 今年     | `createdAt`（引擎已有 `timeRange`）               |
| 排序 | 单选        | 最新 / 最早 / 最近更新的文件夹 | 已有                                              |

**⭐ 默认全部维度未选** = 全部素材、不限类型。**「不选」就是「全部」，不需要一个「全部」按钮**（owner 2026-08-11：初始状态应是全部素材，图片默认不点击）。

- 触发器回显当前值：未选显示维度名（`类型`），选 1 项显示该项名（`图片`），选多项显示 `类型 2`。
- **生效的筛选在顶栏内渲染成可删 chip 行**（仅在有筛选时出现）+ 末尾「清除全部筛选」。一眼看全当前口径，一键回到全部。
- 手机：分面在顶栏内另起一行并允许换行。**不另做一套移动 chips**（一个模型走到底）。

**为什么改**：① 图标组含义不明（地球=已发布？）；② 组内互斥导致无法「图片＋视频」同看；③ 低频搜索框长期占位，而**模型（23 种）和时间是这个库里更有区分度的两个轴**；④ 原「视图」组把状态（收藏/已发布）和来源（本地上传）混成一组，属分类错误。

#### 实现补注（2026-08-11 落地时确定，切片 3）

设计稿没写、但施工时必须定的四条，记在这里免得下一轮重新拍：

| #   | 事项                           | 定案                                                                                                                           | 为什么                                                                                                                                                       |
| --- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **多选的组内语义**             | 类型 / 模型 = **OR**（`outputType: { in }`、`model: { in }`）；状态 = **AND**                                                  | 类型 OR 才有「图片＋视频同看」；状态那三条是三个各自独立的引擎参数（`isLiked` / `isPublic` / `provider`），叠加自然是 AND，且「我上传的 ∩ 收藏的」本身讲得通 |
| 2   | **模型分面的选项表从哪来**     | `getAssetSectionCounts` 新增 `groupBy(['model'])` → `AssetSectionCounts.byModel`，**跟随当前类型口径**                         | ⛔ 不能拿模型目录充数（目录几十个、库里只出现二十来个）。切到「只看视频」时模型下拉里也不该还挂着图像模型                                                    |
| 3   | **`user-upload` 不进模型下拉** | 过滤掉                                                                                                                         | 本地上传把 `model` 也写成 `user-upload`（`constants/uploads`），它不是模型 —— 那条轴归「状态 · 我上传的」                                                    |
| 4   | **两个数字各自的含义**         | 顶栏 `素材 N` = **整库大小**（不随筛选变，由未缩口径的 `byType` 求和）；段头 `全部素材 N` = **当前口径命中数**（live `total`） | 段头若用 `counts.all`（只跟类型口径走），一开时间/模型分面就会出现「全部素材 129」压着一张瓦片                                                               |

**引擎侧同批改动**（原属本包 Forbidden Scope，owner 2026-08-11 明确放行）：`GALLERY_TIME_RANGE_OPTIONS` 补 `month`/`year`；`GallerySearchSchema` 的 `type`/`model` 改成逗号分隔列表（旧 `?type=image` 深链接照常工作，`?type=all` 落到空数组 = 不限）；`GalleryFilters` 的 `type`/`model` 相应改名 `types`/`models` 并全仓改完调用方（公共画廊那几个单选控件按 `[0] ?? 'all'` 读、写回单元素数组）。

> ⚠ **调研过但数据是空的，别做**：`characterCardId` **0 条**、`cardRecipeId` **0 条**、`runGroupId` **仅 1 条**（实测 135 条样本）。所以「按角色卡自动聚类」「按生成批次折叠成一张卡」这两个看起来很美的轴**目前没有数据支撑**。
> ⭐ 但值得记一笔：owner 的文件夹名几乎全是角色名（dainia / 爱弥丝 / 流萤 / 达妮娅 / キサキ）—— **他在手工做 `characterCardId` 本该自动做的事**。若哪天生成链路开始回填这个字段，「按角色自动成夹」会是这个页面最大的一次跃迁。届时再评估，现在不做。

### 段一 · 文件夹门牌行

- 每夹一张门牌卡（宽 168，`rounded-xl` + 1px border）：**最近 4 张真实素材拼贴**（2×2）+ 夹名 + 计数 + 子夹 chips。
- 「未分类」永远是第一张，琥珀「待整理」徽标 —— 它是**待办队列**，不是普通文件夹。
- 横向一行，**按容器宽度自适应张数**：`fit = ⌊(W + gap) / (卡宽 + gap)⌋`，末尾固定预留「+N 查看全部」与「新建文件夹」两张，卡片 `flex-grow` 吃掉余量。**永远铺满一行，右侧不留空白。**
- 排序：见 §4.1。置顶（pin）见 §4.2。

### 夹内页与文件夹总览页 —— ⚠ 必须留在应用壳内

进文件夹**不能把用户踢出应用壳**：全局左侧导航**始终可见**，页面从导航右侧开始（`left: <nav-w>`），不是全屏覆盖。

- 实现上应是**路由**（如 `/assets?folder=<id>`），不是 overlay：浏览器后退可用、URL 可分享、刷新不丢位置。
- 面包屑 `素材 › 鸣潮 › 弗洛洛` 每级可点回；`Esc` 等价于返回上一级。
- 文件夹总览页同理。

### 段二 · 全部素材大河

- 段头：`全部素材 · 总数` + **回流轻徽标**`● 今日回流 N` + 排序下拉。
- justified rows 真实比例网格（算法见 §5）。
- 新回流的资源天然排在「最新优先」最前，不做独立分区。

### 详情

本轮**不重设计** `AssetDetailSheet`，只保证入口与 focus return。点击瓦片打开现有 Sheet。

- 当前筛选结果中存在多张已加载图片时，图片详情左右两侧显示前后切换按钮；首尾按钮禁用，不跨到视频、音频或 3D。
- `←` / `→` 键与按钮行为一致；切换同步更新预览与右侧元数据，并以 200ms 方向淡入过渡保持空间连续性。`prefers-reduced-motion` 下关闭位移与缩放。

> 待定项：是否引入全屏灯箱（方向 A 的深检视层）作为「看原图」路径。**未拍板，不在本轮范围。**

## 4. 文件夹体系（两条路径 + 四层治理）

### 大分类 → 小分类，两条路径并存

| 路径                                                                                                       | 场景     | 成本 |
| ---------------------------------------------------------------------------------------------------------- | -------- | ---- |
| **门牌 chips 直达** —— 父夹门牌上列子夹 chip，点击一步进子夹；超 2 个折叠 `+N`（点 `+N` 进父夹看全部子夹） | 常用子夹 | 1 击 |
| **夹内页下钻** —— 子夹小门牌置顶 + justified 全网格；面包屑 `素材 › 鸣潮 › 弗洛洛` 每级可点回              | 浏览式找 | 2 击 |

> **实现补注（切片 4 落地时确定）**
>
> - **拼贴区高度写死 88px，不是按比例**。踩过两次：①只给 `grid-rows-2` 时行高被图片自然高度撑开，同一行门牌高矮不齐；②改成 `aspect-ratio` 后，因为门牌是 `flex-grow` 吃余量的，夹一少每张卡就变得极宽，比例一挂高度跟着膨胀（实拍到 740×370 的横幅）。固定高度则无论卡多宽，整行高度恒定。
> - **「+N 查看全部」是固定预留位**，不是「有溢出才出现」—— 夹的重命名/删除只在总览页（治理 2），夹少时也必须进得去。
> - **「未分类」的拼贴**由客户端从当前列表里未归夹的素材凑（它不是 Project，服务端没有 `coverUrls`），零额外请求。
> - 服务端 `ProjectRecord.latestGenerationUrl`（单张、无人消费）**退役**，换成 `coverUrls: string[]`（最近 4 张，缩略图优先）。⚠ 缺派生图的视频/音频**整条跳过**——`.mp4` 塞进 `<img>` 只会得到一个永远加载失败的格子。
> - 路由参数沿用既有的 `?projectId=<id>`（夹内页）并新增 `?view=folders`（总览页）；换范围走 `history.pushState` + `popstate` 回读，所以**后退可用、URL 可分享、刷新不丢位置**，同时保留 `useGallery` 的 0ms 缓存切换（不走服务端往返）。

### 4.1 排序 —— ⚠ 现有「最近使用」是假的，施工时必须换数据源

**现状实现**：`AssetFolderTree.tsx` 的 `sortFolderNodes(nodes, mode)`，三档 `recent | name | count`，递归排每一层兄弟节点。`name` = `localeCompare`，`count` = 屏上账本数降序 —— **这两档正确，直接复用**。

**⛔ `recent` 档坏了**：它排的是 `Project.updatedAt`，而全仓**只有两处写 Project 行** —— `updateProject`（重命名 / 改父级 / 改描述）与软删除。**把素材移进文件夹改的是 `Generation.projectId`，不碰 Project 行**，所以往夹里加多少东西，`updatedAt` 都不动。

> 真机实测（2026-08-11，7 个文件夹）：**4 个夹的 `updatedAt` 完全等于 `createdAt`**；`dainia` 有 24 张素材陆续加入，`updatedAt` 仍停在创建当天。
> 也就是说「最近使用」实际含义是「最近被重命名过的，否则就是创建时间」—— 对从不改名的用户，它等于**按创建时间倒序**。名字承诺的事情它一件也没做。

**定案（两件事拆开，各自用诚实的数据源）**：

| 档位                                                          | 语义                                         | 数据源                                                                                                                       | 成本                                            |
| ------------------------------------------------------------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| **最近更新**（替换原「最近使用」，仍为默认）                  | 夹内**最新素材**的时间 —— 「哪个夹最近在动」 | `getAssetSectionCounts` 里**已有**的 `groupBy({ by: ['projectId'] })` 加一个 `_max: { createdAt: true }`，同一次查询顺带取回 | 极低：service 一个聚合字段 + 类型/API/hook 透传 |
| 按名称                                                        | A→Z                                          | `localeCompare`                                                                                                              | 已有                                            |
| 按数量                                                        | 屏上账本数降序                               | 已有                                                                                                                         | 已有                                            |
| **最近用过**（不是排序档，是 picker 导航栏的快捷组，见 §8.1） | **我**最近打开过的夹                         | 客户端 localStorage 记录访问过的 folderId，**零 schema 改动**                                                                | 低                                              |

⚠ 「夹内最近更新」与「我最近用过」是**两件不同的事**，别再用一个字段冒充两者 —— 现有 bug 正是这么来的。

**另一处小缺陷**：`sortMode` 是 `useState` 本地态，刷新回落 `recent`，而同页的密度用 `pv:assets:density` 持久化了。施工时统一持久化（键名 `pv:assets:folder-sort`）。

### 4.2 指定顺序 = 置顶（pin），**与三档排序正交的独立一层**

三档排序都是**算出来的**，用户无法指定「我要这几个在前面、按这个顺序」。补一层 pin：

- **数据**：`Project.pinnedOrder Int?` —— `null` = 未置顶；非 null = 置顶且按升序。**一个可空列，无需回填迁移**。
- **排序关系**：置顶的**永远在前**（按 `pinnedOrder` 手动序），其余跟随当前排序档。⛔ **不给排序下拉加「自定义」第四档** —— pin 是另一层，不是另一种排序；混成一档会出现「按名称排序时我的手动序去哪了」这种无解问题。
- **门牌行** = 置顶的 + 按当前排序补齐到铺满一行。**这才是 pin 的真正目的：决定首页那一行是哪几个夹、什么顺序。**
- **入口**：门牌卡 hover 出图钉；已置顶常显实心图钉。
- **定序**：在**文件夹总览页**分两段 ——「置顶」段（可拖拽调整顺序，拖到谁前面就插到谁前面）+「全部」段（按当前排序）。拖出置顶段 = 取消置顶。
- ⛔ **不做全量手动排序**（给 100 个夹排全序既无必要也维护不动）；要控制的只有头部那几个。

> owner 2026-07 曾在「排序下拉」与「手动拖动排位」之间选了前者。这次是补前者覆盖不到的那部分诉求，两者不冲突：**排序管全体，pin 管头部**。

### 文件夹变多后的四层治理

1. **门牌行只放头部** —— 首页只显示能铺满一行的常用/置顶夹 + `+N 查看全部`。夹从 16 涨到 100，**首页长度不变**。
2. **文件夹总览页**（`素材 › 文件夹`）—— 全部夹的门牌网格 = 夹的专属管理页：跨层级搜索、排序（最近使用/名称/数量）、新建/重命名/删除、**拖素材到门牌归档、拖门牌进门牌变子夹**。
3. **置顶（pin）** —— 钉住的夹永远在门牌行最前。⚠ 需 `Project.pinnedAt`，**属后续切片，不在首批**。
4. **总览页搜索兜底** —— 夹数量增长时，在文件夹总览页跨层级搜索，避免主顶栏常驻一个低频输入框。

远期（不在本轮）：Saved Search 智能夹 / 标签系统。门牌墙结构天然容纳「智能夹门牌」（虚线边框区分），不需改骨架。

## 5. 真实比例网格 · 算法契约

> owner 需求 3「当前尺寸限制死，无法展示真实尺寸与比例」的正式解法。实测比例谱 **0.56 → 2.77**（立绘竖条 → 三视图横带），`aspect-square + object-cover` 对这类内容是毁容级裁切。

1. **justified rows**：贪心装行，行内等高，按真实宽高比分配宽度，**整行铺满容器**。
2. **末行也铺满**；仅当铺满会使行高超过目标行高 **1.5 倍**时，才回落到目标行高（末行只剩一两张的情形）。
3. **超宽图与常规图同排**。⛔ 不给超宽图开「独占整行」的特例 —— 独占会在行右侧留下大片空白，而 justified 本身就已经保证不裁切。
4. `ar` 只做安全阀 clamp `[0.4, 3.2]`（防脏数据），真实数据全部原样参与排版。**无意裁切归零。**
5. 无宽高的存量记录（实测 135 条里有 14 条）按 1:1 兜底参与排行。
6. **密度 = 目标行高**，按断点各有一套刻度：

   | 断点          | S   | M（默认） | L   |
   | ------------- | --- | --------- | --- |
   | 桌面 ≥1280    | 150 | 196       | 260 |
   | 平板 768–1279 | 128 | 168       | 222 |
   | 手机 <768     | 92  | 124       | 168 |

7. 容器宽度取**容器自身 `clientWidth`**，不手算 padding；页面加 `scrollbar-gutter: stable`（滚动条出现会让容器缩水，导致已排好的行溢出）。
8. **验收判据是程序化量测**：`行宽 / 容器 clientWidth ≥ 99%` 且 `documentElement.scrollWidth ≤ clientWidth`。⛔ 不接受目测。

## 6. 各媒体的表达契约

| 类型     | 列表表达                                                                                                                                                                                                      |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **图片** | 真实比例参与排行；hover 才浮出底部渐隐条（提示词或模型 + 像素尺寸），不长期压住作品                                                                                                                           |
| **视频** | poster 帧按真实比例 + 时长角标 + hover 静音预览。⚠ **工程前置：视频当前 thumbnail/preview 全空**（实测 7/7），必须先补 poster 派生                                                                            |
| **音频** | **恒 1:1 封面卡**（owner 需求 4）。回退链：用户设置 → 生成请求/声音角色封面 → provider/模型封面 → 系统默认。卡面 = 封面 + 播放键 + 时长 + 类型徽标。⛔ 没有真实波形数据时**不画伪波形**，封面图形只作抽象表达 |
| **3D**   | poster 按真实比例 + 立方体角标；GLB 交互仍归详情/3D 工作台                                                                                                                                                    |

**实现补注（切片 2 落地时确定）**

- ⛔「不画伪波形」这条**原来是被违反的**：回退链最后一级 `public/asset-previews/audio-default.svg` 本身就画着 10 根柱状条 + 一条起伏曲线，看着像波形、跟音频毫无关系。已换成抽象同心环（唱片/共振语义，浅色中性场），与瓦片内 CSS 兜底封面同一套语言。
- **播放键是非交互标记**（`pointer-events-none` 的 span），不是按钮 —— 瓦片本身就是 `<button>`，里面再嵌一个可点元素在 a11y 上是坏的。它只表示「这是可播放的媒体」，真正的播放在详情面板。音频常显、视频 hover 才浮。
- **渐隐条不挂在音频卡上**：那张卡本来就是「封面 + 播放键 + 时长 + 类型徽标」，再压一层字就糊了。图片/视频/3D 才有。
- 渐隐条的正文取「提示词 → 模型」，但**本地上传的 `model` 是 `user-upload` 哨兵**，这种情况只留像素尺寸，不在作品上压一行英文 slug。
- 对比度按**最坏底（纯白作品图）**算：scrim `rgba(12,12,12,.72)` 合成后 rgb(81,81,81)，白字 7.9:1、次级 `#e8e8e6` 6.4:1。

## 7. 状态矩阵（全部已出稿）

| 状态             | 契约                                                                                                                                                                                                                 |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **新资源回流**   | 段头轻徽标 `● 今日回流 N`，点击跳到最新处。**不打断当前浏览位置，不做独立分区**                                                                                                                                      |
| **空库**         | 大空态：说明「所有生成成品会自动回到这里」+ 上传/去生成两个出口；此时文件夹段一并隐藏                                                                                                                                |
| **空文件夹**     | 「『X』里还没有素材」+ 指路（拖到门牌 / 批量移动）+ 上传到此文件夹                                                                                                                                                   |
| **搜索无结果**   | 回显当前全部生效筛选 + 「清除全部筛选」单一出口                                                                                                                                                                      |
| **整页加载失败** | 错误框（`destructive` 弱化面，非满屏红）+ 重试；文案明确「已加载的内容不会丢失，重试后从中断处继续」                                                                                                                 |
| **分页失败**     | ⭐ **只挡这一段**，已加载内容不动：网格末尾行内错误条 + 重试                                                                                                                                                         |
| **上传队列**     | 右下常驻面板（移动端贴底通栏）：标题 + `N 项 · N 完成 · N 失败` + **落夹目标可改** + 逐项（缩略图/文件名/进度条/结果）+ 失败项行内「重试」+ 底部「全部重试 / 清除已完成」。⛔ 不再只用一个全局 `isUploading` + toast |
| **整页拖入**     | 全屏虚线遮罩「松手上传到『X』」，副文案说明可事后改目标                                                                                                                                                              |
| **选择模式**     | 见 §7.1                                                                                                                                                                                                              |

> **实现补注（切片 5 / 5b 落地时确定）**
>
> - **进度是真的**：`uploadImageFileAPI` 早就支持 `onProgress`（R2 直传走 XHR），队列直接用它的字节级进度，不是假动画。
> - **队列串行跑，不并行**：R2 直传是整文件 PUT，几个大图并发会把上行带宽分光，每一项都变慢，用户反而以为卡死。
> - **占位瓦片的比例**由本地 `Image` 读出来（`URL.createObjectURL`），读不到才落 4:5；完成后占位从队列的 `pendingItems` 移除、真图 prepend 到同一位置。
> - **分页失败不自动重试**：`appendError` 一置上就把 `IntersectionObserver` 停掉 —— 哨兵还在视口里，自动重试会变成死循环打接口。由用户点那条错误条上的「重试」再继续。
> - `useGallery` 因此分出两条错误通道：`error`（整页）与 `appendError`（翻页），外加 `retry` / `retryLoadMore`。⛔ 别再把两者合成一个 —— 合并就一定会出现「翻页失败把整屏内容清空」。

### 7.1 选择模式 —— 三处现状缺口

| 现状                                                                                                               | 改成                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| 批量条只在 `selectionMode && selectedIds.size > 0` 时出现 → **点了「选择」什么都没发生**，用户不知道进没进选择模式 | **进入选择模式即出现批量条**，零选中时显示 `已选 0` + 提示「先选择素材 · Shift 点击可选一段」，四个动作 `disabled`     |
| **零 `shiftKey`**（全仓搜不到），只能一张张点                                                                      | **Shift 范围选**：点第一张设锚点（浅蓝描边），Shift 点最后一张选中整段。整段一律置为选中（不反选，符合文件管理器直觉） |
| 移动后无撤销                                                                                                       | 见 §7.2                                                                                                                |

保留：现有 7 动作语义（移动/收藏/发布/删除…）· 三语义强调色（收藏 rose / 发布反相 / 删除红）· 删除走 AlertDialog · 拖瓦片到门牌也是归档路径。

### 7.2 「移动到文件夹」—— 现状是扁平下拉，夹一多就废

**现状**：`projects.map` 平铺成 `DropdownMenuItem`，**无搜索、无嵌套（子夹与父夹同级平铺）、无最近用过、无新建**。16 个夹已经要滚，100 个不可用。

**改成可搜索目标选择器 overlay**，与 picker 导航栏**同一套结构**（复用，不另造）：

1. 搜索框（跨层级，子夹带父路径）
2. **最近移入过** 3 个（localStorage）
3. **移出** —— 「未分类」单独一组，图标区分（← 出箭头），避免和普通夹混在一起
4. **全部文件夹** 树，`▸` 展开子夹
5. 底部 **+ 新建文件夹并移入…**（现状缺，搜不到时的唯一出路）

**撤销契约**：确认后 toast「已移动 N 项到「X」· 撤销」，6 秒。
⛔ **撤销必须按每一项原来的 `projectId` 分别回写** —— 选中的 4 项可能来自 4 个不同的夹。实现不得偷懒写成「撤销 = 全部丢回未分类」，那是数据损坏。

### 7.3 上传点击后 —— 缺的是「网格里的占位瓦片」

**现状**：`prependGeneration` **只在上传成功后**调用 → 从点下「上传」到成功为止，网格**毫无反应**，用户只能在角落 toast 里找信号。

**改成乐观占位**：

1. 选中文件 → **网格最前面立即插入占位瓦片**（虚线框 + 文件名 + 进度环 + 百分比），同时右下角上传队列面板出现（§7 上传队列）。
2. 单张完成 → **占位瓦片原地换成真图，不跳位**。
3. 单张失败 → 占位瓦片变**错误瓦片**（红边 + 原因如「超过 15 MB 上限」+ 行内「重试 / 移除」），不静默消失。
4. 落夹目标 = **当前范围**（夹内页 = 该夹；顶部「上传到此夹」按钮同理），队列面板顶部显示目标且可改。
5. 若新资源**不属于当前视图**（例如当前在「收藏」）→ 不插网格，改由队列项给「完成 · 查看」跳过去。现有 `shouldKeepAssetAfterPatch` 已有这个判断，接上即可。
6. 占位瓦片按文件真实宽高比参与 justified 排版（本地 `Image` 可先读到尺寸）；读不到时按 4:5 兜底，拿到真图后重排该行。

## 8. picker（`AssetSelectorDialog`）关键切片

**任务型 shell，与 `/assets` 不同构**（owner 2026-08-09 确认）：共享数据、筛选、网格、选择、上传的行为/API/状态/可访问性，**不共享 shell 与信息架构**。

### 8.1 文件夹选择 —— 176px 可折叠导航栏（⚠ 这条是钟摆的第三个位置，别再荡回去）

**历史**：现状是 280px 竖树 → 被判「吃 40% 宽 = 缩小复刻整个素材页」→ 改成**横向 chips rail** → owner 2026-08-11 指出「文件夹多了这边就不好选择了」，正是原始需求 **F1「文件夹选择目前是横向的，很难选」**。⛔ **两端都试过了，都不对**：

| 形态            | 为什么不行                                                                         |
| --------------- | ---------------------------------------------------------------------------------- |
| 280px 竖树      | 吃掉 40% 对话框宽度，网格只剩三列，且把管理页的 CRUD 一起搬了进来                  |
| 横向 chips rail | 一行装不下 16+ 个夹，其余全塞进 `更多文件夹 ⌄`；**不支持嵌套**（子夹根本没有位置） |

**定案**：左侧 **176px 可折叠导航栏**（880 宽对话框里 = **20%**，非 40%）。与旧竖树的关键差别是**它只装导航，不装管理**——没有新建/重命名/删除、没有密度、没有批量动作。分四组：

1. **智能视图**：最近（默认）/ 全部素材 / 收藏 / 未分类（带计数）。
2. **最近用过**：3 个最近用过的文件夹 —— picker 的最高频路径，一击直达。
3. **全部文件夹**：完整树，`▸` 展开子夹，缩进表达层级，滚动容纳任意数量。
4. 栏顶**搜索文件夹**：跨层级扁平命中，子夹显示父路径（`爱弥丝 鸣潮/`）。夹数量增长时唯一 O(1) 的路径。

行为契约：

- 标题栏有**折叠开关**，收起后网格吃满整宽（记忆用户偏好）。
- `未分类` 只在智能视图出现一次，**不在「全部文件夹」里重复列**（它是系统集合不是用户夹）。
- 点 `▸` 只展开/收起，**不切换范围**；点行本体才切范围。
- 当前范围高亮 = 左侧 2px 黑条（与主页夹树同一语言）；同一个夹若同时出现在「最近用过」和树里，两处同时高亮。
- **<768 手机**：导航栏塌成工具行里的**范围触发器**（显示当前范围名）+ 全屏 sheet，**同一份数据换个壳**；选中即收起。

### 8.2 其余结构

- 结构：标题 + 折叠开关 + **只读锁定徽标**（`🔒 仅图片`）+ 关闭 › [导航栏 | 搜索 + justified 小网格（行高桌面 132 / 移动 104）+ 底部条]。
- 顶部搜索只搜**素材内容**（提示词/模型）；**搜文件夹在导航栏自己的搜索框**里，两者不混。
- **首格是内联上传格**（虚线），上传完成即选中。
- **mediaType 锁 = 不渲染，不是灰掉**。锁 `image` 时视频/音频/3D **不进候选** —— 灰掉等于承诺「可选」，会误导。
- 三契约保持：
  - **单选**：点击瓦片 = 选中并立即关闭返回原任务；底部只有「取消」。
  - **多选**：瓦片 checkbox + 底部 `已选 N / 上限 M` + 「添加 N 张」；达到 `maxSelection` 时**拒绝新增并就地红字提示**「最多只能选 M 张，先取消一张再选」，不静默吞掉点击。
  - **mediaType 锁**：见上。

### 8.3 单选 / 多选的判据 —— 看**消费端是槽还是集合**，不是「要不要允许多选」

现状 19 个生产挂载点里**只有 2 个多选**（`LoraTrainingDialog` 训练集、`node/ReferenceLandingTabs` 参考图落地），其余 17 个单选。

**为什么必须保留两种**（⛔ 不要为了「统一」把所有入口改成多选）：

|            | 单选                                                                   | 多选                                                                             |
| ---------- | ---------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| 消费端语义 | **填一个槽**（替换）：`setHydratedGeneration(gen)`、音频封面、角色特写 | **往集合里加**（追加 + 有容量）：`addFromUrl` / `addReferenceFromAsset` / 训练集 |
| 回调       | `onSelect(gen)`                                                        | `onConfirmMany(gens[])` + `maxSelection`                                         |
| 完成动作   | 点一下就返回，**1 击**                                                 | 勾选若干 + 确认，**N+1 击**                                                      |

全改多选 = 17 个高频入口（选参考图是最常做的动作）从 1 击变 2 击，且单槽消费端会收到数组、每个调用点都得自己决定「取哪个算数」。**这是纯回退。**

⚠ **但判据现在用错了地方**：`ReferenceImageChip`（Studio 参考图）的消费端是 `imageUpload.addFromUrl` —— **追加**语义，且 `useImageUpload` 有 `maxImagesRef` 容量（模型声明，如 Kling v3 允许多张）。它是**集合**，却挂成单选 → **要放 4 张参考图得开 4 次弹窗**。`GenerateComposer`（`addReferenceFromAsset`）同型。

→ **施工判据**：逐个调用点问「这个消费端是**替换**还是**追加**」。追加的一律改多选，并把**剩余容量**传给 `maxSelection`（照抄 `LoraTrainingDialog` 的 `MAX_IMAGES - imageUrls.length` 写法）。

**实现补注（切片 6 / 6b / 6c 落地时确定）**

- picker 现在是**自己的组件** `AssetPickerBrowser`，`KreaAssetBrowser` 的 picker 模式（`onSelect` / `pickerMultiSelect` / `pickerMaxSelection` / `mediaType` 四个 prop + 横向 chips rail + 底部确认条）**整体退役，不留开关**。
- 对话框宽度从 `max-w-3xl`(768) 改到 `max-w-4xl`(896)：176px 导航栏在 768 上是 22.9%，超了「≤20%」这条验收判据；896 上是 **19.6%**，也正好回到设计假定的 880 量级。
- 「最近」这一档**没有单独实现**：智能视图里它与「全部素材」在 `sort=newest` 下是**同一条查询**，做两行等于摆一个假承诺。现为 全部 / 收藏 / 未分类 三档，「最近用过」那一组给的是**文件夹**（localStorage，契约本来就这么写）。
- picker 的 localStorage 偏好用**懒初始化**读，不走「先默认值再 effect 纠正」：它挂在 Radix portal 里、服务端从不渲染，因此没有 hydration mismatch，也就不必触发 `setState-in-effect`。
- **6c 复核结果**（判据 = 消费端替换还是追加）：改成多选的三个 —— `ReferenceImageChip`、`LoraReferenceImageCards`（都是 `imageUpload.addFromUrl` 追加 + `useImageUpload.maxImages` 容量，为此给 hook 补了可渲染的 `maxImages`）、`storyboard`（`[...prev, gen]` 追加、无上限）。保持单选的四个 —— `Studio3DWorkspace`（源图/已有 3D 都是填槽）、`AssetDetailSheet`（音频封面填槽）、`ReverseEngineerPanel`（反推一张）、`LoraTrainingDialog`（**本来就是多选**）。`AssistantReferencePicker` 的消费端全在 `node/**`，按 Non-goals 留给画布会话（6d）。

**⚠ 按域分工，17 个调用文件对半开**（本包只动非画布那 8 个）：

| 域                        | 文件                                                                                                                                                                                           | 本包是否动手                                                                   |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| **非画布** 8 个           | `ReferenceImageChip`(⚠错挂) · `Studio3DWorkspace` · `LoraTrainingDialog` · `LoraReferenceImageCards` · `AssetDetailSheet` · `AssistantReferencePicker` · `ReverseEngineerPanel` · `storyboard` | ✅ 动                                                                          |
| **画布 `node/**`\*\* 8 个 | `GenerateComposer` · `VideoComposer` · `ReferenceLandingTabs` · `CanvasAppearancePanel` · `CanvasImageSelectionToolbar` · `CharacterDetailBody` · `ImageFamilyBody` · `VoiceDetailBody`        | ⛔ **只出清单与判据，交画布会话** —— 本包 Non-goals 明写「不接管画布并行任务」 |

**`VideoComposer` ✅ owner 2026-08-11 已拍板：改多选，但必须避免刷屏。** 它选中后走 `spawnReference(...)` —— **每选一张 = 画布上 spawn 一个新节点并连线**，所以「改多选」等于「一次生成 N 个节点」。⚠ **多选只在同一个添加位内成立**（一次选的 N 张共享同一个 `role`，跨 role 批量没有语义）。落位四约束 + 一条「N 个节点会精确重叠」的高置信预测与证伪法：交接 `assets-cf-session-handoff-2026-08-11.md` §六（已随任务包清理，git 历史可取）。**施工归画布会话，不在本包。**

**顺带的实现简化**：`AssetSelectorDialog` 现在按 `multiSelect` 渲染**两整棵 `<KreaAssetBrowser>`**（两套 props、两条路径）。应合成一次渲染 + 一个 `mode` prop —— 两种模式在界面上本就只差 checkbox 与底部条。

- 移动端走底部 drawer（`ResponsiveDialog` 现有行为），关闭后焦点回到触发器。
- ⛔ 不把完整素材页缩进弹窗（现状病根）：**picker 里不出现文件夹门牌墙、不出现密度控制、不出现批量管理动作**。导航栏是任务导航，不是管理面板。

## 9. 响应式契约

| 断点              | 契约                                                                                                                                                                                                                                                    |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **≥1280 桌面**    | 顶栏单行全展开；文件夹门牌行自适应张数；行高 S150/M196/L260                                                                                                                                                                                             |
| **768–1279 平板** | 顶栏收起总数与段头次级链接；行高 S128/M168/L222；门牌张数自动减少                                                                                                                                                                                       |
| **<768 手机**     | 全局导航交回应用壳（底栏/抽屉，非本页设计范围）；顶栏首行保留标题与主动作，五个文字分面在框内另起一行并允许换行；文件夹门牌 132 宽横滚；行高 S92/M124/L168（每行 2–3 张）；上传队列贴底通栏；picker 走底部 drawer，其文件夹导航栏塌成范围 sheet（§8.1） |

实测（iframe 真实视口）：1440 每行 7–8 张 / 1280 每行 6–7 张 / 375 每行 3 张，三档行宽均 ≥99.7% 铺满，无横向溢出。

## 10. 与其他业务域的差异来源

Assets **不能长得像** Canvas（深色工程台）或 LoRA（冷瓷灰白装配台）。但差异**不来自配色**——本页明确跟随全局 shadcn 中性皮。差异来自：

1. **内容即主色**：全页 chrome 中性，唯一有色的是作品本身与「未分类待办」的琥珀。
2. **真实比例的 justified 节奏**：其他域都是规整卡片阵列，只有这里由内容比例决定版式。
3. **文件夹门牌拼贴**：本页独有的标志性组件，把「目录项」变成「有脸的馆藏入口」。

三个标志性组件：**文件夹门牌卡** · **justified 真实比例大河** · **上传队列面板**。

## 11. 不能破坏

双维组织（视图 × 文件夹）· 所有完成产物自动回流 · picker 三契约 · 密度偏好持久化（`pv:assets:density`）· 上传是资产入口而非生成 · 素材页不执行生成 · `generationId` deeplink · 触屏软键盘策略 · ResponsiveOverlay focus return。

## 12. 验收证据

- 网格：程序化量测行宽 / 容器宽 ≥99%，无横向溢出；样本必须含 ar 0.56 与 2.77 两个极端。
- 三档视口实拍（1440 / 1280 / 375）+ 每档的空/错误/上传/picker 状态。
- picker：单选点击即返回、多选达上限被拒且有提示、锁 `image` 时候选里零视频/音频/3D；**文件夹导航栏 ≤20% 对话框宽**、任意深度子夹可达、搜索跨层级命中、折叠后网格吃满整宽、375 上塌成范围 sheet。
- 键盘：Tab 可达、focus 可见、overlay 关闭 focus return。
- 对比度：所有新文案对底 ≥4.5:1（用 `contrast-check`，禁目测）。
- 代码闸门：eslint + 全量 tsc + 相关 vitest；视觉基线按 OS 分套。

## Last Verified

- 2026-08-30 · owner 真机反馈移除顶部搜索框，把 `类型 / 状态 / 模型 / 时间 / 排序` 合入同一个吸顶框；默认保持 56px，激活筛选后 chip 行在框内展开。Chrome 1920×911 验证默认 56px、单条件 86px且无横向溢出。
- 2026-08-11（第五轮）· owner 三问：分类有没有更好的方式 / 初始应是全部素材且图片默认不选 / 进文件夹后侧栏没了。→ 新增 **§3.1 分面筛选栏**（取代两组图标 segmented，五个文字下拉，**默认全不选**，生效项成可删 chip）、**§3 末「夹内页必须留在应用壳内」**。⚠ 实测封杀两个看似诱人的分类轴：`characterCardId` 0 条 / `cardRecipeId` 0 条 / `runGroupId` 1 条，无数据支撑；真正有区分度却没露出的是 **model（23 种）与 时间**。
- 2026-08-11（第四轮）· owner 提出「排序不能指定」「点击选择后没做，尤其放进文件夹和上传到此夹」。新增 **§4.2 置顶定序**、**§7.1 选择模式三处缺口**、**§7.2 移动到文件夹目标选择器 + 撤销契约**、**§7.3 上传占位瓦片**。核出四条现状缺口（批量条零选中不出现 / 全仓零 `shiftKey` / 移动是无搜索无嵌套的扁平下拉 / 上传只在成功后才进网格），已在原型逐条验过。
- 2026-08-11（第三轮）· owner 指出 picker「文件夹多了不好选」= 原始需求 F1。**横向 chips rail 作废**，改 176px 可折叠导航栏（§8.1），真机量测 175/878 = 20%，嵌套可展开、跨层级搜索命中、375 上塌成范围 sheet。
- 2026-08-11 · owner 选定方向 B 并逐条确认：文件夹三路径 + 四层治理、补回视图分类、紧凑化、跟随本地 token、回流段删除、「馆藏」改名「文件夹」；网格铺满三处成因修复；1280/375/picker/上传/错误/空态成稿并在真实 iframe 视口量测通过。
