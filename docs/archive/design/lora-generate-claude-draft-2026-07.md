# Claude 未确认草稿 — LoRA Generate「标本索引」候选切片

> 状态：**候选草稿 / 无页面规范权力（2026-07-19）**。本文由 Claude 在未经过 owner 逐项确认时直接生成，已从 `references/pages/` 移出。它只能在后续正式三方向比较时作为普通材料，**不得作为实现输入，不得据此修改 `src/**`\*\*。
>
> 以下全部内容只描述 Claude 的候选 A，不代表 owner 已选择「标本索引」，也不代表布局、术语、色板、组件或交互已经确认。
>
> 真实业务契约以 `docs/references/domains/lora.md` 为准；当前运行回归以 `docs/references/pages/lora-workbench.md` 为准。

## 1. 页面骨架(桌面 ≥1280)

```
[暗壳侧栏] ┃ 骨白工作单画布
┌ 页头规(2px 墨规):LoRA · 工作单|索引|培育     W-编号 · 承印底模(等宽) ┐
│ 左栏(~55%)                     │ 右栏(~45%)                          │
│  MOUNTS 挂载台账(§2)           │  PLATE 样片(§5)                     │
│  PROVENANCE 谱系铭牌(§3)       │  裁切线图版 · 印制中走字             │
│  TYPESET 排版槽(§4)            │  印样条(结果历史 ≤12)(§5)          │
│  [出 图] 墨块 + 等宽元信息行     │                                      │
└──────────────────────────────────────┘
```

- 域画布 `paper`,与暗壳的交界即「开灯」时刻;**必须显式 `color-scheme: light`**(反向应用已知反色坑)。
- 页头 tab = 工作单/索引/培育(现 生成/库/训练 的域语言名;URL section 契约不变,仅展示文案——i18n 三语新键)。
- 承印底模 = 现底模 Select(云端/Runner 分组行为不变),触发器展示为等宽 `承印 WAI-ILLUSTRIOUS 忠实`。

## 2. MOUNTS 挂载台账

- 行 = `编号 01 · 名称 + 家族缩写 + 兼容号(✓/✕) · 权重规 ▮▮▮▮▮▮▯▯▯▯ · 0.60 · 戳记`。
- 戳记:`在册`(墨框)/`停用`(虚线框灰)/`警示`(朱砂框微倾,兼容不符行并带朱砂等宽注「架构不符·不入版」)。兼容判定引擎沿用(`isLoraBaseModelMountCompatible` + 建议底模动作保留,动作以铅字链呈现)。
- 权重规即控件:拖动/点格调权(格=0.1,fine pointer 支持点击格间;触屏 44px 命中区);数值等宽可直接键入。
- 行悬停:墨下划线 + 行尾铅字动作 `调权 停用 除册`(除册朱砂);键盘可达(行 focus + 方向键调权)。
- 尾行 = `03 — + 自索引添入 …… 容量 2/5`。
- 顺序即挂载顺序,拖拽换序保留(拖起时行投 2px 实体影,同索引卡)。

## 3. PROVENANCE 谱系铭牌

- 发丝规围框:缩略图(点击开大图)+ 等宽参数行(seed/cfg/sampler/steps)+ 提示词单行截断 + **`还原此状态`**(墨下划线一等动作,= 现「一键同款」整单复位语义,含二次确认现契约)。
- 编号 `S-03/11` 前后翻;civitai=配方证据,HF=README 样例(双源字段保真,H1 数据契约沿用)。
- 常同挂 Top3 以等宽小注附于铭牌底行(`常同挂 add-detail-xl ×9 · 挂载`)。
- 无数据:整块不渲染(台账直接接排版槽)。

## 4. TYPESET 排版槽

- 重规(1.5px)围框;内部三段:
  1. **触发词锁排**:灰底铅字块(`paper-dim` 底 + 发丝规),每挂载一块,点击切换启停(停=虚线框);S5 编译契约不变(触发词→tray→正文)。
  2. **正文**:自由文本;tray 已选词以反白墨块嵌排展示(点击墨块=从 tray 移除);S6 索引卡补全(首项反白,热度 ●●○,键盘/IME 契约不变)。
  3. **负向段**:虚线规下折叠,展开同正文形制。
- 助手(dock)结果落地 = **待入册行**:虚线框「拟」行浮于排版槽上沿,`入册`(墨框,并入正文)/`弃`;自由词以 `?` 灰注。
- 出图 = 墨块按钮 `出 图`(letter-spacing 加宽);右侧等宽元信息行 `RUNNER 269/300 · 1:1 · 忠实`(现配额/比例/保真 chips 的语义收纳,点击各自弹现有控件)。
- 不兼容警示行:朱砂等宽一行于出图上方(现 S4 契约:不阻断 + 建议底模铅字链)。

## 5. PLATE 样片 与 印样条

- 图版:四角裁切线;空态 = 等宽 `尚无样片 — 排版后落墨`;印制中 = `印制中 ▓▓▓░░ 38%` 走字(禁扫光);成图后操作行沿用现监视器功能(收藏/入素材/放大)。
- **NSFW 浅底策略(实现时真机定)**:默认档 safe 沿用;非 safe 图在浅底以 `paper-dim` 磨砂遮罩+等宽揭示戳(`点按显影`→暂用「点按显示」,避旧暗房词)。
- 印样条:34px 方格接触印样,≤12 FIFO,点击回填图版;当前格墨框。

## 6. 移动端(<lg)

单列:页头规(tab 横滚)→ 台账(行内动作收进长按菜单)→ 谱系铭牌(可折叠)→ 排版槽 → 出图墨块(通栏)→ 图版 → 印样条。物料/助手入口按 bench-IA 合流(V4 切片定)。触屏:权重规格 44px 命中;软键盘策略沿用全局。

## 7. 状态表

| 状态           | 形                                                      |
| -------------- | ------------------------------------------------------- |
| 空挂载         | 台账仅尾行 `01 — + 自索引添入`;页面照常可出图(纯底模)   |
| 印制中         | 图版走字;出图钮变 `印制中…` 灰墨,不可重复提交           |
| 失败           | 图版内朱砂等宽两行:错因 + `重试` 铅字链(现错误契约文案) |
| 缺 key         | 现 QuickSetupDialog 契约,触发链为铅字链                 |
| 停用挂载       | 行灰 + 虚线戳;编译剔除(现契约)                          |
| reduced-motion | 本就无动效戏;走字降为静态百分比                         |

## 8. i18n(三语新键,LoraWorkbench ns)

`sheetTab`(工作单)/`indexTab`(索引)/`nurtureTab`(培育)/`mountLedger`/`stampRegistered`(在册)/`stampDisabled`(停用)/`stampWarning`(警示)/`removeFromLedger`(除册)/`addFromIndex`(自索引添入)/`restoreThisState`(还原此状态)/`platePending`(尚无样片—排版后落墨)/`platePrinting`(印制中)/`pendingEntry`(拟)/`enterLedger`(入册)/`discardDraft`(弃)/`printSample`(印样)。现有功能键(配额/警示文案等)沿用不动。

## 9. 回归红线(实现时逐条守)

URL/section 深链契约 · 编译顺序(触发词→tray→正文)· 兼容判定与建议底模 · 助手 loraContext 装配 · 还原整单复位语义 · H1 showcase 数据链 · 印样 FIFO · 全部 a11y(role/aria/键盘)与三语。行为红线以 `lora-workbench.md` + 各切片测试为准——**本次只换形,不换骨**。

## Last Verified

2026-07-19 · Claude 未经逐项确认生成，后经 owner 明确降为候选草稿并移出 `references/pages/`；未改 `src/**`。
