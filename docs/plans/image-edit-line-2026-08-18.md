# 图片编辑线 — 任务包（2026-08-18 owner 新开）

> owner 原话：「**图片编辑那一片还没做呢。新开一个专门做这个。**」
>
> ⛔ **本包不重写切片规划**。E0–E5 切片表、拉框 vs 手绘的成本分析、生成后编辑的现状，都在
> [`studio-workbench-redesign-2026-08-14.md`](studio-workbench-redesign-2026-08-14.md) **§4.6 / §4.7 / §4.8**，那是唯一施工图。
> 本包只做三件事：**① 开工前的现状核实（含三条更正）② 范围待 owner 一句话 ③ 硬前置提醒**。

---

## 一、⚠ 范围待 owner 定（开工前唯一阻塞）

「图片编辑那一片」有两种读法，做的事不一样：

| 读法               | 范围                                                                           | 依据                                                                            |
| ------------------ | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------- |
| **A · 编辑交互线** | §4.8 的 E0→E5（验底 → 工作台入口 → 拉框 → 多框编号注释 → 编辑历史 → 画布对齐） | §4.8 已成表，可直接开工                                                         |
| **B · 多图编辑**   | 七条能力现在全是 `minImages:1 / maxImages:1`，**一条多图都没有**               | §4.8 明写「多图编辑…**独立成包**」，而 memory 的分期把它列为 **P0（杠杆最大）** |

⚠ **两处对同一件事的排序是冲突的**：施工图说它独立成包、暂不做；构想 memory 说它是 P0 打底。**我不替 owner 判定**，请一句话定：先做 A、先做 B、还是 A+B 都在本包内。

---

## 二、开工前现状核实（2026-08-18 实查，⚠ 三条与既有记载不符）

| #   | 既有记载                                                     | 实查结果                                                                                                                                                                                   | 影响                                                                                                                                                                      |
| --- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 「能力层建成**八条** ready」                                 | ⛔ **现在是七条** —— `decompose` 已由 `cdab2274`（08-18 工作台重设计）整条删除                                                                                                             | **E0 的验底表从八行改七行**：upscale · remove-background · inpaint · outpaint · extract-element · object-replace · style-transfer（`text-render` 仍 hidden 且 models 空） |
| 2   | 「整套挂 `NEXT_PUBLIC_FF_IMAGE_EDITING`（flag 状态未确认）」 | ⛔ **该 flag 零功能消费者** —— 全仓只有 `constants/feature-flags.ts:23` 的定义、`lib/feature-flags.ts:18` 的 rollout 映射、一条 test、`.env.example` 的 `=false`；**没有任何功能代码读它** | **功能是常开的，E0 验底不需要先开 flag**。同 [[reference-feature-flags-mostly-dead]]（六个 flag 死五个）                                                                  |
| 3   | 「手绘已实现 `StudioInpaintEditor`」                         | ✅ **属实** —— 文件在，`<StudioInpaintEditor` **1 处真实渲染**，消费者 `CanvasImageEditWorkspace`                                                                                          | 可直接复用；缺的是**拉框**（§4.7）                                                                                                                                        |

> ⚠ 第 1、2 条都属「**记载与代码脱节**」，正是本轮反复踩的那个病。核实方法写在这里供复查：能力数 = `grep -c "availability: 'ready'"`；flag 死活 = **grep 驼峰名 `imageEditing` 而不是环境变量名**，且**跑一次零过滤的粗结果**。

---

## 三、⛔ 硬前置：E0 真机验底，没这张表不许往下做

owner 2026-08-14 明说 **画布这边他没测过**。所以「七条能力 ready」只是 `availability` 字段写着 ready，**不等于跑得通**。

E0 = 在画布上把**七条**逐条跑一次，产出一张「能力 → 实测结果（真出图 / 报错 / 无入口）」表。

⚠ 本轮已有三次「代码写着 ≠ 跑得通」的实例（M 的幽灵闸、D0 的入口三个月前被删、C4 的机制根本不存在）。**`availability: 'ready'` 是字段值，不是证据。**

---

## 四、方向上已拍板、别推翻的三条

1. **工作台先做好，画布再对齐工作台**（owner 2026-08-14）。⚠ 这**推翻了**早前「画布已有、搬到工作台」的方向——工作台是基准。今天编辑只长在画布（`LooseImageCard` 那条链），**工作台结果栅格点开一张图没有任何编辑入口**，这是 E1 要补的第一件事。
2. **拉框与手绘都做**，但只限选区类（框 / 画笔 / 套索 / SAM），输出统一 mask。⛔ **画箭头当指令已否**。
3. **多框才能编号 ①②③**（涂抹出的一团 mask 编不了号），编号是注释清单进 prompt 的前提 —— 这是 E3 的机理，也是拉框非做不可的原因。

## 五、首发验收场景（owner 07-10 给的样例）

角色三视图设定图上批量标注（换色 / 去鞋光脚 / 换扇）**一次全改**，输出**干净无标注痕**、三视图一致。→ 标注层 × 服装迁移的交点，E3 按此验收。

---

## 六、接手动作

1. 先拿 owner 对 §一 范围的一句话。
2. 读 `studio-workbench-redesign-2026-08-14.md` §4.6–4.8（唯一施工图）。
3. 跑 E0，产出七行验底表，回填本文 §三。
4. 再按 E1→E5 推进。⚠ 每片先落工作台，画布最后对齐（E5）。

**背景与分期结论**（三迁移别做成三个独立功能、P0/P1/P2 划分、路由选路）见 memory `project-image-edit-extensions-ideation`，**别重新调研**。
