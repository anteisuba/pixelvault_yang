# UI 需求卡 · Studio Video 移动端（2026-09-03 owner 拍板）

> 上游：本卡复用 [`studio-image-mobile-request.md`](studio-image-mobile-request.md) 定义的底部固定 composer 结构（chip 行 + prompt 输入 + 生成按钮），换成视频专属 chip 与等待态；方向结论见本卡正文。默认值按 [`../ui-defaults.md`](../ui-defaults.md)（字体/颜色/动效/移动端配方/8 项完成定义）；对应 `domains/studio.md` 「移动端等级」表中 `/studio/video` 一行（与 `/studio/image` 共用「降级」等级，落卡指针分开）。

> 2026-09-03 起画布区为白卡、参数栏躺灰底，共用工作台脊柱 `.workbench-*`（见
> `../ui-defaults.md` §2.2）；<1024 时地台 padding 收到 10px，舞台仍是一张
> 带圆角描边投影的小卡，本卡下方的移动端结构不变。

## UI 需求卡

1. 页面 / 域： `/studio/video`（Studio Video，生成模式）· `<1024`（`useIsMobile`）。桌面 `≥1024` 不变。
2. 用户来这里完成的一件事： 选好用途（文生 / 首帧生 / 多图参考）和模型 → 写提示词（视频必填，即使已挂参考图）→ 点生成 → 等 2–5 分钟（留在本页看进度）→ 看结果 → 重混或换参数再生成。
3. 改什么（范围）： 空态起手屏（用途分段控件置顶 + 2×2 示例卡 + 继续创作）、底部固定 composer（模型/规格/出声/参考图/音频参考/剧本/助手 chip + prompt 输入 + 生成按钮）、排队中态（`StudioVideoQueueStrip` 移动端可见优先 + 说明文案解除 `hidden xl:block`）、结果态（16:9/9:16 播放器 + 动作行 + 反馈行）。不改 `StudioOperatorDock`（移动端本轮维持 `return null`）、不做后台任务（离开页面丢失该任务，owner 本轮接受）、不加相机 `capture` 属性（记为后续）。
4. 状态矩阵： 空（未生成，起手屏 + 用途分段 + 示例卡）· 排队中（`StudioVideoQueueStrip` 卡片：缩略图/spinner、标题、`mm:ss · 阶段`、进度条、取消）· 有内容（结果已出，播放器 + 动作行）· 错误（生成失败 → 队列卡内重试；缺 API key → `QuickSetupDialog`；模型不支持 worker 提交 → 提交时 toast 报错，已知缺口见第 7 条）· 禁用（生成按钮：`videoQueueFull` / `promptRequired`（视频即使挂了参考图也必须有提示词）/ `videoAudioNeedsVisual` / `referenceRequired`/`referenceUnsupported`，均以 toast 报因）· 选中（用途分段、模型/规格/出声 chip 的已选态、各 sheet/抽屉打开态）。
5. 375px 结构（三态，配方见 `../ui-defaults.md §6` 与 `domains/studio.md` 移动端等级表）：

   | 状态       | 375px 呈现                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
   | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
   | 起手（空） | 顶部用途分段控件（文生视频 / 首帧生视频 / 多图参考，三选一，非 composer chip）+ 标题「想拍什么？」+ 2×2 示例卡（16:9 封面 + 一行 prompt）+「继续创作」条；挂参考图后用途随之自动切换到对应档位                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
   | 排队中     | 舞台顶部露出 `StudioVideoQueueStrip`（移动端设为可见优先，不再隐藏在桌面侧栏语境里）：每条一张卡（缩略图/spinner）、标题、mono `mm:ss · 阶段`、进度条（沿用 `resolveGenerationProgress` 的按时估算，封顶 95%，永不到 100%）、「取消」；条下说明文案 `hidden xl:block` 改为移动端也显示：「生成约需 2–5 分钟，请留在本页；完成后会有提示」                                                                                                                                                                                                                                                                                                                           |
   | 结果       | 舞台承载播放器（16:9 或 9:16，按结果比例，高度受 `--studio-generation-media-max-height` 45vh 封顶）；下方 pill `模型 · 5s · 720p`；动作行 下载 / 原图 / 分享 / 重混 / 更多（无「用作参考」——视频不能当参考图）；再下 mono meta `seed · 时长 · 费用`；再下反馈行；结果卡到达后舞台自动滚动到顶部（每轮一次）                                                                                                                                                                                                                                                                                                                                                         |
   | composer   | 三态共用同一条固定底部 composer（不随状态换结构，仅 chip 值与生成按钮文案变）：第 1 行 chip：`模型 ▾`（单选，`modality="video"`，drill 抽屉，按当前用途经 `filterVideoModelByMode` 过滤）· `规格 ▾`（`5s · 720p · 16:9` 汇总文案）· `🔊 出声`（仅当线路 `generateAudio` 契约暴露该字段才渲染，三态镜像规格 sheet 里的开关）· `＋参考图`（数量角标）· `♪ 音频参考`（数量角标，点开既有 `panels.videoAudio` 对话框）· `剧本`（点开既有 `panels.script`）；chip 过多时本行横向可滑动，不折行；第 2 行：mono 费用行 `≈ $X · Ns × $Y/s`（`StudioCostPreview basis.kind === 'video'`）+ prompt 输入 + 黑色方形生成按钮（`↑ 5s`，标签带 `videoDuration`；生成后变 `↻ 5s`） |

6. 交互动作表：

   | 触发                                                            | 即时反馈                                                                                                                                                                        | 动效（按 ui-defaults §4）                                            | 结果                                                                                                           |
   | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
   | 切换用途分段                                                    | 分段选中态立即切换                                                                                                                                                              | `transition-colors duration-fast`                                    | 模型 chip 的候选集按新用途经 `filterVideoModelByMode` 重新过滤；已选模型若不在新集合中则清空回「选择模型」占位 |
   | 点示例卡                                                        | 卡片按压态 `active:scale-press`                                                                                                                                                 | `duration-fast` 颜色过渡                                             | 提示词回填 prompt 输入，光标定位末尾；卡片对应用途自动置顶，不自动生成                                         |
   | 点「模型 ▾」chip                                                | chip 按压态                                                                                                                                                                     | vaul 全屏抽屉滑入（内置）                                            | 打开 `MainModelPicker layout="drill"`（`modality="video"`），只列当前用途可用模型                              |
   | 抽屉内选模型 + 点 footer 确认                                   | 行按压态 + 选中 check；footer 按钮更新为该模型名                                                                                                                                | `transition-colors duration-fast` + 抽屉关闭动画（内置）             | 写入选中模型，关闭抽屉，chip 文案更新，规格 sheet 的档位随模型能力收窄                                         |
   | 点「规格 ▾」chip                                                | chip 按压态                                                                                                                                                                     | 底部 sheet 滑入（内置）                                              | 打开规格 sheet：时长 / 分辨率 / 比例 chip 组 + 原生出声三态开关（仅契约支持时渲染）                            |
   | sheet 内切时长/分辨率/比例                                      | 对应 chip 高亮立即切换                                                                                                                                                          | `transition-colors duration-fast`                                    | composer 规格 chip 汇总文案与费用行 `≈ $X · Ns × $Y/s` 同步更新                                                |
   | 切原生出声三态开关（sheet 内或 composer `🔊 出声` chip 任一处） | 开关状态立即切换，两处保持镜像                                                                                                                                                  | `duration-fast`                                                      | 写入 `generateAudio`；若当前无可视画面输入触发 `videoAudioNeedsVisual`，生成按钮转禁用并在点击时 toast 报因    |
   | 点「＋参考图」chip                                              | chip 按压态                                                                                                                                                                     | 沿用既有参考图面板动效                                               | 打开参考图选择；选定后 chip 显示数量角标，用途分段可能随之自动切换到「首帧生」/「多图参考」                    |
   | 点「♪ 音频参考」chip                                            | chip 按压态                                                                                                                                                                     | 既有 `panels.videoAudio` 对话框动效                                  | 打开音频参考对话框；选定后 chip 显示数量角标                                                                   |
   | 点「剧本」chip                                                  | chip 按压态                                                                                                                                                                     | 既有 `panels.script` 面板动效                                        | 打开剧本面板                                                                                                   |
   | 聚焦 prompt 输入                                                | 输入框获得焦点环                                                                                                                                                                | 软键盘弹出，composer 随 `keyboard-aware-bottom-padding` 上移         | 输入区域不被遮挡，生成按钮保持可见                                                                             |
   | 点生成（enabled）                                               | 按钮变 loading（Spinner 替换 `↑`，宽度不跳）                                                                                                                                    | 提交后 `StudioVideoQueueStrip` 卡片以 `fade-in-0 duration-base` 出现 | 提交生成请求；composer 保留原 prompt 不清空；舞台顶部出现该条队列卡                                            |
   | 点生成（disabled）                                              | 无 hover/press 视觉，`aria-disabled`；点击弹 toast 报具体禁用原因（`videoQueueFull` / `promptRequired` / `videoAudioNeedsVisual` / `referenceRequired`/`referenceUnsupported`） | 无                                                                   | 不提交；`promptRequired` 与图片端不同——视频挂了参考图仍需要提示词，toast 需说明这一点                          |
   | 点队列卡「取消」                                                | 卡片按压态                                                                                                                                                                      | 卡片移除时 `duration-fast` 淡出                                      | 中止该条任务的轮询（`use-unified-generate.ts` 3s×200 轮询），队列条目移除，composer 不受影响                   |
   | 生成结果到达                                                    | 舞台自动滚动到结果卡顶部（每轮一次，`scrollIntoView`）                                                                                                                          | `duration-base` 平滑滚动，`motion-reduce:` 降级为跳转                | 结果卡置顶可见，动作行、mono meta、反馈行随即出现                                                              |
   | 点结果动作行「更多」                                            | 抽屉按压态                                                                                                                                                                      | vaul 抽屉滑入（内置）                                                | 展开完整动作列表（下载/分享/重混等，无「用作参考」）                                                           |
   | 点「重混」                                                      | 按钮按压态                                                                                                                                                                      | 无（直接跳转）                                                       | 用该结果的参数预填 composer（模型/规格/用途/prompt），滚动回起手位置准备再次生成                               |
   | 生成失败                                                        | 该队列卡显示失败图标 + 「重试」                                                                                                                                                 | 既有队列卡失败样式                                                   | 点「重试」重新提交同一请求的参数，composer 状态不变                                                            |
   | 离开页面再返回                                                  | 无 —— 队列卡与该任务状态一并消失                                                                                                                                                | 无                                                                   | owner 本轮接受的已知缺口：无服务端任务列表，刷新/离开即丢失（第 7 条已记为非目标）                             |

7. 非目标： 不做画布/助手在移动端的验证（`StudioOperatorDock` 继续 `return null`）；不做后台任务/服务端任务列表（离开页面丢失该任务，owner 2026-09-03 本轮接受）；不加相机 `capture` 属性（记为后续待办，见备注）；不做 `CompareGrid`（视频恒单条，不支持批量对比）；不修复「移动端模型 sheet 未按 worker 支持度过滤」这个已知缺口（`canSubmitVideoViaExecutionWorker` 是服务端判定，不支持的 adapter 今天在提交时才报 toast 失败，理想是选择器提前隐藏，本轮先记录、待后定）；不碰 service/API 契约；不改桌面 `≥1024` 布局。
8. 参考（可选）： composer 固定结构与用途分段的分层方法复用 `studio-image-mobile-request.md` 已验证的底部 composer + drill 抽屉方案（不借视觉皮肤，只借结构）；等待态说明文案可见性策略参考同卡「不留白板」的空态原则（`ui-defaults.md §7`）。

## 备注

- `StudioWorkspaceUI.tsx` 今天把移动端 composer 硬编码只给图片模式用；本卡落地时需要把 composer 按 `modality` 分派（图片走 `studio-image-mobile-request.md` 那套 chip，视频走本卡这套），不是新增一份平行组件。
- `use-unified-generate.ts` 是按 item 轮询（3s 间隔、最多 200 次）；取消一条队列卡只需要中止该 item 的轮询，不影响同批次其他条目——同源事实见 `StudioVideoQueueStrip.tsx` 顶部注释「失败只失败它自己」。
- 相机 `capture` 属性（移动端拍摄直传作参考图/首帧）本卡列为已知缺口，不在本轮范围，留给下一张需求卡。
- chip 与抽屉/sheet 状态仍归 `studio-context.tsx` 与既有 hooks 所有，移动端只是呈现层，不新增独立 state 源。
