# 05 · 视频剧本 — 现状映射（v3 Phase 0 进行中）

> 最近更新：2026-04-18 晚 · 对应 Phase 0 v3 启动前的代码基线
>
> **注意**：Phase 0 v2（视觉 token 归一）已合入但被 v3（布局归一）重新界定。本文件是 v3 启动前的基线，v3 完成后会重写。

---

## 已落地的 v2 工作（保留价值）

| 改动                                                            | 文件                                                            | v3 后状态                    |
| --------------------------------------------------------------- | --------------------------------------------------------------- | ---------------------------- |
| 视觉 token 归一（rounded / border / surface / padding / label） | VideoGenerateForm / VideoFormSettings / VideoGenerationProgress | 随组件弃用一起失效，但零损害 |
| Remix outputType 保持                                           | StudioGallery.tsx                                               | **仍然有效**，v3 继续使用    |
| CommandPalette 三模式切换                                       | StudioCommandPalette.tsx                                        | **仍然有效**，v3 继续使用    |
| `cjk` props 清理 + `isCjkLocale` import 移除                    | VideoGenerateForm / VideoFormSettings                           | 随组件弃用一起失效           |

---

## v3 Phase 0 启动前的架构

### StudioWorkspace 分支（要改）

```tsx
// src/components/business/StudioWorkspace.tsx L81-97
{
  state.outputType === 'video' ? (
    <div className="flex-1 overflow-y-auto p-5">
      <div className="mx-auto max-w-3xl space-y-4">
        {' '}
        ← 768px 宽度锁定，空间浪费根因
        <VideoGenerateForm activeCharacterCards={characters.activeCards} />
      </div>
    </div>
  ) : (
    <StudioFlowLayout
      canvas={<StudioCanvas />}
      dock={<StudioBottomDock />}
      gallery={<StudioGallery />}
    />
  )
}
```

### 生成链路（不动）

- `useUnifiedGenerate.generateVideo` L223 **已存在** — `generate({ mode: 'video', video: {...} })`
- `GenerationPreview` L319-325 **已识别** `outputType === 'VIDEO'` → 渲染 `VideoPlayer`
- `useGenerateLongVideo` 独立 hook — 尚未集成到 `useUnifiedGenerate`

### StudioFormState 现状

```ts
// 现有（与 video 相关）
outputType: 'image' | 'video' | 'audio'
prompt: string
aspectRatio: AspectRatio
advancedParams: AdvancedParams // 含 negativePrompt 通用字段

// 缺失（v3 要补）
videoDuration: number
videoResolution: string | null
longVideoMode: boolean
longVideoTargetDuration: number
```

### StudioPromptArea 现状

```tsx
// src/components/business/studio/StudioPromptArea.tsx L72-75
const isAudioMode = state.outputType === 'audio'
const { selectedModel: imageModel, modelOptions } = useImageModelOptions()
const { selectedModel: audioModel } = useAudioModelOptions()
const selectedModel = isAudioMode ? audioModel : imageModel // ← video 被 fallback 到 image，v3 要扩展三路
```

### Toolbar / Dock 三模式现状

- `StudioToolbarPanels` — 只针对 image 和 audio 有按钮矩阵，video 未覆盖
- `StudioBottomDock.tsx:42,77` — CardSection 门控是 `workflowMode === 'card' && outputType !== 'audio'`（v3 要决定 video 下策略）

---

## v3 Phase 0 完成后的期望状态

以下是 v3 目标状态，**尚未达成**：

| 文件 / 区块                                 | 目标                                                                           | v3 WP |
| ------------------------------------------- | ------------------------------------------------------------------------------ | ----- |
| StudioWorkspace.tsx                         | 移除 video 短路，统一走 StudioFlowLayout                                       | L01   |
| studio-context.tsx                          | 新增 videoDuration/videoResolution/longVideoMode/longVideoTargetDuration 字段  | L02   |
| StudioPromptArea.tsx                        | 三模式分叉，video 调 `useVideoModelOptions` + `generate({ mode:'video' })`     | L03   |
| StudioVideoParams.tsx                       | 新组件：Duration / Resolution / Negative Prompt / Long Video / Target Duration | L04   |
| StudioToolbarPanels.tsx + Sheets + Popovers | 三模式显隐矩阵                                                                 | L05   |
| StudioCanvas.tsx                            | handleRemix / handleEdit 读 generation.outputType                              | L06   |
| useUnifiedGenerate.ts                       | generateVideo 内置 longVideo 分支                                              | L07   |
| VideoGenerateForm / VideoFormSettings       | 删除                                                                           | L08   |
| StudioVideoMode.tsx                         | 删除（不再被引用）                                                             | L08   |

---

## 本文件维护

- v3 进行中：本文件保持不变，供 Phase 0 过程中参考"起点"
- v3 完成后：重写本文件，记录 Phase 0 最终代码基线（delete StudioVideoMode / VideoGenerateForm, add StudioVideoParams 等）
- Phase 1 开工后：本文件锁定为历史参考，Phase 1 / 2 / 3 的基线由各自 Phase 文档维护
