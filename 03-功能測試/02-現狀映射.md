# 03 · 功能測試 — 現狀映射（基於當前代碼庫）

> 基準日期：2026-04-22 · 掃描範圍：`**/*.test.ts(x)`, `e2e/**`, `vitest.config.ts`, `playwright.config.ts`, `vitest list --json --staticParse`
>
> **現狀**：全倉 **68 個測試文件**（63 個 Vitest + 5 個 E2E spec）· **467 個 Vitest 用例**（static parse，未執行）· **Route 覆蓋 34/80（43%）** · **直接同名 service 覆蓋 6/37**（另有 2 個 service-domain 測試：`free-tier-boundary`、`storage/r2`）

---

## 3.1 Service 層單測

- **Code Mapping**
  - Existing: `generate-image.service.test.ts`(19), `generate-audio.service.test.ts`(18), `video-generation-validation.service.test.ts`(5), `image-transform.service.test.ts`(6), `recipe-compiler.service.test.ts`(13), `usage.service.test.ts`(6), `free-tier-boundary.test.ts`(7), `storage/r2.test.ts`(15)
  - Infrastructure: `vitest.config.ts`, `src/test/api-helpers.ts`, `vitest.setup.ts`
- **Current Status**：部分存在（直接同名 service 6/37；service-layer 測試文件共 8 個）
- **2026-04-22 註記**：`free-tier-boundary.test.ts` 已存在，但它目前是在**記錄 race window**，不是證明並發上限已被原子性保證
- **2026-04-23 註記**：`generate-audio.service.test.ts` 已補到 outbox-backed async path、cached terminal return、first status poll claim/dispatch、expired execution lease fail-closed、free-tier finalize、missing outbox fail-closed、R2 失敗不記 success usage
- **Current Problem**：character-card / arena / prompt / user / 社群 service 仍未測；coverage 報表目前只統計 `src/lib/**` 與 `src/services/**`
- **Next Breakdown**：按能力域分組補齊 — 生成類 · Card 類 · 社群類 · Arena · Prompt/LLM · 治理 · Usage/User · Storage
- **Dependencies**：vitest · `src/test/api-helpers.ts`（已有 Clerk mock / RateLimit mock / Request builders）
- **Risks**：業務演化快，補測後維護成本；LLM 回傳難以穩定 mock
- **Acceptance Criteria**：每個 service ≥ 1 個正常路徑 + 1 個錯誤路徑測試 · 覆蓋率 ≥ 60%

---

## 3.2 API Route 測試（auth → validate → delegate）

- **Code Mapping**
  - Existing（34 個）:
    - `api-keys/route.test.ts`, `api-keys/[id]/route.test.ts`
    - `arena/` 7 個（matches/, [id], vote, entries, leaderboard, personal-stats, history）
    - `generate-audio/route.test.ts`
    - `generate-audio/status/route.test.ts`
    - `generate-long-video/status/route.test.ts`
    - `generate/route.test.ts`, `generate-video/route.test.ts`, `generate-video/status/route.test.ts`
    - `image-transform/route.test.ts`
    - `generations/[id]/route.test.ts`, `generations/[id]/visibility/route.test.ts`
    - `image/analyze/route.test.ts`, `images/route.test.ts`
    - `projects/[id]/route.test.ts`, `projects/[id]/history/route.test.ts`, `stories/route.test.ts`, `stories/[id]/route.test.ts`
    - `prompt/enhance/route.test.ts`, `studio/generate/route.test.ts`, `studio/select-winner/route.test.ts`
    - `usage-summary/route.test.ts`
    - `users/[username]/route.test.ts`, `users/me/profile/route.test.ts`, `users/me/avatar/route.test.ts`, `users/me/banner/route.test.ts`
    - `video-script/route.test.ts`, `video-script/[id]/route.test.ts`
  - Missing（46 個）: webhooks/clerk · admin/_ · background-cards/_ · card-recipes/_ · character-cards/_ 大部分 · collections/_ · follows · likes · generate-long-video/{route,retry,cancel} · health/_ · image/edit · image/decompose · image/proxy · lora-training/_ · models/_ · projects/route.ts · style-cards/_ · voices/_ · civitai-token 等
- **Current Status**：部分存在（34/80，覆蓋 43%）
- **2026-04-22 註記**：目前 route 測試共 165 個 Vitest case，但大多仍偏 auth / validate / delegate 基礎路徑
- **2026-04-23 註記**：`generate-audio` submit/status 已補 route test，驗證了 auth、schema、rate-limit，以及 sync/async delegate 到 `generateAudioForUser` / `submitAudioGeneration` / `checkAudioGenerationStatus(jobId)` 的 contract；audio async submit 現在只要求返回 durable `jobId`
- **Current Problem**：缺業務變異（如 provider 降級、幂等、補償）；admin / webhook / long-video mutation / card system 仍大面積缺測
- **Next Breakdown**：按能力域補齊 — 生成類 · 資產類 · Card 類 · Arena/Follow/Like · Storyboard/Project/Story · Admin/Health/Usage · Webhooks
- **Dependencies**：`test/api-helpers.ts`、Clerk mock、RateLimit mock
- **Risks**：factory 抽象容易掩蓋 per-route 缺口；webhook 簽名 mock 複雜
- **Acceptance Criteria**：80 個 route 全覆蓋 `auth → validate → delegate` 基礎三檔 · 關鍵業務變異有覆蓋

---

## 3.3 核心 E2E 流程

- **Code Mapping**
  - Existing: `landing.spec.ts`, `studio-auth.spec.ts`, `gallery.spec.ts`, `i18n.spec.ts`, `mobile.spec.ts`
  - Config: `playwright.config.ts`（iPhone 14 mobile profile 已配置）
- **Current Status**：部分存在
- **Current Problem**：現有 E2E 更偏 landing / gallery / auth / i18n smoke，六條核心業務流程尚未成型；無 Clerk test keys 隔離環境
- **Next Breakdown**：六條核心流程 — 新用戶首次生成 · Studio 編排 · Character Card 全週期 · Arena 對戰 · Storyboard 發布 · 社交循環
- **Dependencies**：Playwright · Clerk test instance · 獨立 test database · provider mock server
- **Risks**：跨 provider E2E 不穩定（成本、速率、flaky）
- **Acceptance Criteria**：6 個核心流程 green · 每次提交回歸運行

---

## 3.4 韌性 / 邊界測試

- **Code Mapping**
  - Existing: `lib/circuit-breaker.test.ts`(14), `lib/prompt-guard.test.ts`(29), `lib/with-retry.test.ts`(9), `services/free-tier-boundary.test.ts`(7)
  - Missing: `llm-output-validator` 無測 · provider 超時/降級無統一測試 · free-tier 原子性上限無保證
- **Current Status**：部分存在
- **2026-04-22 註記**：`free-tier-boundary.test.ts` 已覆蓋邊界與競態樣本，但當前並發 case 是在**記錄 race condition**，還不是驗證已修復
- **Current Problem**：`llm-output-validator` 無測試；provider 失敗/降級路徑無統一驗證；free-tier race 仍缺原子性保證
- **Next Breakdown**：Retry · CircuitBreaker（已有）· PromptGuard（已有）· LLMOutputValidator · ProviderTimeout · FreeTierRace
- **Dependencies**：Vitest fake timers、mock providers、Prisma test transaction
- **Risks**：時間敏感測試 flaky；mock provider 偏離真實行為
- **Acceptance Criteria**：5 類邊界每類有正常路徑 + ≥ 2 個失敗路徑 · CI 穩定性 ≥ 99%

---

## 3.5 合約 & Schema 測試

- **Code Mapping**
  - Existing: `types/advanced-params.test.ts`, `i18n/completeness.test.ts`, `i18n/routing.test.ts`, `i18n/clerk.test.ts`, `constants/provider-capabilities.test.ts`, `constants/video-model-capabilities.test.ts`
- **Current Status**：部分存在（i18n 完整度最好 · Zod/Prisma 最弱）
- **Current Problem**：大量入口 Zod schema 無單測（register/generate/card recipe 等）· Prisma 遷移相容性無自動測試
- **Next Breakdown**：Zod schemas（所有入口）· Prisma migration 相容性 · i18n key 完整度（已有）
- **Dependencies**：zod、prisma migrate、三語 json 加載
- **Risks**：schema 變動破壞客戶端 · migration 向後相容被忽略
- **Acceptance Criteria**：所有 POST / PUT 入口 schema 單測 · migration 前後兩版相容 · 三語 key green

---

## 測試基礎設施現狀

- ✅ **vitest** 已配置（jsdom · setup 文件齊）
- ✅ **Vitest static parse** 可用於盤點用例數（`vitest list --json --staticParse`）
- ✅ **Playwright** 已配置（含 mobile profile）
- ✅ **api-helpers.ts**（Clerk mock / RateLimit mock / Request builder）
- ⚠️ **Coverage include** 目前只覆蓋 `src/lib/**`、`src/services/**`，route / hook / component 測試不進 coverage summary
- ❌ **MSW** 無（當前以 `vi.mock` / 手寫 fetch mock 為主）
- ❌ **@axe-core / jest-axe** 無
- ❌ **Chromatic / Percy / Storycap** 無
- ❌ **Cypress** 無
