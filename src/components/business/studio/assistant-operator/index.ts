/**
 * 工作台助手 · 操作员化面板（P2）。
 *
 * 对外只有一颗：**外壳**（`StudioWorkspaceUI` / LoRA 工作台挂）。归属标记 rail
 * 已搬进面板的过程行（owner 2026-09-24），其余组件都是面板内部件，不从这里导出。
 */

export { StudioOperatorDock } from '@/components/business/studio/assistant-operator/StudioOperatorDock'
