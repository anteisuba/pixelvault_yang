# 04 · UI 測試 — 現狀映射（基於當前代碼庫）

> 基準日期：2026-04-22 · 掃描範圍：component / hook / context 測試、Playwright、i18n 測試、a11y/視覺工具
>
> **現狀**：Component 測試 10 · Hook 測試 3 · Context 測試 1 · Playwright spec 5（Desktop Chrome + iPhone 14 兩 project）· i18n 測試 3 · **無 a11y / 無視覺回歸工具**

---

## 4.1 頁面狀態（Empty / Loading / Error / Auth / 邊界）

- **Code Mapping**
  - Existing loading/error 文件: `gallery/loading.tsx`, `gallery/error.tsx`, `(main)/error.tsx`, `arena/error.tsx`, `app/global-error.tsx`
  - 測試: 現有 7 個 component test + 1 個 context test，但幾乎都不直接覆蓋頁面級 empty/loading/error/auth state
  - E2E: `e2e/*.spec.ts`（5 個，未按頁面狀態分類）
- **Current Status**：部分存在（loading/error 文件覆蓋不足 · 頁面狀態測試幾乎為零）
- **Current Problem**：15 個 page.tsx 中僅 3 個有 `error.tsx`、僅 1 個有 `loading.tsx`；未登入/Auth 切換、空態、邊界分頁皆無系統測試
- **Next Breakdown**：Empty · Loading · Error · Auth 未登入切換 · 數據邊界（0/1/超長/末頁）
- **Dependencies**：Playwright、testing-library、MSW（需引入以模擬 API 態）
- **Risks**：無 MSW 導致 state 模擬不真；Clerk auth state 難在 component 層模擬
- **Acceptance Criteria**：15 頁 × 5 狀態矩陣 ≥ 70% 覆蓋 · loading/error 文件覆蓋所有核心頁面

---

## 4.2 用戶交互（表單 / 生成 / Modal / 快捷鍵 / 社交）

- **Code Mapping**
  - Existing: `use-unified-generate.test.ts`, `use-like.test.ts`(5), `use-video-script.test.ts`(11) · `LocaleSwitcher.test.tsx`, `ImageCard.test.tsx`(6), `ImageDetailModal.test.tsx`(8), `PrivateProfileView.test.tsx`(6), `ProfileFeed.test.tsx`(5), `ScriptEditor.test.tsx`(10), `ScriptTopicInput.test.tsx`(5), `StudioInputImage.test.tsx`(4), `StudioScriptPanel.test.tsx`(3), `StudioVariantsGrid.test.tsx`(5)
  - Context: `studio-context.test.ts`(38 含 2 条 `SET_SELECTED_WORKFLOW_ID` reducer case) + `studio-context.test.tsx`(5 provider/hook 集成 case，**2026-04-24 新增**)
  - 相關組件（無測試）: `StudioWorkspace`, `ArenaForm`, `CapabilityForm`, `VideoGenerateForm`, `ApiKeyManager`, `OnboardingTooltip`
- **Current Status**：部分存在（44 個 hook 中 3 個有測試；178 個 component 文件中 10 個有測試；另有 2 個 context 測試文件覆蓋 reducer + provider/hook 兩層）
- **2026-04-23 更新**：`use-unified-generate.test.ts` 已補 audio async 的 `FAILED`、timeout、submit success but missing `jobId`、以及 `IN_PROGRESS -> stage=processing`；前端對 audio async 的消費心智已完全收口到 `jobId -> status polling`
- **2026-04-24 更新**：`studio-context` 引入 `selectedWorkflowId` state（workflow-first shell Phase 2）；`SET_SELECTED_WORKFLOW_ID` reducer 同步 `outputType` 但保留手動 `SET_OUTPUT_TYPE` override；新增 `studio-context.test.tsx` 覆蓋 provider/hook 集成路徑（default workflow / switch video workflow / switch audio workflow / manual override divergence / getSelectedWorkflow）
- **2026-04-24 更新（Phase 3）**：Studio shell 顶部新增 workflow-first 条带——`StudioWorkflowGroupTabs` (图片/视频/声音 tabs) + `StudioWorkflowPicker` (当前组 workflow cards) + `StudioWorkflowSummary` (当前 workflow 名字+一句话承诺)；3 个组件 test 文件新增（共 8 cases）
- **2026-04-24 更新（Phase 4 + Phase 5 首刀）**：`StudioTopBar` 砍 image/video/audio toggle + quick/card toggle → 新增"高级"按钮触发 `StudioAdvancedDrawer`；`StudioWorkflowGroupTabs` 扩 tab 自动选首条 workflow + 幂等；`studio-context` reducer 同步 workflowMode + openPanel；新增 `StudioTopBar.test.tsx` + `StudioAdvancedDrawer.test.tsx`；`StudioWorkflowGroupTabs.test.tsx`、`studio-context.test.{ts,tsx}` 扩充。7 files / 61 tests 通过
- **Current Problem**：~~快捷鍵 input focus 衝突~~ ✅ IME 守護已修復；hook 覆蓋率仍低（3/44）
- **Next Breakdown**：表單校驗 · 生成提交/取消/重試 · 拖拽/Lightbox/面板調整 · Modal/Drawer/Toast · 快捷鍵 / cmdk · Follow/Like/Vote
- **Dependencies**：@testing-library/react、@testing-library/user-event
- **Risks**：Studio 多 hook 協同難以單測；拖拽/縮放在 jsdom 下行為受限
- **Acceptance Criteria**：關鍵 10 個 hook + 15 個核心組件有測試 · 快捷鍵與 input focus 衝突可重現

---

## 4.3 響應式（Mobile / Tablet / Desktop）

- **Code Mapping**
  - Existing: `e2e/mobile.spec.ts` · `playwright.config.ts` 含 `Desktop Chrome` + `iPhone 14` 兩個 project
  - Component: `MobileTabBar`（僅 mobile）、`Navbar`（僅 desktop 鏈接）、`use-mobile` hook
- **Current Status**：部分存在（desktop + mobile 已配置；tablet 無專門覆蓋）
- **Current Problem**：tablet viewport 無專門測試；核心頁面斷點一致性無驗證；MobileTabBar↔Navbar 切換臨界點未測
- **Next Breakdown**：Mobile（≤640）· Tablet（641–1024）· Desktop（≥1025）· MobileTabBar↔Navbar 切換
- **Dependencies**：Playwright 多 viewport 配置
- **Risks**：Tailwind 斷點與組件 `use-mobile` 判斷不一致 → 中間尺寸閃爍
- **Acceptance Criteria**：3 個 viewport × 核心 8 頁無 overflow/元素缺失 · 切換臨界點無 layout shift

---

## 4.4 i18n（三語 / 回退 / 格式化 / 溢出）

- **Code Mapping**
  - Existing: `i18n/completeness.test.ts`, `i18n/routing.test.ts`, `i18n/clerk.test.ts`
  - 資產: `src/messages/{en,ja,zh}.json`（1962–1963 行，同步度高）
  - Component: `LocaleSwitcher.test.tsx`
- **Current Status**：已存在（key 完整度與路由都已驗證）
- **Current Problem**：有 key 完整驗證，無視覺文案溢出測試；日期/數字/貨幣格式化邏輯分散；缺失 key 回退行為無顯式測試
- **Next Breakdown**：三語渲染 smoke · 缺失 key fallback · 日期/數字/貨幣本地化 · 長文本溢出（特別是 ja/zh 字符寬度）
- **Dependencies**：next-intl、Intl.\* API、Playwright 三語 snapshot
- **Risks**：日文長詞組在窄卡片溢出破版；中文無空格換行策略不一致
- **Acceptance Criteria**：三語 smoke 頁面通過 · 長文本不破版 · fallback 顯式可觀測

---

## 4.5 可訪問性（A11y）

- **Code Mapping**：**Missing in current codebase**
  - 無 `@axe-core/*`、無 `jest-axe`、無 `eslint-plugin-jsx-a11y` 專項 CI 檢查
  - 應掛在：Playwright E2E 注入 axe · Vitest 組件測試用 jest-axe
- **Current Status**：缺失
- **2026-04-17 更新**：`prefers-reduced-motion` 守護已實現於 Particles 組件（靜態粒子降級）
- **Current Problem**：無 a11y 工具鏈；現有組件 ARIA 標記完整度未知；色彩對比靠設計審查
- **Next Breakdown**：鍵盤導航 · ARIA 語義化 · 色彩對比（對照 `#faf9f5` / `#141413` / `#d97757`）· 螢幕閱讀器 · `prefers-reduced-motion`
- **Dependencies**：新增 `@axe-core/playwright`、`jest-axe` 依賴
- **Risks**：追加 ARIA 屬性涉及所有核心組件，成本高；reduce-motion 影響現有動效代碼
- **Acceptance Criteria**：核心頁面 axe 無 critical 違規 · 鍵盤可達 100% 核心交互

---

## 4.6 視覺回歸

- **Code Mapping**：**Missing in current codebase**
  - 無 Chromatic · Percy · Storycap · Playwright screenshot 基線
  - 應掛在：Playwright `toHaveScreenshot` 作為入門 · 或引入 Chromatic + Storybook
- **Current Status**：缺失
- **Current Problem**：無設計 token 合規自動檢查；無視覺基線；設計 diff 靠人工
- **Next Breakdown**：設計 Token 合規（color/font/spacing）· 關鍵頁面快照 · Dark/Light（若支持）
- **Dependencies**：Playwright screenshot 或 Chromatic/Percy · 可能需要 Storybook
- **Risks**：基線維護成本；反人眼差異的小幅 diff 誤報
- **Acceptance Criteria**：核心 8 頁 × 3 viewport 快照 · CI flake 率 ≤ 2%

---

## 測試基礎設施現狀（UI 維度）

- ✅ **Playwright** 配置（Desktop Chrome + mobile profile）
- ✅ **vitest + @testing-library/react** 可用
- ✅ **StudioContext 組件層測試** 已有基礎樣本（`studio-context.test.ts`）
- ✅ **i18n 三語 key 完整性** CI 守護
- ❌ **MSW / fetch mock** 無
- ❌ **@axe-core / jest-axe** 無
- ❌ **Chromatic / Percy / Storycap** 無
- ❌ **Storybook** 無
