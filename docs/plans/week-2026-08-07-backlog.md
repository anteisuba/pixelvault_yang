# 本周待办清单（2026-08-07 owner 口述）

> 状态：**H · C4 · J4 · K · L · M · E（含 J3）· C+F 已完成**（C+F = `a1be430e`，2026-08-12，owner 确认「已经做完了」）；其他条目以各自并行任务为准。
>
> ⚠ **本文件曾长期滞后于实现**：`a1be430e` 把「设计收口（08-11）」的文档改动与「施工完成（08-12）」的代码打包在同一个 commit 里，于是状态行停在「待 owner 对比选向」，而实际切片 1–6c 都已落地。**读状态前先 `git log --oneline -20` 对一眼**，尤其本清单定于 08-07，其后画布域另有 60+ commit 不在此表内。
>
> - **2026-08-07**：`eb295d23`+`6c3add69`（H）· `84487a71`（C4）· `f9522e44`（J4）· `a77901db`（K）· `1cf1fe2a`（L）· `dafa2898`（M）
> - **2026-08-08**：`c2729530`+`619f0f9f`（E 含 J3 —— 删 184 个文档件 + 补 212 处引用；再删 17 个没人调的 skill）
>   本文件是索引与去重锚点，逐条拆成独立任务包后各自更新。
>   **新会话第一步：读本文件索引表 → 确认该条是「接续」还是「新做」→ 再进对应域文档。** 完整推进流程见文末 §执行流程。

## 索引

| #     | 诉求                          | 性质         | 澄清后状态                                                                                                                                                                                                                                                                    | 第一步                                                                                     |
| ----- | ----------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| A     | 助手分域回复                  | 接续（新层） | ✅ 四条具体诉求，**方向已被 owner 修正**；对标协议已拿到                                                                                                                                                                                                                      | 写回复协议设计稿给 owner 过目                                                              |
| B     | 画布助手→节点                 | 接续（补口） | ✅ **对标已实拍拆解**，授权边界有答案                                                                                                                                                                                                                                         | 写「确认闸」设计稿 + 补 UI 两道门                                                          |
| ~~C~~ | ~~素材页 UI~~                 | 接续         | ✅ **已完成合入 main**（`a1be430e`）——方向 B「大厅与展馆」，Claude 侧切片 1/2/3/4/5/5b/6/6b/6c 全落                                                                                                                                                                           | ⚠ 见下方「两处待确认」                                                                     |
| D     | 图像优化                      | 🔶 进行中    | 五条全要；**D0 两半句已全部完成（多张 2026-08-14 · 多模型 2026-08-18）**——真根因不是写死 4 而是**入口在 `999ad0fa` 被删**、画布侧早有成品。**D1 工作台切片 0–3 已合入**；切片 4/5 **owner 2026-08-18 已拍板解锁**（4=先补单价表 · 5=自动降档到 4 张，连带项见工作台包 §4.11） | D1 进度见 `studio-workbench-redesign-2026-08-14.md` §4.10；开工前先读 §D0 的「三条被推翻」 |
| ~~E~~ | ~~文档清理~~                  | 文档         | ✅ **已完成合入 main**（`c2729530`）——删 184 个文件 + 补 212 处引用；J3 一并做掉                                                                                                                                                                                              | —                                                                                          |
| ~~F~~ | ~~素材选择器 UI~~             | 接续         | ✅ **已完成合入 main**（`a1be430e` 切片 6/6b/6c）——picker 独立成 `AssetPickerBrowser`，KreaAssetBrowser 的 picker 模式整体退役                                                                                                                                                | —                                                                                          |
| G     | 首页 UI                       | 🔶 待设计    | 字体从「等拍板」改为**「有更好的就再设计」**                                                                                                                                                                                                                                  | 铺三个字体方向做对照给 owner                                                               |
| ~~H~~ | ~~LoRA 做同款多挂载失效~~     | **Bug**      | ✅ **已完成合入 main**（`eb295d23` + `6c3add69`）                                                                                                                                                                                                                             | —                                                                                          |
| ~~I~~ | ~~侧边栏 UI 升级~~            | 新做（UI）   | ✅ **已完成合入 main**（`e5ee42d4`，2026-08-18）——「分段浮岛」；⚠ **前置 J1 并未先做**，侧边栏至今无视觉回归保护                                                                                                                                                              | ⚠ J1 仍欠着                                                                                |
| J     | 基础设施补强 + 常量普查       | 工程         | 🆕 四个子项；**J4 与 J3 已完成**（J3 并进 E）；J1/J2 未动                                                                                                                                                                                                                     | J1 补视觉基线（I 的前置）                                                                  |
| ~~K~~ | ~~LoRA 库界面重构~~           | 重构 + Bug   | ✅ **已完成合入 main**（`a77901db`）——非本周清单内，owner 另行交办                                                                                                                                                                                                            | —                                                                                          |
| ~~L~~ | ~~TTS 上限按 provider 拆分~~  | 工程 + 产品  | ✅ **已完成合入 main**（`1cf1fe2a`）——全局 5000 退役，改 per-model + payload 护栏                                                                                                                                                                                             | —                                                                                          |
| ~~M~~ | ~~角色图 LoRA 加号闸退役~~    | 工程         | ✅ **已完成合入 main**（`dafa2898`）——闸是幽灵闸，owner 拍板连组件一并删除                                                                                                                                                                                                    | —                                                                                          |
| N     | Seedance 2.5 接入（**三站**） | 工程         | 🆕 2026-08-08 月审追加，当日 owner 拍板扩成**火山 + BytePlus + fal 三站**。火山那站零阻塞可立即开工；BytePlus 卡 owner 注册账号；fal 卡 terms 未读完                                                                                                                          | 火山站直接开工；同时去注册 BytePlus 拿 key                                                 |
| O     | runner r4a phase-2            | 工程         | 🆕 2026-08-08 月审追加。r4a 已施工验证绿、生产未切换；含 **LRU 水位转正**（止损项，与 Krea2 无关）                                                                                                                                                                            | 先重测 hires-fix 那个 COMPLETED-无输出 bug                                                 |
| P     | Krea 2 管线（runner r4b）     | 🔶 阻塞中    | 🆕 2026-08-08 月审追加。**两个前置都没满足**：worker-comfyui 未发版 + 写实向是未决的产品方向                                                                                                                                                                                  | 不开工；随 2026-09 月审或发版当天复查                                                      |
| Q     | 画布视频域整理 + 模型分类组件 | 🔶 待设计    | 🆕 2026-08-08 owner 口述。**首尾帧查实是功能缺口不是命名问题**（UI 承诺了后端从未实现的能力，owner 拍板补上）；另含三档模式定义、命名统一表、分类第一层从渠道改公司                                                                                                           | 读整理文档 §7 的四条待确认，owner 拍板后才进设计                                           |

⚠ **七条已变十条，且不是全集** —— owner 原话「我后面还有其他需求」。新条目按字母续编并入本表，不要另起文档。

**N · O · P 来源不同**：这三条不是 08-07 口述清单里的，是 2026-08-08 模型月审的产出，owner 当日确认优先级 **N > O > P**。三条各有自己的任务包，本表只作索引：

| #   | 任务包                                                                                   | 一句话                                                              |
| --- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| N   | [`seedance-25-ga-integration-2026-08.md`](seedance-25-ga-integration-2026-08.md)         | 三站对照在 §1.5；火山触点五处已钉死，BytePlus 照 MiniMax 双站抄九处 |
| O   | [`runner-r4-krea2-multiref-2026-07.md`](runner-r4-krea2-multiref-2026-07.md) §3.1        | phase-2 五步，第一步是修 hires-fix 的 bug 不是上 LRU                |
| P   | 同上 §2.5                                                                                | 版本闸/热度数据/容量算账三份复核结论都在那节                        |
| Q   | [`canvas-video-domain-cleanup-2026-08-08.md`](canvas-video-domain-cleanup-2026-08-08.md) | 首尾帧五层断点在 §1；命名表 §2、三档定义 §3、节点盘点 §5、分类 §6   |

---

## A. 助手分域回复

### 已完成的地基（owner 复述确认）

`cadc78b7` 已交付：图片/视频/LoRA/画布四处**真正的多轮对话**，共享浮卡、模型、历史、分享、附件输入。模型统一为 **OpenAI / Gemini / DeepSeek / Claude**（Qwen 已从共享路由源头移除），显示「图片＋视频 / 图片视觉 / 仅文本」能力档。最多 8 个图片/视频附件，来源支持本地 / 素材库 / 画布；稳定 URL 随消息持久化、恢复、分享，可供后续轮次继续分析。不兼容模型禁止发送并提示切换，不丢附件、不用视频封面冒充视频分析。

能力档现状：OpenAI GPT-5.6 Sol 接图片不接视频（按官方能力）· Gemini 真实视频输入 · DeepSeek 契约为文本 · **Claude 厂商支持视觉但 PixelVault 当前调用未接入图片内容块，故暂标文本模型**（← 这是个已知缺口，可作为 A 的附带修复项）。

契约已同步到 `docs/references/pages/assistant-shell.md` 与 `unified-ai-assistant-2026-08.md`。

### 本周要做的四条

**A1 · 三域历史记录区分** — 现在图片 / LoRA / 画布共用一份历史，要按域隔离。

**A2 · 回复要有「对话感」** ⚠ **方向修正**：

> 原话：「回复的内容应该更有对话的感觉，需要和 AI 讨论然后再出方案和结果，**不应该直接出提示词的结果**。」

这条**推翻了「每域一种主产物、直接出结果」的做法**。要的是：助手先和你讨论（问清意图 / 给方向 / 确认口味），**讨论收敛之后**才产出方案和结果。产物是讨论的结论，不是对第一句话的应答。

→ ✅ **「讨论收敛」信号已有答案（来自 LibTV 实拍）**：不靠助手猜、也不靠用户说暗号，而是**每轮回复结尾固定给两个结构化选项** —— `✅ 满意 → <明确的下一步>` / `🔄 需要调整 → 告诉我方向`。点 ✅ 就是收敛信号，对机器完全明确。配套还有「已询问」折叠块沉淀问答对。详见 `libtv-canvas-ui-teardown-2026-08-07.md`。

**A3 · 画布节点自动填入** — 助手设计节点时自动填入生成的关键词；也可以直接用「我上传的图」或「素材库的图」。（与 B 重叠，实现归 B，回复形态归 A）

**A4 · 输入框 `@` 提及素材** — 上传参考图时可以在输入框中 `@`，指定所使用的素材。

→ 与星流的「选中画布元素 + 自然语言指令」是同一类交互，但 owner 要的是 `@` 语法而非选中态。

### 锚点

`src/services/kernel/prompt-assistant.service.ts` · `src/services/node/node-assistant.service.ts` · `src/services/llm-text.service.ts` · `src/hooks/use-assistant-conversation.ts` · `src/constants/node-studio.ts`

---

## B. 画布助手 → 节点生成 → 提示词自动填入

### 已有地基

- 架构：**助手 = 剧本脑 → ScriptDoc → autospawn 投影节点**
- 两阶段剧本流（大纲 → 镜头 → 节点）**引擎全绿**，缺 **UI 两道门**（shots stage 目前仅 API 可达）
- 文档：`canvas-assistant-pipeline-2026-07-26.md` · `canvas-pipeline-gap-2026-07-31.md`

### 对标调研（2026-08-07 实地跑过，owner 登录态）

owner 确认：**libtv = <https://www.liblib.tv/>**（LibTV，LiblibAI 的视频线，**不是** liblib.art 的星流 Agent —— 我最初按星流查的那版已作废）。

#### 共同结构（两个产品高度同构，且和 PixelVault 架构一致）

首页 = **一个自然语言输入框 + Skill/技能库 + 最近项目列表**。没有参数面板。

- UPDream 输入框：「输入创意想法，按 **`/`** 调用技能开始创作」
- LibTV 输入框：「请输入你的创作灵感，或从下方挑选一个 **Skill** 开始」
- 项目打开 → 独立画布页（LibTV: `/canvas?spaceId=&projectId=`），**左画布 + 右对话面板**
- LibTV 顶部有「画布 1」下拉 → **一个项目可以有多个画布**
- LibTV 左下角固定「资产管理」入口 = 素材库

⭐ **Skill 是可复用、有作者、有使用次数的社区资产**。UPDream 官方技能「短视频脚本创作助手」的能力清单，几乎就是 PixelVault 两阶段剧本流的镜像：生成大纲 → 扩展为分场景脚本 → 优化对白节奏 → **为脚本生成配套的分镜描述和画面提示词**。差别是他们把这条链做成了**可分享的技能**，我们是硬编码的功能。

#### ⭐ LibTV 的对话→生成协议（实拍，直接回答 A2 / A4 / B 三个问题）

以画布项目「AI小剧场」的真实对话为样本，完整链路是：

1. **用户给模糊需求**：两张附图 + 「看下这两个图片的 deepseek 设计的区别。然后设计其他模型的。」
2. **AI 不生成，先分析**：列出图1（表情包风格）4 条特征 vs 图2（三视图风格）4 条特征。
3. **AI 反问**：「你想用哪种风格来设计其他模型？」
4. ⭐ **「已询问」折叠块**：结构化沉淀问答对，可折叠回看 ——
   - Q「ChatGPT、Gemini、Kimi 的角色设计用哪种风格？」→ A「图2风格 — 精致chibi三视图（正式设计稿）」
   - Q「接下来优先设计哪些角色？」→ A「ChatGPT娘 · Gemini娘 · Kimi娘」
5. **确认后才执行**：「好的！用图2的精致chibi三视图风格，同时设计三个角色！并行生成…」
6. **结果用表格汇总差异**：`| 角色 | 配色 | 标志元素 |`，三行三个角色。
7. **结构化的下一步选项**（不是自由文本）：
   - 「✅ 全部满意 → 继续生成各角色的成人版全身立绘」
   - 「🔄 某个需要改 → 告诉我具体调整方向」
8. ⭐⭐ **「图片待确认后生成」占位卡**：AI 提出方案后，对话流里插入一张**灰色图片占位卡**，卡内文字就是「待确认后生成」。**方案已定、图未生成、等用户点头。**
9. ⭐ **节点引用胶囊**：用户消息可嵌入画布节点 —— 形态是 `[🖼 ChatGPT娘 精…] 感觉都是女仆装没有分出特点。`，即**缩略图 + 节点名（截断）+ 自然语言**。
10. **每轮结束都带确认闸**：「效果满意吗？ ✅ 满意 → 确认为正式设计 / 🔄 需要调整 → 告诉我方向」

#### 三条结论

**B 的授权边界 → 答案是第三种，比原先四个选项都好。分界线不是「进不进画布」，而是「花不花钱」：**

> **节点结构立即落画布（免费）；像素等确认（花钱）。**

⚠ 我最初根据对话里的「图片待确认后生成」占位卡，误判为「未确认的东西根本不进画布」。**实测推翻了这一点**（2026-08-07 亲手跑了一次生成，见 `libtv-canvas-ui-teardown-2026-08-07.md` §九）：AI 一决定要生成什么，**立刻**在画布上建好空节点、接好连线、填好标题和完整提示词、挂好参考图，视口还自动跟过去 —— 这一切**都在确认之前，且不花一分钱**。只有点了生成按钮才扣积分、才出像素。

为什么这个分法更好：

- 用户**立刻看到 AI 的计划在画布上的空间结构**（要建几个节点、怎么连），而不是读一段文字描述。
- 提示词可以**在生成前审阅和修改** —— 它已经填在节点里了。
- 不会误烧积分，因为贵的那一步单独设闸。
- 撤销栈也不脏：空节点删掉就是，没有"半成品像素"要清理。

且确认动作是**结构化选项**（满意/需调整）而非自由文本，「确认」这个信号对机器是明确的。

**A2 的「讨论收敛」信号 → 就是那两个结构化选项。** 不需要猜用户什么时候聊够了：每轮 AI 主动给「✅ 满意 → 下一步」和「🔄 调整 → 说方向」，点了 ✅ 就是收敛。这同时解决了「什么时候出结果」和「结果对不对」两件事。

**A4 的 `@` → LibTV 的范围比「指定素材」宽得多，但它是补充入口不是替代。**

实测输入 `@` 弹出的是一个**分组下拉**：

```
节点
  🖼 Gemini娘 精致ch…      ← 带缩略图，名称截断
  🖼 ChatGPT娘 精致…
  🖼 ChatGPT娘 男友…
  ⋯  加载更多                ← 分页，不是全量塞进去
模型
  Seedream 3.0 Pro
  悠船 V8.1 / 悠船 V7
  General image V2 / Lib Image
  Seedance 2.x  💎          ← 会员款带钻石标
  Minimax H3    💎
  Kling O3 / Kling 3.0  💎
```

两条判断：

1. **`@` 是统一的寻址语法，不是「插入素材」的快捷方式。** 节点、模型、工作流、资源都是可被同一套语法引用的对象。「@ChatGPT娘 用 @General image Pro 再来三个变体」一句话就说完了引用 + 选型 + 数量。
2. ⚠ **更正**：我一度判断「模型下拉框被 `@` 吃掉了」——**错的**。`read_page` 交互树显示输入框旁有**独立的「选择模型」按钮**，`@` 能引模型只是额外快捷方式。真正值得注意的是**图片模型与视频模型混在同一个 `@` 列表里**、靠会员标区分档位，没有「先选模态再选模型」这一层。

3. **选中节点即自动插入引用胶囊** —— 点一下画布节点，对话输入框自动多出该节点的引用。`@` 是给「没选中但想引用」的场景兜底的。

PixelVault 若只做「@ 素材」会比对手窄一档。

#### 画布 UI 完整拆解 → 独立文档

节点谱系、三种节点视觉状态、选中态四层 UI、提示词富文本编辑器、缩放双策略、画布外壳控件清单、`@` 选择器结构，全部实拍记录在 **[`libtv-canvas-ui-teardown-2026-08-07.md`](libtv-canvas-ui-teardown-2026-08-07.md)**。做 A/B 两条前先读那份。

其中对本项目最直接的五条：

1. **选中节点即自动进入对话上下文**（输入框自动插引用胶囊），`@` 只是补充入口。
2. **提示词是富文本**，`🖼图片1` 胶囊内嵌在句子中间 —— 「提示词自动填入」和「@ 引用素材」在他们那里是同一件事。
3. **动作挂在节点上**（选中后浮出工具条），不做成节点族。
4. **生成数量「1张」和比例、分辨率同级**，压在一个下拉里 —— 对应 D0。
5. **生成前显示积分消耗 `⚡12`**。

#### 尚未拿到（其余已在 2026-08-07 补完，见拆解文档 §九 / §十）

- 视频 / 音频 / 文本节点的选中态动作条（只看了图片节点）。
- 节点 hover 态、多节点并行生成时的画布布局。
- 「视频合成 `Beta`」「脚本 `NEW`/旧版」「风格库 / 特效库」内部形态。
- UPDream 画布内协议（账号已登录，但最近项目为空，**需新建项目才能进** —— 会在 owner 账号里创建数据，待批准）。

---

## C. 素材页 UI（`/assets` → `KreaAssetBrowser`，当前 2198 行单体）

> ✅ **2026-08-11 设计治理走完，进入施工**（owner「设计这边就可以结束了」）。owner 选定方向 **B「大厅与展馆」**（推翻 08-09 的右侧 Dock）。
> **接手读这三份**：⭐**[`assets-cf-session-handoff-2026-08-11.md`](assets-cf-session-handoff-2026-08-11.md)（交接：判断来历 / 未决 / 陷阱，先读）** → **[`../references/pages/assets.md`](../references/pages/assets.md)（唯一页面契约）** → **[`assets-cf-task-packet-2026-08-11.md`](assets-cf-task-packet-2026-08-11.md)（切片 / 禁改 / 验收）**。
> 设计过程与被否方向留作历史证据：[`assets-cf-design-2026-08-09.md`](assets-cf-design-2026-08-09.md) · [`assets-claude-directions-2026-08-11.md`](assets-claude-directions-2026-08-11.md)。
> ⚠ 施工硬前置 = **切片 0 视频 poster 派生**（实测视频缩略图 7/7 全空）。
>
> ---
>
> ### ✅ 已施工完成（`a1be430e`，2026-08-12）+ ⚠ 两处待确认
>
> **已落地**：切片 1（justified 真实比例网格，排版是纯函数 + 16 条单测）· 2（媒体表达，瓦片拆出 `AssetTile`）· 3（分面筛选 + 搜索露出，`GalleryFilters.type/model` → `types/models` 并把 16 个调用方在同一改动里改完）· 4（文件夹门牌行 + 总览页/夹内页改**路由**不是 overlay）· 5/5b（上传队列 + 占位瓦片 + Shift 范围选）· 6/6b/6c（picker 独立成 `AssetPickerBrowser`）。
> 附带：`KreaAssetBrowser` 9 条 react-hooks 报错清零、缩到 1993 行；画布右键菜单加「从素材库选择」（owner 当面指定，超出本包 Non-goals）。
>
> ⚠ **待确认 1 · 切片 0 做了没有。** 上面那条硬前置在实现 commit 的说明里**一次都没提**。视频缩略图 7/7 全空这件事若仍在，切片 2 的「视频时长角标 + hover 预览」会落在空封面上。→ **开一次素材页看一眼视频瓦片**即可判定，别靠读代码。
> ⚠ **待确认 2 · Codex 侧还有没有活。** 实现 commit 写的是「落地 **Claude 侧**全部切片」，而 08-11 的设计阶段是 Codex V2 与 Claude 两套并行。Codex 那侧是被否了、已合并、还是仍有未做的切片，**我没有依据，不替 owner 判定**。
>
> 另：C4 遗留的**存量缩略图回填**（旧图仍是 384px）此前定为「纳入 C+F」，本次 commit 说明中亦未提及，同属待确认。

Owner 当日补充：所有完成的生成资源需要回流到 Assets 统一分类管理；图片/视频要按真实宽高比观看，声音需要封面表达。

owner 六条：

| #   | 诉求                                       | 备注                                                    |
| --- | ------------------------------------------ | ------------------------------------------------------- |
| C1  | 布局：**图片 size 绑定了，上传的图被截断** | 网格瓦片固定比例裁切，非原始比例                        |
| C2  | 优化搜索                                   | ⭐ 引擎已在 `use-gallery.ts`，只缺 UI 露出              |
| C3  | 文件夹优化                                 | 与 F1 同源                                              |
| C4  | ~~**上传的图片被压缩画质很低**~~ ✅ 已完成 | 真根因=384px 派生图，见下方；**存量缩略图回填纳入 C+F** |
| C5  | 上传中的加载状态需要重新设计               | 目前只有 sonner toast                                   |
| C6  | 各按钮点击后的状态需要动效设计             | 全页按钮反馈缺失                                        |

### C4：✅ 已完成（2026-08-07 实修）—— 真根因是**派生图，不是上传，也不是 next/image**

> 下面「保留为事实」的部分经复核仍然成立；「已作废」的部分是开工前的推理，被实测推翻，按 §5 的规矩就地改掉而不是追加更正。

**保留为事实：上传确实没被压。**

1. `CLIENT_UPLOAD_MAX_BYTES = 15MB`（`src/constants/uploads.ts:29`），注释明写 **「Files at or under this are sent _as-is_ (no re-encode, no quality loss)」**。
2. `prepareImageUpload`（`src/lib/prepare-image-upload.ts:55`）第一行就是 `if (file.size <= maxBytes) return file` —— **15MB 以下原样上传，压缩函数根本不执行**。
3. 素材页走的正是这条路径（`KreaAssetBrowser.tsx:893`）。
4. 实测佐证：一张 user-upload 的原图在 R2 里是 **2752 × 1536**，完好无损。

**⛔ 已作废的两条推理：**

- ~~候选 1（高置信）：网格经 next/image 优化，默认 q=75~~ —— **完全不成立**。`next.config.ts:44` 全局 `images: { unoptimized: true }`（自 `400a2f15`，2026-05-15 起），整个 app **根本不走 `/_next/image`**，不存在 q=75，`sizes` / `imageSizes` 全是死配置。「详情页有 `unoptimized` 所以是原图」这个对比也因此无意义——两边都不优化，差别从来不在优化，**在选了哪个 URL**。
- ~~网格糊、点开清楚~~ —— **详情页当时同样糊**。实测：详情页把 384×214 的缩略图撑进 1376×767，**3.58× 放大**。

**✅ 实测出来的真根因（两条，都已修）：**

1. **详情页永远够不到原图。** `getGenerationPreviewUrl` 是 `previewUrl ?? thumbnailUrl ?? url`；而用户上传只产 thumbnail 不产 preview（`createImageThumbnailAsset`，其注释写的意图恰恰是「a stored upload serves its full original in the detail view」），于是回落链停在 **384px 缩略图**，原图在 R2 里躺着从不显示。API 实测：**27 张 user-upload，previewUrl 命中 0 张**，即 100% 中招。
   → 修法：IMAGE 走 `previewUrl ?? url`，缩略图不再有资格当详情图；VIDEO/AUDIO/3D 保留 thumbnail 一档（它们的 `url` 是媒体文件不是图片）。

   ⭐ **这套派生管线本身的来历（owner 2026-08-07 补的，原 commit 没写）**：`cd34b014` 与 `400a2f15` 是同一天的一套动作——Vercel 上 `/_next/image` 会让 Next 服务器重新拉远程大文件、把慢 CDN 探测变成 500，于是关掉实时优化（`unoptimized: true`），改成在 R2 上预派生变体。**这条约束管的是「走不走 `/_next/image`」，不管派生图多大**：派生只在上传时跑一次 sharp，字节之后由 R2/CDN 直供，不回 Vercel 优化器。判据=同一个函数今天已经在编码 1280px preview，768 严格更便宜。

2. **384px 缩略图对网格瓦片太小。** 瓦片是正方形 + `object-cover`，撑满靠的是**短边**，而 `maxSize` 卡的是长边 —— 16:9 的图短边只有 216px。实测 1920 视口 / DPR1：网格宽 1366，comfortable 4 列 = **336px 瓦片**，20 张可见图里 **13 张在被放大**，最差 1.74×。
   → 384 的来历是对齐 `next.config` 的 `imageSizes` 最大值，而那份配置因 `unoptimized: true` 早已失效 —— 又一个「数字没有活依据」（同 H）。改为 **768**（16:9 短边 432 ≥ 336，不再放大）。

**⚠ 第 2 条只对新上传生效。** 已有素材的缩略图仍是 384px 存量文件，网格里照旧偏软；详情页则因第 1 条对**存量素材立即生效**（原图一直都在）。

→ **回填：owner 2026-08-07 拍板「先不回填，并进 C+F 一起做」。** 理由是 C1 裁切 + 网格布局本来就要走设计治理重做，现在按 768 回填一遍，等新网格定了可能又不对。**做 C+F 时必须把这条捡起来**，三件事别丢：

- ✅ **脚本已经有了，别重造** —— `scripts/backfill-generation-previews.ts`（与派生管线同一个 commit `cd34b014` 引入），默认只补缺失的，`--force` 重新生成两个变体。
- ⛔ **但 `--force` 是覆写同名 key** —— 它用的 `buildDerivativeStorageKey` 和线上一致（`<base>.thumbnail.webp`），而 R2 上传带 `CacheControl: public, max-age=31536000, immutable`（`r2.ts:242`）。**覆写会被缓存挡最长一年**，回填了也看不见。要么改脚本写新 key + 更新 DB，要么走缓存清除。
- 尺寸要等新网格的瓦片尺寸定下来再取，别再拍一个数（384 就是这么来的）。

⚠ 提醒：C1「图片被截断」（`aspect-square` + `object-cover` 裁切）**没动** —— 它属于布局，按 §执行流程要跟 C+F 一起走设计治理。

### 不能破坏

双维组织（系统/智能视图 + 私人文件夹）· 所有完成产物可回流 · 选择器三契约 · 密度偏好持久化 · 上传是资产入口而非生成 · 素材页不执行生成。

---

## F. 素材选择器 UI（`AssetSelectorDialog`，当前 16 个调用文件 / 19 个挂载点）

owner 三条：

- **F1 · 文件夹选择目前是横向的，很难选** ← 最痛的一条
- **F2 · 整体布局与 CSS**
- **F3 · 动效设计**

三契约不能破坏：单选 / 多选（LoRA）/ mediaType 锁。

→ **C 与 F 合并成一个包做**：文件夹（C3/F1）、布局、动效、检索件在两处都要，分批会做两遍。

> ✅ **2026-08-11：F 三条已并入 C 的设计包收口**。picker 关键切片见 [`../references/pages/assets.md`](../references/pages/assets.md) §8（任务型 shell / 三契约 / mediaType 锁改为「不渲染候选」而非灰掉 / 内联上传即选中 / 达上限拒绝并提示）；施工是 task packet 的**切片 6**。⚠ 改 picker 前先 grep 全部 **16 个调用文件 / 19 个挂载点**，在同一改动里改完，不留垫片。

---

## D. 图像优化 —— owner 五条全要（2026-08-12 认领，D0 优先）

> owner 原话（D0，自由输入项，排首位）：「**支持一次性生成多张图片或者用多个模型各生成一张。**」

### D0 · 一次多张 / 多模型各一张 —— ✅ **2026-08-14 完成（Studio 侧），⚠ 三条开工前的判断被推翻**

> **一句话**：不是「数量写死成 4」，是**整条变体链路在 Studio 根本没有入口**——写死的 4 从来没人读到过。做法是给 Studio 补上和画布同款的数量档位（×1/×2/×4），画布侧**早就有，不用动**。

**开工前那张表里，三行有两行是错的**（都由真机 + `git log -S` 查实，不是推理）：

| 开工前写的                                       | 实际                                                                                                                                                                                             |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 一次多张「机制在，数量写死用户选不了」           | ⛔ **入口在 `999ad0fa`（2026-05-22「Polish studio composer interactions」）连同拆分按钮一起被删了**。`generateVariants` 自那天起零调用方，`VARIANT_COUNT` 是**死常量**。「选不了」不是因为写死 4 |
| 多模型各一张「机制在，上限 3」                   | ⛔ **同一个 commit 删的**。`compareMode` 状态、`handleGenerateCompare`、内联多选面板全没了；`COMPARE_MAX_MODELS` 现在只被 `ModelSelector` 的计数提示读，而 `multiSelect` 无人开启                |
| 结果展示「`VariantGrid`/`CompareGrid` 真实渲染」 | ✅ 这行是对的。两者都挂在 `StudioCanvas.tsx:287/296` 上，按 `activeRun.mode` 分派——**渲染端一直是完好的，缺的只是上游没人把 mode 设成 variant**                                                  |

⭐ **待确认第 3 条（画布侧有没有）→ 有，而且已经是成品**。真机开 owner 的画布，选中节点 → 生成框参数条最右就是 `×1` 下拉，档位 **1 / 2 / 4**（`NODE_STUDIO_GENERATE_COMPOSER.batchCounts`）。所以这次不是「两处都要做」，是**把画布已有的答案搬到 Studio**。

⚠ **「先查各 provider 单请求出图数」这条前置不成立，别再花时间查**。本项目**没有**「一次请求出 N 张」这条路——B5、画布批量、音效变体三处都是 **N 次独立请求各配一个 seed**（画布 07-28 的注释已经记过同一件事）。真正的天花板是 `PLATFORM_GENERATION_GUARD.MAX_ACTIVE_JOBS_PER_USER = 4`，且**只管平台出资的请求**（BYOK 不受限）。→ 想加第四档必须先动那个数，两者绑死；已写进常量注释。

**改了什么**（Studio 侧，画布零改动）：

| 层   | 文件                                                            | 改动                                                                                                                                                                            |
| ---- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 常量 | `constants/studio.ts`                                           | `VARIANT_COUNT = 4` 退役 → `IMAGE_BATCH_COUNTS = [1,2,4]` + `DEFAULT_IMAGE_BATCH_COUNT = 1`（与画布、音效逐字相同）                                                             |
| 状态 | `contexts/studio-context.tsx`                                   | `imageBatchCount` 字段 + `SET_IMAGE_BATCH_COUNT` action + `batchCount` 面板名（照 SFX 先例形状）                                                                                |
| Hook | `hooks/use-unified-generate.ts`                                 | `generateVariants(input, count)` 收参数；入口按 `variantCount > 1` 路由。**死字段 `runMode` 整个删掉**，compare 改由 `compareModels` 非空触发（一个能力一个字段，不留冗余开关） |
| UI   | `studio/StudioBatchCountPopover.tsx`（新）+ `StudioToolbar.tsx` | 工具条第 5 颗 chip，排在比例、分辨率之后（对标 LibTV 的 `16:9 · 2K · 1张` 阅读顺序）。复用 `tool-surface` 原语，零新视觉                                                        |
| i18n | `messages/{en,ja,zh}.json`                                      | `StudioV2.batchCountLabel` / `batchCountHint`，三语键数一致（4783）                                                                                                             |
| 测试 | `use-unified-generate.test.ts` · `StudioToolbar.test.tsx`       | 新增 2 条（×4 必须散成 4 个带不同 seed 的 `runGroupType: 'variant'` 请求；×1 必须与不传时逐字节相同）                                                                           |

**验证**：真机 `/zh/studio/image` 实拍 chip 与弹层，程序化读 `aria-checked` 确认状态入 reducer；生成路由改由单测证明（**没拿 owner 的积分去跑真实生成**）。闸门三样全绿：tsc 0 错 · vitest 485 文件 4399 条 · eslint 0 error。

**留给下一轮的两件（都不在 D0 范围内，没动）**：

1. ~~**多模型各一张的入口仍然没有**~~ → ✅ **2026-08-18 补齐，并进 D1 做的**（正如这里建议的）。做法不是「模型 chip 支持多选」而是更彻底：`generateCompare(input, models, perModelCount)` 变成**矩阵**入口，模型集 × 张数摊平成 items，`VariantGrid` 退役、两套栅格合一到 `CompareGrid`。所以「多张」和「多模型」不再是两个功能，是同一个矩阵的两条轴——参数区不设模式开关，靠结果矩阵表达（owner 拍板）。
2. **×4 与平台并发闸的边界没量过**：平台出资时 4 张正好顶满 `MAX_ACTIVE_JOBS_PER_USER`，用户在画布/别的 tab 已有任务时会有几张吃 429（变体网格逐格显示失败，不静默吞）。要不要改成串行（画布 07-28 就是这么选的，代价是 4 倍墙钟）**是产品取舍，我没替 owner 定**。

### D1 · 图片 Studio 界面

UI 页面任务 → ⚠ **走设计治理**（域定义 → 三方向 → 关键切片 → owner 确认 → 才写码）。

### D2 · 生成质量（提示词工程 / 默认参数 / 上采样链路）

### D3 · 加载性能（虚拟化 / blur-up / R2 变体 / LCP）

⚠ **开工前先核 C+F 做掉了多少**：`a1be430e` 的切片 1（justified 真实比例网格）与切片 5（选中即插占位瓦片、本地比例参与排版、真实字节进度）很可能已覆盖这里的一部分。**先看现状再定范围**，别重做。与 C4 同源（派生图那条链）。

### D4 · 编辑能力 → ⭐ **2026-08-18 owner 新开独立任务包，本条只留指针**

owner：「图片编辑那一片还没做呢。新开一个专门做这个。」

→ **入口 = [`image-edit-line-2026-08-18.md`](image-edit-line-2026-08-18.md)**（现状核实 + 范围待定 + 硬前置），施工图是 [`studio-workbench-redesign-2026-08-14.md`](studio-workbench-redesign-2026-08-14.md) §4.6–4.8 的 E0–E5。

⚠ 开工前两条已核实的更正：**ready 能力是 7 条不是 8 条**（`decompose` 已被 `cdab2274` 删）；**`NEXT_PUBLIC_FF_IMAGE_EDITING` 零功能消费者，功能常开，不用开 flag**。
⛔ **E0 真机验底是硬前置**——owner 明说画布这边没测过，`availability: 'ready'` 是字段值不是证据。

---

## E. 文档清理 —— owner 已明确

> 原话：「文档这边主要是清理，**已经实现的可以直接删除**。只保留工作流，或者工作的规则。甚至工作流这边可能也有过时的地方，**需要你和我一一核对**。」

三条动作：

1. **已实现的直接删除**（不是归档 —— owner 明确说删）。
2. **只保留工作流 / 工作规则**。
3. **工作流本身要和 owner 一一核对**过时处。

现状规模：`docs/plans/` 60+ 文件（canvas- 前缀 20+，lora- 前缀 10+），`docs/references/pages/` 18 个，`docs/scenes/` 11 个。

→ **执行方式（防误删）**：先出一份「文件 → 判定（已实现 / 在飞 / 规则）→ 依据（对应 commit）」的清单给 owner 过目，**批准后再删**。「已实现」是我的判断，判错就删掉了正在用的东西。

---

## ~~L. TTS 上限按 provider 拆分~~（工程 + 产品 · ✅ 2026-08-07 完成）

> owner 原话：「**开放TTS上下**，加号闸退役。」→ 按「开放 TTS **上限**」理解。

**做法不是把 5000 调大，是拆成两层**（`resolveAudioTextLimit()`，`src/constants/models/audio.ts`）：

- **`declared`（能力）** = 厂商文档写明的每请求上限，写在 `ModelOption.maxPromptChars` 上，跟着模型走。厂商没写就**不写**（`undefined`），不许填猜的数。
- **`enforced`（护栏）** = `declared ?? AUDIO_PROMPT_PAYLOAD_MAX_CHARS`。护栏 = **40_000**，依据是「任何主流 TTS 厂商公布过的最大单请求上限」（ElevenLabs Flash v2.5；同表 v3=5000 / Multilingual v2=10_000）。比谁都大的单请求不是合成请求，是事故或滥用 —— 但它**不是能力承诺**。

**开工后查出来的、比任务书写的更严重的三条**：

1. ⭐ **ElevenLabs v3 早已 `available: false`（2026-07-26 退役，比 Fish 贵 6.7 倍那次）。** 也就是说 5000 这个数的**唯一合法主人根本不在链路上**，而它还在卡着所有人 —— 比 H 的「三个矛盾数字」更纯粹的同一个病。
2. **受害者不止 Fish 一个。** `GenerateAudioRequestSchema.prompt` 是**三种 kind 共用**的，所以 ElevenLabs **SFX** 与 **音乐** 的 prompt 也一直被这个 TTS 数字管着 —— 两个端点的文档里都没有这个限制。
3. **`referenceText`（Fish 克隆参考音频的文字稿）也在蹭这个数**，而且服务端有一处是**裸字面量 `.max(5000)`**（`generate-audio.service.ts` 的 outbox schema），连常量都没引 —— 全局搜 `TTS_MAX_TEXT_LENGTH` 搜不到它。已改为 `TTS_REFERENCE_TEXT_MAX_CHARS`（值不动：**收紧的风险不对称**，会砸掉正在工作的克隆对）。

**Fish 的依据（本轮独立复核，与 J4 一致且更硬）**：[API reference](https://docs.fish.audio/api-reference/endpoint/openapi-v1/text-to-speech) 对 `text` 没写上限；官方博客明写 S2.1 Pro API 线是 **"no hard character cap (subject to Fair Use Policy)"**；且 API **自带 `chunk_length`（100–300）内部分段**——本来就是给长文本设计的。故 Fish **不声明 declared**。

**改了什么**（六处）：

| 层     | 文件                                 | 改动                                                                                                       |
| ------ | ------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| 常量   | `constants/audio-options.ts`         | `TTS_MAX_TEXT_LENGTH` / `TTS_PROMPT_WARNING_LENGTH` 退役 → 护栏 + 90% 比例 + 参考稿常量                    |
| 目录   | `constants/models/audio.ts`          | v3 挂 `maxPromptChars: 5000`（带出处）；Fish/SFX/音乐**故意不挂**（带证伪方法）+ `resolveAudioTextLimit()` |
| Zod    | `types/index.ts`                     | prompt 只保留 payload 护栏；两处 `referenceText` 换常量                                                    |
| 服务端 | `services/generate-audio.service.ts` | 新增 per-model 闸，**放在 kind 分派之前**（语音/SFX/音乐都按各自模型判）；裸 5000 修掉                     |
| UI     | `StudioPromptArea.tsx`               | 上限按选中模型解析；**没有 declared 时不印分母**（印 40000 等于承诺一个没验过的数）                        |
| i18n   | `messages/{en,ja,zh}.json`           | 新增 `audioPromptMetaNoLimit`；`audioPromptMetaNoModel` 去掉 `{max}`                                       |

**留给下一波的两件事（都已写进代码注释，不用重查）**：

- ⚠ **护栏不是能力承诺**：40_000 字 ≈ 44 分钟语音，几乎必然先撞 `timeoutMs`（Fish 60s）。**这堵墙在哪没量过** —— 要把长文本变成对用户的承诺，先量合成耗时，别再拍数。量法与归属见 `audio-domain-design-2026-07.md` §2.2（触发条件已同步改成 per-model）。
- **SFX / 音乐的 prompt 上限仍属「查了但拿不到」**：两处 API reference + 公开 OpenAPI 都只写了 duration / prompt_influence / music_length_ms，没有字数。按判例②**保持不声明**而不是猜一个，证伪方法写在常量注释里。

---

## ~~M. 角色图 LoRA 加号闸退役~~（工程 · ✅ 2026-08-07 完成合入 main `dafa2898`）

**开工前的任务描述有一条是错的，而且是关键那条。** 我写的是「留它的理由是性质不同——它弹 toast 明示用户，属**拦住并告知**」。实际上：**这道闸没有任何用户能碰到。**

⭐ **真实情况**：`CharacterImageLoraControls` 自 `04f8f6be`（2026-08-05「refine canvas and homepage workflows」）起**零消费者**——那个 commit 把它从详情面板摘掉、同时加了一条断言钉死它不再渲染（`ImageFamilyBody.test.tsx:169`）。全仓 grep 只剩 barrel 的 `export *` 和注释在提它。真机复核（owner 的 dev server + 真实画布）：角色图详情面板七槽是 角色卡 / 绑定音色 / 身份包 / 显示名 / 外观描述 / 听觉身份，**没有任何 LoRA 控件**；节点内联生成条也只有模型、比例、×1。`node.data.loras` 只有两处**读**（generate 路径），没有任何 UI **写**，实测 20 个节点的该字段全为空数组。

**owner 拍板**（2026-08-07，选择题）：① 角色图**不要** LoRA 能力 → 组件 + 测试一并删除；② 卡片域那道 5 上限**先不动**。

**改了什么**：

| 层     | 动作                                                                                                                                                             |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 组件   | `CharacterImageLoraControls.tsx` 整体删除；`node/index.ts` 的 `export *` 同步删                                                                                  |
| 画布   | `StudioNodeWorkbench` 两条 generate 路径的 `node.data.loras` 死读退役；随之 `advancedParams` 与 `hasCapability` 导入一并清理                                     |
| 常量   | `NODE_STUDIO_CHARACTER_IMAGE_LORAS.maxItems`（零注释裸数字 5）退役；四个 scale 字段保留（它们是 provider 真实取值域，仍被 `NodeWorkflowLoraSelectionSchema` 用） |
| 能力表 | `provider-capabilities.ts` 的 `maxLoras` 注释更新——读者从「两个」变成**一个**（只剩竞技场 `CapabilityForm` 的加号闸）                                            |
| i18n   | `StudioNode.characterImage.lora` **整个 namespace**（21 键）+ `detail.fieldLoras` 三语删除，各 -28 行，三语键数一致（4665）                                      |

**故意没删的**：`NodeWorkflowLoraSelectionSchema` 与 node data 的 `loras` 字段。它们是**持久化数据形状**，删掉等于把既有画布里的该字段在 parse 时剥掉；留着零成本，日后把编辑入口接回七槽时也省一步。

**开工后查出的、任务书没写的两条**（都在 M 范围外，owner 已定「先不动」，登记在此）：

1. **卡片域的 5 上限是活的，而且是双层**：`SimpleCardManager` 的 `maxLoras = 5` 默认 prop（**没有任何调用方覆盖它**）+ 服务端 `CreateCharacterCardSchema` / 背景卡 / 风格卡三处 `loras.max(5)`。角色卡、背景卡、风格卡都受它管。这与 H/J4/M 的「挂载路径不设上限」并不矛盾——owner 那次拍板说的是挂载路径，卡片域不在范围内。
2. **`AdvancedParamsSchema.loras.max(5)` 是惰性的**：注释写着「up to 5」，但**没有任何路由整体 parse 过它**（只有两个客户端 helper 用 `.shape.<字段>` 做单字段校验）。唯一会 parse 的 `/api/generate` 由角色卡创建表单调用，而那条路径根本不传 loras；画布走 `/api/studio/generate`，其 `advancedParams` 是开放 `z.record` → 不受这个 5 约束。⚠ 属「写着但不生效」那一类，别把它当成现行上限引用。

**参照判例**：H（`eb295d23` + `6c3add69`）与 J4（`f9522e44`）——不是把 5 调大，是整条闸删掉。M 又加了一条：**闸退役之后要再问一句「这道闸现在还有人能碰到吗」**，否则退役的是个幽灵。

---

## G. 首页 UI

方向已拍板：Mobbin 式，基准 `homepage-motion-design-2026-07-27.md` §9（排版唯一强调 / 纯黑白 / 第二屏放产品界面）。

**字体（2026-08-07 更新）**：owner 原话「**字体如果有更好的可以再设计**」。→ 这条从「等 owner 拍板」变成「**我方出方向**」。**别再用选择题问**（已问过一轮无结果）。做法：铺 3 个字体方向，每个配同一段真实首页文案的对照渲染，让 owner 看着选。排版是这个方向唯一的强调手段，字体不定后面全是白工。

⚠ 「白厅画廊」是**线上现状**已进 main，不是待合方向。
⚠ 首页是全项目唯一允许 GSAP 的域，必须动态导入不进主 chunk（落点＝首页营销域 `home-v4/`）。

---

## ~~H. LoRA「做同款」后多挂载失效~~（Bug · ✅ 2026-08-07 完成）

> owner 原话：「lora这边也有一个要修复，你先记录下，Lora这边多挂载的问题。**点击做同款后多挂载失效**。」

**已合入 main**：`eb295d23`（LoRA 侧）+ `6c3add69`（服务端 compiler 侧）。全量 tsc + vitest（467 文件 / 4188 项）绿。

**真根因**（与开工前的猜测不同，记在这里免得下次重蹈）：挂载上限在代码里有**三个互相矛盾的数字，没有一个来自模型方** ——

- 能力位 `maxLoras`（Replicate=2）**只**被渲染成装配栏 `N/M` 余量小字，全项目没有一处拿它做闸；
- `LORA_STACK_MAX=3` 是「界面清爽」拍的数，却是唯一真做闸的（push 守卫 / 做同款补挂 / stackFull 三处）；
- `card-recipe-compiler.service.ts` 另写死 `REPLICATE ? 1 : 5` 再 slice。

症状因此有两个方向：挂到 3 时计数器显示自相矛盾的 `3/2`；栈一到 3，做同款要补挂的额外 LoRA 被硬上限挡下，只丢一句「仍有 N 个未挂载」不说原因。

**核实结论**：fal 官方文档「You can use any number of LoRAs」· Replicate `delta-lock/noobai-xl` 的 `loras` 是不限长度的列表 · runner 是自家 ComfyUI（一个 LoRA 串一个 `LoraLoader`）。**三个后端都不限数量** → owner 拍板**不设上限**，三处闸门与 compiler 截断一并退役。

**同批附带**：做同款不再无条件全挂 —— 详情弹窗每个额外 LoRA 带复选框（默认全勾、可单独取消、翻图重置）；另修平板布局，装配台外层网格断点 `md:`(768) → `lg:`(1024) 对齐 `useIsMobile`。

**⚠ 开工前写在这里、被实修推翻的三条**（别再照抄）：

1. ~~「上限 3，UI 显示 `0/3`」~~ → UI 实际显示的是 `已挂数/maxLoras`，而 maxLoras=2 与真闸门 3 对不上，**那个读数本身就是 bug 的一部分**。
2. ~~锚点 `LoraSourceRecipeModal.tsx:512`~~ → 那只是「做同款」按钮，不是根因所在；根因跨 `use-active-lora-stack` / `LoraWorkbench` / `provider-capabilities` / `card-recipe-compiler` 四处。
3. ~~「两处入口都要验（Modal 与 Strip）」~~ → Strip **内嵌** Modal，是同一条链路；`CivitaiLibraryPane` 用的是 `variant="library"`，本来就不承担做同款。**只有一个入口。**

---

## I. 侧边栏 UI 升级（2026-08-07 追加）

> owner 原话：「侧边栏这边UI也要做一次升级。」（附了侧边栏截图）

**现状结构**（截图口径，2026-08-07）：

- 顶部：品牌 `ANTEI` + 折叠按钮
- 一级：画廊 · 提示词 · 素材 · 卡片管理
- 分组「工具」：图像 · 视频 · 音频 · 3D · **LoRA**（当前高亮）· 画布 · 竞技场 · `敬请期待 3`（可折叠，带计数）
- 底部：`896`（积分）· `今日免费 20/20` · 用户头像

**性质**：UI 页面任务 → ⚠ **必须走设计治理**（域定义 → 三方向 → 关键切片 → owner 确认 → 才写码）。

⚠ **侧边栏是全局共享件**，所有页面都挂它。按 [[token 最小统一化]] 的「薄脊柱 + 域皮肤」原则，域定义第一件事要想清楚：**侧边栏属于「薄品牌脊柱」，不是任何单域的皮肤**。因此它不能套 lora.css / canvas.css 任何一个域的视觉，改动的判定标准也更严 —— 它同时出现在画廊、Studio、LoRA、画布、素材五个视觉世界旁边。

**锚点**：`AppSidebar` / `AppSidebarContent`（用的是 shadcn sidebar 原语 `SidebarContent` / `SidebarGroup` / `SidebarMenu`）。
**契约文档**：`docs/references/pages/app-shell.md`（改前先读，改后要同步）。

⛔ **前置：先做 J1 补视觉基线。** 侧边栏是全项目唯一「改一次影响所有页面」的件，而它所在的区域**目前零视觉回归保护**（基线只有首页 + Studio 三张）。没有基线就动它 = 改坏了只能靠肉眼在五个页面里找。

---

## J. UI 基础设施补强（2026-08-07 实查发现）

> 起因：owner 问「为了出想要的视觉效果提前做了什么」，实查了一遍动效 / 皮肤 / 验证三层。前两层地基完备，第三层几乎为空，另查出两处死引用。

### 已有的地基（不用重造，直接用）

- **动效 canon 双份同步**：`src/app/globals.css` 与 `src/constants/motion.ts`（135 行）。标准曲线 `cubic-bezier(0.22, 1, 0.36, 1)`；四档时长 `--duration-fast/base/slow/reveal = 120/200/320/500ms`；交错 `STAGGER_STEP_S 0.05` / `STAGGER_MAX_S 0.3`；画布专属 `--ease-ingest`、`--ease-soft-return` 与 `INGEST_MOTION`、`NODE_EDGE_SIGNING_MOTION` 编排常量。**写动效调档位，别现编数字。**
- **薄脊柱 + 域皮肤已落地**：`globals.css` 1741 行（脊柱）+ `canvas.css` 4063 / `home-v4.css` / `lora.css` 133（域皮肤）+ `auth.css` / `legal.css`。
- **动画库分工已执行到位**：`motion` 15 处（app 内部唯一）· `gsap` 仅首页营销域一处（符合「GSAP 只在首页营销域」的约定）。

### J1 · 补视觉基线 ⛔ I 的前置

**现状**：`e2e/visual.spec.ts-snapshots/` 只有 2 张（homepage 桌面 + 移动），`e2e/studio.visual.spec.ts-snapshots/` 1 张（studio）。**共 3 张，且只有 `-win32` 一套。**

→ 画廊 / 素材 / LoRA / 画布 / 卡片管理 **全无视觉回归保护**。

**要补**：画廊 · 素材（`/assets`）· LoRA · 画布 各至少一张。

**可照抄的现成件**：`e2e/tools/canvas-ui-shots.mjs`（画布 UI 实拍夹具，零写库）· `playwright.config.ts` · `e2e/auth.setup.ts`（登录态）。
⚠ 基线按 OS 分套（`-win32` / `-darwin`），Mac 那套需在 Mac 上补跑。

### J2 · 删死依赖 `framer-motion`

`package.json` 同时装了 `motion ^12.38.0` 和 `framer-motion ^12.40.0`（后者是前者的旧包名）。实测 **`from 'framer-motion'` 的 import 数为 0**，是纯粹的死依赖。删掉即可，注意先全量 grep 复核。

### J3 · 修悬空引用 ✅ 已完成（并进 E，2026-08-07）

⚠ **任务书写的是「`globals.css:162` 一处」，实际是 7 处** —— `globals.css:162` · `constants/motion.ts:2` · `hooks/use-mobile.ts:5` · `ui/responsive-dialog.tsx:33` · `ui/responsive-popover.tsx:22` · `studio-shared/primitives/tool-surface.tsx:96` · `business/AssetSelectorDialog.tsx:54`。另有 3 处写的是裸 `direction.md`（`globals.css:341/:860` · `e2e/mobile.spec.ts:6`），**grep 全路径搜不到**。

动效 canon 改指 `src/constants/motion.ts`（它本身就是唯一来源）；断点/覆层规则改指 `docs/references/frontend.md`；composer 象牙台改指 `docs/references/domains/studio.md`。同批还修掉 19 处指向 `docs/spark/**`、`docs/product/roadmap.md`、`docs/reference/design-system.md` 的**更早就断了**的引用。

### J4 · 无依据常量普查（owner 2026-08-07 拍板「顺带扫一遍」）

**起因**：本周三个实修 bug（H、C4、K#2）根因高度同构——**一个数字/顺序对齐了不再有效、或从来不存在的依据**。三次三中，故普查同类。

**判据**：对每个上限/尺寸/回退顺序问两句——**①这个数字（或这个顺序）的依据是什么？②那个依据现在还成立吗？** 答不出①的就是候选。

#### ⭐ 两个已验证的病型（按此分头扫）

**病型 A · 写死的上限对齐了失效依据**

| 案例 | 数字                   | 对齐的依据                    | 依据真实状态                  |
| ---- | ---------------------- | ----------------------------- | ----------------------------- |
| H    | `LORA_STACK_MAX = 3`   | 「界面清爽」                  | 三个后端从来不限量            |
| C4-② | 缩略图 `maxSize = 384` | `next.config` 的 `imageSizes` | 因全局 `unoptimized` 早已失效 |

**病型 B · 派生资源的回退链顺序选错档**（⚠ 三例里占两例，比病型 A 更高发）

| 案例 | 回退链                              | 后果                                                                                        |
| ---- | ----------------------------------- | ------------------------------------------------------------------------------------------- |
| C4-① | `previewUrl ?? thumbnailUrl ?? url` | 用户上传只产 thumbnail 不产 preview（27/27 全中）→ 详情页停在 384px 撑到 1376px，3.58× 放大 |
| K#2  | `thumbImageUrl` 排在三档回退第一位  | 那是 96px 档（给 56px 小方块用的），而卡片约 200px → 放大 2 倍                              |

> **病型 B 的判据**：凡是「多档派生图 + `??` 回退链」，都要问**「这条链的第一档，对得上消费它的那个容器的实际尺寸吗？」**——两次都是链的顺序按「哪档先有」排，而不是按「消费方要多大」排。
> **扫法**：grep `?? .*[Uu]rl`、`thumbnail`/`preview`/`thumbImage`/`cardImage` 这类档位命名，逐个对消费方的实际渲染尺寸。

#### 已扫范围与结果（`src/constants/` 的 47 个 MAX/LIMIT/CAP 常量）

> ✅ **本节两条候选已于 2026-08-07 验证完毕并处置**，结论见每行的「验证结果」列。续扫结果见下方 §续扫。

**⚠ 高度疑似（结构与 H/C4 同型，需实测确认后再动）**

| #   | 位置                         | 现象                                                                 | 为何疑似                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ---------------------------- | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `ArenaForm.tsx:231`          | `getMaxReferenceImages(a)` **只传 adapter 不传 modelId**             | `resolveConfig` 的 `if (!modelId) return base` 会回落到 adapter 级。OPENAI adapter 级 `maxReferenceImages: 1`、模型级 `OPENAI_GPT_IMAGE_2` 是 16 → 竞技场里 OpenAI 从 16 掉到 **1**；且 `Math.min` 会把混选的 Gemini(14)/火山(14) 一起拖到 1。⚠ 注释写「across all selected **models**」而代码取的是 adapters，**注释与实现不一致**。`arenaMaxRefImages` 同时驱动上传闸与「超限」提示，**确实做闸**（已排除 H 那种只显示不做闸的可能） |
| 2   | `TTS_MAX_TEXT_LENGTH = 5000` | 一个数字**一刀切卡两个 TTS provider**（`fish_audio` + `elevenlabs`） | 与 H 完全同型：一个写死上限管多个后端，而各后端限制不同。且它不止是 UI 闸——`types/index.ts:563` 的 Zod schema 也用它，服务端一起卡。注释仅一句同义反复的「TTS text input constraints」，**没写 5000 的来源**                                                                                                                                                                                                                           |

**✅ 两条的验证结果（2026-08-07）**

| #   | 验证方式                                                                                                     | 结果                                                                                                                                                                                                                                   | 处置                                                                                             |
| --- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| 1   | 跑测试实际调用 `getMaxReferenceImages`（**执行**而非读代码），并核对 `/arena` 路由与 `ReferenceImageSection` | **成立**。adapter-only 得 `1`，model-aware 得 `16`；混选时 `Math.min` 把整组拖到 1。且 `maxImages` 是真闸——`canAddMore` 拦添加，`multiple={maxImages > 1}` 让文件选择器**退化成单选**。`/arena` 是活路由，`opt.modelId` 就在同一对象上 | **已修**：`ArenaForm` 传 `opt.modelId`；`provider-capabilities.test.ts` 加回归测试钉死两者的差距 |
| 2   | 查两家官方文档                                                                                               | **成立，但不是「交集」**。ElevenLabs v3 = **5000**（官方文档，恰好等于本项目的数）；Fish Audio 官方 API reference 对 `text` **根本没写上限**。即 5000 是 ElevenLabs 的数被套在 Fish Audio 头上                                         | ✅ **已修（L，2026-08-07）**：拆成 per-model `declared` + payload 护栏，见 §L                    |

⚠ #2 当轮只写注释没改数字（理由：放宽是产品决策，且与 §2.2 长音频管线耦合），owner 随后拍板「开放」→ **已按 §L 落地**。实修还查出**当轮漏掉的三条**：v3 那个唯一合法主人早已 `available: false`、SFX 与音乐也一直被同一个数管着、`referenceText` 蹭这个数且服务端有一处**裸字面量 5000**（grep 常量名搜不到）。→ 📌 **判据补一条**：「一个数字管着几个后端」要**数全**受害者 —— 我当时只数了两个 TTS provider，实际是**四个模型 + 一个无关字段**，而且数字的主人已经退役。**顺着常量名 grep 不够，还要顺着它保护的那个字段 grep**。

**⚠ 无依据但影响面较小（裸数字，注释答不出「依据是什么」）**

`AUDIO_SPEAKER_VOICE_IDS_MAX = 8` · `VOICE_TRAIN_MAX_FILES = 8` · `VOICE_TRAIN_MAX_FILE_BYTES = 10MB`（Fish Audio 训练限制？）· `HUGGINGFACE_LORA_MAX_LIMIT = 40`（HF API 分页上限？）— **这四条本轮未验证，仍是候选。**

~~`OPENAI_GPT_IMAGE_MAX_REFERENCE_IMAGES = 16`~~ 与 Gemini 的 `14` — ✅ **已查证并补上来源注释**：OpenAI `/v1/images/edits` 对 gpt-image 系列确为 16 张；Gemini 3 Pro Image 每次 prompt 上限确为 14 张（含 ≤6 物体 / ≤5 人物）。OPENAI adapter 级的 `1` 不是能力值而是**保守地板**，候选人传了 modelId 就会被覆盖——这一点已写进 `ArenaForm` 的注释与回归测试。

**✅ 已确认有依据（注释写了完整推理，不用动）**

- `REFERENCE_VIDEO_MAX_DURATION_SECONDS = 15` —— 合并时长 ≤15s 的预算分配，且服务端二次校验合并时长
- `HUGGINGFACE_LORA_MAX_FILE_BYTES = 2GB` —— Anima 完整 UNET ~4.18GB vs rank-512 提取 adapter <2GiB 的分界，超过即属 base-model 而非 LoRA
- `CIVITAI_LORA_CONTENT_TYPE_MAX_FETCH_LIMIT = 480` —— 兜底 `page=99999` 类篡改，并明写了「触顶后深页可能提前 hasNextPage=false」这个已知取舍

> ⭐ 这三条正是**好注释的样板**：写清依据来源 + 推理 + 已知取舍。新写上限常量照这个写。

#### 续扫结果（2026-08-07，覆盖原「尚未扫」四块中的后四块）

**🆕 新查出一条，与 H 同型：`FAL_LORA_CAP = 5`（`src/lib/merge-stack-loras.ts:3`）**

零注释的裸数字，`.slice(0, FAL_LORA_CAP)` 截断合并后的 LoRA 列表 —— 是 H 那轮漏网的**第四个**写死挂载上限（H 退役了 `MAX_STACK`、`loraMountCapacity`、配方编译器三个）。而 fal 侧实测「any number」，所以它对齐的依据从来不存在。

⚠ **但它不是活的 bug**：全仓 grep（含 `workers/`、脚本、配置）只有它自己的测试文件引用 `mergeStackLoras`，**生产零调用方**。所以 H 当时没修它不是漏判，是它根本不在链路上。
→ **处置：整个模块 + 测试一并删除**（与 J2 删 `framer-motion` 同类：死依赖带着一个幽灵上限，留着只会在下次普查里再被当成 bug 查一遍）。

**其余三块：未发现新的「无依据上限」**

| 块                    | 结论                                                                                                                                                                                                                                                                  |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 220 处 `.slice(0, n)` | 绝大多数是**展示截断**（错误体 `.slice(0, 200)`、日期 `.slice(0, 10)`、alt 文案），不构成能力闸。真正发往 provider 的几处**都写了依据**：Veo 3.1 的 3 张、Seedance 的「9 张图且图+视频+音频 ≤12」、火山 Seedream 的 14 / Seedance 的 9，注释均点明出处                |
| `config.ts` 四个      | `RUNNER_MONTHLY_LIMIT` 有完整成本推算 + 文档链接；`RATE_LIMIT_CONFIGS` / `MAX_DURATION_CONFIGS` 是自选策略值（不对齐任何外部事实，不属本病型）；`LLM_TEXT_DEFAULT_MAX_TOKENS.DEFAULT = 1024` 看着裸，但**依据写在消费它的函数上**，且所有真实调用方都显式传 maxTokens |
| 各 `*_LIMITS` 对象    | `NODE_STUDIO_ASSISTANT_LIMITS` 明写「这是 DoS/payload 护栏不是产品上限」；`NODE_ASSISTANT_OP_LIMITS` 每条都有中文依据。属自选策略值                                                                                                                                   |
| `workers/` 同类常量   | 火山 / MiniMax 的 `MAX_REFERENCE_IMAGES = 9` 等均带「Multimodal caps」出处注释，且与 app 侧一致。⚠ 注意 worker 的 9 是 **Seedance 视频**、adapter 的 14 是 **Seedream 图片**，两者不同产品，**不是矛盾**                                                              |

⭐ **续扫同时验出一条现成的好范式**：`src/lib/node-video-send-preview.ts:140` 的 **「上限不可得时诚实沉默不硬造」**——cap 未知时 `undefined` 直接跳过截断，而不是回落到某个保守数字。这正是 #1 那类 bug 的解药，新写上限逻辑照抄它。

#### ⚠ 待 owner 拍板（本轮验完但没动的）

1. ~~**Fish Audio 的 TTS 字数上限**要不要单独放开？~~ —— ✅ **owner 拍板「开放」，已按 §L 落地**：上限改为 per-model（厂商文档写了才声明），Fish 不声明，只留 payload 护栏。⚠ 遗留一件**没量的事**：护栏 40_000 字 ≈ 44 分钟语音，几乎必然先撞 `timeoutMs`（Fish 60s）—— 那堵墙在哪未测，见 §L 末尾。
2. ~~**画布仍在按 `maxLoras` 截断 LoRA**~~ —— ✅ **owner 当场拍板「一并退役，画布也不设上限」，已改**：`StudioNodeWorkbench` 两条 generate 路径的 `.slice(0, maxLoras)` 整条删除，`getCapabilityConfig` 导入随之退役，能力表注释同步更新。
   - ~~⚠ **还剩一个没动的**：`CharacterImageLoraControls.tsx:57-68` 的**加号闸**仍封顶……它与 generate 路径性质不同（**弹 `maxReached` toast 明示用户**，不是静默截断）……**现状已比改之前严格更好**：手动加满 5 个仍有闸……要不要连这个闸也退役，请 owner 再定一次。~~
     → ✅ **owner 拍板退役，已按 §M 落地（`dafa2898`）。**
     ⛔ **上面划掉那段的三处判断全是错的**，留作反例：那个组件自 `04f8f6be`（2026-08-05）起**零消费者**，该 commit 摘掉它时还加了断言钉死不再渲染。所以「弹 toast 明示用户」——**那句 toast 从来没人看见过**；「手动加满 5 个仍有闸」——**手动根本加不了，没有入口**；「现状已比改之前严格更好」——**无从更好，用户碰不到它**。最终不是改闸，是**连组件一并删除**。
     ⚠ 教训见交接文档 §一.10：**描述一个 UI 行为前，先确认那个组件有人渲染**（`grep '<ComponentName'` 而非 `grep 'ComponentName'`——后者会被 barrel 的 `export *` 与注释骗到）。
3. **`VOLCENGINE_SEEDREAM_MAX_REFERENCE_IMAGES = 14` 仍无来源**。本轮查证失败（火山文档 JS 渲染取不到；能查到的 ≤10 是 **Seedream 4.0** 的数，而本项目跑 4.5/5.0，属近似不属证据）。**故意没有下调**——凭近似值收紧上限会砸掉正在工作的多图流程。证伪方法已写进常量注释。

#### 尚未扫

- ⭐ **派生资源回退链**（病型 B）—— **本轮完全没扫**：它是并行会话在本轮进行中才立的病型（`f32d5835`），与「无依据常量」是两种病。grep `?? .*[Uu]rl` 与 `thumbnail`/`preview`/`thumbImage`/`cardImage` 档位命名，逐个对消费方实际渲染尺寸。**优先级最高，接手请从这里开始。**

⚠ 上表「未发现」只代表**按本病型的判据**没扫出新的；不代表这些数字都是最优值。

### 待评估（不列为任务）

`canvas.css` 4063 行 > `globals.css` 1741 行，域皮肤比脊柱厚一倍多。画布本身确实复杂，不一定是问题，但值得在动画布视觉时顺带看一眼有没有可以上收到脊柱的部分。

---

## 仍待 owner 拍板

1. **排期** —— 十条的先后由 owner 定。
2. ~~G 首页字体~~ → 已解锁：owner 说「有更好的可以再设计」，**由我方出三个方向做对照**，不再是等他拍板。

### 建议顺序（owner 未定，仅供参考）

~~H~~ · ~~C4~~ · ~~E + J3~~（✅ 均已完成）**→ A+B → D0 → J1（补基线）→ I 侧边栏 → C+F → G（先出字体方向）**

四批的分法与理由：

| 批次      | 内容             | 为什么排这里                                                                     |
| --------- | ---------------- | -------------------------------------------------------------------------------- |
| 一·修痛点 | ~~H~~ · ~~C4~~   | ✅ 两条都已完成。**两条的锚点都被实测推翻**——这一批把「先验证再改」验了两遍      |
| 二·清地基 | ~~E（+J3）~~     | 已完成。实际成本远超预期：删除本身 20 分钟，**改被删文档的 100+ 处引用花了大头** |
| 三·主线   | A+B → D0         | 调研成果全服务于此；A 与 B 耦合必须同做；D0 复用 B 的参数结构                    |
| 四·视觉   | J1 → I → C+F → G | ⚠ **顺序不能倒**：先补基线才敢动全局共享的侧边栏；薄脊柱定死了域皮肤才有基准     |

⚠ J2（删 `framer-motion`）不占批次，任何时候顺手做，但要先全量 grep 复核。

---

## 执行流程

### 新会话开场（每次都做）

1. 读本文件**索引表**，确认该条是「接续」还是「新做」——**A/B/C/F/G 五条是接续，直接开工极易重造已交付的东西**（D/E/I/J 是新做；~~H~~ 已完成）。
2. 读该条的正文段（诉求原话 + 已有地基 + 锚点）。
3. 按 `docs/WORKFLOW.md` 的**任务类型 × 业务域路由矩阵**进对应 `docs/scenes/<场景>.md`。
4. A/B 两条额外读 `libtv-canvas-ui-teardown-2026-08-07.md`（对标实拍，别从零设计）。

### 逐条推进路径

| 条目      | 场景              | 推进路径                                                                                                                                                                                                                                                                                            |
| --------- | ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A**     | ui-page + service | ①先出**回复协议设计稿**（三域各自的系统提示词结构 / 结构化选项模板 / 「已询问」沉淀件）给 owner 过目 → ②A1 历史按域隔离（纯工程，可并行）→ ③落地 → ④`docs/checklists/ui.md`                                                                                                                         |
| **B**     | ui-page + service | ①出**「确认闸」设计稿**（节点结构免费落 / 像素等确认）→ owner 确认 → ②补 shots stage 的 **UI 两道门**（引擎已绿，别重造）→ ③A4 的 `@`/引用胶囊                                                                                                                                                      |
| **C+F**   | ui-page           | ⚠ **走设计治理**：域定义 → 三方向 → 关键切片 → owner 确认 → **才写码**。C4 画质是唯一例外（纯工程 bug），**已单独修完**，剩下的 C1/C2/C3/C5/C6 + F 全部照走治理。⚠ C1 裁切与 C4 不同源，别当成一起修完了。⭐ **还欠一件 C4 的尾巴**：存量 384px 缩略图回填（owner 定了并进这一批），约束见 §C4 末尾 |
| **D**     | 多场景            | D0（一次多张 / 多模型各一张）优先，它同时影响 Studio 和画布两处 → D1 UI 走设计治理 → D2/D3/D4 各自拆包                                                                                                                                                                                              |
| **E**     | 文档              | ①先出「文件 → 已实现/在飞/规则 → 依据 commit」**清单**给 owner 过目 → ②批准后才删 → ③工作流部分**逐条与 owner 核对**                                                                                                                                                                                |
| **G**     | ui-marketing      | ①**先铺 3 个字体方向做对照**（别用选择题）→ owner 选定 → ②按 `homepage-motion-design-2026-07-27.md` §9 推进 → GSAP 只能动态导入，且只在首页营销域                                                                                                                                                   |
| ~~**H**~~ | ~~bugfix~~        | ✅ 已完成（`eb295d23` + `6c3add69`）——详见上方 H 段的「真根因」与「被推翻的三条」                                                                                                                                                                                                                   |
| **I**     | ui-page           | ⛔ **先做 J1**，再走设计治理：域定义先确立「侧边栏 = 薄品牌脊柱，不属任何单域皮肤」→ 三方向 → 关键切片 → owner 确认 → 才写码 → 同步 `app-shell.md`                                                                                                                                                  |
| **J**     | testing / 工程    | J1 补基线（画廊/素材/LoRA/画布各一张，照抄 `e2e/tools/canvas-ui-shots.mjs`）→ J2 删 `framer-motion` 死依赖 → J3 修 `globals.css:162` 悬空引用（可并进 E）                                                                                                                                           |

### 通用闸门（任何一条都适用）

- ⚠ **锚点 ≠ 根因**（H 实修换来的教训，成本最高的一条）。本文档里所有「锚点」都是**链路起点**，不是结论。H 的锚点写的是 `LoraSourceRecipeModal.tsx:512` 那个按钮还标了「别重找」，真根因却是三个互相矛盾的写死上限、跨四个文件、和当时点名的那个 commit 也无关。
  → **开工先问「这个数字/行为的依据是什么」**，别因为文档说「已定位」就跳过自己找根因。
  ⚠ **「查不到依据」要分两种，处理方式相反**（J4 实践细化，火山那条是反例）：
  ① **注释里没写** → 去查，不是直接退役。查证成功就按事实改（H：三个后端都不限量 → 上限退役）。
  ② **去查了但拿不到权威数据** → **保持原值 + 把证伪方法写进注释**。`VOLCENGINE_SEEDREAM_MAX_REFERENCE_IMAGES = 14` 就是这种：火山文档 JS 渲染取不到，能查到的 ≤10 是 Seedream 4.0 的数而项目跑 4.5/5.0，属近似不属证据。**凭近似值收紧上限会砸掉正在工作的流程** —— 放宽和收紧的风险不对称，拿不准时不动。
  → ⭐ **C4 又验了一遍，而且更狠**：文档标「高置信」的候选（网格 q=75）**整条不成立**——全局 `unoptimized: true` 早在三个月前就关掉了 next/image，那个候选描述的机制在这个项目里从不存在。真根因在完全另一层（派生图尺寸 + 回落链选错 URL）。**「高置信」是我自己给的标签，不是证据**；一次实测（读 `next.config` + 真机读 `naturalWidth`）就够推翻整段推理。而 384 这个数又是「对齐一份已失效的配置」——和 H 的三个写死上限同一个病。
- **改颜色前**跑 `contrast-check` skill，禁目测、禁信 review agent 的算术。
- **UI 改完**跑 `verify-real` skill（claude-in-chrome 真机 + 程序化读值 + 截图）。
- **声称绿之前**跑 `full-gate` skill（全量 tsc + 全量 vitest，串行，只在 commit 前跑一次）。
- **改 i18n** 三语必须同步，⚠ 禁止用正则改 `src/messages/*.json`。
- **提交**：owner 点头才提交；`git commit` 后台跑（pre-commit >2 分钟）；用 `git commit -F <文件>` 不用 `-m @'…'@`。
- **dev server**：3000 被占 = owner 开的，直接复用，绝不另起实例，不并行 build。

（建议顺序见文档上方 §仍待 owner 拍板。）

---

## 附：本轮调研的遗留

- ⚠ LibTV 的 owner 项目「AI小剧场」画布上留了一个空的**「导演台 2」节点**，需 owner 手动删除（删除操作被安全策略拦截）。
- 本轮实扣 LibTV 积分 12（185 → 173），用于跑通生成全流程。
