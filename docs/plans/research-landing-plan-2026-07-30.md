# 调研落地：梳理 + 开发设计 + 施工顺序（2026-07-30）

> **权力级别**：active plan（路线与任务包索引）。
> **上游**：`docs/plans/research/` 全部 15 份调研。
> **边界**：日常「当前在干什么」仍只认 [`docs/status.md`](../status.md)；本文只回答**「这 15 份调研怎么变成可施工的东西、按什么顺序」**。
> **本文不是**：视觉规范、page 文档、契约 SoT。拍板后的稳定结论要**回写** `docs/references/`，不留在本文。

---

## 0 · 执行摘要

| 问题                      | 答                                                                                                                                                                                                                                                                                   |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 15 份调研是同一种东西吗？ | **不是。** 混了三类：**事实澄清**（已经是答案）、**待拍板契约**（阻塞下游）、**可直接施工**（有落点）。混在一起读，就会觉得「很多东西但不知道从哪下手」——这是清单的问题，不是内容的问题。                                                                                            |
| 已经消化掉哪些？          | **别再排期这五项**：模型升级审计 5 项里 **4 项当天已实现**；`interaction.md` 已升格为现行契约；画布浅色已拍板且与代码一致；`canvas-workbench.md` 色板横幅已打；`check-model-docs.mjs` 已修好（本机 exit 0）。另有 **5 份属纯事实澄清**（§1.1），只需回写 `references/`，不占开发期。 |
| 真正卡住的是什么？        | ①**卡片总线契约**（owner 已后置）②**剧本节点形态仍模糊**（owner 定：需单独设计轮）③**文档比代码旧**——07-26 的断层清单四天内至少三项已变（§2.5）。                                                                                                                                    |
| 顺序为什么不能随便排？    | 存在一条**唯一的依赖脊柱**（§3）。且第一优先不是施工而是**校准**：不先实测，就会为已经不存在的缺口做设计。                                                                                                                                                                           |
| 第一刀砍哪？              | **包 1 文档止血（零风险）→ 包 2 现状实测校准（不改码）→ 包 3 投影补齐静帧**。见 §6.1。                                                                                                                                                                                               |

**一句话：** 先校准、再补投影、再立审核、最后才谈剧本节点；把已经做完的从清单里划掉，主链其实只有 8 包。

> **执行 SoT = §6**（分包，严格串行）。§5 保留为**分期主题视角**，两者不冲突时以 §6 为准。

### 0.1 · Owner 拍板记录（2026-07-30）

| #   | 议题                        | 拍板                                                        | 对本文的影响                                                                                                                                                                                                |
| --- | --------------------------- | ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **全局壳色向**（§4.4）      | ✅ **A' 浅壳浅台** —— 壳走近白/浅灰，与已落地的浅色画布同族 | 壳级 page 可开工；顺手收掉「画布是浅色孤岛、`html.dark` 让脊柱仍深」这条缝。**不再考虑 B 中性深壳**（会让 07-27 的 token 反转白做）                                                                         |
| 2   | **卡片总线契约**（§4.1）    | ⏸ **整体后置**，§9.4 三问暂不拍                             | **W3 必须重新划范围**（见 §5-W3）：审核态与 harvest 门禁照做，但只对「**节点自带媒体**」生效；卡→Seedance 编译器两个纯函数**移出 W3**。剧本节点的 G3 定妆门也随之降级为「用节点上的图」而非「用卡编译结果」 |
| 3   | **剧本节点 Q1**（§4.2）     | 🔶 **形态仍模糊 → 需要单独设计轮**                          | 剧本节点**不能靠「把七问写成契约」了事**；它要走完整设计轮（域定义 → 方向 → 关键切片 → owner 确认）。排到 §6 包 7，且前置九类节点定型                                                                       |
| 4   | **剧本节点 Q5 分镜静帧**    | ✅ **默认开 · 项目级可关**                                  | ⚠ **今天没有能承接静帧的节点**（投影只造 `role=character`）→ 直接生出 §6 包 3「投影补齐」，且它是这条决策的落地前提                                                                                         |
| 5   | **主流程形态**（Q6 的回答） | ✅ **两步：①聊故事大纲 ②设计镜头（镜头阶段接 AI 出图）**    | 这不是「上传拆解一期/二期」的答案，而是**重新定义了主流程**。它恰好对上仓里已有的 `ScriptDoc` 两阶段（`outline` / `shots`）—— 见 §2.5                                                                       |
| 6   | **执行方式**                | ✅ **严格串行：前一个没做完不做下一个**                     | §6 已按硬依赖重排；真正无依赖的三项单列为「可插包」并标明                                                                                                                                                   |

⚠ **卡片后置的代价（记下来，不是反对）：** 没有卡片权威模型，**同一角色多镜的一致性只能靠用户手动在每个节点挂图**。剧本节点铺出 6 镜后，6 个角色槽要各自挂参考——这正是 §3.2「卡是总线不是孤岛」想解决的问题。后置是合理的（契约没想清就写代码更贵），但**做剧本节点之前必须回来补**，否则「一句话铺满画布」会退化成「一句话铺出 6 个要手动填的空槽」。

---

## 1 · 15 份调研分类对账

> 「它其实是什么」比文件名重要——有两份文件名会误导路由（见 §1.5）。

### 1.1 A 类 · 事实澄清（**已经是答案，只需回写 references，不需排期**）

| 文档                     | 它其实是什么                                                                                  | 消化状态                                                                                                               | 下一步动作（很轻）                                                                                                                             |
| ------------------------ | --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `模型接入原生与中转调研` | 一张「谁是原生 / 谁是 fal 聚合 / 谁是 Runner」的对照表 + 「没有接野鸡中转」的结论             | 结论稳定，**未回写**                                                                                                   | 把 §5.5「**新模型默认策略**」（有官方直连→原生；仅聚合可买→fal；社区权重→Runner）写进 `references/providers.md`；顺带在设置 UI 标注路由类型    |
| `组件盘点与模型UI调研`   | 组件分层事实 + 模型 UI 的**一级分类是模态**（IMAGE/VIDEO/AUDIO/MODEL_3D）+ 4 个「再通用」候选 | 事实正确，**未回写**                                                                                                   | 一句话进 `studio-shared/README.md`：新模型入口只走 `MainModelPicker` + 对应 hook，**禁止再写第四套 ModelSelector**                             |
| `LoRA底模与工作流调研`   | Civitai 社区事实（Pony = 角色 LoRA 之王 / IL·NoobAI = 新默认质量 / Anima DiT ≠ Pencil）       | 事实正确，**未回写**                                                                                                   | 升格 §5.2「family 文案 + prompt 方言」进 `references/domains/lora.md`；这是后面「family 前缀一键」的数据源                                     |
| `3D与音频扩展及Fish对齐` | ①单图→GLB **早就有了**（`/studio/3d` 全链）②音频缺口清单 ③Fish 对齐约 70–80%                  | 事实正确；**产品定位是真缺口**                                                                                         | 改 `references/product.md`：3D 从「**搁置**」改为「**可用 · 非北极星**」——能力已有，问题是入口不可见                                           |
| `全站模型升级审计`       | 相对业界的 catalog 差距 + **§9 实现记录**                                                     | ⚠ **5 项里 4 项当天已实现**（Fish s2.1-pro / Kling O3 Pro / EL Music v2 / FLUX.2 Pro Edit）；Gemini Omni GA 被上游卡住 | **把 §9 回写 `model-catalog.md`**，否则下一轮会当成 backlog 重做一遍。剩余：Gemini preview 债（等 Google）、火山 endpoint 月审（下次 2026-08） |

### 1.2 B 类 · 待拍板契约（**阻塞下游，必须 owner 参与**）

| 文档                            | 核心产出                                                                                           | 待拍板                                                                     | 拍板后产出什么文件                                                                |
| ------------------------------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| `角色卡片联动设计问题清单`      | **卡 = 跨域身份总线**；§9.4 三格决策表；**§10 卡→Seedance 编译器**（卡不是模型 token）             | §9.4 三问（多镜权威 / 无源图 / variants 语义）                             | **新建 `references/domains/cards.md`** ← 现在**不存在**，是 §3 脊柱上的硬缺口     |
| `video-shotcraft与助手合并评估` | §0–11：Remotion **不并**，只蒸馏方法论；**§12–14：剧本节点 + HITL 审核门**（本轮最大的新产品设计） | §14.3 七问（是否一等节点族 / 默认 Ask / 未过审能否进视频 / 助手能否自批…） | `references/domains/canvas.md` 增「剧本节点 + 审核态」节；节点卡面走 `ui-page` 门 |
| `响应式设计优秀实践调研`        | **布局模式表**（compact / cozy / comfortable / wide）取代 `<1024` 二值                             | 冻结模式表 + 与 `lg`/1024 的对齐说明                                       | 模式表进 `references/frontend.md`（或 `interaction.md` 旁注）                     |
| `UI首页画布与全局策略`          | 全局壳该不该离开「AI 默认纯黑」；§3.3 三方案 A/B/C                                                 | 壳色向选一                                                                 | 壳级 page 文档（现在**没有**单一 SoT，audit §2.4 已点名）                         |

### 1.3 C 类 · 可直接施工（**结论够用，只差排期**）

| 文档                           | 可直接开工的东西                                                                                                                               | 依赖                                                                               |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `画布媒体模型调研`             | ①各节点角色的**默认模型**建议（叙事→Seedream 5 Pro / 写实→FLUX.2 Pro / 二次元→IL·Runner）②Seedance Reference 端到端路径已核 ③**§4.2 八条灰区** | 灰区 #1（cap 按 effective model 算）**已修**；#2（纯视频源不算 reference）**仍开** |
| `LoRA提示词与角色工具对标调研` | **L0–L4 提示分层** + §7 五个切片（触发词 chips → 发送全文预览 → family 前缀 → 词条 MVP → 关键词注入）                                          | 无硬依赖，可全程并行                                                               |
| `画布与LoRA产品路线建议`       | 画布 **W1–W4 里程碑**（半自动投影 → 依赖队列 → 视频收口 → 工作流模板）                                                                         | W1/W2 = 助手管道 P1/P2/P3 的产品说法，**不是第二套计划**                           |
| `UI丝滑交互与动效库选型`       | 已升格 `references/interaction.md` ✅；结论：**工程页不放开 GSAP**                                                                             | §5 落地五步：清点硬编码 transition → 切片示范（Assets 网格 或 模型选择器展开）     |

### 1.4 D 类 · 元文档

| 文档                     | 状态                                                                                                                                                      |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `文档厘清与过期冲突清单` | 本轮的 audit。⚠ **它自己已部分过期**：§5.2 的 P0 里「画布双 SoT」「workbench 横幅」「check-model-docs 脚本」**三项已完成**（本文 §2 已复核）。剩余见 §2.4 |
| `说明.md`                | 目录索引。⚠ **有一条悬空条目**（见 §2.2）                                                                                                                 |

### 1.5 两个会误导路由的文件名（**读之前先知道**）

| 文件名                          | 你会以为                           | 实际内容                                                                                                    |
| ------------------------------- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `LoRA提示词与角色工具对标调研`  | LoRA 的 danbooru 标签语法          | **主要服务角色卡**（酒馆式分层组装）；LoRA 侧只拿 family 方言。要设计 Cards **必须读它**                    |
| `video-shotcraft与助手合并评估` | 一份「要不要合并某个开源库」的评估 | 前 11 节是（结论：不合），**§12–14 才是重点**——剧本节点 + HITL 的完整设计草案。这是全 15 份里最重的产品设计 |

---

## 2 · 卫生问题（W0 就修，零风险、不碰业务代码）

### 2.1 ⚠ research 目录**整个没进 git**

```
$ git status --short docs/plans/research/
?? docs/plans/research/
```

15 份、约 230KB、其中包含**唯一一份**剧本节点设计草案和卡→Seedance 编译契约。一次 `git clean -fd` 就全没了。**这是本文里唯一的紧急项。**

### 2.2 ⚠ `说明.md` 索引悬空

索引第 41 行列了 `火山豆包音频模型接入调查-2026-07.md` —— **文件不在磁盘上，也从没进过 git**。同时索引**缺**本文提到的分类（`全站模型升级审计` / `UI丝滑` / `3D与音频` 已在，但 audit §1 的清单只有 12 行、少 3 篇）。

处置：要么补写那份火山调查，要么删掉该行。**别留悬空链接**——每个新会话都会去点它然后自己兜底（和 CLAUDE.md 里删空指针 skill 同一个教训）。

### 2.3 ⚠ 已实现结论没回写，会被重做

`全站模型升级审计 §9` 记了当天已实现 4 项，但：

- `说明.md` 仍把该文描述为「升级优先级」（像 backlog）
- `model-catalog.md` 未见对应回写

**下一个读到这份调研的会话会把 Fish s2.1 / Kling O3 / Music v2 / FLUX Edit 当成待办重新排期。**

### 2.4 audit §5.2 剩余项（已复核，只剩两条半）

| audit 原项                                           | 本文复核结论                                                                                           |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| 画布视觉双 SoT（workbench 深 vs skin-spec 浅）       | ✅ **已解**：owner 拍浅；`canvas-workbench.md` 文首已有「色板已 superseded」横幅                       |
| 修 `check-model-docs.mjs`                            | ✅ **已修**：已改读 `src/constants/models/` 目录，且加了「解析到 0 个模型就报错」的护栏；本机跑 exit 0 |
| `node-canvas.md` 行为段（卡匣 / 吞噬 / `fusedInto`） | ⬜ **仍开**：卡匣已是定位器、`fusedInto` 已退役，但长文仍写注册表/投喂/吞噬食物链                      |
| `backend.md` route 数                                | ⬜ **仍开**：文档写 149，实际 **156**（`find src/app/api -name route.ts \| wc -l`）                    |
| 建 `domains/cards.md`                                | ⬜ **仍开**，且**优先级应从 P2 提到 P0**——它是 §3 脊柱的硬缺口，不只是文档整洁问题                     |

---

### 2.5 ⚠ 关键校准：主流程比文档写的走得远（2026-07-30 本机实读）

> 起因：owner 把主流程描述成「①聊故事大纲 ②设计镜头，镜头这边通过 AI 出图」。去代码里找这条流程，发现**它有一半已经建好了，而现行 plan 仍写成缺口**。

| 文档怎么说                                                                                 | 代码实际是什么                                                                                                                                                                                                                                | 影响                                                                                                                                  |
| ------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `canvas-assistant-pipeline §0` 断层③：「**剧本没落成结构化数据**，助手输出 Markdown 散文」 | **`ScriptDoc` 两阶段引擎 + UI 都在**：`types/script-doc.ts:146` 有 `stage: outline \| shots`；`ScriptDocWorkspace.tsx`（**1429 行**）实现了大纲/镜头两阶段切换、分模块 ✨ 定向编辑、`onBack` 回大纲；已挂在 `StudioNodeAssistantDock.tsx:579` | **owner 描述的两步流程有现成载体**。缺口不是「没有结构化路径」，而是「**自由对话**那条路不落 ScriptDoc」——两回事，工作量差一个数量级  |
| 同上：投影「只投 `role=character`，需补 shot 静帧与 background 场景图」                    | ✅ **仍然准确**：`node-workflow-script-doc.ts:228` 只造 `role: character`；另造 `shotText` / `seedance` / `voice` / `videoMerge`                                                                                                              | **这是 Q5「静帧默认开」的硬阻塞**——镜头阶段要出图，但投影没有承接静帧的节点                                                           |
| 断层②：助手拿不到节点名字，回复出现 `****`                                                 | 🔶 **该层已加兜底**：`node-assistant-request.ts:38` 现为 `node.title.trim() \|\| node.type`，第 55 行还会丢掉空名节点                                                                                                                         | `****` 的直接成因已堵；但**上游是否真传用户命名进来，只有真机能证**（我的 grep 证不了）                                               |
| 断层①：助手不能建节点/连线                                                                 | 未见写画布工具；但 `ScriptDocWorkspace → use-node-workflow:1496 → projectScriptDocToGraph` 这条**确定性投影链是通的**                                                                                                                         | **助手有没有「手」不是两步流程的前提**：结构化工作区 + 投影本身就能铺图。P1 的价值是「加个节点 / 重做这张」这类细粒度介入，不是主流程 |
| `canvas-assistant-pipeline §2.4` 卡匣降级、并建议「与 P0-A 合并做」                        | ✅ **卡匣降级已完成**（`status.md`：已降级为分组/搜索/定位/选中入口）                                                                                                                                                                         | 「合并做」这条建议**已过期**，别再照着它排期                                                                                          |

**结论（决定了 §6 的排序）：** 现行 plan 的断层清单是 **2026-07-26 的真机快照**，四天内至少三项已变。
👉 所以 **§6 包 2 必须是「现状实测校准」**——在投任何设计工时之前，先用真机把「大纲 → 镜头 → 投影 → 出图」跑一遍，产出**今天真实的缺口清单**。否则我们会为已经不存在的缺口做设计。

---

## 3 · 依赖脊柱（为什么顺序只能这样）

```text
                    ┌─────────────────────────────────────┐
                    │  W1 契约拍板（owner，纯纸面）        │
                    │  ①卡片总线 §9.4  ②剧本节点 §14.3    │
                    └───────────────┬─────────────────────┘
                                    │
     ┌──────────────────────────────┴──────────────────────────────┐
     │                                                             │
     ▼  工程轨（不需过设计门）                        设计轨（要过 ui-page 门）
┌──────────────────────────┐                    ┌───────────────────────────┐
│ P0-A 节点名进助手上下文   │                    │ status.md 当前焦点：      │
│      （+ 卡匣注册表合并） │                    │ 节点详情页详细设计         │
│ P0-B @ 过滤顺序           │                    │ （九类节点信息层级/状态）  │
└────────────┬─────────────┘                    └─────────────┬─────────────┘
             ▼                                                │
┌──────────────────────────┐                                  │
│ P1 助手写画布工具         │                                  │
│    （建节点/连线/改名/生成）│                                │
└────────────┬─────────────┘                                  │
             ▼                                                │
┌──────────────────────────┐                                  │
│ P2 散文 → ScriptDoc       │                                  │
│    + 补 shot/background 投影│                                │
└────────────┬─────────────┘                                  │
             ▼                                                │
┌──────────────────────────┐   ┌────────────────────────────┐  │
│ 审核态 awaiting_review    │◄──┤ 卡→Seedance 编译器（纯函数）│  │
│ approved / rejected       │   │ resolveCardVisualRefs      │  │
│ 硬规则：harvest 只吃 approved│ │ buildSeedanceIdentityPrompt │ │
└────────────┬─────────────┘   └────────────────────────────┘  │
             │                                                 │
             ▼                                                 ▼
      ┌──────────────────────────────────────────────────────────┐
      │  剧本节点 L2 壳 + 审阅网格 + propose_generation_batch 门  │
      │  ⚠ 有卡面 = 必须过设计门，所以它排在设计轨汇流之后         │
      └──────────────────────────────────────────────────────────┘
```

### 三条硬依赖（不是偏好，是事实）

| #   | 依赖                              | 为什么绕不开                                                                                                                                                                                        |
| --- | --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **剧本节点 ← 助手管道 P1 + P2**   | 剧本节点的价值是「助手铺一张可审的图」。P1 不做，助手**自己说它建不了节点**（2026-07-26 真机实测原话）；P2 不做，剧本还是 Markdown 散文，`projectScriptDocToGraph` 吃不下。先做壳只会得到一个空壳。 |
| 2   | **审核态 ← 卡片契约**             | 审核门的硬规则是「**未过审的身份图不得进 `image_urls`**」。要判断「什么算这一镜的身份图」，必须先答 §9.4 Q1（卡赢 / 节点赢 / 锁定快照赢）。契约不定，审核规则写不出来。                             |
| 3   | **剧本节点卡面 ← 节点详情设计门** | 剧本节点是第十类节点卡。status.md 明写九类节点的信息层级/状态/操作区**尚未确认**。在九类没定型前加第十类，等于给未完成的设计再加一个变量。                                                          |

**推论：** 工程轨（P0/P1/P2 + 编译器纯函数）**可以和设计轨完全并行**——它们碰的是 context 构造、工具调用、投影和纯函数，没有视觉产出。这是本轮最大的并行机会。

---

## 4 · 待拍板契约（附推荐默认，owner 只需改格子）

### 4.1 卡片总线 —— 采纳/驳回 `角色卡片联动 §9.4`

| #   | 问题                        | 推荐默认                                                   | 一句话理由                                                         |
| --- | --------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------ |
| 1   | 同一角色多镜，谁是权威？    | **C 混合**：默认引用卡 → 节点可覆盖本镜 → 「锁定」冻结快照 | 纯卡赢会冲掉用户调好的单镜（毁排演信任）；纯节点赢会让卡库形同虚设 |
| 2   | 允许纯文案卡（无源图）？    | **角色卡创建 ≥1 图**（维持现状）；纯文案只做草稿层         | Seedance 只认 `image_urls`，不认卡对象——没图就锁不住脸             |
| 3   | `variants` 是换装还是分叉？ | 默认「**造型 / 画风模式**」；真分叉走「另存为独立角色」    | 业界换装 = 新 reference sheet；真分叉是少数高级需求                |

**拍板后立刻产出：** `references/domains/cards.md`（域职责 + 三态权威 + §10 编译契约 + 明确非目标）。

### 4.2 剧本节点 + HITL —— `video-shotcraft §14.3` 七问逐条展开

> owner 要求逐条评估（2026-07-30）。每条列：**选项 → 业界依据 → 代价 → 建议**。
> 七问不是同一量级：**Q1 / Q3 / Q5 是架构级**（决定数据结构与烧钱边界），Q2 / Q4 是行为默认值，Q6 / Q7 已被排期与许可事实基本决定。

---

#### Q1 · 剧本节点是不是一等节点族（新 node type）？【架构级】

| 选项                             | 形态                                                 | 优点                                                       | 代价                                                                                                                                |
| -------------------------------- | ---------------------------------------------------- | ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| **S0 仅 dock**                   | 剧本只活在助手对话里                                 | 开发量最小，今天就能有                                     | 画布上没有锚点 → 「我的片子现在什么样」答不出来；进度/待审计数无处挂；关掉 dock 就丢上下文                                          |
| **S1 剧本节点 + 子节点**（推荐） | L2 壳（目录+状态机+放行开关）+ L3 执行图仍是独立节点 | 对齐「节点=对象」心智；AI 与手动双轨同一真相源；可审可定位 | 要补投影（`role=shot`/`background`）、审核态、审阅网格；**且它是第十类节点卡 → 必须过设计门**                                       |
| **S2 超级剧本卡吞一切**          | 一张卡内嵌全部生成与进度                             | 表面上最简单                                               | 违背「一个对象一个可操作的地方」；12 路进度塞一张卡；无法连线、无法手动介入。**与刚做完的卡匣降级（同一身份不要两份副本）自相矛盾** |

**业界依据：** LTX Studio 的 `Elements` 是**注册表 + 引用**而非把一切塞进一个面板；`canvas-assistant-pipeline` §2.4 里 owner 自己拍过「卡匣是索引不是操作场所」——S2 等于把刚拆掉的双份副本又造回来。
**建议：S1。** 但**排期上它必须在九类节点定型之后**（§3 依赖 3）。
**若选 S0 作过渡：** 可以，且成本低——但要接受「剧本没有画布锚点」，并且以后升 S1 时 dock 里的状态得迁移。

---

#### Q2 · 默认「生成前先问」还是「自动生成」？

| 选项                 | 依据                                                                                                                                                 | 代价                                                                      |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| **默认 Ask**（推荐） | Runway Agent 官方 help 就是这个开关（`Ask before generating media` vs `Automatically generate`）；shotcraft 共创模式同构；SaaS 每次生成都真扣 credit | 多一次点击；批量时要有「本批全部放行」不然会烦                            |
| 默认自动 + 事后审    | 快                                                                                                                                                   | ⚠ **烧钱不可撤销**。铺 12 镜自动跑 = 12 次扣费，用户第 3 张就发现方向错了 |

**建议：默认 Ask，且「全权连跑」必须显式开启 + 附预估 credit。**
⚠ **前置硬条件：** 全权模式**不能在取消功能存在之前上线**——`VideoComposer.tsx:229` 明写真实进度与取消是 P2，今天没有取消。没有取消的自动连跑 = 用户只能看着钱烧完。

---

#### Q3 · 未过审的定妆图能否进视频？【架构级 · 硬规则】

| 选项               | 后果                                                                                                               |
| ------------------ | ------------------------------------------------------------------------------------------------------------------ |
| **否**（推荐）     | `awaiting_review` / `rejected` 的图不进 `image_urls`，排除时给提示。规则清晰、可测试                               |
| 是（允许静默使用） | 一次「脸不对」的定妆图会污染下游全部镜头，而用户以为自己已经把它打回了。**这是最容易产生「产品在骗我」体感的一条** |
| 是，但要显式确认   | 折中；后期可作高级操作                                                                                             |

**建议：否。** 这条是审核门存在的意义——**审核不影响下游 = 审核是装饰**。
🔶 **卡片后置后的调整：** 门禁的判定对象从「卡编译出的身份图」缩小为「**节点自带媒体 + 其 review 状态**」。这仍然完整可用（`harvestUpstreamImageReferences` 本来就是按节点收割），只是失去了「改一次卡、全片跟新」那层。

---

#### Q4 · 助手可否自动把节点标 approved？

| 选项             | 依据                                                                                                                                   |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| **否**（推荐）   | 防确认偏差——shotcraft 专门用**干净上下文的 subagent 做独立终检**，就是因为「制作者自检必然放水」。助手最多可标 `generating` / `failed` |
| 可以（用户可关） | 等于把审核门变成默认关闭                                                                                                               |

**建议：否，且写成硬约束**（`set_review_state` 工具禁止写 `approved`）。这条几乎没有争议空间，建议直接钉死，不留开关。

---

#### Q5 · 分镜静帧（G5）是否强制？【架构级 · 影响成本与质量】

| 选项                           | 优点                                                                                                                   | 代价                                           |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| **默认开、可按项目关**（推荐） | 关键帧质量直接决定 I2V 结果（`画布媒体模型调研` §1.3「先图后视」）；静帧比视频便宜得多，先审静帧再烧视频是最省钱的顺序 | 多一步；简单项目会觉得啰嗦                     |
| 强制开                         | 质量最稳                                                                                                               | 「我就想快速试一条」的路径被堵                 |
| 默认关                         | 最快                                                                                                                   | 直接 T2V 出片 → 风格跨镜漂移，且贵的那一步先烧 |

**业界依据：** shotcraft 的「**分镜放行后才全量采素材**」和学术 STAGE 的「起止关键帧锚定」是同一个道理——**便宜的确认物要排在贵的执行前面**。
**建议：默认开、项目级可关。** 这是本组七问里 ROI 最高的一条。

---

#### Q6 · 上传素材拆解分析轨，一期还是二期？

| 选项             | 说明                                                                                             |
| ---------------- | ------------------------------------------------------------------------------------------------ |
| **二期**（推荐） | 一期先把 ScriptDoc + 审核 + 网格跑通。分析轨自己还需要一道确认门（避免「拆错人设却连烧 20 张」） |
| 一期             | 你如果主要工作流是「手上有参考片，要反推出同款」，那它其实是入口而非附加功能——这时值得提到一期   |

**建议：取决于你的真实起手势。** 从零写 logline → 二期；从参考素材反推 → 提到一期。这条我不替你定，因为它取决于你自己怎么用。

---

#### Q7 · Remotion 进主路径？

| 选项           | 事实                                                                                                                                                                                            |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **否**（推荐） | 两条成片物理世界不同：shotcraft 的像素来自**产品页截图 + 代码动画**（确定性），PixelVault 来自**生成模型**（随机 + 参考约束）。且服务端渲染要 Chromium 并发 + **Remotion 公司商业许可**单独评估 |
| 是             | 双引擎 → 产品叙事分裂，运维面翻倍，且会拖死助手管道 P0–P2                                                                                                                                       |

**建议：否。** 只做知识蒸馏：从 104 张配方卡抽 **20–40 条运动/机位词表**注入分镜 prompt（Apache-2.0 允许衍生，保留 NOTICE 即可）。可选：外链她的 Gallery 给你「挑运动感觉」。

---

**七问小结（建议默认）：** Q1 **S1 但排在九类定型后** · Q2 **Ask，且全权模式等取消功能** · Q3 **否，硬规则** · Q4 **否，钉死无开关** · Q5 **默认开可关** · Q6 **看你的起手势** · Q7 **否，只蒸馏词表**。

**拍板后立刻产出：** `domains/canvas.md` 增「剧本节点 + 审核状态机」节（G0–G8 八门 + 节点态图 + 下游消费硬规则）。

### 4.3 响应式布局模式表（冻结即可，无争议）

| 模式        | 宽度      | 壳                | 内容                                  |
| ----------- | --------- | ----------------- | ------------------------------------- |
| compact     | <768      | TabBar + 精简顶栏 | 单列；一屏一任务；次要进 sheet        |
| cozy        | 768–1023  | 窄 rail 或抽屉    | 单主列 + 可选右抽屉；**禁止三栏硬塞** |
| comfortable | 1024–1439 | 侧栏 + 主区       | 允许双栏（60/40）                     |
| wide        | ≥1440     | 同上 + 更宽 max   | 限行长；侧栏可双开                    |

**为什么值得单独拍：** 现在几乎只有 `<1024` 二值，**768–1023 平板是最丑区间**（手机 chrome + 半桌面内容）。冻结这张表本身不改代码，但能止住各域各自为政。

### 4.4 全局壳色向 —— ✅ 已拍板 A'（2026-07-30）

| 方案                    | 壳              | 工作台       | 评价                                                                        |
| ----------------------- | --------------- | ------------ | --------------------------------------------------------------------------- |
| **A 浅壳深台**          | 近白/浅灰       | 略深工作面   | ⚠ **画布已在 2026-07-27 反转成浅**（`--canvas-bg #f1f1f1`），"深台"已不成立 |
| **A' 浅壳浅台**（建议） | 近白/浅灰       | 与画布同族浅 | **与已落地代码一致、改动最小**；壳与画布不再双黑打架                        |
| B 统一中性深壳          | 炭灰非纯黑      | 同系深       | 要认真做表面梯度；且要把画布再反转回去 = 白做                               |
| C 跟随系统              | light/dark 双套 | 域可覆盖     | 工程量最大，建议后置                                                        |

⚠ 已知缝（`canvas.css` 头注释已记）：app 全局 `html.dark` 仍让脊柱/传送门偏深，**画布是一座浅色孤岛**。选 A' 顺手就能把这条缝收掉。

---

## 5 · 施工顺序

### W0 · 文档止血（半天 · 零风险 · 不碰 `src/`）

| 任务                                                                      | 判据                                                                       |
| ------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| **research 15 份进 git**                                                  | `git status docs/plans/research/` 干净                                     |
| 修 `说明.md` 悬空条目 + 补齐索引                                          | 索引每一行都能点开                                                         |
| `全站模型升级审计 §9` 回写 `model-catalog.md`                             | 下一个会话不会重做 Fish/Kling/Music/FLUX                                   |
| A 类四条回写（§1.1 动作列）                                               | providers 有「新模型默认策略」；lora 有 family 方言；product 3D 去「搁置」 |
| `node-canvas.md` 顶部加**行为事实框**（卡匣=定位器 / `fusedInto` 已退役） | 不再有会话按吞噬语义施工                                                   |
| `backend.md` 149 → 156（或写「以 glob 为准」）                            | —                                                                          |

**不做：** 删 research（它们是决策证据）；用一句「已废」盖住 200 行冲突正文（`forbidden.md` 禁止）。

### W1 · 契约拍板（owner 主导 · 纯纸面）

按 §4 四张表逐格拍。产出三个文件：`domains/cards.md`（新）、`domains/canvas.md` 剧本节点节（增）、布局模式表（进 frontend/interaction）。

**为什么必须在 W2 之前：** 审核态的硬规则要引用卡片权威模型（§3 依赖 2）。

### W2 · 管道打通（纯工程 · 与设计轨并行 · 上游 = `canvas-assistant-pipeline`）

| 序  | 任务                                                                    | 落点                                                                 | 判据                                                                            |
| --- | ----------------------------------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| 1   | **P0-A** 节点名进助手上下文（**与卡匣注册表合并做**——碰同一处数据结构） | `lib/node-assistant-request.ts`；`maxNodeLabelLength` 有常量无值     | 助手回复里不再出现 `****` / 空名字；裸 UUID 不进可见文本                        |
| 2   | **P0-B** `@` 过滤顺序                                                   | `StudioNodeWorkbench.tsx` ~1240 先砍容量、~1291 才按 `@` 过滤 → 调换 | 被点名的图不会因排在 cap 之外先被砍掉                                           |
| 3   | **视频灰区 #2** `hasReferenceInputs` 纳入纯视频源                       | `hooks/node/use-video-composer.ts:177` 现只看 visual/keyframe/voice  | 只挂参考视频时 UI 模型态与提交一致（灰区 #1 已修，cap 已按 effective model 算） |
| 4   | **P1** 助手写画布工具（建节点 / 连线 / 改名 / 触发生成）                | 必须走 `node-connection-rules`；复用既有「应用前先询问」骨架         | 助手能建出节点，且写操作不绕合法性事实源                                        |
| 5   | **P2** 散文 → ScriptDoc + 补 `role=shot` / `role=background` 投影       | `types/script-doc.ts` 已定义；两阶段引擎已绿，缺对话路径接线         | 对话产出的是 JSON 不是 Markdown；投影覆盖八种而非六种                           |

**不做：** P3 队列——⚠ **取消功能今天不存在**，没有取消的批量队列 = 用户点 10 个任务只能干等。**先补取消，再做队列。**

### W3 · 审核态（工程为主 · 视觉极小 · **已按卡片后置重新划范围**）

| 任务                                                   | 说明                                                                                                                                     |
| ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 节点态新增 `awaiting_review` / `approved` / `rejected` | 现有四态（缺必填/就绪/生成中/失败）**少一态：已出但未审**                                                                                |
| **硬规则：harvest 只吃 `approved`**                    | 判定对象 = **节点自带媒体 + 其 review 状态**（卡片后置后的缩小版）。`awaiting_review` / `rejected` 不得静默进 `image_urls`；排除时要提示 |
| 打回载荷                                               | `rejectReason` · `promptPatch` · `reviewedAt` · **保留上一版媒体 URL 作对比**（不立刻删 R2）                                             |
| 「重做」语义钉死                                       | **保留节点、保留连线、保留提示词，只换 seed 重跑**——不新建节点、不断线，否则重来三次多三个孤儿                                           |

**⏸ 移出本档（随卡片契约一起后置）：** 卡→Seedance 编译器两个纯函数（`resolveCardVisualRefs` / `buildSeedanceIdentityPrompt`）、无图卡门禁。
**为什么可以拆开：** `harvestUpstreamImageReferences` 本来就是**按节点收割**，不是按卡收割。审核态挂在节点上即可独立成立；卡片只是未来多一个「图从哪来」的来源，不改门禁逻辑本身。

### W4 · 剧本节点（**要先过设计门**）

前置：status.md 的九类节点详情设计收口 + W1 剧本节点契约拍板。

| 任务                              | 说明                                                                                          |
| --------------------------------- | --------------------------------------------------------------------------------------------- |
| 审阅网格 MVP                      | 一次铺 12 个节点后逐张点开审是灾难——这已从「加分项」升为**必需**                              |
| 剧本节点 L2 壳                    | 片名 + 阶段进度条（G0–G8）+ 计数（角色 n / 镜 m / 已过审·待审·失败）+ 参考匣 + **唯一主 CTA** |
| `propose_generation_batch` 费用门 | 列本批将烧的节点 + 模型 + 估 credit，**停等用户**                                             |
| 助手工具边界                      | `set_review_state` **禁止**助手标 approved                                                    |

**不做（明确）：** 剧本卡内嵌完整时间线编辑器；12 路生成进度全塞一张卡；Remotion 进主路径。

### 并行常设债（不排在主线后面，全程穿插）

| 线                 | 内容                                                                                                     | 上游                          |
| ------------------ | -------------------------------------------------------------------------------------------------------- | ----------------------------- |
| **LoRA 提示分层**  | ①触发词 chips ②发送全文只读预览 ③family 质量前缀一键 ④词条/设定本 MVP（手动启用）⑤关键词注入（有预算后） | `LoRA提示词与角色工具对标 §7` |
| **响应式 compact** | Studio / LoRA 单列主路径 + 固定出图；768–1023 cozy 专项；共享组件上 `@container`                         | `响应式调研 §5`               |
| **丝滑切片**       | 清点硬编码 `transition` → 切一片示范（Assets 网格滚动 **或** 模型选择器展开）                            | `references/interaction.md`   |
| **3D 去搁置**      | 导航/首页入口可见 + Assets/Gallery 图「变 3D」深链 + 结果回 Assets                                       | `3D与音频 §1.4`               |
| **音频情绪**       | 行内情绪 token + 词表 chips（Fish 最大差异化已在 API 侧，UI 没用满）                                     | `3D与音频 §2.2`               |
| **组件抽取**       | 先「参考槽列表」+「媒体卡壳」（多域痛点），再动 LoRA/节点                                                | `组件盘点 §4.2`               |

### 明确后置

| 项                                  | 原因                                                            |
| ----------------------------------- | --------------------------------------------------------------- |
| 工作流模板 / 批处理（W4 里程碑）    | 先把单条流水线跑通再谈复跑                                      |
| 上传素材拆解分析轨                  | §14.3 第 6 问建议二期                                           |
| 宣发 Remotion 旁路（B0/B1/B2）      | 仅当有 B2B 宣发刚需；B2 要评 Remotion 商业许可 + 渲染成本       |
| Meshy / 完整 Tripo / 世界模型 Splat | **缺的是 GLB 下游，不是第 6 个同质 I2-3D**                      |
| 音乐 / 播客长音频 / Voice Design    | Music v2 已接目录；UI 与 kind 矩阵后续                          |
| BFL 官方 API / 可灵官方直连         | fal 双轨已够；换原生 = 新 provider 工程，不是改 endpoint 字符串 |
| 再堆云图模 / Midjourney             | MJ 无合规 API 且 ToS 禁自动化；云图旗舰已在当前代               |

---

## 6 · 分包执行：一档 = 一个 chat

> **owner 要求（2026-07-30）：** 工程量太大，先把设计面定死，再一档一档执行，解决完一个再开下一个。
> 所以下面每个包都按「**一个 chat 装得下**」切，自带**必读 / 禁改 / 判据 / 交接**四件套。
> ⚠ **不要在一个 chat 里跨两个包**——包与包之间的边界就是上下文该清空的地方。

### 6.0 · 开新 chat 的标准开场

```text
接 docs/plans/research-landing-plan-2026-07-30.md §6 的【包 N】。
先只读该包「必读」列的文件，读完先跟我确认范围，不要直接改码。
```

跨会话进度用 `context-save` / `context-restore` skill；**每个包做完回写本文 §6 的状态列**，这样下一个 chat 只看这张表就知道到哪了。

### 6.1 · 主链（严格串行 · 前一个没做完不做下一个）

> owner 定：串行推进。下表每一行的「**为什么必须在前一个之后**」都是硬依赖，不是偏好排序。

| 包    | 名字                                          | 状态 | 量级 | 为什么必须排在前一个之后                                                                                                        | 设计门 |
| ----- | --------------------------------------------- | ---- | ---- | ------------------------------------------------------------------------------------------------------------------------------- | ------ |
| **1** | 文档止血 + 现状校准回写                       | ✅ `19ee23a2` | 小   | 起点。research 还裸在 git 外；且 §2.5 的校准结论必须先写进文档，否则后面每个包都会再读到 07-26 的旧断层                         | 否     |
| **2** | **现状实测校准**（真机跑通两步流程）          | ⬜   | 中   | 只有包 1 把文档理顺，才知道该验哪几条。**必须在任何设计/施工之前**——§2.5 已证四天内三项断层已变，不实测就会为不存在的缺口做设计 | 否     |
| **3** | 投影补齐 `role=shot` 静帧 + `role=background` | ⬜   | 中   | 要等包 2 确认「静帧到底缺在哪一环」。这是 **Q5「静帧默认开」的硬阻塞**，也是 owner 第二步「镜头阶段出图」的落点                 | 否     |
| **4** | 审核态 + harvest 门禁                         | ⬜   | 中   | 要等包 3 **先有图可审**。没有生成资产时，`awaiting_review` 是抽象的、测不了                                                     | 否     |
| **5** | 助手写画布工具 P1                             | ⬜   | 大   | 要等包 4 的审核态先立起来，否则助手一旦有手就能绕过审核门直接铺+烧                                                              | 否     |
| **6** | 审阅网格                                      | ⬜   | 大   | 要等包 5 能一次铺多个节点。铺不出 12 个节点时，网格没有服务对象                                                                 | **是** |
| **7** | **剧本节点设计轮**                            | ⬜   | 大   | 要等包 6 + 九类节点详情定型。owner 已定：形态仍模糊，**需要单独设计轮**，不是写契约                                             | **是** |
| **8** | 剧本节点实现                                  | ⬜   | 大   | 要等包 7 出关键切片并被确认                                                                                                     | 承包 7 |

### 6.2 · 可插包（**真的没有硬依赖** —— 诚实标注）

> 串行原则我照做，但不能假装这三个有依赖。它们与主链零交集，插在任意两个主链包之间都不会打架。

| 包     | 名字                                             | 量级     | 建议插点                                                                                          | 设计门 |
| ------ | ------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------- | ------ |
| **I1** | 视频灰区 #2（`hasReferenceInputs` 纳入纯视频源） | **很小** | 包 2 之后顺手做——包 2 真机时必然碰到它                                                            | 否     |
| **I2** | LoRA 提示分层（可再拆 5 片，每片一个 chat）      | 中       | 主链任意空档                                                                                      | 部分   |
| **I3** | 壳级 A' 浅壳 Phase 1                             | 中       | 主链任意空档；已拍板可随时开                                                                      | **是** |
| ⏸      | 卡片总线契约 + 卡编译器                          | —        | **owner 后置**；但**必须在包 7 之前回来补**（否则剧本节点铺出的角色槽全要手填，见 §0.1 代价说明） | 否     |

### 6.3 · 逐包细则

#### 包 1 · 文档止血 + 现状校准回写 ✅ 2026-07-31（`19ee23a2`）

> **交付实测**：5 条判据全过——`git status docs/plans/research/` 干净；`说明.md` 14 个链接逐个验证可打开（脚本逐文件 `test -f`）；`model-catalog` §⑧ 已建；`backend.md` 156；`canvas-assistant-pipeline` 校准注记已在文首。
> **超出原范围的两项**：①`references/interaction.md` 也未进 git（同类风险），一并收进 ②`docs/` 根目录有 **39 张散落 PNG** 未处理（audit §3.1 的 P2 项，需 owner 决定移 archive 还是保留）。

|                |                                                                                                                                                                                                                                                         |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**       | research 进 git；索引无悬空；已实现结论回写；**§2.5 的校准结论写进 `canvas-assistant-pipeline`**                                                                                                                                                        |
| **必读**       | 本文 §2（含 §2.5）· `说明.md` · `文档厘清与过期冲突清单 §5.2`                                                                                                                                                                                           |
| **改**         | 只改 `docs/**`                                                                                                                                                                                                                                          |
| **禁改**       | `src/**` 一个字都不动                                                                                                                                                                                                                                   |
| **判据**       | `git status docs/plans/research/` 干净；`说明.md` 每行可点开；`model-catalog.md` 能查到 Fish s2.1 / Kling O3 / Music v2 / FLUX Edit 已实现；`backend.md` route 数正确；`canvas-assistant-pipeline` 文首有「断层清单为 07-26 快照，已知三项变化见 §2.5」 |
| **为什么第一** | 后面每个包的「必读」都指向这些文档。不先理顺，每个新 chat 都会重新踩一遍旧断层                                                                                                                                                                          |

#### 包 2 · 现状实测校准 ⬜（**所有设计与施工的前提**）

|                |                                                                                                                                                                                                                                |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **目标**       | 用**真机**把 owner 的两步流程跑一遍，产出「**今天真实的缺口清单**」，取代 07-26 的旧断层表                                                                                                                                     |
| **必读**       | 本文 §2.5 · `canvas-assistant-pipeline §0` · `ScriptDocWorkspace.tsx`（读，不改）                                                                                                                                              |
| **要验的六条** | ①大纲阶段能否产出 ScriptDoc ②能否切到镜头阶段并补 camera/emotion ③投影后画布上真实出现哪几类节点 ④**镜头阶段有没有任何地方能出图** ⑤助手回复里节点名是否为用户命名（还是回落成 type）⑥自由对话（不走 workspace）是否仍只出散文 |
| **工具**       | `verify-real` skill（claude-in-chrome + 程序化读值 + 截图）。⚠ 先读 `document.visibilityState`——后台 tab 的 rAF 是冻结的，别把「没触发」当结论                                                                                 |
| **禁改**       | **本包不改任何代码**。发现的 bug 记进缺口清单，不顺手修（顺手修会让这个 chat 失控）                                                                                                                                            |
| **判据**       | 一张六行的「实测 → 现状 → 缺口」表，且每行附截图或程序化读值。包 3 起的所有排期以它为准，不再引用 07-26 断层表                                                                                                                 |
| **顺带产出**   | 若 ④ 确认无处出图 → 直接成为包 3 的验收前提；若 ⑤ 名字仍空 → P0-A 重新入链                                                                                                                                                     |

#### 包 3 · 投影补齐 `role=shot` 静帧 + `role=background` ⬜

|          |                                                                                                                                        |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| **目标** | 镜头阶段的每一镜能投出**可生成的静帧图节点**；场景投出背景图节点                                                                       |
| **依据** | owner 已拍 Q5「分镜静帧默认开、项目级可关」——但今天投影只造 `role=character`，静帧无处落地                                             |
| **必读** | 包 2 的缺口清单 · `lib/node-workflow-script-doc.ts` · `constants/node-types.ts`（role 定义）                                           |
| **落点** | `projectScriptDocToGraph`（现造 `image(role=character)` / `shotText` / `seedance` / `voice` / `videoMerge`）                           |
| **红线** | **投影必须幂等** —— 重投不得复制已有节点（`scriptRef` 已有此机制，别绕）。项目级「关静帧」开关要在这一包一起定，否则默认开就成了强制开 |
| **判据** | 一句话 → 大纲 → 镜头 → 投影，画布上出现角色 + 背景 + 每镜静帧位；再投一次节点数不变                                                    |

#### 包 4 · 审核态 + harvest 门禁 ⬜

|                       |                                                                                                              |
| --------------------- | ------------------------------------------------------------------------------------------------------------ |
| **目标**              | `awaiting_review` / `approved` / `rejected` 三态 + 「harvest 只吃 approved」硬规则 + 打回载荷 + 「重做」语义 |
| **依据**              | owner 已拍 Q3「未过审不得进视频」为硬规则、Q4「助手不得自批」钉死无开关                                      |
| **必读**              | 本文 §4.2 的 Q3/Q4 段 · §5-W3 · `harvestUpstreamImageReferences`                                             |
| **范围**              | ⚠ **只对节点自带媒体生效**（卡片契约后置）；卡编译器不在本包                                                 |
| **判据**              | 未过审的图不进 `image_urls` 且有排除提示；打回保留上一版 URL 可对比；重做只换 seed，不新建节点、不断连线     |
| **为什么在包 3 之后** | 包 3 先让镜头阶段能出图；没有生成资产时审核态是抽象的、测不出来                                              |

#### 包 5 · 助手写画布工具 P1 ⬜

|                       |                                                                                                                                                                        |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **目标**              | 助手能建节点 / 连线 / 改名 / 触发生成，全部经既有审批门                                                                                                                |
| **必读**              | `canvas-assistant-pipeline §1 P1` · `node-connection-rules` · 现有「应用前先询问」实现                                                                                 |
| **红线**              | 写操作**必须**走 `node-connection-rules`；助手**不得**直接改 `NodeWorkflowProject.state`；复用既有审批骨架，**不新建第二套**；`set_review_state` **禁止**写 `approved` |
| **判据**              | 真机：要求「加一个角色节点并连到镜头」，节点数真的变化且连线合法性未被绕过                                                                                             |
| **为什么在包 4 之后** | 审核态先立起来，否则助手一有手就能绕过审核门直接铺 + 烧 credit                                                                                                         |

#### 包 6 · 审阅网格 ⬜（**要过设计门**）

|          |                                                                              |
| -------- | ---------------------------------------------------------------------------- |
| **目标** | 一次铺多镜后能在网格里批量过审 / 打回，不必逐张点开                          |
| **必读** | 包 4 的审核态实现 · `canvas-assistant-pipeline §2.2 ③` · `scenes/ui-page.md` |
| **依据** | 管道 plan 已把它从「新增能力」升为**必需**——铺 12 个节点后逐张点开审是灾难   |
| **判据** | 12 节点场景下能扫读全部状态并批量操作；375px 有可用形态                      |

#### 包 7 · 剧本节点设计轮 ⬜（**要过设计门 · owner 定：形态仍模糊**）

|                    |                                                                                                                                                                |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **前置**           | 包 6 完成 **且** `status.md` 的九类节点详情设计已收口 **且** 卡片契约已回来补                                                                                  |
| **不是**           | ❌ 不是「把 §4.2 七问写成契约就完事」——owner 明确形态还模糊                                                                                                    |
| **是**             | 完整设计轮：域定义 → 三方向 → 关键切片（桌面 + 375）→ owner 逐项确认 → 写 page 文档                                                                            |
| **必读**           | `video-shotcraft §13`（**只读 §13，不要读全 816 行**）· 本文 §4.2 · `scenes/ui-page.md` · `domains/canvas.md`                                                  |
| **要解的核心矛盾** | 剧本节点是**第十类节点卡**，而它承载的是「目录 + 状态机 + 放行开关」这种和其它九类**性质不同**的东西。它到底是节点、是面板、还是一个视图——这就是「模糊」的所在 |
| **判据**           | owner 确认关键切片；`shotcraft §14.4` 五条成功判据可逐条对照                                                                                                   |

#### 包 8 · 剧本节点实现 ⬜

承包 7 的确认结果。含 `propose_generation_batch` 费用门（列本批节点 + 模型 + 估 credit，停等用户）。
⚠ **全权连跑不得在取消功能存在前上线**（`VideoComposer.tsx:229` 明写取消是 P2，今天没有）。

---

### 6.4 · 可插包细则

#### 包 I1 · 视频灰区 #2 ⬜（很小）

`hooks/node/use-video-composer.ts:177` 的 `hasReferenceInputs` 只看 visual/keyframe/voice，不含纯视频源 → 只挂参考视频时 UI 模型态与提交不一致。**建议在包 2 真机时顺手确认，紧接着修掉。**

#### 包 I2 · LoRA 提示分层 ⬜（可再拆 5 片，每片一个 chat）

|          |                                                                                           |
| -------- | ----------------------------------------------------------------------------------------- |
| **必读** | `LoRA提示词与角色工具对标 §4 + §7` · `pages/lora-generate.md`                             |
| **五片** | ①触发词 chips ②发送全文只读预览 ③family 质量前缀一键 ④词条/设定本 MVP ⑤关键词注入（后置） |
| **禁**   | 不推翻已确认的桌面结构；只在搭配条展开态 / 助手 / 词条上增量                              |

#### 包 I3 · 壳级 A' 浅壳 Phase 1 ⬜（**要过设计门**）

|          |                                                                                                            |
| -------- | ---------------------------------------------------------------------------------------------------------- |
| **依据** | §0.1 已拍 A' 浅壳浅台                                                                                      |
| **必读** | `UI首页画布与全局策略 §3.1–3.2` · `brand-dna.md` · `canvas.css` 头注释（浅色孤岛那条缝）                   |
| **范围** | **只改 layout 壳**：`AppSidebar` / `MobileTabBar` / 默认 `bg-background`；Studio/画布先「吃壳的 semantic」 |
| **禁**   | 一天内 globals 换肤 + 全站搜 class 替换；把首页营销留白直接贴进工作台                                      |
| **判据** | 壳与画布不再双色系打架；改任何颜色前先跑 `contrast-check`（禁目测）                                        |

---

## 7 · 本轮特别容易犯的反模式

| 反模式                                               | 为什么会犯                                                     | 正确做法                                                                          |
| ---------------------------------------------------- | -------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| 把 research 当施工 SoT                               | 内容写得像规范                                                 | 每份文首都写着「**非实现授权**」；施工前必须升格进 `references/` 或开 task packet |
| 一次把 15 份丢给设计代理「全部详细设计」             | 想省事                                                         | audit §1.2 明列为禁止；按 §6 单包喂                                               |
| 用 research 的「30 天路线」抢 `status.md` 的当前焦点 | 两处都像计划                                                   | **status 赢日常**；本文只管调研 → 可施工的顺序                                    |
| 视觉项跳过 `ui-page` 门直接改码                      | 契约已写清就想动手                                             | 任务包 7 明标「**是**」；九类节点未定型前不加第十类                               |
| 引 GSAP 到工程页                                     | gsap-skills 的 description 自带「除非指定其它库否则推荐 GSAP」 | 本项目 **已指定 motion**；GSAP 只在首页营销域                                     |
| 把整张卡 JSON 塞进 Seedance prompt                   | 「卡里信息很全」                                               | 模型不解析卡协议；多余外貌词还会和 `@Image` 打架——**瘦 prompt、胖参考**           |
| 因为「3D 能力已有」就去堆 3D 模型                    | 看起来最容易加                                                 | 缺的是 GLB **下游**（定妆多视角回灌），不是第 6 个 I2-3D                          |

---

## 8 · Source of Truth

| 问题              | 读                                                                                              |
| ----------------- | ----------------------------------------------------------------------------------------------- |
| 现在在干什么      | `docs/status.md`（**唯一**焦点源）                                                              |
| 调研证据          | `docs/plans/research/*` + `说明.md`                                                             |
| 全局视觉法 / 禁忌 | `docs/brand-dna.md` · `docs/forbidden.md`                                                       |
| 交互与动效契约    | `docs/references/interaction.md`（已升格）                                                      |
| 画布业务 / 外观   | `domains/canvas.md` · `plans/canvas-skin-spec-2026-07-26.md` + `src/app/canvas.css`（浅色 SoT） |
| 画布实现事实      | 代码 + `status.md`（`node-canvas.md` 行为段部分过时）                                           |
| 助手管道断层      | `plans/canvas-assistant-pipeline-2026-07-26.md`（真机实测，非推演）                             |
| 模型目录          | `references/model-catalog.md` + `src/constants/models/*`                                        |
| 卡片契约          | ⬜ `domains/cards.md` **尚不存在**（W1 产出）                                                   |

---

## Last Verified

2026-07-30 · 通读 `docs/plans/research/` 15 份全文；对照 `status.md`、`canvas-assistant-pipeline`、`references/` 树。
**Owner 决策（同日）：** 壳色向 **A' 浅壳浅台**；卡片总线契约 **整体后置**（W3 已按此缩小范围）；剧本节点七问 **逐条评估中**（§4.2 已展开）；执行方式 **一档一个 chat、串行推进**（§6）。
**本机实测复核：** `docs/plans/research/` 全目录 git untracked；`火山豆包音频模型接入调查-2026-07.md` 不存在于磁盘且无 git 历史；`references/domains/cards.md` 不存在；`references/interaction.md` 存在；`canvas-workbench.md` 已有色板 superseded 横幅；`check-model-docs.mjs` 已改目录加载 + 零模型护栏，exit 0；`find src/app/api -name route.ts | wc -l` = **156**（`backend.md` 写 149）；`use-video-composer.ts` 的 cap 已按 `effectivePreviewModel` 计算（灰区 #1 已修），`hasReferenceInputs` 仍不含纯视频源（灰区 #2 未修）。
**未改任何业务代码。**
