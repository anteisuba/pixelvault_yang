# 项目状态

- 画布助手节点编排修复（2026-09-21）：恢复节点快照与逐步同步，接通完整模型渠道解析及失败原因；修正 `tool` 与 `finished` 同时返回时丢弃末步的问题。节点候选模型去重并使用独立 64 项上限，避免 28 个视频候选触发请求拒绝。真实助手对话及节点面板已验收：图片的模型／提示词／参考图，文本正文，视频的模型／提示词／首帧与文本连线。六个图片节点配置及 24 步撤销通过回归测试；未触发付费图片或视频生成。全量 Vitest 765 文件、8946 项通过／1 跳过；tsc 零错；lint 0 错误／19 条既有警告。未推送。契约见 [画布助手](references/pages/node-canvas-v2.md#13-助手)。

- Qwen-Image-2.1 内部评估（2026-09-21）：已创建独立 RunPod 端点 `ok6riemrmpdiic`（Min 0 / Max 1 / Idle 5s），保留原 SDXL 端点；ComfyUI v0.37.0 评估镜像 digest `sha256:633487282f1e295d9b482fabfe2ace08daada32604e738f9f2c31e5039022839`。本地图片工作台已接入 `qwen-image-2.1-runner`，支持 CFG／步数／负面提示词／seed、最多 10 张原生参考图；目录与服务端均限制 development 模式。Execution Worker 已部署版本 `c88661d7-7379-4186-8165-49982ba12762`，提交、轮询和取消使用独立端点。**本地页面实际生成 1 张 1024×1024 陶瓷杯图，RunPod completed=1，归档 CDN 返回 200。** 多参考图编辑仅完成代码测试，未做额外 GPU 出图；显存峰值与每图费用未测量。Worker 7 文件 233 项通过；应用全量 Vitest 765 文件、8950 项通过／1 跳过，tsc 零错，lint 0 错误／19 条既有警告，模型文档检查零错误／零警告；未 push 主仓。契约见 [Runner 域](references/domains/runner.md#qwen-image-21-内部评估2026-09-21)。

- NAI 标签台 A 两栏已本地实现（2026-09-21）：提示词、角色就地编辑、可启停画师串与参数集中左栏；右侧按需展开构图、资料和分块编辑。Danbooru 登录态目录接口复用既有 connector，可查角色/画师、核对全年龄来源样图并勾选导入；NAI 官方补全独立保留。停用/空白角色在发送前排除，原始分块、角色和负向标签刷新可恢复。真实页面已验证 Denia/Anmi 查询、选词导入、构图键盘定位、分块启停、滚动与草稿恢复。最终全量 Vitest 765 文件 / 8929 项通过（1 跳过），tsc 零错，lint 零错 / 19 条既有警告。未新增付费生成，未推送；375px 真机视觉与参考图生成链路未在本轮验收，Denia 成图一致性仍未解决。

- NAI 官方标签补全（2026-09-21）：桌面/手机正负标签及角色栏已接官方按模型建议，服务端使用用户 key；Denia 候选可查并已在真实页面选中，角色外观问题仍未解决。无新增出图、未推送。全量 763 文件 / 8911 项通过（1 跳过），tsc 零错，lint 零错 / 19 条既有警告；首轮 1 个测试进程启动超时，单项复核及全量重跑通过。

- NAI 调参与角色参考（2026-09-21）：基础参数补 CFG 重缩放、图生图噪声与 V4.5 标准质量标签；无图隐藏参考控件，移除无效 Vibe 占位。V4.5 单张精确角色参考已接服务端补边与 Worker 独立请求，明示额外 5 Anlas；尚未部署新版 Worker、未做精确参考付费联调。两张 V5 Curated 实图以相同步数 23 / CFG 7 / seed 20260921 对比 Denia 标签下划线与空格写法，同视口截图逐字节相同，角色仍不正确，不支持分隔符导致问题的假设；没有自动替换标签。A 布局已按 owner 确认实装，当前验收见上方两栏状态。验证：应用全量 Vitest 760 文件、8897 项通过 / 1 项跳过；Worker 6 文件、229 项通过；tsc 零错误；lint 0 错误 / 19 条既有警告。文件副本错误路由变异触发预期失败；前两次全量复跑遇到测试进程启动超时，最终双进程全量退出码 0。未推送、未部署。契约见 [providers.md](references/providers.md#novelai-基础参数与精确角色参考verified-2026-09-21)。

- PixAI 暂时下架（2026-09-20，owner 确认）：Tsubaki.2 / Haruka v2 / Hoshino v2 退出可用模型目录和新增 key 入口，旧 DB 配置不能重新启用；服务端阻止 PixAI 新生成，包括自定义型号 ID + 旧 key。保留已有作品、key、模型标识及已提交任务处理；本轮 style / LoRA 控件扩展已撤回。另修复多选时主模型裁剪掉其他已选模型专属参数的问题，专属卡保留适用范围提示并取消灰化。验证：应用全量 Vitest 760 文件、8,885 项通过 / 1 项跳过；Worker 6 文件、224 项通过；`tsc --noEmit` 零错误；lint 0 错误 / 19 警告。文件副本回归验证：旧逻辑 2 项失败，修复后 4 项通过。多选交互在独立浏览器标签页核对；最终下架页面复核因浏览器连接超时未完成，未做真实付费生成。未推送、未部署。契约见 [providers.md](references/providers.md#pixai-接入2026-09-20-暂时下架历史契约已核实)。

- 助手一张脸（2026-09-19，进度表 22 落地）：画布并入统一 dock（`StudioOperatorDock` + `StudioOperatorPanel`，`domain = canvas`），宿主收成三处——工作台 · 画布 · LoRA；**配音间不挂助手**（owner 09-19，D7 Q5 改口，LoRA 的「改」同时改口为保留）。新增 canvas 域三条工具（`canvas_apply` 走 v4 op 真值表 · `canvas_plan_rerun` 归看 · `canvas_generate` 归请求生成，服务端仍只吐载荷），`read_state` 在画布上读分层快照（当前镜 + 相邻两镜完整，其余一行，折叠镜的节点不进准入名单）。画布旧引擎整套删除：`StudioNodeAssistantDock` · `ShellAssistantFrame` · `/api/studio/node-assistant` · `node-assistant.service` · `use-assistant-conversation`。收起态换成 44px 近黑头像按钮 + 数字角标（待确认 + 未读结果，开面板清零），微状态卡与手机 fab 一起删；问题卡固定带「其他：自己填」（`otherText`）；五动词常驻条确认不要。被改节点 outline 闪 320ms（reduced-motion 降到 1ms），撤销沿线性栈撤到该步；面板宽度记忆归 dock。板上三条侧入口接上：重跑下游 → `canvas_plan_rerun`，文本卡续写/重写 → `canvas_apply` 的 `set_text`，剪辑台一句话排片 → 只到面板。钱闸的结构性证明从「扫 generate 字样」改为闭合 allow-list，闸本身未松。规范见 `references/pages/assistant-shell-v2.md`、`node-canvas-v2.md` §13、`lora-assistant.md`。
  已知缺口：① 剪辑台排片拿不到可套用的时间线提案，`deliverTimelineProposal` 无生产者，等助手有能做时间线的工具；② `CanvasAssistantHistory` / `CanvasAssistantRouteSelector` / `CanvasAssistantReferencePicker` 三个组件仍在，它们同时是旧 studio dock 的件，随 57 一起走；③ `ScriptDocWorkspace`（剧本笺与它那份只读转录，仍用 `CanvasOpProposalCard`）待 24。

- `/settings` 整页与入口收口（2026-09-18，进度表 13 + 14，设计画板 E3 完成、E4 起步）：新增 `/settings` 与 `/settings/[section]` 四分区（key / 用量 / 偏好 / 助手），桌面 200px 导航 + 720px 内容、手机停一级列表；新增 `GET /api/usage/by-model`（只数次数，单价与花费留客户端）。key 管理收成按 provider 一行的四态列表，配置与换 key 都开 `QuickSetupDialog`。侧栏账户区、`ApiKeyDrawerTrigger`、画布 `ShellApiKeys`、`StudioApiRoutesSection` 与 `ApiKeyManager` 一族同轮整删，三语里跟着走的键一并清掉；侧栏收成「顶端头像 + 最底设置行」，手机抽屉补「我」区与设置行，首页浮岛已登录态换成齿轮 + 头像，3D 工作台最后两处缺 key 死控件改为可点开弹层。行为见 `references/pages/settings.md`、`app-shell.md` §8、`node-canvas-v2.md` §7、`home.md`。未提交／发布。
  待办：① icon 桶为解 RSC 下 `createContext` 崩溃加了 `'use client'`（`aca8b753`），与 `@phosphor-icons/react/dist/ssr` 这个服务端出口之间怎么取舍未定——整桶标 client 会把引用它的服务端组件一起拉下水；② 助手记忆区只有 UI 与空态，行为等进度表 56 定数据形状；③ 真机 1440 / 820 / 375 **已登录态**目检待 owner。

- 首页视差（2026-09-16）：按 owner 最新反馈，五个分类全部相邻模型切换与画布三步统一改为上下纵向视差，向下滚动时当前内容上移、下一项从下方进入；画布展示区裁切防止内容越过步骤导航。手机保留整步操作，减少动态效果禁用连续视差与画布成片自动播放。方向调整后首页 92 项定向测试、目标 lint 通过，桌面中间态与 390px 手机边界核对；此前全量类型检查被 `assistant-operator.service.test.ts` 的 4 处类型错误阻挡，本轮未重跑。未提交／发布。行为见 `references/pages/home.md`。

- 画布助手展开宽与粘贴卡比例（2026-09-15）：展开两列从 `min(64rem, 72vw)` 收到 720（可拖 560–800），对话态仍 380（320–520）。粘贴/上传回填 `mediaWidth`/`mediaHeight`；已有图缺尺寸时按 `naturalWidth`/`naturalHeight` 补进当前版。收起卡按原图比例，宽钳 320–480，缺尺寸才退 16:9。未提交／发布。

- Seedance 2.5 画布时长 3s（2026-09-15）：官方档位 [4,30]，存量 3s 会 400「输入无效」。发送与 chip 现吸附到最近合法档（3→4），1080p 吸附到 720p。定向 77 项通过。未提交／发布。

- 画布图片节点参考图（2026-09-15）：镜头图 / 角色 / 背景 / 结果的提示词栏补上参考轨，可挂画布已生成的图、上传、素材库，正文 `@` 与视频卡同一套 `@图N`。叶子参考图仍无入口。定向 41 项通过。未提交／发布。

- 助手 Grok 接续超时（2026-09-15）：grok-4.6 默认高推理且不能关，流式首包被 30s 窗口掐断，用户看到「连接不上」。现发 `reasoning_effort: low`、首包窗口 90s、可见输出走 `max_completion_tokens`。未实测真实 Grok 往返，未提交／发布。事实见 `references/model-catalog.md` xAI 段。

- 助手问答快车道（2026-09-15）：问句本轮先答、不改表单；「改 / 换成 / 挂上 / 出图」才走工具。收尾轮 `message` 可发 `partial: true` 前缀，定稿仍按 id 覆盖且不折叠。开口前强制视觉分析只留给「问句 + @ 图 + 当前模型看不见图」。角色四段链与画风鉴定长文按需附录。自动档问答走快模型（OpenAI Luna / Gemini 3.8 Flash）；点名的模型不变。提示词冲突第二次不再「已停止尝试」收工，改为问题卡让创作者取舍；选「按我的要求写」后同一条参考检查不再挡写入。规范见 `references/pages/assistant-shell-v2.md` A 节。参考图绑定相关 48 项通过。助手输入：IME 选字回车不再发送；提示词框失焦后清掉组字旗，助手写入能进编辑器。反问卡常驻「你的看法」输入框，不必先点其他。本轮 `@` 了哪些参考图，分析就只准碰那些。未提交／发布。

- NAI5 官方文档核对与多人构图（2026-09-14）：按已确认方向留在 Image，V5 Full / Curated 显示可折叠角色控件，最多 22 人，独立正／负描述及自动／手动坐标；应用校验、Worker 映射、快照、草稿和图库回填已接通，多模型对照仅向 V5 传递。官方能力缺口与全部聊天决定写入用户要求的 HTML 跟踪页（会话 visualization 目录）；稳定契约见 providers reference。全量 720 文件／8299 项通过（1 跳过），补充定向 113 项与多模型 32 项通过，Worker 193 项、类型和目标 lint 通过；桌面交互及刷新恢复通过，窄屏视觉、真实付费生成未验证。未提交／发布。后续缺口：V5 自然语言增强、Text:／质量标签协调、透明背景等快捷控件、Full inpaint、助手角色结构；Vibe / Precise / Curated inpaint 当前 V5 支持待核实。图库长配置仍有 URL 长度局限，恢复后须选择对应模型。

最后更新：2026-09-13

唯一活跃进度文档。保持短，覆盖更新，不追加历史。

## 当前交付

- 助手 A 方向（2026-09-15）：owner 选择连续对话并授权修复。执行记录与参考依据默认折叠；同轮只显示一条未解决阻塞，后续成功消除旧阻塞，历史记录与恢复入口保留；删除五动词常驻条，补齐 en/ja/zh 成像介质翻译。提示词冲突修正一次仍失败即结束工具环并说明具体问题。手机半屏为移出视口的区域预留空间，输入区和发送按钮保持可见。1440/820/375 浏览器实测折叠、参考详情、键盘操作及抽屉全屏/关闭通过，未发现页面错误；类型与目标 lint 无错误，格式通过。全量应用 Vitest 722 文件、8309 项通过（1 跳过）；目标 lint 另有 9 条已有测试 warning。未做真实付费模型复测、实体手机软键盘或 reduced-motion 专项实测；复用 3000，未构建、提交或发布。规范见 `references/pages/assistant-shell-v2.md` A 方向节。 补充：加载状态支持轻量脉冲与减少动态效果设置；参考分析改为流式收齐后校验，避免 120 秒缓冲上限中断正常输出。提交前隔离暂存内容验证 33 文件、895 项相关测试及类型检查通过；未实测线上 Grok。

- 镜头失败反馈补漏：生成错误（原文/错误码/翻译键）随项目保存，卡片、展开态和移动端持续显示；旧失败节点显示通用错误，重试保留已有视频，开始新任务或成功替换素材后清除旧错误。补充 75 项定向测试、类型和目标 lint 通过；全量 718 文件、8287 项通过（1 项跳过）。已发布 `fe6d5930`：提交版本 pre-push 718 文件、8193 项通过（1 项跳过），生产构建及 28 项移动端检查通过；CI、Vercel/Worker 部署、健康与冒烟检查均成功。线上 eva 项目已验证旧失败卡片及展开态持续显示错误；未发起真实付费生成。
- 画布节点已发布（`29ea72a8`）：镜头展开前后共用提示词，素材轨改为横向缩略图与统一添加菜单；画布上传显示逐文件加载态，文字节点支持双击原位编辑。生成失败清任务 ID、状态回填按最新图合并，镜头取消接入服务端并保留已完成产物；取消终态停止轮询，旧任务回包不覆盖新任务。96 项画布定向测试与类型检查、目标 lint 通过；桌面浏览器已验证提示词往返、素材菜单及文字编辑，上传加载态用并发延迟测试验证，未发起真实付费生成。线上 eva 镜头对应 Worker 已完成失败回传，无在跑任务需要取消。本轮独立提交在隔离工作树验证：718 个文件、8190 项测试通过（1 项跳过），全量类型检查通过，lint 0 错误、14 条既有警告；原位编辑已关闭聚焦时自动滚动。已 push main；隔离生产构建、移动端 e2e 28 项、正常 pre-push 三关、GitHub CI（含 Production Build）、Worker 部署、Vercel Production 与两套部署后冒烟全部通过。
- LoRA 提示词触发词高亮已本地修复：背板与输入共用内边距/滚动槽，输入改为块级以消除底部基线空隙，增强底色和下划线；多触发词分别匹配，停用 LoRA 不高亮也不自动注入其触发词。58 项定向测试、类型检查通过，目标 lint 无错误（1 条既有警告）。使用来源长提示词在隔离浏览器页面复现旧错位并核对桌面/375px 修复后滚动与换行对齐；未验证登录态整页，未提交/发布。

- LoRA「做同款」与助手质量链已本地修复：来源 checkpoint hash 贯穿解析与 Runner 提交；恢复原始尺寸及 LoRA 权重，等实际挂载/启用/兼容状态齐全后出图。助手现能读取来源配方与当前设置、分析参考图、评审用户显式 @ 的结果，并将七项 Runner 参数写入实际出图控件；连续调参可完整撤销。候选确认先等待真实导入挂载，再携带资产 ID / 错误与最新快照继续规划，移除提前报成功的乐观状态。Sue 历史成图实际仅两把 LoRA、768×1024；源码推导底模曾回落 WAI v15，历史 GPU graph 未留存。本轮追加：猜测或未知来源触发词默认关闭，候选挂载不再自动追加猜词；SDXL 来源 Latent 二采样已接通，保持原提示词和权重。加载节点输出实际文件 SHA-256/大小/权重，经 PNG 证据与图像 hash 校验进入回调快照。用户已部署 Execution Worker；fork `92ef778` 已构建并切换，移除基础镜像 `--disable-metadata` 后，真实 RunPod 任务 `b1fc4795-5767-4c5c-93ca-341488c3f6ff-e1` 成功返回 968×1424 PNG。保持当前提示词、seed 2092427729 与 0.9/0.8/0.8 权重；PNG graph 确认二次采样 25 步、CFG 7、denoise 0.45，实际底模 SHA-256 与来源 RIN v4 一致，三枚 LoRA 文件与权重及图片 hash 均有证据。追加应用端实测任务 `f6521add-8454-4ab0-aad1-7d32bc53e311` 完成，归档 `b59bd2cf-0402-4afd-a7b4-442db8de4001` 保存 968×1424 PNG；提示词逐字一致，回调模型记录与图片 hash 与直接 GPU 实测完全相同。登录态助手可读出三枚 LoRA 权重、采样参数、来源底模和精修，并保存问答；首次只答“已核对”漏掉明细，追问后才完整回答，旧任务决定仍混入本轮结论。恢复 Chrome 后，助手 `set_lora_parameters` 与 `set_lora_weight` 两项真实工具回执成功，页面值核对一致，提示词和触发词开关未变。成图从素材库附入后被同步挂成 reference，`critique_result` 因来源引用保护拒绝；`analyze_references` 能读取画面，但助手重复分析/评审后仍未完成成图评审，分析卡显示未翻译的 `StudioOperator.referenceAnalysis.renderingMedium`。评审附件已清理，原配方保留；助手整体未通过验收。本轮应用回归 713 文件、8250 项通过（1 项跳过），另 5 文件因测试进程启动超时未执行；降低并发补跑后 5 文件、34 项全部通过。Execution Worker 全量 190 项、fork Python 45 项通过；类型与目标格式检查通过，目标 lint 无错误（10 条既有警告）。GPU 加载、二次采样与 PNG 保存已实测通过；主应用未提交/发布。事实见 `references/pages/lora-assistant.md` §11。

- 助手 v2（Operator 壳层改版）整轮完成并推送：五动词工具入口（看／查／问／改／生成）收口 37 个旧工具，SSE 帧由 14 收到 10，卡片由 14 类收到 5 类；每轮结账为结论记录（事实／决定／待办／证据编号）写进 `AssistantConversation.rounds` 并在下一轮注入，证据本按编号 `#eN` 按需回看；查证拆成核对与找图两个入口并支持来源白／黑名单；素材库四条写操作带撤销，标签写入改为单条原子 SQL 以防并发丢更新；上下文卡改为「提议→确认」，助手设置提供三档人设与文本模型选择（持久化在 `AssistantPersona`）。皮肤按 owner 选定的「玻璃仪表」方向落地，移动端半屏 Sheet 可拖拽。六个迁移（route model／round summaries／context card status／persona preferences／project rule kind／persona archetype）owner 已按发布流程先行 `migrate deploy`，代码后到。图片工作台完成 11 步真机实测并修掉 13 处：看不到参考图、问题卡答案跨轮丢失、参考分工简报 schema 漂移即整轮拒写、`@` 弹层被玻璃面板裁切、覆盖三选过度追问、上下文卡决定不落账导致每轮重发同一张卡等。查证新增题型偏置：画风／技法类问题的改写补技法限定词并剔除生平词，选源改为 web→danbooru→bilibili→wiki，结果排序加次级偏置但不越过印证数。发布前闸门：全量 Vitest 714 文件 8018 项通过（1 项跳过）、`tsc --noEmit` 通过、lint 0 错误（14 条既有 unused 警告）、隔离工作树内生产构建通过、移动端 e2e 28 项通过。遗留：`recall_evidence` 一次参数形状错误未复现定位；选源偏置是否应压过印证数待定；素材库四条写操作与真库行锁语义未实测。

- 助手视频／MP3 的 `@` 引用改为简短名称，旧消息的媒体长地址同步按名称显示；原生视频与音频输入已接通 Gemini，其他路由明确提示切换。图片评审名单保持独立。493 项定向测试、类型检查通过，目标 lint 无错误（3 条已有警告）；未调用真实付费模型，未提交／发布。

- 修复图片助手「分析成功 → 写入仍要求分析 → 重复分析被拦 → 再让用户确认」循环：写入按 URL 复用并重排跨轮完整视觉缓存，缺失依据仍需分析；前置条件失败明确允许分析后同轮重试，去掉相互矛盾的禁止重试指令。已读取 Chrome 当前会话及更早 29 条，确认末轮此前已成功写入；未发送新请求或生成。269 项工具链测试、类型检查通过；目标 lint 无错误（3 条既有测试警告），未提交。

- 助手历史入口合并到左上标题，下拉列表支持重命名与确认删除；切换显示加载态，失败保留原对话，续聊保留手动名称。修复展开动画残留 transform 导致 @ 菜单定位异常，真实页面已验证候选展示、插入引用与重命名弹窗；64 项定向测试、类型检查与目标 lint 通过。读取性能补修：三域摘要合并为一次查询，数据库只返回摘要；合并在途请求，30 秒内重复展开复用结果。新增回归覆盖重复读取、过滤与项目隔离，36 项定向测试、类型检查与目标 lint 通过；真实页面已读回列表。未修改真实会话数据，未运行生产构建或提交。

- 桌面图片/视频助手收起入口按 owner 指定 mock 改为右上角「星光图标 + 助手」玻璃按钮，删除整高竖轨；保留状态/进度及原展开、收起规则。开/关都有尺寸过渡与内容淡入淡出，关闭时立即禁用内容交互并在动画后移除；真实图片页已核对展开、关闭过渡及最终 44px 按钮，未留下透明遮挡。未发送助手请求，未提交。

- 视频用途按钮修复漏译：补齐 en/ja/zh 的 `image-reference`，显示「多图参考」，分段按钮等宽且不换行。真实视频页已核对三个标签正常；6 项国际化完整性测试与目标 lint 通过，未执行生成或提交。

- 提示词与 3D 两页按 owner 确认稿实装：提示词改为正文卡、搜索与弹窗新建，补齐复制降级、编辑取消还原及使用确认；3D 改为左设置 / 右预览，补齐形体示意与正面/侧面/复位浮动栏（真实模型相机控制），模型整卡切换、纵向下拉字段与紧凑参考图入口对齐确认稿，高级参数折叠，窄屏参数抽屉与固定费用/生成操作。375 / 820 / 1440px 布局与交互核对，97 项定向测试、类型检查与目标 lint 通过；未运行生产构建或真实付费生成，未提交/发布。设置栏补修通过 5 项定向测试、类型与目标 lint，实际切换 Trellis 后参数/费用同步且面板关闭；预览补修另通过 8 项定向测试、桌面/375px 核对，真实模型相机由组件测试覆盖，未做真实模型端到端验证。页面基准见 `references/pages/prompts.md`、`references/pages/studio-3d.md`。

- 助手画风问答改为当前视觉模型直接接收本轮指向的参考图；纯文本模型复用完整视觉事实或自动借用视觉路由，旧失败回复不阻止新轮看图。结构化分析新增渲染方式与立体特征，旧缓存缺项补读；分工与写入检查识别旧「纯 2D／排除 CG」对当前 3D 画风的冲突。定向 264 项、类型检查、格式检查通过，目标 lint 无错误（3 条已有测试警告）。全量 7184 项通过、1 项跳过，另有未修改的 InspirationCard 预览文案测试失败。经用户授权完成真实问答（GPT-5.6 Sol 看图 1 次、模型往返 1 次），准确回答图3为风格化 3D；人工整理提示词后，用 GPT Image 2.5 Flare 生成 1 张 1024×1024 对照图。成图保留拥抱构图和 3D 体积，但发丝及材质细节仍偏精细 CG，尚未准确复刻图3，亦未验证助手自动改词全链路；本次补修未提交/发布。

- 助手跨模型响应错误已修复：GPT、DeepSeek、Qwen、Grok、Gemini、Claude 的流式空回复、拒绝、截断、上游错误与异常断流保留可分类错误；非流式也拒绝异常结束的部分正文。助手不会把这些错误当作 JSON 格式问题重试，出错轮次不会执行工具。全量 Vitest 659 文件、7163 项通过（1 项跳过），类型检查通过，目标 lint 无错误（3 条已有测试警告）；新增夹具类型修正后另通过 159 项定向测试。未调用真实付费模型，未提交/发布。

- 助手消息首行显示头像与名称，正文从下一行开始；用户使用账号显示名，助手默认 `ANTI`，可在助手设置中自定义，清空后恢复默认。当前消息与历史记录共用布局，头像首字母预览同步默认名称。桌面与390px手机检查通过，自定义名称保存、刷新保留和清空恢复均已实测；51项定向测试、类型检查与目标lint通过。未提交/发布。

- 助手新增「恢复到这一步」：图片/视频快速模式将已完成配置快照保存至会话历史；恢复提示词、参考图顺序、模型和规格，清除待确认与续跑计划，不触发生成。追加/覆盖/保留确认移入助手时间线并展示完整文本。桌面及 390px 实测确认、刷新后恢复、四图顺序与行内引用恢复通过；旧记录无完整快照时明确不可恢复。全量 Vitest 658 文件 7043 项通过（1 项跳过），末轮引用联动修复另通过 81 项回归；类型检查及目标 lint 无错误（3 条已有测试警告）。本轮未生成图片，未提交/发布。

- 图片助手看图与参考分工已拆开：`analyze_references` 支持只看指定图片并保留视觉事实；`set_prompt` 才构建用途分工，分工失败不作废看图结果、不修改提示词。图片编号与 URL 由程序绑定，纯视觉结果可显示并随历史保存；JSON、字段与编号失败按阶段记录，独立提示分工失败，避免误报为图片不可读。原失败图3已实际下载解码成功（HTTP 200、约3.8 MB），但此前模型原始返回未保留，不能断言两次失败的具体字段。全量659文件、7176项测试通过（1项跳过），类型和格式检查通过，目标lint无错误（3条已有测试警告）。浏览器连接超时，未完成本轮页面实测；尚未以真实付费模型复测。已本地提交，未推送/发布。

- 图片工作台新增当前标签页内、按账号隔离的草稿保存：刷新恢复提示词、负面提示词和参考图顺序；URL 回填支持参考图并消费已应用参数。登录态浏览器完成恢复、修改、整页刷新验证，4 张参考图和最新提示词保留，未触发生成。全量 Vitest 657 文件、7026 项通过（1 项跳过），补充定向 7 项、类型检查与目标 ESLint 通过；未运行生产构建，未提交/发布。

- 素材选择弹窗按账号与筛选恢复图片缓存，后台更新当前第一页；图片不等待总数，分类计数在图片就绪后加载，文件夹与计数复用 30 秒缓存并合并在途请求，上传后刷新计数。分类数据库查询由 5 次降为 2 次。登录态浏览器重开及收藏/全部切换通过，重开无加载转圈；只读测量的新连接约 2.43 秒、网络往返约 234 毫秒，查询执行均不到 1 毫秒，首次加载仍受连接与网络延迟影响。全量 Vitest 655 文件、7019 项通过（1 项跳过），类型检查及全量 ESLint 通过；复用 3000 开发实例，未运行生产构建，未提交/发布。

- 助手与图片工作台共用参考图列表：助手上传、素材选择、拖入和结果投递统一进入宿主参考图，两边使用相同编号与缩略图；移除同步清理草稿引用，发送后保留共享参考图。重复 URL 去重，超容量图片保留为不可用；音频/视频附件保持原通道。桌面及 375px 手机双向同步与 @ 选择已实测，菜单置于助手输入区内避免外部点击收起；全量 654 文件、6999 项通过（1 项跳过），补充定向 67 项通过，类型检查与目标 lint 通过。未执行真实付费请求，已本地提交，未推送/发布。

- 图片工作台提示词支持 `@` 引用当前已添加且启用的参考图：选择菜单和正文标签显示缩略图，删除参考图自动移除对应引用并更新编号，原位编辑同步缩略图；发送时按最终参考图顺序转换引用，兼顾提示词卡片附带的参考图。桌面 1440px、平板 820px、手机 375px 实测通过；全量 Vitest 7005 项通过（1 项跳过），补充定向 16 项通过，类型检查与目标 lint 通过。未进行真实付费生成，已本地提交，未推送/发布。
- 图片工作台接入 GPT Image 2.5 Flare / Sunburst：规格新增六档生成画质、背景和可选预览；费用按实际尺寸与画质估算，输入另计。助手可设置并传递这些参数。共用图片编辑器显示实际型号，支持两款 2.5 的局部重绘、物体替换、元素提取及整图连续修改；模型/画质/背景/预览收进「编辑设置」。预览通过 Worker 回调或编辑 SSE 更新，完成结果归档，连续修改沿用上一轮图片及历史回退，未接 Responses 对话上下文。桌面及 390px 窄屏交互检查通过；全量 Vitest 653 文件、6988 项通过（1 项跳过），Worker 156 项通过，类型检查及目标 lint 无错误（4 条现有 unused 警告）。未进行真实付费生成，未提交/发布；生成预览需部署本轮 Worker 才在线上生效。
- 助手调查过程默认折叠：历史与实时文字调查及过渡说明合并为可展开的「调查过程」，最终结论、失败、改动和可操作结果保留。三语文案同步；浏览器实测 7 条历史调查记录合并、展开/收起正常，10 项定向测试与目标 lint 通过。
- 助手消息左侧列宽由 78px 收窄至 24px，头像右缘与文字框间距由 66px 缩至 12px，所有节点与正文继续对齐。
- 助手头像同步修复：历史消息补传 persona；设置保存、头像上传/移除成功后通知已挂载的 persona 使用方，对话区立即更新。浏览器已确认历史消息使用已保存图片且加载成功；6 项定向测试、类型检查与目标 lint 通过。
- 助手对话时间线头像从 20px 放大至 32px，移除所有行的时间显示（含 hover）；保留消息内容与节点分级。浏览器确认头像 32×32、无时间元素；8 项定向测试、目标 lint 与类型检查通过。
- 助手设置补齐自定义头像预览：已保存头像独立展示，与上传按钮分开，点击预览不触发上传，保留移除按钮。浏览器已确认用户现有图片加载成功并可见，目标 lint、格式与 diff 检查通过；局部展示修复，未重跑全量测试。
- 助手计划回复失败已定位并修复：真实模型两轮返回合法 JSON，但 pending 项缺少 label；输出提示现明确必需字段，重试反馈具体校验路径，保留严格 schema 与两次失败上限。图片助手实测已显示 5 步计划和 3 组待选项，停在确认前，未出图；141 项定向测试、类型检查与目标 lint 通过；全量 Vitest 627 文件通过（6602 项通过、1 项跳过）。
- 本地运行修复：按 owner 确认继续连接 aiproject 的 production 分支（ep-solitary-dew…），未新建/切换分支。Execution Worker 在 127.0.0.1:8787，health 200，签名回调连通；开发 APP_URL 为 localhost:3000。已应用助手分域、ResearchRun、GenerationJob CANCELLED/providerJobId、AssistantPersona/ProjectRule 共 5 条已有迁移；105 条 LoRA、1195 条生成、12 条对话全部保留（7 条 STUDIO 映射 IMAGE_STUDIO）。刷新后的助手 persona/conversation 读取已恢复成功日志。其余 7 条语音/镜像/LoRA 去重迁移仍未应用，本库未全量同步 schema。本地 RUNPOD_KEY 已配置并通过现有端点 health 鉴权（200，2 个 ready worker）；已启用本地 NEXT_PUBLIC_FF_COMFY_RUNNER，Anima 页面不再提示需要 API key。未执行真实付费生成。
- 全局侧边栏默认收起为窄图标栏（容器 56px），点击开关展开完整导航；当前应用内切页保留选择，刷新后重新收起，不从 cookie 恢复展开。按 owner 纠正恢复图标栏，移除完全隐藏时的悬浮按钮和主区顶部占位。图片工作台实测收起/展开/刷新通过，目标 lint 与格式检查通过。
- LoRA 搜索与收藏详情：宽松搜索默认推荐排序；收藏卡片点击封面打开共享详情。收藏详情现复用库的按需样图加载，即使收藏记录未保存预览图也可展示来源样图并切换大图；Endfield 桌面/375px 已实测。相关测试 14 项通过，全量 Vitest 627 文件通过（6601 项通过、1 项跳过），类型、目标 lint/格式检查通过。未 commit/push 本轮修复。
- LoRA 收藏 500 已修复：2026-09-06 经 owner 授权，仅向本地开发配置所连的 Neon `ep-solitary-dew…` 应用已有 `20260821091947_lora_source_snapshot` 迁移，补齐可空 JSONB 字段；原有 103 条 LoRA 保留。浏览器实测「Endfield 3D Style」收藏成功，刷新后仍显示「取消收藏」。未应用其他待执行迁移，不能视为该库已与当前 schema 全量同步。
- 本轮技能维护（2026-09-12）：按 OpenAI 技能维护文章修正 16 个技能、共 30 个客户端入口；精简触发描述，重写 audit / critique / polish / diagnosing-bugs，解除强制交接、审美判错和无复现禁止调查等冲突。保留客户端元数据与项目授权边界；验证范围为 Markdown、YAML、引用与副本一致性，未运行应用测试或模型行为基准，未 commit/push。
- 上一批已推送 main（8d0168af）：GitHub CI、Worker 部署、Vercel Production 和两套线上冒烟均通过。
- 本地待提交：保留现有 Studio 布局，调整助手入口、参数栏、负面提示词顺序、参考图大小、素材弹窗与 LoRA 窄栏。
- 竞技场按 owner 确认退役；历史数据库记录保留，模型路由历史胜率读取保留。
- 本轮验证：类型检查、lint、89 项相关测试及 28 项移动端测试通过；真实页面已核对 LoRA 44px 收起栏及竞技场 404。弹窗后续纠正：最近素材恢复直接显示，240px 宽并受参数栏边界限制，点击内部内容不关闭；22 项定向测试通过。

## 未决（等 owner 拍板）

- **`npm run preflight:migrations` 是断头路**：它要 `NEON_API_KEY` + `NEON_PROJECT_ID`，
  这两个变量 `.env.example` 与 `.env.local` 里**都没有**，脚本自 08-22 写就起从未在真实
  key 下跑通过。而 `bb9e7bae` 之后 Preview 也不再跑迁移——**「合并前验一次迁移」现在
  没有任何工具兜着**。三条路选一：配上这两个变量、给 Preview 配 Neon 分支库、或明确
  接受这个缺口。
- **2026-09-06 模型修复**：Gemini Omni 执行 ID 升级为 `gemini-omni-1.1-flash`，应用/画布发送闸门与 Worker 的 Interactions → Files → R2 链已接通；文本 enhance/assistant/planner 的 3.7 档升级为 3.8 Flash。未部署、未真实付费生成。稳定契约见 `references/model-catalog.md` §⑦。
- **Fish 已按 owner 2026-09-06 决定同时保留 `s2.1-pro` 和 `s2.1-pro-free`**：付费档保持默认，按 $15 / 百万 UTF-8 字节计费；免费档独立可选，受上游 Fair Use 限制。两者均需 BYOK；保留历史资产/音色卡的目录 ID，同步三语档位说明。未部署、未真实付费生成。

## Current Focus

- **C+F 素材页与选择器 UI 正在做 Codex / Claude 设计对比**：域要求与 shared/dedicated shell 边界已确认；右侧 Dock 是当前工作方向。Owner 2026-08-11 否决 V1 视觉，Codex 已重做“编辑型私人档案馆”V2（文件夹/详情两态），并准备独立的 Claude 设计简报（`assets-claude-design-brief-2026-08-11.md`）。完整账本 = `assets-cf-design-2026-08-09.md`（两份任务包均已随 plans 清理，git 历史可取）；`src/**` 继续冻结，待 owner 对比后选向。
- **本周十条待办**（owner 2026-08-07 口述），索引 = 任务包
  `week-2026-08-07-backlog.md`，交接 =
  `week-backlog-session-handoff-2026-08-07.md`（均已随 plans 清理，git 历史可取）。
  已完成：**H** LoRA 挂载不设上限（`eb295d23` + `6c3add69`）· **C4** 上传图画质（`84487a71`）·
  **J4** 无依据常量普查（`f9522e44`）· **K** LoRA 库重构（`a77901db`）· **L** TTS 上限按 provider
  拆分（`1cf1fe2a`）· **M** 角色图 LoRA 加号闸退役（`dafa2898`）· **E** 文档清理 + **J3** 悬空引用（`c2729530`）。
  C+F 已进入设计治理；其余并行条目的实时状态以各自 active plan 与代码为准：A 助手分域回复 · B 画布助手→节点 · D 图像优化 ·
  G 首页 UI · I 侧边栏 UI（前置 = J1 补视觉基线）· J1/J2。
- **调研落地路线图主链 7 / 10 已交付**，入口 = 任务包
  `research-landing-plan-2026-07-30.md` §6（已随 plans 清理，git 历史可取）。
  已完成：包 1 文档止血 · 包 2 现状实测校准 · 包 2.5 剧本长度悬崖 ·
  包 3 分镜静帧投影 · 包 4 审核态门禁 · 包 4.5 显示名收口 · 包 5 助手写画布。
- **下一个 = 包 6 审阅网格**，是主链上**第一个要过 `ui-page` 设计门**的包：
  域定义 → 三方向 → 关键切片（桌面 + 375）→ owner 逐项确认 → 写 page 文档 → 才实现。

## Completed / Stable Enough to Build On

### LoRA 挂载持久化

- 2026-08-31 修复 `use-active-lora-stack` 首帧水化会先把空栈写回用户分槽的竞态：
  持久化现在跳过每次 Clerk 水化前的旧 `items`，避免 React Strict Mode 重放时把已挂 LoRA
  稳定覆盖为空。回归测试断言水化期不再出现 `{ items: [] }` 写入；定向 Vitest 32/32、
  目标 ESLint / Prettier 通过，登录态 Chrome 中助手切 SDXL 后挂载的 LoRA 连续两次刷新均保留。

### 视频参考请求边界

- 2026-08-31 视频参考图/音频的请求硬上限收敛到 `src/constants/video-reference-limits.ts`：
  UI 模型槽位、公开请求 schema 与 Worker payload schema 共同读取 30 图 / 10 音频；达到
  UI 上限后所有新增入口禁用并显示移除提示，避免第 4 条音频或第 10 张图后才由 API 返回 400。
  相关 7 个定向 Vitest 文件共 63 项通过。

### Claude API key 验证

- 2026-08-31 补齐 `apiKey.service` 遗漏的 Anthropic 验证分支：按官方契约使用
  `GET /v1/models`、`x-api-key` 与 `anthropic-version` 做无生成费用的鉴权探测；版本号和路径
  收敛到 `ANTHROPIC_API`，生成与验证共用。Quick Setup 同时支持新式未绑定单一 Workspace 的
  identity-linked key，可选 Workspace ID 会随验证和实际 Claude 请求发送。修复前真机稳定误报
  “API key 无效”；两轮回归测试先红后绿，`apiKey.service` + `llm-text.service` 定向 Vitest
  72/72、i18n 完整性 5/5、目标 ESLint / Prettier 通过。

### 助手读取素材文件夹

- 2026-08-31 工作台助手新增 `list_asset_folders` → `inspect_asset_folder` 只读闭环：先按名称列出当前用户真实文件夹与完整路径，再检查所选文件夹直接归属的已完成图片；当前限制为最新 24 张、每批 8 张、不递归子文件夹、不处理视频 / 音频 / 3D。服务端逐批要求结构化结果完整覆盖实际输入，日志显示 `实际检查数 / 文件夹总数` 并只展示真正看过的证据图。全链不创建 Generation、不写库、不上传下载、不改表单；6 个定向测试文件 161 条用例、全量 TypeScript 与目标 ESLint 通过。

### 素材详情浏览

- 2026-08-30 图片详情补齐左右浏览：仅在当前筛选结果中至少有两张已加载图片时显示箭头，首尾禁用且不跨媒体；支持键盘方向键，预览与右侧元数据同步更新。图片切换使用项目统一的 200ms 方向淡入过渡，减弱动效偏好下关闭位移与缩放。

### 素材库上传

- 2026-08-30 素材库上传入口继续扩到音频（MP3 / WAV / M4A / FLAC / OGG / audio-WebM）。
  音频与视频一样浏览器直传 R2，完成端只读取对象元数据与前 4 KiB 验证真实容器，不进入图片压缩；
  成功后按 `AUDIO` 归档并保留浏览器读取到的时长。队列中的音频使用方形音乐占位，支持进度、失败重试与查看。
- 2026-08-29 素材库上传入口已从图片扩到图片 + 视频（MP4 / MOV / WebM）。视频走浏览器直传 R2，
  单次 PUT 上限采用 R2 的 5 GiB 平台边界，预签名窗口 1 小时；完成接口只用 HEAD 核对真实大小并
  Range 读取前 4 KiB 验证容器签名，不把整段大视频下载进应用服务器内存。视频记录按 `VIDEO`
  归档，并把客户端截帧写入 thumbnail 字段。

### 模型目录

- fal 图生图路由（2026-08-30）：挂参考图时 `SEEDREAM_50_PRO` / `SEEDREAM_50_LITE` /
  `FLUX_2_PRO` / `FLUX_2_FLASH` 从 T2I 端点切到官方 `/edit`，发 `image_urls[]`
  （上限 10 / 10 / 8 / 4）。此前能力表写 `maxReferenceImages: 0`，工作台会 400
  「does not support reference images」。Recraft / Illustrious 在 fal 上没有对等
  edit 端点，仍是 0。火山/BytePlus Seedream 本来就能带参考图，未改。
- NovelAI 已捞回 Image（2026-08-24）：V4.5 Full/Curated + V5 Full/Curated 四档
  `available: true`、**只 BYOK**，worker 认 V5 id（`params_version: 4`，不发
  `skip_cfg_above_sigma`）。参考图可选、最多 1 张、按 img2img 发；不进 LoRA；助手 LLM
  路由不改（NovelAI 不是聊天模型）。V4/4.5 的多图 Director 在 worker 侧从未实现，
  能力已收 `max: 1`。

### 图片工作台结果区

- owner 2026-08-23 选定方向 A「对照台」打底，并采用方向 B 的顶部常驻参考轨与底部唯一动作栏。
  `CompareGrid` 现按模型分行、点击只聚焦、定最佳需显式提交；图上不再叠按钮或 Gallery 元信息。
  `StudioReferenceRail` 让参考图在生成前后都可切换 / 编辑 / 删除；编辑舞台高度与标注图框同步修正。
  施工契约见 [`docs/references/pages/studio-image-workbench.md`](references/pages/studio-image-workbench.md)。

### 画布管道（本轮主链产出）

- **「聊 → 大纲 → 镜头 → 投影 → 出图」整链真机走通**（包 2 实测，两轮复跑）。
  投影幂等成立；缺口清单 = `canvas-pipeline-gap-2026-07-31.md`（已随 plans 清理，git 历史可取），
  它**取代** `canvas-assistant-pipeline` §0 的 07-26 断层表。
- 剧本提示词改**按预算装配**，修掉 4000 字符静默悬崖（此前故事一丰富就全线 500）。
- 分镜静帧已能投影（`role=shot`）；`role=background` **有意砍出**——它会渲染成身份卡，
  而身份卡存废未定（G5）。
- **审核态门禁**：生成物默认待审，未过审不进 `image_urls`；过审可逆。
- 助手拿到的是**真实显示名**，不再是本地化类型标签。
- **助手能写画布**：建节点 / 连线 / 改名 / 标审核态 / 触发生成五种 op，
  全部经「提案 → 用户点 → 才发生」；助手**不得自批** `approved`。
- 2026-08-10 owner 真机反馈已收口：删除视频节点重复工具条及文本/图片状态脚；镜头文本改白色纸面；
  身份代表图按原图比例展示并恢复卡外名字；声音节点恢复卡外名字；MiniMap 可直接拖动画布；
  节点拖动不再叠两张图；模型选择行悬停不改变高度。Seedance 2.0 选择器现明确区分
  fal.ai、火山方舟（国内）与 BytePlus ModelArk（国际）三条独立通道；2026-08-11 Seedance 2.5
  也已补齐同样三渠道，并按官方 schema 分开 fal 与 Ark 的参考音频约束。桌面画布助手现可直接拖动
  标题栏空白区且保持原尺寸，按钮不会误触拖动；历史记录迁入左侧第三视图；Script 展开态由约 627px
  提升到约 811px（当前 1127px 视口），仍限制在画布工作区内；助手默认顶部改为 64px，画布外观入口固定在顶栏最右侧。
- 节点详情页 Round 2 A「媒体优先」已按确认契约实现并完成定向验证；后续真机反馈中的
  左栏圆角、参考视频比例、音色选择后试听与助手默认模型显示也已修复。详情双栏断点已从
  `1120px` 收窄到 `960px`，避免 1092px 画布误进上下布局。方向 E 只保留为历史基线。
  owner 的真机复核仍在继续。
- 图片族详情来源已收口：右栏只保留「参考图 + 添加参考图」，其浮层统一上传 / 素材库 / 粘贴；
  主图上传/素材库替换迁到左侧媒体上的「替换图片」，Studio 与未完成 LoRA 不再占用详情 UI。

### 早前已稳定

- 画布视频节点 = 纯视频卡 + 固定右侧紧凑编排器；仅显式扩大按钮进详情。
- 视频引用沿真实连线收割图/视频/声音；预览与提交共用 `buildVideoSendPreview`。
- `video-model-send-plan.ts` 按 Seedance / Kling / HappyHorse / Gemini 定义素材槽与能力。
- 旧 `fusedIntoNodeId` 隐藏通路已退役；旧项目走兼容迁移。
- 左侧 Cast 卡匣已降级为定位器（分组 / 搜索 / 定位 / 选中），不再编辑与新建。
- 首页模型卡使用官方品牌素材；LoRA 底模选择器接入同源素材。
- 本地化 Clerk 登录/注册 catch-all 路由已恢复。
- 本地 dev「跑不出生成」根因已解：`NEXT_PUBLIC_APP_URL` 的 `https://localhost`
  协议错配；已加派发前守卫，生产零行为变化。

## Design Status

- 2026-08-30 素材页顶栏按 owner 确认的第二轮 mock 与后续真机反馈收口：桌面为内容区 `11/12`、最大 `1536px` 的居中 `rounded-2xl` 实色工具条；顶部搜索框已移除，类型/状态/模型/时间/排序五个分面直接进入原区域，生效条件的可删除 chip 也留在吸顶框内。首次进入、吸顶与密度切换动效继续遵循减弱动效设置。稳定契约已写入 `docs/references/pages/assets.md` §3。
- 2026-08-30 首页功能页 01 调整为图片优先：860px 工作台内输入列 315px、四宫格 500px，单图 245px；移动端补齐纵向回落，四宫格宽度 100%。未改变首页其他功能页、动效时钟或产品内页。
- 2026-08-05 LoRA 域已按 owner 真机反馈统一为系统白色浅色工作台；无 `section` 的
  `/studio/lora` 默认入口改为 Generate，Library / Mine / Train 显式深链保持不变。
- 2026-08-05 后续反馈已完成：首页图片案例改为“左侧文案内含交互 / 右侧纯图片”；视频生成、
  参考视频、视频合成共享白色浅色初始空态；添加菜单只保留一个直接创建的「收集」入口。
- Round 2 共同要求：主体媒体显著放大；镜头图/关键帧重做；删除独立「视频素材/文生模式/空闲」摘要条；
  视频模型复用通用两级选择器；管理素材不得覆盖提示词。
- 已选向：A「媒体优先」；详情展开层的信息 / DOM / 键盘顺序已确认改为
  **主体 → 编排 → 素材 → 关系/证据 → 动作**。
- 关键切片“视频详情 + 管理素材开/关”已完成三档验证；现行 `>960px` 保持“大媒体左 / 编辑右”，
  只有 `681–960px` 才切换为“媒体上方全宽 + 下方提示词 / 素材双列”。
- 宽屏编辑栏已确认固定为 `384px`；素材管理已确认在参考素材栏内原位切换，不遮挡提示词并保留焦点回返。
- 移动端已确认在 `≤680px` 进入“媒体 → 提示词/模型 → 参考素材”的完整单列，主动作粘附底部。
- 图片族主图与参考图保持不同数据语义，但来源入口不再重名堆叠；LoRA 存量字段未做破坏性迁移。
- **包 6 审阅网格的造型同样未确认**，必须走完设计门再实现。
- 稳定方向与未决问题原记录在交接 `canvas-session-handoff-2026-07-30.md`（已随 plans 清理，git 历史可取）；
  详情页当前结构见 `docs/references/pages/canvas-node-detail.md`，
  其状态已升级为「已实现、待 owner 真机复核」。

## Validation

- 2026-09-06 Fish 生产档切换：定向 71 条、全量应用 600 files / 6213 passed / 1 skipped、TypeScript、目标 ESLint、Prettier 与 diff 检查通过。文档巡检 0 error，唯一 warning 为本次预期的免费 → Pro ID 漂移，已定向同步 Fish 监控基线；未真实付费生成、未部署。

- 2026-09-06 模型修复：全量应用 Vitest 600 files / 6213 passed / 1 skipped，Execution Worker 6 files / 155 tests 通过；全量 TypeScript、目标 Prettier 与 `git diff --check` 通过；目标 ESLint 0 error（Worker 既有 ZIP 常量 warning）。模型文档巡检识别 58 个启用模型，0 error / 0 warning。开发库未发现 Omni/Fish 的 ModelConfig 覆盖；生产覆盖与真实付费生成未验证。未提交、未部署，发布时先 Worker 再应用。

- 2026-09-01 素材夹内页补齐「新建文件夹」：入口位于面包屑行末，复用现有创建弹窗并以当前夹为 `parentId`，成功后进入新子夹；根目录和总览页入口不变。`AssetFolderBreadcrumb` 定向 Vitest 1/1、目标 ESLint、Prettier、全量 TypeScript 通过；本地 dev 未运行，未做修改后页面真机目检。
- 2026-08-30 图片工作台助手附件面板：补齐 200ms 淡入/上移动效与 reduced-motion 降级；
  面板内部、触发器及素材选择 Dialog 内点击不关闭，点击外部关闭；键盘打开后聚焦首个操作，
  `Escape` 只关闭这一层并把焦点还给触发器。定向 Vitest 1 file / 10 tests、目标 ESLint、
  全量 TypeScript 通过；现有 `/zh/studio/image` 真机确认动效起止态、内部点击保持、
  参数区点击关闭附件面板并收起助手。
- 2026-08-30 素材详情图片切换：`MediaDetailViewer` 定向交互测试与 i18n 完整性共 2 files / 9 tests 通过；全量 TypeScript、目标 ESLint、目标 Prettier 通过。Chrome 已刷新到最新 `/zh/assets` 并确认 111 条素材正常渲染；打开详情后的自动控制连续超时，因此箭头位置与真实切换动效未完成浏览器自动化复核。
- 2026-08-30 素材页筛选合并顶栏：Chrome 1920×911 实测默认工具条 `x=215.5 / width=1536 / y=16 / height=56`，搜索输入数量为 0，五个分面全部在框内；选择“图像”后工具条高 86px，可删除 chip 与清除入口保持在框内，右侧上传/选择/密度未溢出，页面无横向溢出、控制台无 error。目标 Prettier、ESLint、全量 TypeScript 与 `git diff --check` 通过；owner 的 3000 dev 实例运行中，按规则未并行 build。
- 2026-08-30 素材库音频上传：先以 `audio/mpeg` 不在素材入口白名单的回归用例稳定复现两次；修复后
  定向 Vitest 6 files / 64 tests 通过，`npx tsc --noEmit`、目标 ESLint、目标 Prettier 通过；Chrome
  实查 `/zh/assets` 文件输入已包含 MP3 / WAV / M4A / FLAC / OGG / audio-WebM，页面现有音频队列
  `3 项 · 3 完成 · 0 失败`，详情存在可播放 `<audio>`、`user-upload` 与时长，控制台无上传错误。
  全量 Vitest 为 562 files / 5673 tests 通过、1 条失败；失败只来自同工作树在飞且未纳入本任务的
  `StudioOperatorLightbox` / `StudioOperatorLogItem` 缺三条 `StudioOperator` 消息键，素材上传相关用例无回退。
  owner 的 3000 dev 实例运行中，按规则未并行 build。
- 2026-08-30 首页图片功能页：Chrome 1920×855 实测输入 315px / 结果 500px / 单格 245px，页面 `scrollWidth === innerWidth`；`HomeV4Fn.test.tsx` 40/40 通过，目标 ESLint 0 error，`npm run typecheck` 通过，`git diff --check` 无空白错误。owner 的 3000 dev 实例运行中，按规则未并行 build。
- 2026-08-29 素材库视频上传：定向 Vitest 7 files / 62 tests 通过（含 80 MB 完成校验与
  `bytes=0-4095` 范围读取断言）；本次改动文件 ESLint 0 error；`git diff --check` 无空白错误；
  Chrome 实查 `/zh/assets` 文件输入已声明 JPG / PNG / WebP / GIF / MP4 / MOV / WebM。
  全仓 TypeScript 被并行音频改动缺失的 `AUDIO_EMOTION` / `AudioEmotion` 导出阻断，非本链路错误；
  全量 Vitest 在工作区另两套既有 Vitest 进程并行占用资源时长时间无汇总，已停止本次重复运行；owner
  的 3000 dev 实例运行中，按规则未并行 build。

- **2026-08-25 待推的 18 笔，闸门逐笔记在各自提交消息里**：`2321dbf5` 全量 tsc 零错、
  受影响 18 文件 358 条 vitest 全绿；`5c6c67f9` / `bf965440` 同日全量 vitest
  **532 files / 5148 passed**（当时唯二失败来自同工作树在飞的 3D 改动，已随 `8c7b7814`
  落地）；`4dbb2724` worker 95/95 + 计划内文件三绿；`8c7b7814` 定向 33 绿 + worker 95 绿；
  `08f27813` 真机验过成本预览逐档价（Wan 5s/720p $0.50 → 30s/1080p $6.00 →
  30s/480p $1.50，Seedance 2.0 720p $1.52 / 1080p $3.41，Kling 无旋钮路径 $0.84）；
  `d083ba0c` 的心跳端点用 Vercel MCP 实查生产验证。
  ⚠ **本文件不预先声称全量闸门绿**：完整的一次（tsc + lint + vitest +
  Playwright mobile + production build）按 `docs/checklists/release.md` 在 push
  前跑，以那次结果为准；owner 的 3000 dev 实例跑着时不并行 build。
- 2026-08-23 图片工作台结果区：开发态 `/zh/dev/ui-states` + `scripts/ui-probe.js` 复测同一状态，
  交互遮挡 4→0、矩形相交 16→0、无切换控件计数 1→0，9:16 单格高度从 767px 降到 257px，
  2×2 结果首屏由 3/4 提升为 4/4；全量 TypeScript 通过，全量 ESLint 0 error（5 条既有 warning），
  全量 Vitest 537 files / 5196 tests 通过。未执行 production build：owner 的 3000 dev 实例正在运行。
  （文件数此后降到 532，是 `4dbb2724` 删掉死执行链连带删测试所致。）
- 2026-08-18 图层分解整条删除（owner：功能废弃）：`LayerDecomposePanel` / `use-layer-decompose` /
  `image-decompose.service` / `/api/image/decompose` 四个文件删除；画布 `decompose` 能力与工作区那整段
  （预览 / 全选 / 放置图层）删除，ready 能力 8 → 7；`'layers'` 交互、`'image-layers'` 输出、
  `'derive-layers'` 策略、`derivedBatchId`（全仓只写不读）一并清掉；三语各删 39 个叶子，**删完重新 parse
  比对前后叶子集合，新增 0 / 意外删除 0**（禁正则改 messages JSON）。跑的是 HF Space `xiuruisu/see-through`。
  TypeScript 0 错 · 全量 Vitest 485 files / 4409 tests 绿 · 目标 ESLint 0 warning。真机 `/zh/studio/image`
  确认「图像」弹层只剩 上传 / 最近素材 / 素材库。
- 2026-08-18 图片工作台重设计（D1 切片 0–3）：图片模态换横向外壳 `StudioWorkbenchLayout`（`lg:w-72`
  参数栏 + 结果区，断点 lg 与 `useIsMobile` 对齐），视频 / 音频维持 `StudioFlowLayout` 不动；
  `generateCompare(input, models, perModelCount)` 收敛成矩阵入口（模型集 × 张数），`VariantGrid` 退役、
  图墙统一到 `CompareGrid`；`BaseModelPickerPanel` 加 `layout="columns"` 三列居中 Dialog + 多选 + 品牌图，
  搜索框按 owner 删；规格三档合成 `StudioSpecPopover`；助手改右上角浮标 `StudioAssistantFab`。
  真机 1920 实测：浮标开 720px / 关 0px、参数栏坐标全程不动；生成按钮阻塞态文案 18.15:1（`color(srgb …)`
  分量是 0–1，按 0–255 读会算出 1.50 的假值）。TypeScript 0 错 · 全量 Vitest 绿 · 目标 ESLint 0 warning。
  ⛔ 切片 4（成本预览）已落地：图片档 `5f3a3c77`，视频档随 `08f27813` 扩齐；切片 5（平台出资降档）
  仍未做，卡 owner 一句话；编辑线 E0–E5 一条未开工。
- 2026-08-17 Studio Image 参数栏弹层回归：`ResponsivePopover` 在窄视口 + fine pointer 时保留锚定 Popover，
  touch-primary 紧凑态仍走 Drawer；`StudioPromptArea` 的 document pointer handler 不再抢先关闭当前
  `图像` / `规格` 触发器。定向 Vitest 3 files / 25 tests、全量 Vitest 485 files / 4409 tests、
  TypeScript、目标 ESLint 与 `git diff --check` 通过；现有 3000 页面实测两个入口均可“打开 → 再点同一按钮关闭”。
- **2026-08-11 及更早的逐条验证记录不再在本文件复述**（画布助手几何与交互 · Seedance 2.5 三渠道 ·
  身份卡空态回归 · 视频紧凑编排器缺 Key 路由 · 08-10 画布反馈 · 08-05 统一助手切片 / 模型选择器 /
  UI 收口 / 图片族来源 · 08-04 节点详情 Round 2 与画布反馈 · 包 5 真机验收）：全部已验完并进 main，
  真机数值见各自 `docs/references/pages/*.md` 与 git 历史。
  与发布相关的唯一一条留在这里：**移动端 Playwright 最近一次记录在案的结果是 2026-08-05 的
  单 worker 30/30**，此后未再跑。

## Next

1. **推这 18 笔**：过 `docs/checklists/release.md` P0；**fork 镜像先上 RunPod，app 再上线**。
2. **包 6 审阅网格**：按 `ui-page` 走设计门（域定义 → 三方向 → 关键切片 → owner 确认）。
3. 包 7 剧本节点**设计轮**（owner 已定：形态仍模糊，不是写契约）；其前置除包 6 外，
   还包括**卡片总线契约必须回来补**——否则剧本节点铺出的角色槽全要手填。
4. 链外登记 G1 参考图接不到素材库 / G3 政策归因 / G4 进度·取消·失败可见性 / G5 身份卡存废。
   G2 模型选择器已修（`df12cf19`）。
5. 可插包 I1 视频灰区 #2（很小，仍未做）· I2 LoRA 提示分层 · I3 壳级 A' 浅壳。
6. 本轮留下的两个同族收尾：`deleteProject` 删到最后一个项目时新建的本地 id 不在服务端确认集合里
   （`acc8a668` 登记）· `prompt/enhance` 与 `prompt/feedback` 仍是 `maxDuration = 30`，小于新的
   120s 缓冲超时，走 Grok 时超时永远轮不到触发（`5c6c67f9` 登记，要不要一起提由 owner 定）。

## Blocked

- **包 6 / 包 7 仍需要 owner 对关键切片逐项确认**；节点详情 Round 2 已实现，不再构成实现授权阻塞。
- **卡片总线契约** owner 已后置，但**包 7 之前必须回来补**（G5 身份卡存废与之同源）。
- OpenAI key 无效（GPT Image 2 报 401）；VolcEngine 未绑 key ——
  后者挡着 G3 的归因对照实验。
- 真实扣费 provider smoke 仍未执行（需要有效且经 owner 授权的 API key，会产生费用）。
  Wan 3.0 因此有三件事未定：位置引用是否必需、首尾帧是否真生效、30s 是否超过轮询上限
  （`maxAttempts × pollInterval = 600s`，全局常量未动）。
