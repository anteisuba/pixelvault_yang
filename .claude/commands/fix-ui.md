分析以下组件的 UI 问题并提出修改方案：$ARGUMENTS

请按以下步骤执行：

1. 读取目标文件
2. 逐条对照 CLAUDE.md 中「设计语言」section 的规范，检查：
   - 色彩：是否使用了规定的色值（#faf9f5 背景、#141413 主文字、#d97757 强调色等），是否有禁止色（纯白 #ffffff、蓝紫渐变）
   - 字体：是否遵守 sans + serif 配对，是否用了 Inter/Roboto 等通用字体
   - 间距：Section 间距是否达到 80-128px，正文列宽是否 ≤720px
   - 动效：是否只用 fade-in + translate-up，是否有禁止的 bounce/spring/视差效果
   - 整体风格：是否存在重投影卡片、霓虹光效、generic AI 美学
3. 输出违规清单，格式如下：
   - `[FAIL]` 文件名:行号 — 违规内容 → 对应的规则
   - `[WARN]` 文件名:行号 — 潜在问题 → 建议
4. 针对每条违规给出具体修改方案（Tailwind class 或 CSS 值替换）
5. 最后按影响程度排优先级（高 / 中 / 低）
