重构以下 section 的布局和样式：$ARGUMENTS

请按以下步骤执行：

1. 读取指定文件，定位目标 section 的代码范围
2. 分析当前问题：结构、色彩、间距、字体层级、动效等方面的偏差
3. 提出重构方案，包含：
   - 布局结构调整（如何改、为什么改）
   - 色彩替换清单（旧值 → 新值，对应设计规范中的哪条）
   - 字体/间距调整说明
   - 动效方案（仅 fade-in + translate-up，duration 300-600ms，ease-out）
   - 需要注意的边界情况（移动端响应式等）
4. 等待用户确认方案后，再输出完整替换代码

设计规范参考 CLAUDE.md 中「设计语言」section。
