# 04 · UI 測試 — 工作包細分（L3）

> 基準日期：2026-04-15
>
> 本文件嚴格對齊 `docs/plans/ui/03-工作包細分.md` 的 23 個 UI 工作包。**不發散新測試範圍**，只把 UI WP 內原本就聲明的測試任務抽出成獨立測試 WP。
>
> 決策/重構/文檔化類 UI WP（WP-Studio-01、WP-Global-07）不產測試工作包。
>
> 工具鏈缺失的項（4.5 A11y、4.6 視覺回歸）已在現狀映射中標為「暫緩」，本輪不出測試 WP。
>
> 狀態說明：本文件是**UI 測試待辦工作包**，不等於當前已存在測試清單；判斷現況請先讀 `02-現狀映射.md`。

---

## 映射索引（UI WP → 測試 WP）

| UI WP         | 測試 WP         | Test Layer       |
| ------------- | --------------- | ---------------- |
| WP-Studio-01  | _—（去留決策）_ | —                |
| WP-Studio-02  | TC-Studio-02    | component + e2e  |
| WP-Studio-03  | TC-Studio-03    | hook             |
| WP-Studio-04  | TC-Studio-04    | hook             |
| WP-Studio-05  | TC-Studio-05    | component        |
| WP-Gallery-01 | TC-Gallery-01   | e2e              |
| WP-Gallery-02 | TC-Gallery-02   | component        |
| WP-Gallery-03 | TC-Gallery-03   | component        |
| WP-Gallery-04 | TC-Gallery-04   | hook + e2e       |
| WP-Gallery-05 | TC-Gallery-05   | route + e2e      |
| WP-Gallery-06 | TC-Gallery-06   | hook             |
| WP-Profile-01 | TC-Profile-01   | component + e2e  |
| WP-Profile-02 | TC-Profile-02   | component        |
| WP-Profile-03 | TC-Profile-03   | component        |
| WP-Profile-04 | TC-Profile-04   | component + hook |
| WP-Profile-05 | TC-Profile-05   | route            |
| WP-Global-01  | TC-Global-01    | component + e2e  |
| WP-Global-02  | TC-Global-02    | e2e              |
| WP-Global-03  | TC-Global-03    | service (lib)    |
| WP-Global-04  | TC-Global-04    | component + e2e  |
| WP-Global-05  | TC-Global-05    | e2e              |
| WP-Global-06  | TC-Global-06    | component + e2e  |
| WP-Global-07  | _—（文檔化）_   | —                |

**共 21 個 UI 測試工作包**

---

## Studio

### TC-Studio-02 · Studio loading/error 觸發與渲染

- **Covered Work Package**：WP-Studio-02
- **Test Layer**：component + e2e
- **Test Cases**
  1. `studio/loading.tsx`：渲染骨架與佈局一致（CLS < 0.1）
  2. `studio/error.tsx`：ApiKeysProvider 失敗時觸發；重試按鈕可用；返回 Gallery 按鈕可用
  3. 三語（en/ja/zh）文案 key 存在
  4. E2E：模擬 Provider throw → error 邊界攔截
- **Fixtures / Mocks Needed**：mock `ApiKeysProvider` 拋錯 · `useTranslations('Studio')` mock
- **Risks**：loading 骨架與實際佈局差異造成視覺跳變
- **Acceptance Criteria**：3 語文案 · CLS 低 · error 重試路徑 E2E green
- **Priority**：P1
- **Effort**：S

---

### TC-Studio-03 · useUnifiedGenerate / useStudioGenerate 職責測試

- **Covered Work Package**：WP-Studio-03
- **Test Layer**：hook
- **Test Cases**
  1. `use-unified-generate`（擴充既有 test）：B4（比較）/ B5（變體）模式分支
  2. `use-studio-generate`：`isGenerating` / `lastGeneration` 狀態變化
  3. use-studio-generate 作為 view-model 不觸發副作用
  4. 兩 hook 消費同一 `useUnifiedGenerate` 源時結果一致
- **Fixtures / Mocks Needed**：StudioContext mock wrapper（3 provider 可注入初始值）· API mocks（studioGenerateAPI 等）
- **Risks**：Studio 核心流程回歸風險
- **Acceptance Criteria**：正常 + 錯誤路徑各覆蓋 · 既有 `use-unified-generate.test.ts` 不破
- **Priority**：P1
- **Effort**：M

---

### TC-Studio-04 · 快捷鍵 Input Focus 衝突測試

- **Covered Work Package**：WP-Studio-04
- **Test Layer**：hook
- **Test Cases**
  1. Focus prompt textarea → `Cmd+Enter` → 觸發 generate
  2. Focus card search input → `Cmd+Enter` → 預期行為（依團隊決策）
  3. IME 組字中（composition start/end）→ 不誤觸
  4. 無 focus 時快捷鍵正常
  5. `Cmd+Shift+Enter` → 觸發變體
- **Fixtures / Mocks Needed**：`@testing-library/user-event` · DOM keyboard event simulation
- **Risks**：IME composition 在 jsdom 下難以完整模擬
- **Acceptance Criteria**：5 路徑 · IME 組字中無誤觸
- **Priority**：P1
- **Effort**：S

---

### TC-Studio-05 · Studio 核心組件測試骨架

- **Covered Work Package**：WP-Studio-05
- **Test Layer**：component
- **Test Cases**
  1. **StudioPromptArea**：渲染、輸入、aspectRatio 切換
  2. **StudioCanvas**：空態 / 比較模式 / 變體模式三種渲染
  3. **HistoryPanel**：篩選 tab 切換（all / image / video / audio）
  4. StudioContext mock wrapper：3 provider 可注入 + 複用
- **Fixtures / Mocks Needed**：`src/test/studio-context-helpers.tsx`（新建 mock wrapper）· api-helpers 基礎
- **Risks**：StudioContext 3-provider mock 侵入性高；Pragmatic Drag-and-Drop 在 jsdom 不可用
- **Acceptance Criteria**：3 組件測試 green · mock wrapper 可複用 · CI 時長增加 ≤ 5s
- **Priority**：P1
- **Effort**：M

---

## Gallery

### TC-Gallery-01 · Particles 性能降級 E2E 測試

- **Covered Work Package**：WP-Gallery-01
- **Test Layer**：e2e
- **Test Cases**
  1. `prefers-reduced-motion` 啟用 → canvas 不掛載
  2. CPU throttling 4x → 粒子數降至 60（若實作）
  3. 卸載頁面 → rAF 清理，無內存洩漏
  4. mousemove listener 是 passive
- **Fixtures / Mocks Needed**：Playwright `emulateMedia({ reducedMotion: 'reduce' })` · CDP CPU throttling · heap snapshot
- **Risks**：視覺退化需設計確認；heap 斷言在 Playwright 實現複雜
- **Acceptance Criteria**：reduced-motion 分支 E2E green · 低端 CPU 下 CPU 使用 < 30%
- **Priority**：P1
- **Effort**：S

---

### TC-Gallery-02 · ImageDetailModal + ImageCard 測試（P0）

- **Covered Work Package**：WP-Gallery-02
- **Test Layer**：component
- **Test Cases**
  1. **ImageDetailModal**：open / close / ESC 關閉
  2. Like 按鈕樂觀更新 → 失敗回滾
  3. **ImageCard**：點擊媒體觸發 modal 開啟
  4. `ImageCardActions` 右上懸停顯示
  5. `ImageCardVisibility` 僅 `showVisibility=true` 時出現
  6. 非自己作品 → visibility 切換不可見
- **Fixtures / Mocks Needed**：`use-like` mock · generation fixture · Radix Dialog 需 `user-event` 而非 fireEvent
- **Risks**：Radix focus trap 在 jsdom 下行為特殊
- **Acceptance Criteria**：6 路徑測試 · modal 打開關閉正常 · like 回滾路徑覆蓋
- **Priority**：P0
- **Effort**：M

---

### TC-Gallery-03 · 篩選 URL 狀態與空態測試

- **Covered Work Package**：WP-Gallery-03
- **Test Layer**：component
- **Test Cases**
  1. **GalleryFilterBar**：filter 變更觸發 `window.history.replaceState`
  2. 空結果 + 有 filter → Clear Filters 按鈕出現
  3. 空結果 + 無 filter → Empty state（`Gallery.empty.noResults`）
  4. `?model=nonexistent` → safe fallback 到全部
  5. `?liked=true` 未登入 → 登入提示
  6. **GalleryFeed**：空態渲染三語
- **Fixtures / Mocks Needed**：`use-gallery` mock · Next.js router mock · i18n messages
- **Risks**：`buildGalleryQueryString` 鍵順序變動影響快取
- **Acceptance Criteria**：6 路徑測試 · 三語文案齊
- **Priority**：P1
- **Effort**：S

---

### TC-Gallery-04 · 無限滾動 Sentinel 穩定性測試

- **Covered Work Package**：WP-Gallery-04
- **Test Layer**：hook + e2e
- **Test Cases**
  1. `use-gallery` loadMore 在 `isLoading=true` 時不重複觸發
  2. 篩選切換 → 取消進行中請求（AbortController 或 request id 守護）
  3. page=1 與 loadMore 結果合併順序正確
  4. E2E：Playwright 快速滾動到底部 + 快速切篩選 20 次無重複請求
- **Fixtures / Mocks Needed**：IntersectionObserver polyfill（jsdom）· fetch mock 可延遲
- **Risks**：jsdom 下 IntersectionObserver 需 polyfill；Playwright 滾動時序
- **Acceptance Criteria**：快速滾動 + 切換 20 次無重複 · 取消請求正常
- **Priority**：P1
- **Effort**：M

---

### TC-Gallery-05 · 詳情頁 JSON-LD + OG 路徑測試

- **Covered Work Package**：WP-Gallery-05
- **Test Layer**：route + e2e
- **Test Cases**
  1. `generateMetadata`：image generation → ImageObject JSON-LD 結構完整
  2. video generation → VideoObject JSON-LD 含 duration/contentUrl
  3. 缺 `appUrl` 環境變數 → fallback
  4. OG route 被調用：`/api/og?type=generation&id={id}`
  5. E2E：Playwright 抓取 `<script type="application/ld+json">` 斷言
- **Fixtures / Mocks Needed**：`getGenerationById` mock（image + video 兩型）· env mock
- **Risks**：schema.org 規範演進；JSON-LD 嵌入位置變動
- **Acceptance Criteria**：image/video 各驗證 JSON-LD · OG route 404/200 分支 · E2E smoke
- **Priority**：P1
- **Effort**：M

---

### TC-Gallery-06 · Like 樂觀更新競態測試（P0）

- **Covered Work Package**：WP-Gallery-06
- **Test Layer**：hook
- **Test Cases**
  1. `use-like`：連按 5 次 → 最終狀態與服務器一致
  2. API 失敗 → UI 回滾至 pre-optimistic 狀態
  3. 多個 ImageCard 並發 toggle → hook 實例隔離，不交叉污染
  4. 未登入觸發 like → 跳轉登入或提示
  5. inflight 守護：同一 id 不重複發送
- **Fixtures / Mocks Needed**：`toggleLikeAPI` mock · Clerk auth mock（未登入/已登入）
- **Risks**：樂觀 + 回滾測試 flakiness
- **Acceptance Criteria**：5 路徑測試 · 連按 5 次收斂 · 失敗回滾視覺穩定
- **Priority**：P0
- **Effort**：S

---

## Profile

### TC-Profile-01 · /profile 未登入頁內提示測試

- **Covered Work Package**：WP-Profile-01
- **Test Layer**：component + e2e
- **Test Cases**
  1. RSC：Clerk `auth()` 返回 null → 渲染 syncPendingTitle 頁內提示
  2. 三語文案齊（`Profile.syncPendingTitle` / `.syncPendingDescription`）
  3. CTA 按鈕跳轉目標正確（`/studio` 或 `/sign-in`，依決策）
  4. 已登入 → 渲染 ProfileFeed
  5. E2E：Playwright 未登入訪問 `/en/profile` 斷言頁內提示
- **Fixtures / Mocks Needed**：Clerk `auth()` mock（null + userId 兩態）
- **Risks**：RSC async 測試需 Next.js test util
- **Acceptance Criteria**：三語提示 + 已登入分支 · E2E smoke green
- **Priority**：P2
- **Effort**：S

---

### TC-Profile-02 · ProfileFeed 批量操作測試

- **Covered Work Package**：WP-Profile-02
- **Test Layer**：component
- **Test Cases**
  1. 進入選擇模式 → 全選 / 反選 / 部分選計數正確
  2. 批量刪除：confirm dialog → 循環請求 `/api/generations/[id]` DELETE
  3. 批量隱私切換：public ↔ private → `/api/generations/[id]/visibility` PATCH
  4. 中途失敗 → 部分成功，失敗項回滾 UI
  5. 空選取時批量按鈕 disabled
- **Fixtures / Mocks Needed**：`use-gallery` mock（mine=true）· API mock 可注入部分失敗
- **Risks**：批量循環 rate-limit 觸發；中途失敗 UX 需定義
- **Acceptance Criteria**：5 路徑測試 · confirm + 失敗可見
- **Priority**：P1
- **Effort**：M

---

### TC-Profile-03 · Creator/Private View 切換測試

- **Covered Work Package**：WP-Profile-03
- **Test Layer**：component
- **Test Cases**
  1. `getCreatorProfile` 返回 `{ private: true, ... }` → 渲染 PrivateProfileView
  2. 返回正常 profile → 渲染 CreatorProfileView
  3. username 不存在 → 404
  4. PrivateProfileView 三語文案
  5. 自己訪問自己（isOwnProfile=true）的隱私分支
- **Fixtures / Mocks Needed**：`getCreatorProfile` mock（3 種返回）· Next.js RSC test
- **Risks**：切換邏輯在 service 層，服務 API 變動需同步
- **Acceptance Criteria**：3 路徑 RSC 測試 + PrivateProfileView 組件測試
- **Priority**：P1
- **Effort**：S

---

### TC-Profile-04 · ProfileEditModal 隱私開關完整性

- **Covered Work Package**：WP-Profile-04
- **Test Layer**：component + hook
- **Test Cases**
  1. **ProfileEditModal**：toggle → onChange → 提交 → 樂觀更新
  2. API 失敗 → 回滾 + error toast
  3. **useMyProfile**：`isPublic` 狀態同步
  4. 三語隱私說明文案
  5. 變更後 `/u/[myUsername]` 訪問反映（E2E 可選）
- **Fixtures / Mocks Needed**：`/api/users/me/profile` mock（成功 + 失敗）· SWR/React Query cache
- **Risks**：樂觀更新與快取策略錯配
- **Acceptance Criteria**：5 路徑測試 · hook + 組件雙層
- **Priority**：P1
- **Effort**：M

---

### TC-Profile-05 · /u/[username] OG 快取失效測試

- **Covered Work Package**：WP-Profile-05
- **Test Layer**：route
- **Test Cases**
  1. `user.updatedAt` 變化 → `ogImageUrl` 的 cache buster 變化
  2. OG route 對 profile 類型正確返回 1200×630
  3. username 不存在 → 404（與 TC-Storage-03 交叉）
  4. Cache-Control header 存在
- **Fixtures / Mocks Needed**：`user.service` mock · 與 TC-Storage-03 共用測試檔案
- **Risks**：外部平台（Twitter/FB）快取不受控
- **Acceptance Criteria**：4 路徑 · cache buster 與 updatedAt 綁定
- **Priority**：P2
- **Effort**：S

---

## Global

### TC-Global-01 · not-found 三語渲染測試

- **Covered Work Package**：WP-Global-01
- **Test Layer**：component + e2e
- **Test Cases**
  1. `[locale]/not-found.tsx` 渲染：三語文案（`NotFound.title` / `.description` / `.cta.home` / `.cta.gallery`）
  2. 根級 `not-found.tsx` 渲染（無 locale 時的 fallback）
  3. E2E：訪問 `/zh/__nonexistent__` → 404 頁
  4. E2E：訪問 `/__no_locale__` → 根 fallback
  5. CTA 按鈕可用
- **Fixtures / Mocks Needed**：`useTranslations('NotFound')` mock · Playwright 404 場景
- **Risks**：middleware `localePrefix='always'` 可能影響根 fallback 觸發
- **Acceptance Criteria**：三語 404 + 根 fallback + E2E smoke
- **Priority**：P0
- **Effort**：S

---

### TC-Global-02 · loading.tsx CLS 測試

- **Covered Work Package**：WP-Global-02
- **Test Layer**：e2e
- **Test Cases**
  1. Studio / Profile / u / Arena / Storyboard 各頁：loading → loaded 無 layout shift
  2. CLS < 0.1（Web Vitals）
  3. 骨架 h1 位置與實際頁面 h1 對齊
  4. `PageSkeleton` 3 個 variant（list / detail / dashboard）可複用
- **Fixtures / Mocks Needed**：Playwright + web-vitals library · 慢網路模擬
- **Risks**：CLS 測量在 jsdom 下不可行，必須 Playwright；不同 viewport 結果不同
- **Acceptance Criteria**：5 頁 CLS < 0.1 · 3 個 PageSkeleton variant 有快照
- **Priority**：P1
- **Effort**：M

---

### TC-Global-03 · Toast Helper 單測

- **Covered Work Package**：WP-Global-03
- **Test Layer**：service (lib)
- **Test Cases**
  1. `toastSuccess(i18nKey)` → 正確調用 sonner
  2. `toastError` / `toastLoading` / `toastPromise` 同上
  3. 傳 i18n key → 自動翻譯；傳字串 → 直接顯示
  4. `grep` 確認無業務代碼直接 `import "sonner"`
  5. 所有 toast 文案三語有對應 key
- **Fixtures / Mocks Needed**：sonner mock · `useTranslations` mock · 項目範圍 grep（作為 lint）
- **Risks**：批次替換現有 sonner 呼叫點易漏
- **Acceptance Criteria**：helper 4 API 有測試 · 無直接 sonner import（CI guard）
- **Priority**：P2
- **Effort**：M

---

### TC-Global-04 · MobileTabBar ↔ Navbar 斷點測試

- **Covered Work Package**：WP-Global-04
- **Test Layer**：component + e2e
- **Test Cases**
  1. `tailwind.config` 的 `md` 與 `MOBILE_BREAKPOINT` 一致（單測）
  2. **MobileTabBar**：SignedIn 5 tab / SignedOut 2 tab
  3. **Navbar**：用戶菜單、usage 指示顯示
  4. E2E：viewport 320 / 640 / 768 / 1024 / 1440 下 TabBar/Navbar 可見性
  5. 斷點臨界（767/768）無雙顯示 / 無 layout shift
- **Fixtures / Mocks Needed**：Playwright 多 viewport config · Clerk SignedIn/SignedOut mock · `useUsageSummary` mock
- **Risks**：Tailwind 斷點與 matchMedia 可能 1px 差異
- **Acceptance Criteria**：5 viewport × 2 auth 矩陣 · 臨界點無閃爍
- **Priority**：P1
- **Effort**：S

---

### TC-Global-05 · LocaleSwitcher 跨頁 E2E

- **Covered Work Package**：WP-Global-05
- **Test Layer**：e2e
- **Test Cases**
  1. `/en/gallery?sort=newest` → 切 zh → `/zh/gallery?sort=newest`
  2. Studio 頁切 locale → Context 狀態保留（或明確是 reset）
  3. 三語輪替切換
  4. Clerk session 不中斷
  5. searchParams 完整保留
- **Fixtures / Mocks Needed**：Playwright · Clerk test session
- **Risks**：Clerk domain/cookie 與 locale 互動
- **Acceptance Criteria**：3 語切換保留 query params · Clerk session 穩定
- **Priority**：P1
- **Effort**：S

---

### TC-Global-06 · 全局錯誤邊界三語測試

- **Covered Work Package**：WP-Global-06
- **Test Layer**：component + e2e
- **Test Cases**
  1. `global-error.tsx`：靜態 JSON 載入三語 fallback
  2. `(main)/error.tsx` / `arena/error.tsx` / `gallery/error.tsx`：`useTranslations` 渲染
  3. 三語 key 統一命名（`Errors.global.*` / `Errors.main.*` 等）
  4. CTA 按鈕（重試 / 返回首頁）可用
  5. E2E：人為觸發錯誤（query param trigger 或錯誤 mock）
- **Fixtures / Mocks Needed**：Playwright 錯誤 trigger 機制 · i18n messages
- **Risks**：`global-error` 無 hook 限制 i18n 能力；trigger 錯誤測試機制需設計
- **Acceptance Criteria**：4 個錯誤層三語齊 · 視覺統一 · ≥ 1 個 E2E smoke
- **Priority**：P2
- **Effort**：M

---

## 工作包索引

| ID            | Covered WP    | Layer          | Priority | Effort |
| ------------- | ------------- | -------------- | -------- | ------ |
| **Studio**    |               |                |          |        |
| TC-Studio-02  | WP-Studio-02  | component+e2e  | P1       | S      |
| TC-Studio-03  | WP-Studio-03  | hook           | P1       | M      |
| TC-Studio-04  | WP-Studio-04  | hook           | P1       | S      |
| TC-Studio-05  | WP-Studio-05  | component      | P1       | M      |
| **Gallery**   |               |                |          |        |
| TC-Gallery-01 | WP-Gallery-01 | e2e            | P1       | S      |
| TC-Gallery-02 | WP-Gallery-02 | component      | **P0**   | M      |
| TC-Gallery-03 | WP-Gallery-03 | component      | P1       | S      |
| TC-Gallery-04 | WP-Gallery-04 | hook+e2e       | P1       | M      |
| TC-Gallery-05 | WP-Gallery-05 | route+e2e      | P1       | M      |
| TC-Gallery-06 | WP-Gallery-06 | hook           | **P0**   | S      |
| **Profile**   |               |                |          |        |
| TC-Profile-01 | WP-Profile-01 | component+e2e  | P2       | S      |
| TC-Profile-02 | WP-Profile-02 | component      | P1       | M      |
| TC-Profile-03 | WP-Profile-03 | component      | P1       | S      |
| TC-Profile-04 | WP-Profile-04 | component+hook | P1       | M      |
| TC-Profile-05 | WP-Profile-05 | route          | P2       | S      |
| **Global**    |               |                |          |        |
| TC-Global-01  | WP-Global-01  | component+e2e  | **P0**   | S      |
| TC-Global-02  | WP-Global-02  | e2e            | P1       | M      |
| TC-Global-03  | WP-Global-03  | service (lib)  | P2       | M      |
| TC-Global-04  | WP-Global-04  | component+e2e  | P1       | S      |
| TC-Global-05  | WP-Global-05  | e2e            | P1       | S      |
| TC-Global-06  | WP-Global-06  | component+e2e  | P2       | M      |

**共 21 個 UI 測試工作包** · P0: 3 · P1: 13 · P2: 5 · S: 11 · M: 10 · L: 0

---

## 非對齊項說明

以下 UI WP 不產生測試工作包：

- **WP-Studio-01**（StudioWorkbenchDraft 去留決策）：✅ DONE (2026-04-25) — 處置方式為下線；route + 組件 + i18n key 全刪，無新測試產出
- **WP-Global-07**（Provider 嵌套文檔化）：純文檔 + 可選 CI grep 守護，無新測試

以下維度暫不出測試 WP（與 `02-現狀映射.md` 暫緩結論一致）：

- **4.5 A11y**：項目無 `@axe-core` / `jest-axe` 工具鏈
- **4.6 視覺回歸**：項目無 Chromatic / Percy / Playwright snapshot 基線

本輪**不發散**到 UI WP 未聲明的測試（例如：不新增 Arena / Storyboard 頁測試 — 這些屬於未被勾選的頁面組，需先完成 01-UI 的 L3 拆分）。
