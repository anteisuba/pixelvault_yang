/**
 * 浏览器端 best-effort 媒体探测的时间预算（毫秒）。
 *
 * ⚠ **这不是保险丝，是真踩到的**（2026-08-30 真机探针回
 * `{"outcome":"TIMEOUT","log":[]}` —— 注意 `log` 是空的，一个事件都没有）：
 * `<video>` / `<audio>` 的 `loadedmetadata` / `seeked` / `error` 在**标签页隐藏时
 * 一个都不派发**。不是慢，是永远不来，连 `error` 都没有。靠这三个事件收尾的探测
 * 于是让 `await` 永久挂住：上传 chip 停在 0%、发送键一直灰着，界面上没有任何东西
 * 说明发生了什么。
 *
 * 探测本身就是 best-effort（探不到时 `width`/`height` 服务端收 0，封面缺席也只是
 * 没封面），所以超时按「没探到」继续往下传，
 * ⛔ 不因为探测失败把一次本来能成功的上传判死。
 *
 * ⭐ **兜底必须落在探测函数内部**：在调用处套一层 `Promise.race` 只能让调用方不再
 * 等，那个挂住的 `<video>` 和它的 object URL 还在——每挂一次漏一份，而且下一个
 * 调用方还得自己记得再套一次。
 */
export const MEDIA_PROBE_TIMEOUT_MS = 15_000
