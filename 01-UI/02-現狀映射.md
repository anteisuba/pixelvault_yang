# 01 · UI — 現狀映射（基於當前代碼庫）

> 基準日期：2026-04-22 · 掃描範圍：`src/app/[locale]/**`, `src/components/**`, `src/hooks/**`

---

## 1.1 公開頁（首頁 / 登入 / 註冊）

- **Code Mapping**
  - Route: `src/app/[locale]/page.tsx`（RSC）, `(auth)/sign-in/[[...sign-in]]/page.tsx`, `(auth)/sign-up/[[...sign-up]]/page.tsx`
  - Component: `HomepageShell / HomepageHero / HomepageModels / HomepageFeatures / HomepageWorkflow` + Clerk `<SignIn> <SignUp>`
  - Test: ❌
- **Current Status**：已存在（首頁完整，Auth 頁完整）
- **2026-04-17 更新**：Auth 頁新增 `generateMetadata`（title + description + `robots: noindex, nofollow`），i18n 三語 Metadata.signIn / signUp 已補齊
- **Current Problem**：~~Clerk 頁面無 metadata~~ ✅ 已修復；首頁組件被拆 5 段但無 SEO 多語差異化內容
- **Next Breakdown**：按 Section（Hero / Models / Features / Workflow / CTA / Footer）
- **Dependencies**：Clerk、next-intl、字體鏈（Space Grotesk + Lora + CJK）
- **Risks**：首頁 bundle 過大；Clerk 頁視覺與主站風格脫節
- **Acceptance Criteria**：首頁 LCP < 2.5s · 三語 metadata · Auth 成功後跳轉正確

---

## 1.2 創作區（Studio）

- **Code Mapping**
  - Route: `(main)/studio/page.tsx`（RSC；ApiKeysProvider 已提升到 `(main)/layout.tsx`）· `(main)/studio/draft/page.tsx`（RSC wrapper + draft UI 組件）
  - Component: `StudioWorkspace`, `StudioWorkbenchDraft`, `CharacterCardManager`, `StyleCardManager`, `HistoryPanel`, `PromptAssistantPanel`, `ReverseEngineerPanel`, `LayerDecomposePanel`
  - Hook: `use-studio-generate`, `use-unified-generate`, `use-studio-shortcuts`, `use-card-manager`, `use-image-editing`, `use-reverse-image`, `use-image-upload`
  - Test: `use-unified-generate.test.ts`、`StudioInputImage.test.tsx`、`StudioVariantsGrid.test.tsx`
- **Current Status**：`/studio` 完整 · `/studio/draft` 半成品
- **2026-04-17 更新**：新增 Transform 面板（StudioTransformPanel + 5 子組件）；IME isComposing 守護已加入 shortcuts + command palette；死代碼 `use-generate.ts` 已刪除（useUnifiedGenerate 完全取代）；`/studio` metadata 加 `robots: noindex`；Video model options 提取為 `use-video-model-options.ts`
- **2026-04-22 校正**：`/studio/draft` 已有 `generateMetadata`，不再是「無 metadata 的純 Client 路由」
- **2026-04-24 更新**：Studio shell 顶部新增 workflow-first 条带（位于 `StudioTopBar` 正下方、`StudioFlowLayout` 上方）；由 `StudioWorkflowGroupTabs` + `StudioWorkflowPicker` + `StudioWorkflowSummary` 三组件组合；Balanced 8 workflow 可点选，触发 `SET_SELECTED_WORKFLOW_ID` 同步 `outputType`
- **2026-04-24 更新（Phase 4 + Phase 5 首刀）**：`StudioTopBar` 原有 image/video/audio toggle + quick/card toggle **已砍**；TopBar 新增"高级"按钮触发 `StudioAdvancedDrawer`（232 行，三段 section：模式选择 / 路由与模型 / provider controls）。`StudioWorkflowGroupTabs` 切 tab 自动选该组首条 workflow（幂等：当前已在目标组则不重设），吸收此前 Summary-tab 不一致。条带容器移除 `mx-auto max-w-6xl`，宽度跟随 Studio 主区域自适应。SET_SELECTED_WORKFLOW_ID 现同步 outputType + workflowMode + 打开 default panel（不关其他已打开 panel）。**未完成**：浏览器手工 smoke（项目规则 dev server 由用户启动）、Phase 6 polish（i18n 校验、mobile 体验、drawer 内 provider controls 真实集成）、workflowId 传 submit（跨层 micro-packet 待吸收）
- **Current Problem**：draft 頁仍缺 `robots: noindex` 與獨立 loading/error 邊界；主頁與 draft 頁的定位和狀態隔離策略仍未明確
- **Next Breakdown**：Left Panel（Cards）· Center Canvas · Right Panel（Params/History）· Toolbar · Modal 集合
- **Dependencies**：`StudioContext`、`ApiKeysProvider`、8 providers、R2
- **Risks**：多 hook 協同重渲染；shortcut 與 input focus 衝突
- **Acceptance Criteria**：三步生成鏈路 + 歷史回溯 + 模型切換 + 快捷鍵全工作

---

## 1.3 作品與展示（Gallery）

- **Code Mapping**
  - Route: `(main)/gallery/page.tsx`（RSC）+ `loading.tsx` + `error.tsx` · `gallery/[id]/page.tsx`（RSC + JSON-LD）
  - Component: `GalleryFeed`, `GalleryFilterBar`, `GalleryGrid`, `GalleryDetailVideoPlayer`, `ImageDetailModal`, `Particles`（背景）
  - Hook: `use-gallery`, `use-like`, `use-follow`
  - Test: `route.test.ts`（images, generations/[id], generations/[id]/visibility）· `ImageCard.test.tsx`(6) · `ImageDetailModal.test.tsx`(8) · `use-like.test.ts`(5)
- **Current Status**：已存在（最完整的頁面組）
- **2026-04-17 更新**：Particles 新增 `prefers-reduced-motion` 守護（靜態粒子降級）；Gallery sentinel double-fetch 競態修復（isFetchingRef 同步）；ImageCard hover shadow 降級 `shadow-lg` → `shadow-md`；GalleryHeader 三 pill toggle + 搜索重構完成；CardDrawer 新增導航入口
- **Current Problem**：~~Particles 背景性能風險~~ ✅ reduced-motion 守護已修復；詳情頁互動（like/comment）整合未驗證
- **Next Breakdown**：FilterBar · Grid · DetailModal · DetailVideoPlayer · Like/Collect 按鈕 · OG 分享
- **Dependencies**：`/api/images`, `/api/generations`, `/api/likes`
- **Risks**：無限滾動 + 背景動效的性能；SSR/CSR 切換閃爍
- **Acceptance Criteria**：分頁+篩選正確 · 詳情 OG 圖生效 · Like/Follow 即時同步

---

## 1.4 競技場（Arena）

- **Code Mapping**
  - Route: `(main)/arena/page.tsx`（RSC+Client 混合，`ArenaPageClient`）· `arena/layout.tsx`（arena metadata）· `arena/history/page.tsx`（Client）· `arena/leaderboard/layout.tsx`（leaderboard metadata）· `arena/leaderboard/page.tsx`（Client）
  - Component: `ArenaForm`, `ArenaHistory`, `ArenaLeaderboard`, `ArenaPersonalStats`
  - Hook: `use-arena`, `use-arena-history`, `use-arena-personal-stats`
  - Test: Arena route 有較完整 route.test.ts（matches, vote, leaderboard, history, personal-stats）
- **Current Status**：主頁半成品 · 子頁骨架
- **2026-04-22 校正**：arena 子頁並非「無 metadata」；`/arena/history` 繼承 `arena/layout.tsx` metadata，`/arena/leaderboard` 由專屬 layout 提供 metadata
- **Current Problem**：history / leaderboard 仍為純 Client 頁，缺獨立 loading 邊界；只有 arena 主段有 `error.tsx`，子頁的錯誤/載入治理與主頁不完全一致
- **Next Breakdown**：MatchView · VotePanel · HistoryTable · LeaderboardTable · PersonalStatsCards
- **Dependencies**：`/api/arena/*`, generation service, ELO 配置（`constants/config.ts`）
- **Risks**：投票並發競爭；leaderboard 大數據量無分頁
- **Acceptance Criteria**：完整 match→vote→history→leaderboard 鏈路 · metadata ownership 清晰 · 子頁 loading/error 策略一致

---

## 1.5 用戶空間（Profile / u/[username]）

- **Code Mapping**
  - Route: `(main)/profile/page.tsx`（RSC + metadata fetch）· `(main)/u/[username]/page.tsx`（RSC + OG metadata）
  - Component: `ProfileFeed`, `ProfileHeader`, `CreatorProfileView`, `PrivateProfileView`
  - Hook: `use-my-profile`, `use-creator-profile`, `use-follow`
  - Test: `ProfileFeed.test.tsx`(5) · `PrivateProfileView.test.tsx`(6) · `users/me/profile/route.test.ts`(8) · `users/me/avatar/route.test.ts`(5) · `users/me/banner/route.test.ts`(5) · `users/[username]/route.test.ts`(5)
- **Current Status**：已存在（完整）
- **2026-04-17 更新**：Profile API route 全覆蓋（me/profile GET+PUT, me/avatar POST, me/banner POST, [username] GET）；ProfileFeed batch 操作測試；PrivateProfileView 渲染測試；`/profile` metadata 加 `robots: noindex`
- **Current Problem**：`/profile` 與 `/u/[username]` 的 feed 組件是否共享未明確；未登入訪問 `/profile` 的重定向未測
- **Next Breakdown**：Header · Stats · Feed · Cards · Follow 按鈕 · 隱私切換
- **Dependencies**：`/api/users`, `/api/follows`, Clerk
- **Risks**：隱私開關在 SSR 下洩露資訊；用戶名變更後 OG 快取失效
- **Acceptance Criteria**：自己/他人/私密三態渲染正確 · OG 圖正常

---

## 1.6 Storyboard

- **Code Mapping**
  - Route: `(main)/storyboard/layout.tsx`（共享 metadata）· `(main)/storyboard/page.tsx`（Client）· `storyboard/[id]/page.tsx`（Client）
  - Component: `StoryCard`, `StoryScrollRenderer`, `StoryComicRenderer`, `StoryImagePicker`, `StoryExportButton`, 內聯 CreateForm
  - Hook: `use-storyboard`, `use-projects`
  - Test: route.test.ts for `stories/route`, `stories/[id]`, `projects/[id]`
- **Current Status**：列表骨架 · 詳情半成品
- **2026-04-22 校正**：Storyboard 已由 `storyboard/layout.tsx` 提供共享 metadata，不再是「列表頁無 metadata」
- **Current Problem**：列表與詳情仍缺獨立 loading/error 邊界；創建表單內聯耦合在 page.tsx；兩種 renderer 切換邏輯未封裝
- **Next Breakdown**：CreateForm · StoryList · ScrollRenderer · ComicRenderer · ImagePicker · ExportButton · Page 重排
- **Dependencies**：`/api/stories`, `/api/projects`, `story.service`, LLM text
- **Risks**：renderer 切換時狀態丟失；長篇 token 超限
- **Acceptance Criteria**：創建→選 renderer→編輯→導出鏈路通 · metadata / loading / error 邊界責任清晰

---

## 1.7 全域 UI

- **Code Mapping**
  - Layout: `app/layout.tsx`（ClerkProvider + 字體）· `app/[locale]/layout.tsx`（NextIntlClientProvider）
  - Component: `components/layout/{Navbar, MobileTabBar, LocaleSwitcher, MainProviders}.tsx` · `components/ui/sonner.tsx`
  - Error: `app/global-error.tsx`, `(main)/error.tsx`, `arena/error.tsx`, `gallery/error.tsx`
  - Test: `LocaleSwitcher.test.tsx`（唯一的 component 測試）
- **Current Status**：已存在
- **2026-04-17 更新**：`bg-white` → `bg-card` 修復 audio-player；shadow-lg → shadow-md（ImageCard/ArenaGrid/PolaroidCard）；Skeleton 組件標準化（CardDropdown/ProjectSelector/HistoryPanel）
- **2026-04-22 校正**：Toast 已有集中 Provider 與 helper：`(main)/layout.tsx` 掛載 `Toaster`，`src/lib/toast.ts` 已存在統一封裝
- **Current Problem**：Modal 基線仍分散；error/loading 邊界覆蓋仍不均；部分頁面尚無 route-level loading.tsx
- **Next Breakdown**：Navbar 下拉 · MobileTab 5 入口 · LanguageSwitcher · Error/Loading/404 · Modal/Dialog baseline 一致化
- **Dependencies**：Clerk、next-intl、shadcn/ui
- **Risks**：Mobile↔Desktop 切換閃爍；ClerkProvider 嵌套順序影響 hydration
- **Acceptance Criteria**：三語切換即時 · 響應式無閃爍 · 全局錯誤邊界多語
