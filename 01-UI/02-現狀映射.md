# 01 · UI — 現狀映射（基於當前代碼庫）

> 基準日期：2026-04-15 · 掃描範圍：`src/app/[locale]/**`, `src/components/**`, `src/hooks/**`

---

## 1.1 公開頁（首頁 / 登入 / 註冊）

- **Code Mapping**
  - Route: `src/app/[locale]/page.tsx`（RSC）, `(auth)/sign-in/[[...sign-in]]/page.tsx`, `(auth)/sign-up/[[...sign-up]]/page.tsx`
  - Component: `HomepageShell / HomepageHero / HomepageModels / HomepageFeatures / HomepageWorkflow` + Clerk `<SignIn> <SignUp>`
  - Test: ❌
- **Current Status**：已存在（首頁完整，Auth 頁完整）
- **Current Problem**：Clerk 頁面無 metadata 生成邏輯；首頁組件被拆 5 段但無 SEO 多語差異化內容
- **Next Breakdown**：按 Section（Hero / Models / Features / Workflow / CTA / Footer）
- **Dependencies**：Clerk、next-intl、字體鏈（Space Grotesk + Lora + CJK）
- **Risks**：首頁 bundle 過大；Clerk 頁視覺與主站風格脫節
- **Acceptance Criteria**：首頁 LCP < 2.5s · 三語 metadata · Auth 成功後跳轉正確

---

## 1.2 創作區（Studio）

- **Code Mapping**
  - Route: `(main)/studio/page.tsx`（RSC + ApiKeysProvider）, `(main)/studio/draft/page.tsx`（Client）
  - Component: `StudioWorkspace`, `StudioWorkbenchDraft`, `CharacterCardManager`, `StyleCardManager`, `HistoryPanel`, `PromptAssistantPanel`, `ReverseEngineerPanel`, `LayerDecomposePanel`
  - Hook: `use-studio-generate`, `use-unified-generate`, `use-studio-shortcuts`, `use-card-manager`, `use-image-editing`, `use-reverse-image`, `use-image-upload`
  - Test: `use-unified-generate.test.ts`（僅此一個 hook 有測試）
- **Current Status**：`/studio` 完整 · `/studio/draft` 半成品
- **Current Problem**：draft 頁純 Client 無 metadata 無 error 邊界；主頁與 draft 頁的狀態同步/隔離策略未明
- **Next Breakdown**：Left Panel（Cards）· Center Canvas · Right Panel（Params/History）· Toolbar · Modal 集合
- **Dependencies**：`StudioContext`、`ApiKeysProvider`、8 providers、R2
- **Risks**：多 hook 協同重渲染；shortcut 與 input focus 衝突
- **Acceptance Criteria**：三步生成鏈路 + 歷史回溯 + 模型切換 + 快捷鍵全工作

---

## 1.3 作品與展示（Gallery）

- **Code Mapping**
  - Route: `(main)/gallery/page.tsx`（RSC）+ `loading.tsx` + `error.tsx` · `gallery/[id]/page.tsx`（RSC + JSON-LD）
  - Component: `GalleryFeed`, `GalleryFilterBar`, `GalleryGrid`, `GalleryDetailVideoPlayer`, `ImageDetailModal`, `Particles`（背景）
  - Hook: `use-gallery`, `use-like`, `use-follow`
  - Test: `route.test.ts`（images, generations/[id], generations/[id]/visibility）
- **Current Status**：已存在（最完整的頁面組）
- **Current Problem**：詳情頁有 JSON-LD 但互動（like/comment）整合未驗證；Particles 背景性能風險
- **Next Breakdown**：FilterBar · Grid · DetailModal · DetailVideoPlayer · Like/Collect 按鈕 · OG 分享
- **Dependencies**：`/api/images`, `/api/generations`, `/api/likes`
- **Risks**：無限滾動 + 背景動效的性能；SSR/CSR 切換閃爍
- **Acceptance Criteria**：分頁+篩選正確 · 詳情 OG 圖生效 · Like/Follow 即時同步

---

## 1.4 競技場（Arena）

- **Code Mapping**
  - Route: `(main)/arena/page.tsx`（RSC+Client 混合，`ArenaPageClient`）· `arena/history/page.tsx`（Client）· `arena/leaderboard/page.tsx`（Client）
  - Component: `ArenaForm`, `ArenaHistory`, `ArenaLeaderboard`, `ArenaPersonalStats`
  - Hook: `use-arena`, `use-arena-history`, `use-arena-personal-stats`
  - Test: Arena route 有較完整 route.test.ts（matches, vote, leaderboard, history, personal-stats）
- **Current Status**：主頁半成品 · 子頁骨架
- **Current Problem**：history / leaderboard 為純 Client、無 metadata、無 error.tsx、與主頁 wrapping 不一致
- **Next Breakdown**：MatchView · VotePanel · HistoryTable · LeaderboardTable · PersonalStatsCards
- **Dependencies**：`/api/arena/*`, generation service, ELO 配置（`constants/config.ts`）
- **Risks**：投票並發競爭；leaderboard 大數據量無分頁
- **Acceptance Criteria**：完整 match→vote→history→leaderboard 鏈路 · 三個子頁都有 metadata+error+loading

---

## 1.5 用戶空間（Profile / u/[username]）

- **Code Mapping**
  - Route: `(main)/profile/page.tsx`（RSC + metadata fetch）· `(main)/u/[username]/page.tsx`（RSC + OG metadata）
  - Component: `ProfileFeed`, `ProfileHeader`, `CreatorProfileView`, `PrivateProfileView`
  - Hook: `use-my-profile`, `use-creator-profile`, `use-follow`
  - Test: ❌
- **Current Status**：已存在（完整）
- **Current Problem**：`/profile` 與 `/u/[username]` 的 feed 組件是否共享未明確；未登入訪問 `/profile` 的重定向未測
- **Next Breakdown**：Header · Stats · Feed · Cards · Follow 按鈕 · 隱私切換
- **Dependencies**：`/api/users`, `/api/follows`, Clerk
- **Risks**：隱私開關在 SSR 下洩露資訊；用戶名變更後 OG 快取失效
- **Acceptance Criteria**：自己/他人/私密三態渲染正確 · OG 圖正常

---

## 1.6 Storyboard

- **Code Mapping**
  - Route: `(main)/storyboard/page.tsx`（Client）· `storyboard/[id]/page.tsx`（Client）
  - Component: `StoryCard`, `StoryScrollRenderer`, `StoryComicRenderer`, `StoryImagePicker`, `StoryExportButton`, 內聯 CreateForm
  - Hook: `use-storyboard`, `use-projects`
  - Test: route.test.ts for `stories/route`, `stories/[id]`, `projects/[id]`
- **Current Status**：列表骨架 · 詳情半成品
- **Current Problem**：列表頁無 metadata/error/loading；創建表單內聯耦合在 page.tsx；兩種 renderer 切換邏輯未封裝
- **Next Breakdown**：CreateForm · StoryList · ScrollRenderer · ComicRenderer · ImagePicker · ExportButton · Page 重排
- **Dependencies**：`/api/stories`, `/api/projects`, `story.service`, LLM text
- **Risks**：renderer 切換時狀態丟失；長篇 token 超限
- **Acceptance Criteria**：創建→選 renderer→編輯→導出鏈路通 · 列表頁三件套齊備

---

## 1.7 全域 UI

- **Code Mapping**
  - Layout: `app/layout.tsx`（ClerkProvider + 字體）· `app/[locale]/layout.tsx`（NextIntlClientProvider）
  - Component: `components/layout/{Navbar, MobileTabBar, LocaleSwitcher}.tsx`
  - Error: `app/global-error.tsx`, `(main)/error.tsx`, `arena/error.tsx`, `gallery/error.tsx`
  - Test: `LocaleSwitcher.test.tsx`（唯一的 component 測試）
- **Current Status**：已存在
- **Current Problem**：Toast/Modal 系統無集中 Provider；error 邊界覆蓋不均；部分頁面無 loading.tsx
- **Next Breakdown**：Navbar 下拉 · MobileTab 5 入口 · LanguageSwitcher · Error/Loading/404 · Toast Provider
- **Dependencies**：Clerk、next-intl、shadcn/ui
- **Risks**：Mobile↔Desktop 切換閃爍；ClerkProvider 嵌套順序影響 hydration
- **Acceptance Criteria**：三語切換即時 · 響應式無閃爍 · 全局錯誤邊界多語
