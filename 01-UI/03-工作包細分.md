# 01 · UI — 工作包細分（L3）

> 基準日期：2026-04-15 · 範圍：1.2 Studio / 1.3 Gallery / 1.5 用戶空間 / 1.7 全域 UI
>
> 全部工作包**嚴格基於當前代碼庫**，只針對已存在或部分存在的模塊。

---

## 1.2 創作區（Studio）

### WP-Studio-01 · StudioWorkbenchDraft 孤立頁的處置

- **Goal**：明確 `/studio/draft` 這 693 行純靜態 UI 的去留（刪除 / 保留為設計稿 / 合併到 Storybook）
- **Current Code Touchpoints**：`src/app/[locale]/(main)/studio/draft/page.tsx`（25 行 RSC）· `src/components/business/studio/StudioWorkbenchDraft.tsx`（693 行，零 hook 零 API）
- **Files to Modify**：`(main)/studio/draft/page.tsx` · `constants/routes.ts`（若下線）· `messages/{en,ja,zh}.json`（移除 draft 相關文案）
- **Files to Add**：若走 Storybook 方案 → `StudioWorkbenchDraft.stories.tsx`；若下線 → 無
- **Concrete Tasks**
  1. grep 確認 draft 路由除 MobileTabBar/Navbar 外的入口
  2. 與 PM/設計確認定位（我方建議：下線 route + 保留組件為內部參考）
  3. 若下線：刪除 route、`ROUTES.STUDIO_DRAFT`（如有）、i18n key、draft 的 metadata
  4. 若保留：加 `robots` noindex + `x-gsc-draft` 標記避免被搜尋引擎抓取
- **Dependencies**：無
- **Risks**：誤刪組件影響將來設計參考；保留會造成路由污染、迷惑新人
- **Acceptance Criteria**：`/studio/draft` 路由狀態明確 · grep 無殘留引用 · i18n 三語同步
- **Priority**：P2
- **Effort**：S
- **Out of Scope**：重構 Studio 主頁佈局

---

### WP-Studio-02 · Studio 主頁 loading / error 補全

- **Goal**：`/studio` 目前無 `loading.tsx` / `error.tsx`，補上頁級邊界
- **Current Code Touchpoints**：`src/app/[locale]/(main)/studio/page.tsx` · `(main)/error.tsx`（通用）· `(main)/gallery/loading.tsx`（參考樣式）
- **Files to Modify**：無
- **Files to Add**：`src/app/[locale]/(main)/studio/loading.tsx` · `src/app/[locale]/(main)/studio/error.tsx`
- **Concrete Tasks**
  1. 複製 gallery/loading.tsx 骨架屏結構，適配 Studio 三欄佈局
  2. 建 studio/error.tsx（Client），含重試按鈕、返回 Gallery 按鈕，讀取 `Studio` i18n namespace
  3. 確認 (main)/error.tsx 不會被搶先捕獲
  4. 三語文案 key 新增：`Studio.loading.title`、`Studio.error.title`、`Studio.error.retry`
- **Dependencies**：無
- **Risks**：loading 骨架與實際佈局差異大會有視覺跳變
- **Acceptance Criteria**：ApiKeysProvider 失敗時 error.tsx 正確觸發 · 首屏骨架 ≤ 100ms 可見 · 三語完整
- **Priority**：P1
- **Effort**：S
- **Out of Scope**：設計全新 Studio 骨架視覺

---

### WP-Studio-03 · 生成 Hook 職責統一（useUnifiedGenerate vs useStudioGenerate）

- **Goal**：釐清 `useUnifiedGenerate`（主協調器）與 `useStudioGenerate`（簡化包裝）職責邊界，消除重複
- **Current Code Touchpoints**：`src/hooks/use-unified-generate.ts`（已有測試）· `src/hooks/use-studio-generate.ts` · `src/contexts/studio-context.tsx`（GenContext）· 所有 StudioCanvas/StudioPromptArea 消費者
- **Files to Modify**：`use-studio-generate.ts`、`StudioWorkspace.tsx` 及其消費組件 · `use-unified-generate.test.ts` 擴充
- **Files to Add**：`src/hooks/use-studio-generate.test.ts`
- **Concrete Tasks**
  1. 在 use-studio-generate 頂部用 JSDoc 寫明「本 hook 只是 useUnifiedGenerate 的 view-model，不含副作用」
  2. grep 所有消費者，改為直接調用 useUnifiedGenerate 或明確聲明 view-model 邊界
  3. 補 use-studio-generate.test.ts 的 `isGenerating` / `lastGeneration` 測試
  4. 若確認冗餘則合併至 unified（需跨組件改動 ≥ 5 處）
- **Dependencies**：StudioContext 3-provider 不改動
- **Risks**：Studio 核心流程回歸風險；測試覆蓋不足時難以安全合併
- **Acceptance Criteria**：單一真相來源 · 既有 E2E mobile.spec.ts 不回歸 · 測試覆蓋兩 hook 正常+錯誤路徑
- **Priority**：P1
- **Effort**：M
- **Out of Scope**：修改 useUnifiedGenerate 的 B4/B5 分支邏輯

---

### WP-Studio-04 · 快捷鍵與 Input Focus 衝突驗證

- **Goal**：`useStudioShortcuts` 綁定 `Cmd+Enter` / `Cmd+Shift+Enter`，需驗證在 prompt textarea / card search input 等焦點下行為正確
- **Current Code Touchpoints**：`src/hooks/use-studio-shortcuts.ts` · `src/components/business/studio/StudioPromptArea.tsx` · 所有 Studio 內 input/textarea
- **Files to Modify**：`use-studio-shortcuts.ts`（可能需要白名單/黑名單 target 判斷）
- **Files to Add**：`src/hooks/use-studio-shortcuts.test.ts`
- **Concrete Tasks**
  1. 列出當前 shortcut 觸發條件（是否檢查 `event.target` 類型）
  2. 寫 testing-library 測試：focus textarea → 按 Cmd+Enter → 應觸發生成；focus Input（搜尋卡片）→ 按 Cmd+Enter → 預期行為待定義
  3. 若缺白名單邏輯則補
  4. 驗證 IME 輸入中（zh/ja 組字）不誤觸
- **Dependencies**：use-unified-generate 的 generate 函數穩定
- **Risks**：IME composing state 在 jsdom 下難以完整模擬
- **Acceptance Criteria**：prompt 輸入時快捷鍵正確觸發 · IME 組字中無誤觸 · 有對應 hook 測試
- **Priority**：P1
- **Effort**：S
- **Out of Scope**：新增額外快捷鍵（如歷史切換）

---

### WP-Studio-05 · Studio 核心組件測試骨架建立

- **Goal**：60+ 組件僅 1 個有測試，建立 Studio 區核心組件測試模板 + 覆蓋 3 個最關鍵組件
- **Current Code Touchpoints**：`StudioWorkspace`, `StudioSidebar`, `StudioCanvas`, `StudioPromptArea`, `StudioProjectHistory`, `HistoryPanel` · `src/test/api-helpers.ts`（mock 工具）· `LocaleSwitcher.test.tsx`（唯一現有組件測試範本）
- **Files to Modify**：`src/test/api-helpers.ts`（若需新增 StudioContext mock helper）
- **Files to Add**：
  - `src/test/studio-context-helpers.tsx`（Form/Data/Gen Provider 的 mock wrapper）
  - `src/components/business/studio/StudioPromptArea.test.tsx`
  - `src/components/business/studio/StudioCanvas.test.tsx`
  - `src/components/business/HistoryPanel.test.tsx`
- **Concrete Tasks**
  1. 建立 StudioContext mock wrapper（3 個 provider 可注入初始值）
  2. StudioPromptArea：渲染、輸入、aspectRatio 切換
  3. StudioCanvas：空態、比較模式、變體模式三種渲染
  4. HistoryPanel：篩選 tab 切換（all/image/video/audio）
- **Dependencies**：WP-Studio-03 職責釐清後可降低 mock 複雜度
- **Risks**：StudioContext 3-provider mock 侵入性高；Pragmatic Drag-and-Drop 在 jsdom 下不可用
- **Acceptance Criteria**：3 個組件測試 green · mock wrapper 可被後續測試複用 · CI 運行時長增加 ≤ 5s
- **Priority**：P1
- **Effort**：M
- **Out of Scope**：拖拽功能的交互測試（移交 E2E）

---

## 1.3 作品與展示（Gallery）

### WP-Gallery-01 · Particles 背景性能防護

- **Goal**：`/gallery` 頁使用 120 粒子 Canvas + mousemove 監聽，低端設備存在性能風險，需加入條件降級
- **Current Code Touchpoints**：`src/components/ui/particles.tsx` · `src/app/[locale]/(main)/gallery/page.tsx`（z-0 fixed）
- **Files to Modify**：`particles.tsx`（新增 `respectReducedMotion` / `disableOnLowEnd` props）· `gallery/page.tsx`（傳入 props）
- **Files to Add**：無
- **Concrete Tasks**
  1. 讀取 `prefers-reduced-motion` media query，若命中則不掛 canvas
  2. 基於 `navigator.hardwareConcurrency < 4` 降粒子數到 60
  3. passive mousemove listener；unmount 時清理 rAF
  4. Playwright mobile spec 補 CPU throttling 驗證
- **Dependencies**：無
- **Risks**：視覺退化需設計確認；prefers-reduced-motion 會同時影響其他動效
- **Acceptance Criteria**：低端設備 CPU 佔用 < 30% · 有 `prefers-reduced-motion` 時 canvas 不掛載 · 無內存洩漏
- **Priority**：P1
- **Effort**：S
- **Out of Scope**：重寫整個粒子系統

---

### WP-Gallery-02 · ImageDetailModal 測試補齊

- **Goal**：ImageDetailModal 是 Gallery 列表頁主要互動點（點卡→開 modal→like/download/visibility），目前無測試
- **Current Code Touchpoints**：`src/components/business/ImageDetailModal.tsx` · `src/components/business/image-card/ImageCard.tsx`（消費者）· `use-like`, `use-gallery`
- **Files to Modify**：無
- **Files to Add**：`src/components/business/ImageDetailModal.test.tsx` · `src/components/business/image-card/ImageCard.test.tsx`
- **Concrete Tasks**
  1. ImageDetailModal：打開/關閉、Like 按鈕樂觀更新、close on ESC
  2. ImageCard：點擊媒體打開 modal、ImageCardActions 右上懸停、visibility 僅在 `showVisibility=true` 顯示
  3. Mock use-like 的 toggleLike（樂觀→失敗→回滾）
  4. 驗證 visibility 切換僅在自有作品出現
- **Dependencies**：api-helpers 已有 Clerk mock
- **Risks**：Radix Dialog focus trap 在 jsdom 下需 `@testing-library/user-event` 而非 fireEvent
- **Acceptance Criteria**：modal 打開關閉正常 · like 樂觀 + 失敗回滾路徑覆蓋 · visibility 權限正確
- **Priority**：P0
- **Effort**：M
- **Out of Scope**：下載行為的文件系統驗證（屬 E2E）

---

### WP-Gallery-03 · 篩選 URL 狀態與空態完整性

- **Goal**：篩選條件通過 URL 查詢參數同步（Zod `GallerySearchSchema`），需覆蓋空結果、無效參數、空 model 列表三種邊界
- **Current Code Touchpoints**：`(main)/gallery/page.tsx`（`GallerySearchSchema.safeParse`）· `GalleryFilterBar.tsx` · `GalleryFeed.tsx` · `constants/pagination.ts`（`GALLERY_SORT_OPTIONS`）
- **Files to Modify**：`GalleryFilterBar.tsx`（如缺空態清除按鈕則補）· `GalleryFeed.tsx`（空態文案）
- **Files to Add**：`src/components/business/GalleryFilterBar.test.tsx` · `src/components/business/GalleryFeed.test.tsx`（空態路徑）
- **Concrete Tasks**
  1. Filter：三語空態文案 key（`Gallery.empty.noResults` / `Gallery.empty.withFilters`）
  2. Clear Filters 按鈕出現時機（有篩選且結果為 0 時）
  3. 測試：?model=nonexistent → safe fallback
  4. 測試：liked=true 但未登入 → 顯示登入提示
- **Dependencies**：i18n messages 文件
- **Risks**：buildGalleryQueryString 的鍵順序變動可能影響快取
- **Acceptance Criteria**：空態 UI 三語 · 無效參數回退到預設 · filter 變更後 URL 同步
- **Priority**：P1
- **Effort**：S
- **Out of Scope**：新增篩選維度（例如尺寸、provider）

---

### WP-Gallery-04 · 無限滾動 Sentinel 穩定性

- **Goal**：GalleryGrid 雙層無限滾動（頁級 + 批次 20），驗證 IntersectionObserver 在快速滑動、快速篩選切換下無重複請求
- **Current Code Touchpoints**：`src/hooks/use-gallery.ts`（sentinelRef + loadMore）· `GalleryGrid.tsx`（批次 20）· `/api/images` route
- **Files to Modify**：`use-gallery.ts`（若發現競態，加入 AbortController 或請求 id 守護）
- **Files to Add**：`src/hooks/use-gallery.test.ts`
- **Concrete Tasks**
  1. 測試 loadMore 在 isLoading 時不重複觸發
  2. 篩選切換時取消進行中的請求 / 丟棄過期結果
  3. Playwright：快速滾動到底部不觸發多次 fetch
  4. 驗證 page=1 與 loadMore 的結果合併順序正確
- **Dependencies**：`/api/images/route.test.ts` 已存在（可串接）
- **Risks**：jsdom 下 IntersectionObserver 需 polyfill 或手動 mock
- **Acceptance Criteria**：快速滾動 + 篩選切換 20 次無重複或錯序結果 · 取消請求正常
- **Priority**：P1
- **Effort**：M
- **Out of Scope**：改為游標分頁（server-side 改動）

---

### WP-Gallery-05 · 詳情頁 JSON-LD 與 OG 鏈路守護

- **Goal**：`/gallery/[id]` 頁動態生成 JSON-LD（VideoObject / ImageObject）+ OG 圖（`/api/og?type=generation&id={id}`），需加入回歸測試
- **Current Code Touchpoints**：`gallery/[id]/page.tsx`（L108-131 JSON-LD）· `src/app/api/og/route.tsx`（Satori）· `generateMetadata()`
- **Files to Modify**：無（若發現 ld 缺欄位則補）
- **Files to Add**：`src/app/[locale]/(main)/gallery/[id]/page.test.ts`（metadata 單測）· `src/app/api/og/route.test.ts`
- **Concrete Tasks**
  1. 對 image / video 兩型 generation 分別驗證 JSON-LD 結構完整性
  2. OG route：測試 generation 不存在（404）、video 類型（需 poster 圖）
  3. generateMetadata 測試：缺 appUrl 環境變數時 fallback
  4. E2E：Playwright 抓取 `<script type="application/ld+json">` 斷言內容
- **Dependencies**：無
- **Risks**：Satori 在 test 環境初始化時間長（> 1s）
- **Acceptance Criteria**：JSON-LD 對 image/video 均符合 schema.org · OG route 200 正常，缺 id 時 404
- **Priority**：P1
- **Effort**：M
- **Out of Scope**：OG 圖視覺基線（屬 4.6 視覺回歸）

---

### WP-Gallery-06 · Like 樂觀更新競態保護

- **Goal**：`use-like` 樂觀更新 + `/api/likes`，需驗證快速連按 / 多卡片同時操作的一致性
- **Current Code Touchpoints**：`src/hooks/use-like.ts` · `image-card/ImageCardActions.tsx` · `/api/likes/route.ts`
- **Files to Modify**：`use-like.ts`（若缺 debounce / inflight 守護則加）
- **Files to Add**：`src/hooks/use-like.test.ts`
- **Concrete Tasks**
  1. 測試 toggle 連按 5 次最終狀態與服務器一致
  2. API 失敗時 UI 回滾到 pre-optimistic state
  3. 多個 ImageCard 並發 toggle 不交叉污染（hook 實例隔離）
  4. 未登入狀態觸發 like → 應跳轉或顯示登入提示
- **Dependencies**：`/api/likes` route 有測試則需對齊
- **Risks**：樂觀 + 失敗回滾的測試 flakiness
- **Acceptance Criteria**：連按 5 次收斂一致 · 未登入路徑有明確提示 · 失敗回滾視覺穩定
- **Priority**：P0
- **Effort**：S
- **Out of Scope**：like 計數的實時訂閱機制

---

## 1.5 用戶空間（Profile）

### WP-Profile-01 · 未登入訪問 /profile 的頁內提示優化

- **Goal**：`/profile` 未登入時返回 syncPendingTitle 頁內提示而非 HTTP redirect，目前此分支無測試覆蓋
- **Current Code Touchpoints**：`src/app/[locale]/(main)/profile/page.tsx`（L74-94 `!clerkId` 分支）· i18n key `Profile.syncPendingTitle`
- **Files to Modify**：`profile/page.tsx`（若 CTA 按鈕文案/跳轉目標需調整則改）
- **Files to Add**：`src/app/[locale]/(main)/profile/page.test.ts`（RSC async 單測）
- **Concrete Tasks**
  1. 確認 CTA 按鈕跳轉目標（目前是 `/studio`，考慮 `/sign-in`）
  2. 三語 key 檢查：syncPendingTitle / syncPendingDescription
  3. RSC 單測：mock Clerk auth 返回 null → 渲染頁內提示
  4. Playwright：未登入訪問 `/en/profile` → 斷言頁內提示文字
- **Dependencies**：Clerk auth mock（api-helpers 已有）
- **Risks**：若改成 HTTP redirect 會影響 middleware 白名單策略
- **Acceptance Criteria**：三種 locale 頁內提示正確 · CTA 跳轉目標已確認 · 單測覆蓋
- **Priority**：P2
- **Effort**：S
- **Out of Scope**：middleware 層重寫

---

### WP-Profile-02 · ProfileFeed 批量操作測試

- **Goal**：ProfileFeed 支援選擇模式 + 批量刪除 + 批量隱私切換，目前無測試
- **Current Code Touchpoints**：`src/components/business/ProfileFeed.tsx`（選擇模式、批量操作）· `use-gallery`（`mine: true`）· `/api/generations/[id]`（DELETE）· `/api/generations/[id]/visibility`（PATCH）
- **Files to Modify**：`ProfileFeed.tsx`（若批量 API 循環呼叫需改為 POST batch 端點則提案）
- **Files to Add**：`src/components/business/ProfileFeed.test.tsx`
- **Concrete Tasks**
  1. 測試：進入選擇模式、全選、反選、部分選取計數正確
  2. 批量刪除：確認 dialog → 請求循環觸發、失敗項回滾
  3. 批量隱私切換：public ↔ private 狀態轉換
  4. 空選取時批量按鈕 disabled
- **Dependencies**：generations/[id] route test 已存在
- **Risks**：批量循環調用的 rate-limit 觸發；中途失敗的 UX 未定義
- **Acceptance Criteria**：批量操作有 confirm + 失敗可見 · 空選取 disabled · 測試覆蓋
- **Priority**：P1
- **Effort**：M
- **Out of Scope**：新增批量 API 端點（屬功能線 2.2）

---

### WP-Profile-03 · Creator/Private View 切換邏輯測試

- **Goal**：`/u/[username]` 根據 `getCreatorProfile()` 返回的 `profile.private` 切換 Creator vs Private 視圖，此分支邏輯無測試
- **Current Code Touchpoints**：`src/app/[locale]/(main)/u/[username]/page.tsx`（L77 切換）· `CreatorProfileView.tsx` · `PrivateProfileView.tsx` · `services/user.service.ts`（getCreatorProfile）
- **Files to Modify**：無
- **Files to Add**：`src/app/[locale]/(main)/u/[username]/page.test.ts`（RSC 單測）· `src/components/business/PrivateProfileView.test.tsx`
- **Concrete Tasks**
  1. mock getCreatorProfile 返回 `{ private: true }` → 渲染 PrivateProfileView
  2. mock 返回正常 profile → 渲染 CreatorProfileView
  3. 不存在用戶 → 404
  4. PrivateProfileView 組件測試：文案三語、Follow 按鈕是否顯示策略
- **Dependencies**：user.service mock
- **Risks**：切換邏輯在 service 層，若服務 API 變動需同步更新
- **Acceptance Criteria**：3 個分支 RSC 單測 · PrivateProfileView 組件測試 · 三語顯示
- **Priority**：P1
- **Effort**：S
- **Out of Scope**：改 user.service 判斷規則

---

### WP-Profile-04 · ProfileEditModal 隱私開關完整性

- **Goal**：隱私開關 `isPublic` 在 ProfileEditModal 切換，需驗證 UI ↔ API ↔ DB 三者一致
- **Current Code Touchpoints**：`src/components/business/ProfileEditModal.tsx`（L280）· `use-my-profile` · `/api/users/me/profile`（PUT）· `user.service.updateProfile`
- **Files to Modify**：無
- **Files to Add**：`src/components/business/ProfileEditModal.test.tsx` · `src/hooks/use-my-profile.test.ts`
- **Concrete Tasks**
  1. 組件測試：toggle → onChange → 提交 → 樂觀更新 `useMyProfile` 快取
  2. API 失敗時回滾 + 錯誤 toast
  3. 變更後訪問 `/u/[myUsername]` 應反映（E2E 可選）
  4. 三語隱私說明文案檢查
- **Dependencies**：`/api/users/me/profile` route test（目前缺失）
- **Risks**：樂觀更新 + SWR/React Query 快取策略可能有錯配
- **Acceptance Criteria**：toggle 與 API 結果一致 · 失敗回滾視覺正確 · 有 hook + 組件測試
- **Priority**：P1
- **Effort**：M
- **Out of Scope**：設計新的設定頁面（項目無 settings 頁，inline modal 是既定方案）

---

### WP-Profile-05 · /u/[username] OG 圖快取失效驗證

- **Goal**：用戶改名或更新頭像後，`/api/og?type=profile&username=X` 必須失效舊快取
- **Current Code Touchpoints**：`src/app/[locale]/(main)/u/[username]/page.tsx`（L37 ogImageUrl）· `src/app/api/og/route.tsx`
- **Files to Modify**：`og/route.tsx`（可能需加 `Cache-Control: max-age=60` 或基於 `updatedAt` 加版本查詢參數）
- **Files to Add**：`og/route.test.ts`（與 WP-Gallery-05 共用）· 無
- **Concrete Tasks**
  1. 查當前 OG route 的 Cache-Control header
  2. 在 ogImageUrl 加 `&v={user.updatedAt}` 作為 cache buster
  3. 測試：user.updatedAt 變化後 OG URL 變化
  4. 社交平台分享快取不在控制範圍，需文檔化
- **Dependencies**：user.service.updatedAt 字段（需確認存在）
- **Risks**：cache buster 可能導致 Satori 過度生成；Twitter/Facebook 快取獨立不受影響
- **Acceptance Criteria**：改名後 ogImageUrl 變化 · route 有 Cache-Control · 文檔化外部平台快取行為
- **Priority**：P2
- **Effort**：S
- **Out of Scope**：設計 OG 圖的視覺樣式

---

## 1.7 全域 UI

### WP-Global-01 · 補 `not-found.tsx`（404 頁面缺失）

- **Goal**：項目目前**完全無 404 頁**（`app/not-found.tsx` 和 `app/[locale]/not-found.tsx` 均不存在），需補三語 404
- **Current Code Touchpoints**：`src/app/layout.tsx` · `src/app/[locale]/layout.tsx` · `i18n/routing.ts`（`localePrefix: 'always'`）· `middleware.ts`
- **Files to Modify**：無
- **Files to Add**：
  - `src/app/[locale]/not-found.tsx`（三語 Client）
  - `src/app/not-found.tsx`（根級，無 locale 時的 fallback，純英文）
- **Concrete Tasks**
  1. 建 `[locale]/not-found.tsx` 使用 `useTranslations('NotFound')`
  2. 設計 CTA：返回首頁、返回 Gallery
  3. 三語 key 新增：`NotFound.title` / `.description` / `.cta.home` / `.cta.gallery`
  4. 根級 `not-found.tsx` 作為 locale 無法解析時的 fallback
  5. 手動訪問 `/zh/__nonexistent__` 驗證
- **Dependencies**：無
- **Risks**：middleware localePrefix='always' 可能導致無 locale 路徑進入根 not-found
- **Acceptance Criteria**：三語 404 頁渲染正常 · CTA 可用 · 根級 fallback 不破版
- **Priority**：P0
- **Effort**：S
- **Out of Scope**：自定義錯誤報告

---

### WP-Global-02 · 關鍵頁面 loading.tsx 補齊

- **Goal**：目前僅 `gallery/loading.tsx` 存在，其他 RSC 頁（studio / profile / u / arena / storyboard）均無 loading 邊界
- **Current Code Touchpoints**：`(main)/gallery/loading.tsx`（唯一樣板）· 各 page.tsx 的 RSC 入口
- **Files to Modify**：無
- **Files to Add**：
  - `src/app/[locale]/(main)/studio/loading.tsx`（與 WP-Studio-02 合並）
  - `src/app/[locale]/(main)/profile/loading.tsx`
  - `src/app/[locale]/(main)/u/[username]/loading.tsx`
  - `src/app/[locale]/(main)/arena/loading.tsx`
  - `src/app/[locale]/(main)/storyboard/loading.tsx`
- **Concrete Tasks**
  1. 抽象 `<PageSkeleton variant="list|detail|dashboard">` 元件（src/components/ui/page-skeleton.tsx）
  2. 每個 loading.tsx 選擇對應 variant
  3. 確認骨架佈局與實際頁面 h1 位置一致（減少 CLS）
- **Dependencies**：WP-Studio-02 會先產出 studio/loading.tsx
- **Risks**：骨架 vs 實際 UI 不匹配造成 layout shift
- **Acceptance Criteria**：6 個 loading.tsx 存在 · CLS < 0.1 · 通用 PageSkeleton 可複用
- **Priority**：P1
- **Effort**：M
- **Out of Scope**：頁面級 streaming SSR 改造

---

### WP-Global-03 · Toast 使用規範與一致性

- **Goal**：Sonner Toaster 掛在 `(main)/layout.tsx`，但項目內 `toast.*` 調用散落，需確立使用規範
- **Current Code Touchpoints**：`(main)/layout.tsx`（L23 Toaster）· `src/components/ui/sonner.tsx`（wrapper）· 所有 hook/組件內的 `toast.*` 調用
- **Files to Modify**：`src/lib/toast.ts`（若不存在則新增統一 helper）· 所有直接 import sonner 的檔案
- **Files to Add**：`src/lib/toast.ts`（統一 API：`toastSuccess / toastError / toastLoading / toastPromise`，自帶 i18n）
- **Concrete Tasks**
  1. grep 所有 `import.*sonner` 使用點
  2. 統一封裝 helper，強制傳 i18n key 或已翻譯字串
  3. 批次替換呼叫點
  4. Unit test helper 本身
- **Dependencies**：i18n messages
- **Risks**：散落 toast 替換時漏改導致不一致；i18n key 預先不存在
- **Acceptance Criteria**：無直接 `import "sonner"` 在業務代碼 · helper 有測試 · 所有 toast 三語
- **Priority**：P2
- **Effort**：M
- **Out of Scope**：Toast 視覺設計改動

---

### WP-Global-04 · MobileTabBar ↔ Navbar 斷點測試

- **Goal**：MobileTabBar 用 `md:hidden`（Tailwind 768px）+ `useIsMobile` hook（768px），驗證斷點兩側無雙顯示或隱藏
- **Current Code Touchpoints**：`src/components/layout/MobileTabBar.tsx`（L72 `md:hidden`）· `Navbar.tsx` · `src/hooks/use-mobile.ts`（MOBILE_BREAKPOINT=768）· `tailwind.config.ts`
- **Files to Modify**：若 hook 與 Tailwind 斷點不一致則統一
- **Files to Add**：`src/components/layout/MobileTabBar.test.tsx` · `src/components/layout/Navbar.test.tsx`
- **Concrete Tasks**
  1. 確認 `tailwind.config` 的 `md` 斷點值與 `MOBILE_BREAKPOINT` 一致
  2. Playwright 多 viewport 測試：320 / 640 / 768 / 1024 / 1440 下的 TabBar / Navbar 可見性
  3. 斷點臨界點（767/768）無閃爍
  4. SignedIn / SignedOut 兩種狀態 tab 數量正確（5 vs 2）
- **Dependencies**：Playwright（已配置）
- **Risks**：Tailwind 斷點與 matchMedia 可能有 1px 差異
- **Acceptance Criteria**：5 個 viewport × 2 auth 狀態矩陣測試 green · 斷點無雙顯示
- **Priority**：P1
- **Effort**：S
- **Out of Scope**：響應式佈局重構

---

### WP-Global-05 · LocaleSwitcher 跨頁 E2E Smoke

- **Goal**：LocaleSwitcher 用 i18n Link（`@/i18n/navigation`）切換語言，需驗證保留 pathname + searchParams
- **Current Code Touchpoints**：`src/components/layout/LocaleSwitcher.tsx` · `LocaleSwitcher.test.tsx`（已有組件單測） · `i18n/navigation.ts` · `i18n/routing.ts`
- **Files to Modify**：無
- **Files to Add**：`e2e/locale-switch.spec.ts`
- **Concrete Tasks**
  1. E2E：訪問 `/en/gallery?sort=newest` → 切 zh → URL 變 `/zh/gallery?sort=newest`
  2. 在 Studio 頁切 locale 後 StudioContext 狀態不丟失（或確認是預期行為）
  3. 切 locale 後 Clerk session 不中斷
  4. 三種 locale 切換輪替一遍
- **Dependencies**：Playwright
- **Risks**：Clerk 域名/cookie 與 locale 互動
- **Acceptance Criteria**：所有三語切換保留 query params · Clerk session 穩定
- **Priority**：P1
- **Effort**：S
- **Out of Scope**：localePrefix 策略變更

---

### WP-Global-06 · 全局錯誤邊界三語覆蓋

- **Goal**：global-error / (main)/error / gallery/error / arena/error 需三語 + 一致視覺
- **Current Code Touchpoints**：`src/app/global-error.tsx`（無 hook，直接 import JSON）· `(main)/error.tsx` · `arena/error.tsx` · `gallery/error.tsx`
- **Files to Modify**：4 個 error.tsx（若 i18n key 缺失則補）
- **Files to Add**：`src/app/global-error.test.tsx`
- **Concrete Tasks**
  1. 確認 `global-error.tsx` 使用靜態 JSON 載入三語（目前是 L7-9）
  2. 三語 key 統一命名：`Errors.global.title` / `Errors.main.title` / 等
  3. Playwright：人為拋錯觸發各層 error.tsx（目前難，可用 query param trigger）
  4. 視覺一致：同一 container 類 + CTA 按鈕組
- **Dependencies**：i18n messages
- **Risks**：global-error 無 hook 限制 i18n 能力；trigger 錯誤的測試機制需設計
- **Acceptance Criteria**：4 個錯誤層三語 key 齊 · 視覺統一 · 最少 1 個 E2E smoke
- **Priority**：P2
- **Effort**：M
- **Out of Scope**：設計新的錯誤視覺系統

---

### WP-Global-07 · Provider 巢狀順序文檔化

- **Goal**：ClerkProvider → NextIntlClientProvider → (main) Layout 的嵌套順序是專案核心約定，需寫入 CLAUDE.md 防止未來誤改
- **Current Code Touchpoints**：`src/app/layout.tsx`（Clerk + 字體 6 變數）· `src/app/[locale]/layout.tsx`（NextIntl）· `(main)/layout.tsx`（Navbar/Toaster）· `CLAUDE.md`
- **Files to Modify**：`CLAUDE.md`（新增「Provider 嵌套順序」章節）
- **Files to Add**：無（或在 `docs/architecture/system-architecture.md` 加 provider 圖）
- **Concrete Tasks**
  1. 畫出三層 provider 嵌套圖（文字版 + mermaid）
  2. 寫出每層職責、為什麼不能換層
  3. 6 個字體變數注入位置的解釋
  4. 加一個 CI 腳本：grep layout 文件中 ClerkProvider / NextIntlClientProvider 的層級，若有越界提示
- **Dependencies**：無
- **Risks**：文檔容易過期
- **Acceptance Criteria**：CLAUDE.md 增章 · 嵌套圖清晰 · 可選 CI guard
- **Priority**：P2
- **Effort**：S
- **Out of Scope**：重構 provider 結構

---

## 工作包索引

| ID            | 標題                            | Priority | Effort |
| ------------- | ------------------------------- | -------- | ------ |
| **Studio**    |                                 |          |        |
| WP-Studio-01  | StudioWorkbenchDraft 孤立頁處置 | P2       | S      |
| WP-Studio-02  | Studio 主頁 loading/error 補全  | P1       | S      |
| WP-Studio-03  | 生成 Hook 職責統一              | P1       | M      |
| WP-Studio-04  | 快捷鍵與 Input Focus 衝突驗證   | P1       | S      |
| WP-Studio-05  | Studio 核心組件測試骨架         | P1       | M      |
| **Gallery**   |                                 |          |        |
| WP-Gallery-01 | Particles 背景性能防護          | P1       | S      |
| WP-Gallery-02 | ImageDetailModal 測試補齊       | P0       | M      |
| WP-Gallery-03 | 篩選 URL 狀態與空態完整性       | P1       | S      |
| WP-Gallery-04 | 無限滾動 Sentinel 穩定性        | P1       | M      |
| WP-Gallery-05 | 詳情頁 JSON-LD 與 OG 鏈路守護   | P1       | M      |
| WP-Gallery-06 | Like 樂觀更新競態保護           | P0       | S      |
| **Profile**   |                                 |          |        |
| WP-Profile-01 | 未登入頁內提示優化              | P2       | S      |
| WP-Profile-02 | ProfileFeed 批量操作測試        | P1       | M      |
| WP-Profile-03 | Creator/Private View 切換測試   | P1       | S      |
| WP-Profile-04 | ProfileEditModal 隱私開關完整性 | P1       | M      |
| WP-Profile-05 | OG 圖快取失效驗證               | P2       | S      |
| **Global**    |                                 |          |        |
| WP-Global-01  | 補 not-found.tsx                | **P0**   | S      |
| WP-Global-02  | 關鍵頁面 loading.tsx 補齊       | P1       | M      |
| WP-Global-03  | Toast 使用規範與一致性          | P2       | M      |
| WP-Global-04  | MobileTabBar↔Navbar 斷點測試    | P1       | S      |
| WP-Global-05  | LocaleSwitcher 跨頁 E2E         | P1       | S      |
| WP-Global-06  | 全局錯誤邊界三語覆蓋            | P2       | M      |
| WP-Global-07  | Provider 巢狀順序文檔化         | P2       | S      |

**共 23 個工作包** · P0: 3 · P1: 13 · P2: 7 · Effort S: 13 · M: 10 · L: 0
