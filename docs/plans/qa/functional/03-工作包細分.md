# 03 · 功能測試 — 工作包細分（L3）

> 基準日期：2026-04-15
>
> 本文件嚴格對齊 `docs/plans/feature/03-工作包細分.md` 的 24 個功能工作包。**不發散新測試範圍**，只把功能 WP 內原本就聲明的測試任務抽出成獨立測試 WP。
>
> 文檔化/決策類功能 WP（WP-Infra-02、WP-Infra-04）不產測試工作包。
>
> 狀態說明：本文件是**測試工作包待辦**，不是當前已存在測試清單；判斷現況請先讀 `02-現狀映射.md`。

---

## 映射索引（功能 WP → 測試 WP）

| 功能 WP                | 測試 WP                | Test Layer      |
| ---------------------- | ---------------------- | --------------- |
| WP-Image-01            | TC-Image-01            | service         |
| WP-Image-02            | TC-Image-02            | service         |
| WP-Image-03            | TC-Image-03            | service         |
| WP-Image-04            | TC-Image-04            | service + route |
| WP-Video-01            | TC-Video-01            | service         |
| WP-Video-02            | TC-Video-02            | route + e2e     |
| WP-Video-03            | TC-Video-03            | service         |
| WP-Prompt-01           | TC-Prompt-01           | service         |
| WP-Prompt-02           | TC-Prompt-02           | service         |
| WP-Prompt-03           | TC-Prompt-03           | service         |
| WP-Prompt-04           | TC-Prompt-04           | service (lib)   |
| WP-Usage-01            | TC-Usage-01            | service         |
| WP-Usage-02            | TC-Usage-02            | service         |
| WP-Usage-03            | TC-Usage-03            | service + route |
| WP-Storage-01          | TC-Storage-01          | service         |
| WP-Storage-02          | TC-Storage-02          | route           |
| WP-Storage-03          | TC-Storage-03          | route           |
| WP-Infra-01            | TC-Infra-01            | service (lib)   |
| WP-Infra-02            | _—（文檔化）_          | —               |
| WP-Infra-03            | TC-Infra-03            | service (lib)   |
| WP-Infra-04            | _—（決策化）_          | —               |
| WP-StyleConsistency-01 | TC-StyleConsistency-01 | service         |
| WP-StyleConsistency-02 | TC-StyleConsistency-02 | service         |
| WP-StyleConsistency-03 | TC-StyleConsistency-03 | service         |

**共 22 個功能測試工作包**

---

## 2.2 圖像生成

### TC-Image-01 · generate-image 持久化路徑補測

- **Covered Work Package**：WP-Image-01
- **Test Layer**：service
- **Test Cases**
  1. `generateImageForUser` 成功 → `generation.service` 被調用寫入 DB（mock assertion）
  2. 驗證呼叫順序：provider 調用成功 → 寫 Generation → 寫 ApiUsageLedger
  3. 寫 DB 失敗時的錯誤傳遞行為
- **Fixtures / Mocks Needed**：Prisma mock · provider adapter mock · existing `generate-image.service.test.ts` 已有 mock 基礎
- **Risks**：mock 順序斷言過於嚴格，重構時易破
- **Acceptance Criteria**：新增 ≥ 3 個測試 · 驗證 `@layer business → @layer db` 調用方向
- **Priority**：P2
- **Effort**：S

---

### TC-Image-02 · image-edit Service 單測

- **Covered Work Package**：WP-Image-02
- **Test Layer**：service
- **Test Cases**
  1. `upscale` 正常路徑（mock FAL response）
  2. `removeBackground` 正常路徑
  3. FAL 失敗 → `ProviderError`
  4. `resolveEditProvider(editType)` 返回正確 adapter（僅 'fal'）
  5. 未知 editType → 錯誤
- **Fixtures / Mocks Needed**：FAL adapter mock（`fal-ai/aura-sr`、`fal-ai/birefnet/v2` 回應）· R2 upload mock
- **Risks**：FAL 回應結構變化需同步 fixture
- **Acceptance Criteria**：2 個 editType × 3 路徑測試 green · 能力矩陣查詢有測試
- **Priority**：P2
- **Effort**：S

---

### TC-Image-03 · Provider Adapter Contract + 3 熱門 Adapter 單測

- **Covered Work Package**：WP-Image-03
- **Test Layer**：service
- **Test Cases**
  1. **通用契約**：每個 adapter 的 `generateImage` 返回 `{ url, metadata }` 結構
  2. 錯誤映射：4xx/5xx/timeout 都映射為 `ProviderError`，帶正確 flag
  3. FAL：`generateImage` + `submitVideoToQueue` 兩入口
  4. Replicate：predictions 輪詢狀態機
  5. Gemini：native multimodal ref image 支援
- **Fixtures / Mocks Needed**：`vi.mock` fetch · `__fixtures__/fal-response.json`、`replicate-response.json`、`gemini-response.json`
- **Risks**：真實 provider 響應結構複雜，mock 易失真；retry+breaker 在 adapter 層不應重複包裝
- **Acceptance Criteria**：3 個 adapter 每個 ≥ 4 個測試 · contract 可複用於剩餘 5 個
- **Priority**：P1
- **Effort**：M

---

### TC-Image-04 · 240s Timeout 與降級路徑測試

- **Covered Work Package**：WP-Image-04
- **Test Layer**：service + route
- **Test Cases**
  1. Provider 模擬超時 → 在 220s 邊界觸發 `ProviderError(timeout=true)` 而非平台 500
  2. Circuit-breaker 在超時後計入失敗（驗證 breaker state 改變）
  3. withRetry 在超時後是否繼續重試（依 isRetryable 策略）
  4. 結構化日誌：`{ provider, attempt, elapsedMs, outcome }` 被 logger 接收
- **Fixtures / Mocks Needed**：Vitest fake timers · provider mock 可控延遲 · logger spy
- **Risks**：時間敏感 flaky；220s vs 240s 邊界與 Vercel 冷啟動相關
- **Acceptance Criteria**：3 個 route（generate / studio/generate / generate-video）的 timeout 路徑測試 · 日誌 spy 有斷言
- **Priority**：P1
- **Effort**：M

---

## 2.3 視頻生成

### TC-Video-01 · video-pipeline.service 單測（933 行最大缺口）

- **Covered Work Package**：WP-Video-01
- **Test Layer**：service
- **Test Cases**
  1. `createLongVideoPipeline`：clip 數計算（整除 / 有餘數 / 超長 / 恰好 maxDuration）
  2. Prisma transaction：建 pipeline + N 個 clips 原子性（失敗全回滾）
  3. `checkPipelineStatus`：所有 clip COMPLETED → pipeline COMPLETED；任一 FAILED → FAILED；混合 → RUNNING
  4. `cancelPipeline`：RUNNING → CANCELLED；COMPLETED → 400 拒絕；跨用戶 → 403
  5. `retryPipelineClip`：FAILED → 重新提交；COMPLETED → 拒絕；跨用戶 → 403
- **Fixtures / Mocks Needed**：Prisma test transaction · provider queue submit mock
- **Risks**：DB mock 深度大；分段計算邊界值易漏
- **Acceptance Criteria**：4 個核心函數各 3 個路徑 · 計算邏輯 100% branch coverage
- **Priority**：P0
- **Effort**：L

---

### TC-Video-02 · Long-Video Cancel/Retry Route + E2E

- **Covered Work Package**：WP-Video-02
- **Test Layer**：route + e2e
- **Test Cases**
  1. `POST /api/generate-long-video/cancel`：auth 401 / 跨用戶 403 / 狀態非 RUNNING 400 / 正常 200
  2. `POST /api/generate-long-video/retry`：auth 401 / FAILED clip 正常 / 非 FAILED 400
  3. Retry 不觸發重複 requestCount / ledger 記錄（與 WP-Usage-01 transaction 交叉驗證）
  4. E2E：mock provider 首次失敗 → 客戶端觸發 retry → pipeline 恢復
- **Fixtures / Mocks Needed**：`api-helpers` · 可控失敗的 provider mock · Playwright（E2E 可選）
- **Risks**：E2E 需穩定 provider mock 或錄播；retry 與計費互動邊界
- **Acceptance Criteria**：兩個 route 有 auth/validation/業務三層測試 · 至少 1 個 E2E smoke
- **Priority**：P1
- **Effort**：M

---

### TC-Video-03 · Validation 模型矩陣補測

- **Covered Work Package**：WP-Video-03
- **Test Layer**：service
- **Test Cases**
  1. Kling / MiniMax / Runway / SeedDance 各 1 模型：duration / resolution / aspectRatio 合法路徑
  2. duration 不在 `supportedDurations` → 錯誤
  3. resolution 不支持 → 錯誤
  4. 需參考圖模型但缺 referenceImage → 錯誤
  5. 非 VIDEO 類型 model → 錯誤
  6. 無效 modelId → 錯誤
- **Fixtures / Mocks Needed**：`video-model-capabilities` 靜態 fixture
- **Risks**：模型能力矩陣變更需同步
- **Acceptance Criteria**：4 provider × 4 驗證路徑 · 既有 `video-generation-validation.service.test.ts` 擴充
- **Priority**：P1
- **Effort**：S

---

## 2.9 Prompt & LLM

### TC-Prompt-01 · 4 個 Prompt/LLM Service 單測骨架

- **Covered Work Package**：WP-Prompt-01
- **Test Layer**：service
- **Test Cases**
  1. `enhancePrompt`：正常 / LLM 失敗 fallback 到原 prompt / 未綁 key 錯誤
  2. `chatPromptAssistant`：單輪 / 多輪 flattenConversation / markdown 代碼塊提取
  3. `getPromptFeedback`：Zod schema 驗證 `{ overallAssessment, suggestions[], improvedPrompt }`
  4. `llmTextCompletion`：Gemini → OpenAI → VolcEngine fallback 鏈
  5. 各 service 調用 `validateLlmPromptOutput`（spy）
- **Fixtures / Mocks Needed**：`resolveLlmTextRoute` mock · `llmTextCompletion` mock 可注入不同 LLM response
- **Risks**：LLM mock 需模擬 JSON 回傳多種變異；fallback 鏈測試順序斷言易破
- **Acceptance Criteria**：4 個 service 各有正常 + ≥ 2 個錯誤路徑 · fallback 鏈有斷言
- **Priority**：P1
- **Effort**：M

---

### TC-Prompt-02 · 5 種 Enhance 風格 Golden Set 測試

- **Covered Work Package**：WP-Prompt-02
- **Test Layer**：service
- **Test Cases**
  1. 5 風格（detailed / artistic / photorealistic / anime / lora）各 3-5 條輸入 → 固化 output fixture
  2. mock LLM 返回 fixture output → 斷言 service 輸出不做額外變形
  3. lora 風格：output 符合 Danbooru tag 格式（逗號分隔）
  4. 模型 hint 注入：`getModelEnhanceHint(modelId)` 被 spy 觸發
- **Fixtures / Mocks Needed**：`__fixtures__/prompt-enhance-golden.json`（需人工審核建立）· LLM mock
- **Risks**：baseline 主觀；LLM 版本升級需重新 baseline
- **Acceptance Criteria**：5 風格 golden fixture 齊 · CI 回歸 green
- **Priority**：P2
- **Effort**：M

---

### TC-Prompt-03 · Prompt-Assistant 上下文窗口保護測試

- **Covered Work Package**：WP-Prompt-03
- **Test Layer**：service
- **Test Cases**
  1. 50 條對話輸入 → 只保留窗口內（`MAX_HISTORY_CHARS=12000`）
  2. 保留首條 system + 最後 N 條 user/assistant
  3. 截斷發生時 `logger.info` 被觸發（spy）
  4. 窗口內 <= 12000 字 → 不截斷
  5. 單輪場景（無 history）行為不變
- **Fixtures / Mocks Needed**：LLM mock · logger spy · 長對話 fixture（50 條）
- **Risks**：字符數 ≠ tokens，實際 LLM 限制可能不同
- **Acceptance Criteria**：5 個測試 · 截斷日誌可觀測
- **Priority**：P1
- **Effort**：S

---

### TC-Prompt-04 · llm-output-validator 單測（Lib）

- **Covered Work Package**：WP-Prompt-04
- **Test Layer**：service (lib)
- **Test Cases**
  1. Meta-commentary 檢測：`"Here's the enhanced..."` / `"The improved prompt..."` 被識別
  2. System prompt leak：`"you are an expert"` / `"I am an AI"` 被識別
  3. 長度截斷：超過 `MAX_COMPILED_PROMPT_LENGTH=8000` 自動截斷 + warning
  4. 敏感詞：加 minimal list 後的過濾測試
  5. Fallback：驗證失敗時呼叫方得到 fallback prompt
- **Fixtures / Mocks Needed**：各類 LLM output 樣本
- **Risks**：敏感詞 false-positive；與 `prompt-guard`（input）邊界混淆
- **Acceptance Criteria**：5 類驗證各 ≥ 2 個測試 · false-positive rate < 1% in golden set
- **Priority**：P1
- **Effort**：S

---

## 2.12 計費與用量

### TC-Usage-01 · Job ↔ Ledger Transaction 原子性測試（P0）

- **Covered Work Package**：WP-Usage-01
- **Test Layer**：service
- **Test Cases**
  1. Job 完成 + Ledger 寫入：成功 → 兩邊一致
  2. Ledger 寫入失敗 → Job 狀態回滾（或不前進）
  3. Job 寫入失敗 → Ledger 不產生
  4. 並發雙寫（多請求）不產生漂移
  5. 漂移日誌：job.status=COMPLETED 但 ledger 缺失時 `logger.warn` 觸發
- **Fixtures / Mocks Needed**：Prisma `$transaction` mock 可注入失敗 · logger spy
- **Risks**：transaction 範圍過大導致測試假陽性通過（長事務在真實環境才暴露）
- **Acceptance Criteria**：5 個測試 green · 回滾路徑可驗證
- **Priority**：P0
- **Effort**：M

---

### TC-Usage-02 · FreeTier 並發邊界測試（P0）

- **Covered Work Package**：WP-Usage-02
- **Test Layer**：service
- **Test Cases**
  1. 同一用戶 25 並發生成請求 → 通過數 ≤ 20
  2. 20 並發恰達限 → 全部成功
  3. 21st 請求 → `FREE_LIMIT_EXCEEDED`（當前類型為 `InsufficientCreditsError`）
  4. 跨日邊界（UTC vs 用戶時區）：確認計數重置時機
  5. 不同用戶並發計數隔離
- **Fixtures / Mocks Needed**：Prisma with `SELECT ... FOR UPDATE` 或 Redis counter mock · Vitest fake timers（跨日）
- **Risks**：現有測試已記錄 race window，這個 WP 要把「記錄缺口」升級為「證明原子性」；並發測試本身也容易 flaky
- **Acceptance Criteria**：並發 25→20 嚴格一致 · 時區邊界文檔化
- **Priority**：P0
- **Effort**：M

---

### TC-Usage-03 · Ledger 漂移檢測測試

- **Covered Work Package**：WP-Usage-03
- **Test Layer**：service + route
- **Test Cases**
  1. `usage-audit.service`：查詢出歷史漂移案例（job=COMPLETED 無 ledger）
  2. 反向漂移（ledger 存在但 job.status=FAILED）
  3. `POST /api/admin/usage-audit`：非 admin → 403
  4. admin → 返回 report 結構
  5. 巡檢不自動寫入補償（dry-run）
- **Fixtures / Mocks Needed**：seed 漂移測試資料 · admin role mock
- **Risks**：admin role 判斷需與現有 auth / route-factory 邊界對齊
- **Acceptance Criteria**：service + route 各 3 路徑 · dry-run 保證無副作用
- **Priority**：P2
- **Effort**：S

---

## 2.13 存儲與資產

### TC-Storage-01 · R2 Wrapper 單測（P0）

- **Covered Work Package**：WP-Storage-01
- **Test Layer**：service
- **Test Cases**
  1. `generateStorageKey(outputType, userId, audioFormat?)`：組合正確
  2. `uploadToR2`：成功返回 publicUrl；CacheControl `immutable`
  3. `uploadToR2` 失敗 → withRetry 三次後拋
  4. `fetchAsBuffer`：404 / 500 拋錯；正常返回 `{ buffer, mimeType }`
  5. `streamUploadToR2`：multipart 流式；retry=2 次
  6. `deleteFromR2`：火忘式，不因失敗阻塞調用方
- **Fixtures / Mocks Needed**：`@aws-sdk/client-s3` 的 `PutObjectCommand` / `GetObjectCommand` / `DeleteObjectCommand` mock
- **Risks**：AWS SDK mock 覆蓋多個命令類；multipart 邏輯複雜
- **Acceptance Criteria**：5 個 export 全有測試 · 無真實 R2 調用 · retry 可觀測
- **Priority**：P0
- **Effort**：M

---

### TC-Storage-02 · Sitemap 分頁測試

- **Covered Work Package**：WP-Storage-02
- **Test Layer**：route
- **Test Cases**
  1. 1 / 50 / 1000 public generation 下 sitemap 大小正確
  2. 三語 URL 複製：每個 generation 產生 3 條（en/ja/zh）
  3. 靜態 entries（HOME、GALLERY、ARENA、LEADERBOARD）恆存在
  4. 分片（若採 sitemap index）：index → 多個 sub-sitemap 正確連結
  5. 空 generation：僅返回靜態 entries
- **Fixtures / Mocks Needed**：`generation.service.getPublicGenerations` mock 可返回不同數量
- **Risks**：動態 sitemap DB 查詢性能需 cache，但 cache 層會影響測試
- **Acceptance Criteria**：4 個數量級 × 三語複製測試 · 靜態 entries 守護
- **Priority**：P1
- **Effort**：S

---

### TC-Storage-03 · OG Route 測試

- **Covered Work Package**：WP-Storage-03
- **Test Layer**：route
- **Test Cases**
  1. `type=generation&id=xxx` 正常 → 1200×630 PNG
  2. `type=generation` id 不存在 → 404
  3. `type=profile&username=xxx` 正常
  4. `type=profile` username 不存在 → 404
  5. video generation → 渲染海報（非嵌入視頻）
  6. fallback（無參數）→ 預設 OG
  7. `Cache-Control` header 正確設定
- **Fixtures / Mocks Needed**：`generation.service` + `user.service` mock · Satori 初始化
- **Risks**：Satori 冷啟動 > 1s，測試運行時間膨脹
- **Acceptance Criteria**：7 路徑測試 green · 與 UI 線 TC-Gallery-05 / TC-Profile-05 共用
- **Priority**：P1
- **Effort**：S

---

## 2.14 基礎設施

### TC-Infra-01 · with-retry 單測（P0）

- **Covered Work Package**：WP-Infra-01
- **Test Layer**：service (lib)
- **Test Cases**
  1. 5xx / 429 / network error → 重試
  2. 4xx（除 429）→ 不重試
  3. 驗證錯誤 → 不重試
  4. 達 `maxAttempts=3` 後拋原錯
  5. Backoff delay：`baseDelay * 2^(attempt-1) + jitter`，封頂 `maxDelayMs=15000`
  6. Jitter 在預期範圍
  7. 自定義 `isRetryable` 覆蓋預設
- **Fixtures / Mocks Needed**：`vi.useFakeTimers` · 固定 seed 或 mock `Math.random` 控制 jitter
- **Risks**：時間敏感 flaky；jitter 隨機性需固定
- **Acceptance Criteria**：7 路徑 + 時間斷言 · CI 穩定性 100%
- **Priority**：P0
- **Effort**：S

---

### TC-Infra-03 · Rate-Limit Upstash 降級路徑測試

- **Covered Work Package**：WP-Infra-03
- **Test Layer**：service (lib)
- **Test Cases**
  1. Upstash 正常 → sliding window 限流生效
  2. Upstash 連接失敗 → in-memory Map fallback 仍限流
  3. Fallback 切換 → `logger.warn` 觸發
  4. 不同 key 獨立計數
  5. 窗口過期後重置
  6. `RATE_LIMIT_CONFIGS` 預設（promptEnhance=20/60s 等）被套用
- **Fixtures / Mocks Needed**：Upstash Ratelimit mock（正常 + 連接失敗）· logger spy
- **Risks**：in-memory Map 在 serverless 多實例不一致，只能驗證單實例
- **Acceptance Criteria**：6 路徑測試 · fallback 切換有 warning
- **Priority**：P1
- **Effort**：S

---

## 2.5 風格一致性切片

### TC-StyleConsistency-01 · compileRecipe 兩階段單測（P0）

- **Covered Work Package**：WP-StyleConsistency-01
- **Test Layer**：service
- **Test Cases**
  1. Stage 1 Template fallback：character + freePrompt + background + style 拼接順序
  2. Stage 2 LLM Fusion 正常：system prompt 含 CHARACTER/BACKGROUND/STYLE 標籤
  3. Stage 2 失敗 → 降級到 Stage 1
  4. NovelAI（tag-based）vs Gemini/OpenAI（natural）system prompt 差異
  5. freePrompt only（無卡片）→ 直接返回原 prompt
  6. `modelId` / `adapterType` 從 styleCard 正確取得
  7. `referenceImages` 優先級：character → style → background
- **Fixtures / Mocks Needed**：character / style / background card fixture · LLM mock · WP-Prompt-01 的 mock 框架
- **Risks**：兩階段邏輯複雜，fallback 路徑易漏
- **Acceptance Criteria**：7 路徑測試 · 兩階段 + 3 種 adapter 分支覆蓋
- **Priority**：P0
- **Effort**：M

---

### TC-StyleConsistency-02 · LoRA 合併 / 去重 / Civitai Token 測試

- **Covered Work Package**：WP-StyleConsistency-02
- **Test Layer**：service
- **Test Cases**
  1. 優先級合併：character → style → styleCard.advancedParams → background
  2. 去重：同 URL 不同源，保留第一個
  3. FAL 限制：超過 5 個 LoRA → 截斷至 5
  4. Replicate 限制：超過 1 個 → 截斷至 1
  5. Civitai token 注入：有 token → URL 加參數；無 token → 原 URL
  6. 邊界：無 LoRA / 全部無效 URL
- **Fixtures / Mocks Needed**：LoRA fixture（多源）· `civitai-token.service` mock
- **Risks**：provider 上限變化需同步
- **Acceptance Criteria**：6 路徑測試 · 合併於 TC-StyleConsistency-01 同檔案
- **Priority**：P0
- **Effort**：S

---

### TC-StyleConsistency-03 · Character Attributes → Prompt 3 格式測試

- **Covered Work Package**：WP-StyleConsistency-03
- **Test Layer**：service
- **Test Cases**
  1. NovelAI tag 格式：`"anime, blue hair, happy"`（逗號分隔 Danbooru）
  2. FLUX/SD weighted 格式：`"(blue hair:1.3), (happy:1.1)"`
  3. Gemini/OpenAI natural 格式：完整句子
  4. 屬性優先級：artStyle → expression → hair → eyes → skin → body → outfit → accessories → pose → distinguishing features
  5. Fallback：無結構屬性 → 用 `freeformDescription`
  6. 空 card → 空 prompt
- **Fixtures / Mocks Needed**：character card fixture 多種屬性組合
- **Risks**：模板字串調整需同步 fixture
- **Acceptance Criteria**：3 格式 × 5 屬性組合測試 · fallback 路徑覆蓋
- **Priority**：P1
- **Effort**：S

---

## 工作包索引

| ID                     | Covered WP             | Layer         | Priority | Effort |
| ---------------------- | ---------------------- | ------------- | -------- | ------ |
| TC-Image-01            | WP-Image-01            | service       | P2       | S      |
| TC-Image-02            | WP-Image-02            | service       | P2       | S      |
| TC-Image-03            | WP-Image-03            | service       | P1       | M      |
| TC-Image-04            | WP-Image-04            | service+route | P1       | M      |
| TC-Video-01            | WP-Video-01            | service       | **P0**   | L      |
| TC-Video-02            | WP-Video-02            | route+e2e     | P1       | M      |
| TC-Video-03            | WP-Video-03            | service       | P1       | S      |
| TC-Prompt-01           | WP-Prompt-01           | service       | P1       | M      |
| TC-Prompt-02           | WP-Prompt-02           | service       | P2       | M      |
| TC-Prompt-03           | WP-Prompt-03           | service       | P1       | S      |
| TC-Prompt-04           | WP-Prompt-04           | service (lib) | P1       | S      |
| TC-Usage-01            | WP-Usage-01            | service       | **P0**   | M      |
| TC-Usage-02            | WP-Usage-02            | service       | **P0**   | M      |
| TC-Usage-03            | WP-Usage-03            | service+route | P2       | S      |
| TC-Storage-01          | WP-Storage-01          | service       | **P0**   | M      |
| TC-Storage-02          | WP-Storage-02          | route         | P1       | S      |
| TC-Storage-03          | WP-Storage-03          | route         | P1       | S      |
| TC-Infra-01            | WP-Infra-01            | service (lib) | **P0**   | S      |
| TC-Infra-03            | WP-Infra-03            | service (lib) | P1       | S      |
| TC-StyleConsistency-01 | WP-StyleConsistency-01 | service       | **P0**   | M      |
| TC-StyleConsistency-02 | WP-StyleConsistency-02 | service       | **P0**   | S      |
| TC-StyleConsistency-03 | WP-StyleConsistency-03 | service       | P1       | S      |

**共 22 個測試工作包** · P0: 7 · P1: 10 · P2: 5 · S: 13 · M: 8 · L: 1

---

## 非對齊項說明

以下功能 WP 不產生測試工作包：

- **WP-Infra-02**（Errors 映射矩陣文檔化）：純文檔任務，無測試產出
- **WP-Infra-04**（Circuit breaker 持久化策略決斷）：策略決斷 + 文檔，無新測試（既有 `circuit-breaker.test.ts` 不動）

本輪**不發散**到功能 WP 未聲明的測試（例如：不新增 `webhooks/clerk` 測試，不新增各社群 service 測試 — 這些屬於未被勾選的能力域）。
